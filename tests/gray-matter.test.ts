// tests/gray-matter.test.ts — Phase 2 Wave-0 failing-stub.
// Wave 2 (Plan 02-02) owns: gray-matter@4.0.3 parse + stringify round-trip on INDEX.md frontmatter.
// Maps to: REQ-02 acceptance (SPEC L134).
import { describe, test, expect } from "vitest";

describe.skip("Wave 2 implements gray-matter parse + stringify round-trip", () => {
    test("placeholder — replace describe.skip + body in Wave 2", () => {
        expect.fail("Wave 2 implements");
    });
});
