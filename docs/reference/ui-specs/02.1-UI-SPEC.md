---
phase: "02.1"
slug: ui-fixes-from-dogfood-and-audit
status: stub_no_new_surfaces
created: 2026-05-17
audit_baseline: ../02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md (Phase 02 — 9 surfaces, 1521 lines, approved 2026-05-15)
revision: 1
---

# Phase 02.1 — UI Spec (stub: no new design surfaces)

> **Stub UI-SPEC** — Phase 02.1 introduces ZERO new visual surfaces. All visual changes are localized fixes to surfaces already specified in `02-UI-SPEC.md` (Phase 02 parent spec). Specific visual decisions for the 3 fixes that change pixels are captured in `02.1-CONTEXT.md` D-04 (B1 DropzoneOverlay backdrop) and D-05 (W2 Onboarding step transition). Other fixes are state-machine / functional changes that do not alter the rendered surface (B2 token swap is semantically a recolor but token system per UI-SPEC §4 already governs it).

---

## Why this stub exists

The autonomous workflow (`/gsd-autonomous --only 02.1`) auto-detects "UI" in the ROADMAP phase entry and would spawn `/gsd-ui-phase` to generate a fresh UI design contract. For a FIX-only phase that doesn't introduce surfaces, that work is duplicative — the parent Phase 02 UI-SPEC.md is already the design contract, and Phase 02.1 CONTEXT.md D-04 + D-05 capture the surgical visual changes.

This stub satisfies the workflow's "UI-SPEC exists for this phase" gate without committing to a redundant fresh design.

---

## Surface inventory (delegated)

All 9 surfaces from Phase 02 remain authoritative. Phase 02.1 touches these subsets:

| Surface (parent UI-SPEC §) | Phase 02.1 change | Reference |
|---------------------------|-------------------|-----------|
| §8.4 DropzoneOverlay | Backdrop redesign (cream → dark veil + blur) | CONTEXT.md D-04 + UI-REVIEW B1 |
| §8.1 Settings (Vault category) | Move-complete token swap (--color-success → --color-warm-dark-soft) | CONTEXT.md D-03 / D-04 not applicable; UI-REVIEW B2 |
| §8.1 Onboarding (Steps 1-6) | Step transitions (fly + opacity, 220ms, prefers-reduced-motion) | CONTEXT.md D-05 + UI-REVIEW W2 |
| §8.2 Settings panel rail | :focus-visible outline | UI-REVIEW W3 |
| §8.1.3 Step 3 VaultPicker | Secondary "Choose another folder" link | UI-REVIEW W4 |
| §8.8 ImportStatusPill | Spinner SVG + state icons + bg per state | UI-REVIEW W1 |
| §8.7 ImportHistoryModal | Token-ize 56px hardcode | UI-REVIEW I2 |
| §8.1.1 Step 1 Welcome | Spacing pattern fix (parent gap, not negative margin) | UI-REVIEW I3 |
| §8.8 TitlebarMeta | Drag region (`-webkit-app-region: drag`) | UI-REVIEW W8 |
| (text only) File row | Size column populate | UI-REVIEW I1 |

No new components. No new tokens. No new section additions to parent UI-SPEC.

---

## Routing

`/gsd-plan-phase 02.1 --tdd` proceeds with this stub + 02.1-CONTEXT.md as planning input. `/gsd-ui-review 02.1` after execute will re-audit against parent `02-UI-SPEC.md` to confirm fixes don't regress other surfaces.
