---
phase: 01-tauri-shell-foundation-subprocess-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/tauri.conf.json
  - src-tauri/icons/icon.icns
  - src-tauri/icons/32x32.png
  - src-tauri/icons/128x128.png
  - src-tauri/icons/128x128@2x.png
  - src-tauri/icons/icon.ico
  - rust-toolchain.toml
  - src/app.html
  - src/routes/+layout.ts
  - src/routes/+page.svelte
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - vendor/claude-code-parser/src/parseLine.ts
  - vendor/claude-code-parser/src/types.ts
  - vendor/claude-code-parser/src/index.ts
  - vendor/claude-code-parser/LICENSE
  - vendor/claude-code-parser/VENDOR.md
autonomous: true
requirements: [REQ-01, REQ-02, REQ-10]

must_haves:
  truths:
    - "`npm run tauri dev` from repo root launches a window titled Mneme with productName Mneme and identifier dev.mneme.app"
    - "Vendored parser preserves MIT LICENSE attribution and is NOT in package.json deps"
    - "Vitest harness can run a placeholder test from `tests/` and exit 0"
    - "Window initial size 1280x860, min 1024x600, decorations:true + titleBarStyle:Overlay + hiddenTitle:true (D-06 chrome lock)"
    - "NO cost-meter task lands in this plan (Round 5 A-04 — cost meter fully gone; replaced by Ctx + Session usage meter in 01-06 per A-09)"
  artifacts:
    - path: "package.json"
      provides: "frontend dep manifest with marked / katex@^0.16.45 / dompurify / @tauri-apps/api / @tauri-apps/plugin-shell / vitest / husky"
      contains: "\"katex\""
    - path: "src-tauri/Cargo.toml"
      provides: "Rust dep manifest with tauri / tauri-plugin-shell / nix (signal+process)"
      contains: "nix = "
    - path: "src-tauri/tauri.conf.json"
      provides: "productName Mneme, identifier dev.mneme.app, window 1280x860 min 1024x600, titleBarStyle Overlay"
      contains: "\"productName\": \"Mneme\""
    - path: "rust-toolchain.toml"
      provides: "Rust ≥ 1.88 pin (KD-03)"
      contains: "channel"
    - path: "src/app.html"
      provides: "CSP meta tag (REQ-5 starting point)"
      contains: "Content-Security-Policy"
    - path: "vendor/claude-code-parser/LICENSE"
      provides: "Upstream MIT attribution preserved per KD-12 + D-13"
      contains: "MIT"
    - path: "vendor/claude-code-parser/VENDOR.md"
      provides: "Snapshot date + commit hash + frozen status note"
      contains: "frozen reference per KD-12"
    - path: "vitest.config.ts"
      provides: "jsdom test env for sanitize/dispatch unit tests"
      contains: "jsdom"
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri builder + tauri_plugin_shell::init() + setup hook that creates ~/.mneme/scratch/"
      contains: "tauri_plugin_shell::init"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/icons/icon.icns"
      via: "bundle.icon[]"
      pattern: "icons/icon\\.icns"
    - from: "src/routes/+layout.ts"
      to: "SPA mode"
      via: "ssr=false / prerender=true"
      pattern: "ssr\\s*=\\s*false"
    - from: "tsconfig.json"
      to: "vendor/claude-code-parser"
      via: "$vendor/* path alias"
      pattern: "\\$vendor"
    - from: "src-tauri/src/lib.rs"
      to: "~/.mneme/scratch/"
      via: "setup hook create_dir_all"
      pattern: "\\.mneme/scratch"
---

<objective>
Wave 0 / Wave 1 bootstrap. Bring up the empty Mneme repo to "`npm run tauri dev` runnable" by laying down the entire Tauri 2 + SvelteKit + adapter-static + Svelte 5 project skeleton, locking Phase 0 identity (productName "Mneme", identifier "dev.mneme.app", icons from `icon-assets/icon.icns`), wiring the test harness (Vitest 4 + jsdom + Husky uninitialized — install in 01-07), pinning the Rust toolchain (KD-03 ≥ 1.88), adding the `nix` Cargo dep with `signal+process` features (D-11), creating the auto-`~/.mneme/scratch/` setup hook, vendoring `claude-code-parser` (KD-12 + D-13 — src + LICENSE + VENDOR.md only, NO npm dep), and writing the CSP meta tag into `app.html` (REQ-5 surface). This plan does NOT implement any subprocess streaming, capability hardening, sanitize logic, splitter, or chat panel — those are Wave 2/3/4 plans. The deliverable is a runnable shell that displays a default browser-styled SvelteKit page (per SPEC L102: "Phase 1 may emit unstyled / browser-default UI") with the production identity + chrome locked.

**Round 5 amendment scope (A-04):** This plan absorbs zero cost-meter tasks. Round 4 SPEC already deleted the cost-meter feature (`SPEC.md` L100/L123); the planner-iteration-1 leak that placed a cost-meter TDD task in 01-06 is fixed by AMENDMENT A-04. There is no `~/.mneme/usage.jsonl`, no daily-$cap state machine, no `result.total_cost_usd`-driven runtime decision in this plan. The verify line in Task 5 retains the `claude-code-parser` absence check; A-04 enforcement lives downstream in 01-06 and 01-07.

Purpose: Establish the canonical project layout that every other Phase 1 plan operates inside. Wave 2 plans (01-02 SSOT, 01-03 sanitize, 01-04 lifecycle) all assume `package.json` + `Cargo.toml` + `tsconfig.json` paths + `vitest.config.ts` + `vendor/claude-code-parser/src/types.ts` already exist.
Output: Runnable Tauri 2 + SvelteKit shell at the repo root with Phase 0 identity + chrome locked, vendored parser, test harness ready, no production code yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/dependencies.md
@.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-SPEC.md
@.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-CONTEXT.md
@.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-AMENDMENT-2026-05-09.md
@.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-AI-SPEC.md
@.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-RESEARCH.md
@.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-PATTERNS.md
@.planning/phases/00-identity-branding-lock/00-04-SUMMARY.md
@.claude/skills/spike-findings-mneme/SKILL.md

<interfaces>
<!-- Phase 0 inheritance -->
- productName "Mneme" + identifier "dev.mneme.app" + window title "Mneme" (Phase 0 D-10 / D-14)
- Icon source at repo root: `icon-assets/icon.icns` (committed Phase 0); copy + the 7 PNG variants from `icon-assets/iconset.iconset/` into `src-tauri/icons/`
- Existing files in repo root: `CLAUDE.md`, `LICENSE`, `README.md`, `.gitignore`, `icon-assets/`, `.planning/` — DO NOT delete or overwrite

<!-- Tauri / Svelte / vendor versions verified per RESEARCH §4 -->
- Tauri 2 stack: tauri 2.x + tauri-plugin-shell 2.3.5 (cargo) + @tauri-apps/api 2.11.0 + @tauri-apps/plugin-shell 2.3.5 (npm)
- SvelteKit: @sveltejs/kit 2.59.1 + @sveltejs/adapter-static 3.0.10 + svelte 5.55.5
- Frontend libs (KD-02): marked 18.0.3 + katex 0.16.45 (≥ SPEC floor 0.16.21) + dompurify 3.4.2
- Test harness: vitest 4.1.5 (jsdom env) + husky 9.x (devDep, NOT initialized in this plan — that's 01-07)
- Rust deps: nix 0.31.2 with `features = ["signal", "process"]` (D-11 finalized choice from CONTEXT.md, AI-SPEC §4 verifies)

<!-- Vendoring contract -->
- `vendor/claude-code-parser/` — copy src/ + LICENSE from upstream `udhaykumarbala/claude-code-parser` MIT
- VENDOR.md content (D-13 spec): snapshot date 2026-05-08 + upstream commit hash + statement "frozen reference per KD-12; upstream effectively unmaintained; types-only consumption (Path 2 per RESEARCH §4.8)"
- MUST NOT appear in package.json deps OR devDeps
- AGPL posture (D-09): only MIT-licensed code is vendored. AGPL projects (opcode, siteboon/claudecodeui) are READ-ONLY REFERENCE — no code, snippets, or derivative work appears in `vendor/` or anywhere else in the repo. mneme retains full license selection freedom.

<!-- Spike-002 reference paths -->
- `.planning/spikes/002-tauri-claude-shell/app/package.json` — dep version baseline
- `.planning/spikes/002-tauri-claude-shell/app/src/routes/+layout.ts` — SPA mode `ssr=false; prerender=true`
- `.planning/spikes/002-tauri-claude-shell/app/src-tauri/tauri.conf.json` — outer skeleton (rewrite identity + window block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Tauri 2 + SvelteKit project skeleton at repo root</name>
  <files>
    package.json,
    src-tauri/Cargo.toml,
    src-tauri/tauri.conf.json,
    src-tauri/src/main.rs,
    src-tauri/src/lib.rs,
    src-tauri/build.rs,
    src/app.html,
    src/routes/+layout.ts,
    src/routes/+page.svelte,
    tsconfig.json,
    svelte.config.js,
    vite.config.ts,
    rust-toolchain.toml,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-AMENDMENT-2026-05-09.md (5th contract artifact — read before any Phase 1 work; A-04 confirms no cost meter task in this plan; A-15 pins Mneme.html as visual SSOT — irrelevant to bootstrap but ambient context for downstream waves),
    .planning/handoff/2026-05-09-mneme-prototype/mneme/project/Mneme.html (visual-fidelity SSOT per A-15 — this plan creates only the bootstrap placeholder Svelte route; no visual decisions yet, but executor should be aware the prototype is the SSOT for Wave 3+ rebuilds),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-PATTERNS.md (Tauri/Svelte sections L31-95 + L127-170 + L243-326),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-RESEARCH.md L474-491 (Wave 0 sequence),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-SPEC.md L37-52 (REQ-1 acceptance + initial size lock),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-CONTEXT.md L46-66 (D-01..D-07 layout/window/chrome locks),
    .planning/spikes/002-tauri-claude-shell/app/package.json (dep version reference),
    .planning/spikes/002-tauri-claude-shell/app/src-tauri/tauri.conf.json (outer skeleton — rewrite identity + window),
    .planning/spikes/002-tauri-claude-shell/app/src-tauri/Cargo.toml (Cargo dep reference),
    .planning/spikes/002-tauri-claude-shell/app/src/routes/+layout.ts (SPA mode lift verbatim),
    .planning/phases/00-identity-branding-lock/00-04-SUMMARY.md (Phase 0 identity contract — Mneme + dev.mneme.app),
    .planning/STATE.md (current `phase-1-ui-locked` state + Phase 0 baseline notes),
    /Users/qinyuan/claude/r1ckyIn_GitHub/mneme/CLAUDE.md (project L3 with authoritative status overrides),
    /Users/qinyuan/.claude/CLAUDE.md (L1 user profile — Chinese tech discussion / English code comments)
  </read_first>
  <action>
Bootstrap a Tauri 2 + SvelteKit project IN PLACE at the repo root `/Users/qinyuan/claude/r1ckyIn_GitHub/mneme/`. Do NOT use `npm create tauri-app` directly because that creates a NEW subdirectory; instead, write the canonical files explicitly so we keep the existing `.planning/`, `icon-assets/`, `CLAUDE.md`, `LICENSE`, `README.md`, `.gitignore`, `.git/` untouched.

Create files with these EXACT contents (paths relative to repo root):

**`package.json`** (frontend manifest, Phase 1 deps + scripts):
```json
{
  "name": "mneme",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "tauri": "tauri",
    "test": "vitest run",
    "test:watch": "vitest",
    "prebuild": "node scripts/gen-capabilities.ts && bash scripts/audit-capabilities.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "marked": "^18.0.3",
    "katex": "^0.16.45",
    "dompurify": "^3.4.2"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.59.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tauri-apps/cli": "^2.1.0",
    "@types/dompurify": "^3.0.5",
    "@types/katex": "^0.16.7",
    "@types/marked": "^6.0.0",
    "husky": "^9.1.7",
    "jsdom": "^25.0.1",
    "svelte": "^5.55.5",
    "svelte-check": "^4.1.1",
    "tslib": "^2.8.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^4.1.5"
  }
}
```

Note: `claude-code-parser` is intentionally absent from both `dependencies` and `devDependencies` (KD-12 + D-13). Husky is installed but NOT initialized — initialization happens in 01-07.

**`src-tauri/Cargo.toml`** (Rust manifest with `nix` for PGID kill — D-11 finalized to `nix`, NOT `libc`):
```toml
[package]
name = "mneme"
version = "0.1.0"
description = "Personal desktop learning app — Tauri 2 shell wrapping Claude Code CLI"
authors = ["r1ckyIn"]
edition = "2021"

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-shell = "2.3.5"
nix = { version = "0.31", features = ["signal", "process"] }
home = "0.5"
```

The `home` crate is for `home::home_dir()` in the setup hook (auto-create `~/.mneme/scratch/`).

**`src-tauri/tauri.conf.json`** (Phase 0 identity + D-05/D-06 window chrome):
```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Mneme",
  "version": "0.1.0",
  "identifier": "dev.mneme.app",
  "build": {
    "beforeDevCommand": "npm run dev",
    "beforeBuildCommand": "npm run build",
    "devUrl": "http://localhost:5173",
    "frontendDist": "../build"
  },
  "app": {
    "windows": [
      {
        "title": "Mneme",
        "width": 1280,
        "height": 860,
        "minWidth": 1024,
        "minHeight": 600,
        "decorations": true,
        "titleBarStyle": "Overlay",
        "hiddenTitle": true
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

`security.csp: null` is correct (RESEARCH §4.10 + spike-002 baseline) — the CSP meta tag in `app.html` (Task 4) governs.

**`src-tauri/src/main.rs`** (template default — 5 lines, no edits per PATTERNS.md L380-395):
```rust
// Prevents additional console window on Windows in release, DO NOT REMOVE!!
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    app_lib::run();
}
```

**`src-tauri/src/lib.rs`** (Phase 1 baseline — Tauri builder + plugin-shell init + setup hook for `~/.mneme/scratch/`. State machine + kill_pgid + register_session_pid all come in plan 01-04. This file is the SHELL that 01-04 will extend; we just need the empty-builder runnable now):
```rust
// Phase 1 baseline — built up by plan 01-04 with state machine + kill_pgid + hook union.
// This skeleton must already exist so `npm run tauri dev` runs end-to-end after Wave 1.

use std::fs;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_shell::init())
        .setup(|_app| {
            // Auto-create ~/.mneme/scratch/ on first launch — REQ-10 --add-dir target;
            // create_dir_all is idempotent (silent no-op if path already exists).
            if let Some(home) = home::home_dir() {
                let _ = fs::create_dir_all(home.join(".mneme/scratch"));
            }
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

**`src-tauri/build.rs`** (Tauri 2 standard build script):
```rust
fn main() {
    tauri_build::build()
}
```

**`src/app.html`** (with CSP meta tag locked per SPEC L140 + RESEARCH §4.10):
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'" />
    <link rel="icon" href="%sveltekit.assets%/favicon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    %sveltekit.head%
  </head>
  <body data-sveltekit-preload-data="hover">
    <div style="display: contents">%sveltekit.body%</div>
  </body>
</html>
```

The CSP string MUST match SPEC L140 byte-for-byte; acceptance test greps for the literal `Content-Security-Policy` header.

**`src/routes/+layout.ts`** (SPA mode lift verbatim from spike-002):
```typescript
export const ssr = false;
export const prerender = true;
```

**`src/routes/+page.svelte`** (placeholder for Wave 1 bootstrap; Wave 3 plan 01-05 replaces this with the splitter + tokens.css; Wave 4 plan 01-06 wires the chat panel inside the right pane):
```svelte
<script lang="ts">
  // Phase 1 Wave 1 placeholder. Wave 3 plan 01-05 replaces this with the
  // three-pane splitter + tokens.css; Wave 4 plan 01-06 wires the chat panel.
</script>

<main>
  <h1>Mneme — Phase 1 Wave 1 bootstrap</h1>
  <p>SvelteKit shell runnable. Splitter + chat panel arrive in Waves 3 + 4.</p>
</main>
```

**`tsconfig.json`** (path aliases per RESEARCH §4.8 — `$vendor/*` is critical for vendored parser import):
```json
{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "sourceMap": true,
    "strict": true,
    "moduleResolution": "bundler",
    "paths": {
      "$lib/*": ["./src/lib/*"],
      "$vendor/*": ["./vendor/*"]
    }
  }
}
```

**`svelte.config.js`** (SvelteKit + adapter-static):
```javascript
import adapter from '@sveltejs/adapter-static';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

const config = {
  preprocess: vitePreprocess(),
  kit: {
    adapter: adapter({
      pages: 'build',
      assets: 'build',
      fallback: 'index.html',
      precompress: false,
      strict: true
    })
  }
};

export default config;
```

**`vite.config.ts`** (Vite + SvelteKit + Tauri-friendly host config):
```typescript
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [sveltekit()],
  clearScreen: false,
  server: {
    port: 5173,
    strictPort: true,
    host: 'localhost',
    hmr: { protocol: 'ws', host: 'localhost', port: 5173 }
  }
});
```

**`rust-toolchain.toml`** (KD-03 ≥ 1.88; using `1.88` permissive form per Phase 0 LEARNINGS macOS-CLI version-precision lesson — both 1.88 and 1.88.0 work; choosing 1.88 to allow patch updates without re-pinning):
```toml
[toolchain]
channel = "1.88"
```

**`.gitignore`** (extend existing — DO NOT overwrite, only ADD lines if missing). Read existing first, append only missing entries from this list:
```
node_modules/
build/
dist/
.svelte-kit/
src-tauri/target/
src-tauri/Cargo.lock
*.log
.DS_Store
.env
.env.*
.husky/_
```
The existing root `.gitignore` already covers CLAUDE.md, *_CN.md, .env*, *.pem (from project-template); we add only the build-output + tooling cruft. DO NOT add `vendor/` to gitignore — vendored code is checked in.
  </action>
  <verify>
    <automated>test -f package.json && test -f src-tauri/Cargo.toml && test -f src-tauri/tauri.conf.json && test -f src/app.html && test -f rust-toolchain.toml && test -f tsconfig.json && test -f svelte.config.js && test -f vite.config.ts && grep -q '"productName": "Mneme"' src-tauri/tauri.conf.json && grep -q '"identifier": "dev.mneme.app"' src-tauri/tauri.conf.json && grep -q '"width": 1280' src-tauri/tauri.conf.json && grep -q '"minWidth": 1024' src-tauri/tauri.conf.json && grep -q '"titleBarStyle": "Overlay"' src-tauri/tauri.conf.json && grep -q '"hiddenTitle": true' src-tauri/tauri.conf.json && grep -q 'channel = "1.88"' rust-toolchain.toml && grep -q 'Content-Security-Policy' src/app.html && grep -q "default-src 'self'" src/app.html && grep -q "'wasm-unsafe-eval'" src/app.html && grep -q '"\$vendor/\*": \["./vendor/\*"\]' tsconfig.json && grep -q 'nix = { version = "0.31"' src-tauri/Cargo.toml && grep -q 'features = \["signal", "process"\]' src-tauri/Cargo.toml && grep -q 'tauri-plugin-shell = "2.3.5"' src-tauri/Cargo.toml && grep -q 'home = "0.5"' src-tauri/Cargo.toml && grep -q 'tauri_plugin_shell::init' src-tauri/src/lib.rs && grep -q '\.mneme/scratch' src-tauri/src/lib.rs && ! grep -q 'claude-code-parser' package.json && grep -q 'ssr = false' src/routes/+layout.ts && grep -q 'prerender = true' src/routes/+layout.ts</automated>
  </verify>
  <done>
    All bootstrap files exist with the exact identity + chrome + dep + path-alias values specified. `claude-code-parser` does NOT appear in package.json. `nix` has both `signal` and `process` features. `home` crate is added for the setup hook. CSP meta tag matches SPEC L140 byte-for-byte.
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy Phase 0 icon assets into src-tauri/icons/</name>
  <files>
    src-tauri/icons/icon.icns,
    src-tauri/icons/icon.ico,
    src-tauri/icons/32x32.png,
    src-tauri/icons/128x128.png,
    src-tauri/icons/128x128@2x.png,
    src-tauri/icons/icon.png
  </files>
  <read_first>
    .planning/phases/00-identity-branding-lock/00-02-SUMMARY.md (icon production pipeline outcome),
    .planning/phases/00-identity-branding-lock/00-04-SUMMARY.md (where icons live in repo),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-PATTERNS.md L171 (icon paths in tauri.conf.json),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-RESEARCH.md L483 (Wave 0 step 5 icon copy)
  </read_first>
  <action>
Run these commands EXACTLY (the iconset directory was committed in Phase 0 at `/Users/qinyuan/claude/r1ckyIn_GitHub/mneme/icon-assets/`):

```bash
mkdir -p src-tauri/icons
cp icon-assets/icon.icns src-tauri/icons/icon.icns
# Per Apple iconset convention, icon.icns + the iconset PNGs cover macOS;
# Tauri's bundle.icon array also expects an .ico (Windows) and 3 named PNGs.
# The Phase 0 iconset stores these at icon-assets/iconset.iconset/.
cp icon-assets/iconset.iconset/icon_32x32.png src-tauri/icons/32x32.png
cp icon-assets/iconset.iconset/icon_128x128.png src-tauri/icons/128x128.png
cp icon-assets/iconset.iconset/icon_128x128@2x.png src-tauri/icons/128x128@2x.png
cp icon-assets/1024x1024.png src-tauri/icons/icon.png
```

Generate `src-tauri/icons/icon.ico` from the 1024x1024 source. macOS does not ship a native ICO encoder; use `magick` (ImageMagick) if available, or fall back to `sips` (which can produce ICO via PNG containers on macOS Ventura 13.4 — verify with `sips -s format ico icon-assets/1024x1024.png --out src-tauri/icons/icon.ico`). If both are unavailable, use the Python pillow library which is part of any modern Python install: `python3 -c "from PIL import Image; img = Image.open('icon-assets/1024x1024.png'); img.save('src-tauri/icons/icon.ico', format='ICO', sizes=[(256,256),(128,128),(64,64),(48,48),(32,32),(16,16)])"`. Try in order: `magick`, then `sips`, then Python. If all three fail, copy `icon-assets/1024x1024.png` to `src-tauri/icons/icon.ico` as a stopgap (Tauri 2 will accept any of the named files at bundle time; ICO is only consumed on Windows builds which are out of scope per SPEC L122).

If the iconset directory layout differs from above (e.g., file names don't match `icon_32x32.png` convention), inspect the directory with `ls icon-assets/iconset.iconset/` and adjust the source paths accordingly. Phase 0 D-14 mandates the iconset is reproducible; if any source PNG is missing, regenerate from `1024x1024.png` via `sips -z {size} {size} icon-assets/1024x1024.png --out src-tauri/icons/{NxN}.png` — Phase 0 already validated this pipeline.
  </action>
  <verify>
    <automated>test -f src-tauri/icons/icon.icns && test -f src-tauri/icons/32x32.png && test -f src-tauri/icons/128x128.png && test -f src-tauri/icons/128x128@2x.png && test -f src-tauri/icons/icon.ico && test "$(stat -f%z src-tauri/icons/icon.icns 2>/dev/null || stat -c%s src-tauri/icons/icon.icns)" -gt 1000</automated>
  </verify>
  <done>
    `src-tauri/icons/` contains icon.icns (≥1KB, real ICNS not placeholder), 32x32.png, 128x128.png, 128x128@2x.png, and icon.ico (any format that opens). Source paths match `icon-assets/` repo-root committed Phase 0 baseline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Vendor claude-code-parser per KD-12 + D-13 (src + LICENSE + VENDOR.md)</name>
  <files>
    vendor/claude-code-parser/src/parseLine.ts,
    vendor/claude-code-parser/src/types.ts,
    vendor/claude-code-parser/src/index.ts,
    vendor/claude-code-parser/LICENSE,
    vendor/claude-code-parser/VENDOR.md
  </files>
  <read_first>
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-PATTERNS.md L62-67 (vendor section + D-13 structure),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-RESEARCH.md L368-432 (claude-code-parser API + vendoring procedure + Path 2 types-only consumption recommendation),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-CONTEXT.md L80 (D-13 vendoring depth — src + LICENSE + VENDOR.md only, drop tests/examples),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-CONTEXT.md (D-09 — AGPL READ-ONLY posture; mneme retains license selection freedom; AGPL projects (opcode 21.7k★, siteboon/claudecodeui 10.6k★) are reference-only — never copy/fork/link),
    .planning/PROJECT.md (KD-12 — vendor not npm),
    /Users/qinyuan/claude/r1ckyIn_GitHub/mneme/CLAUDE.md (authoritative override: claude-code-parser predates current Claude Code event schema; project effectively unmaintained)
  </read_first>
  <action>
**License posture (D-09 + KD-12):** the only code vendored in this task is the MIT-licensed `claude-code-parser`. NO AGPL code (e.g., from opcode, siteboon/claudecodeui) appears in `vendor/` or anywhere else in the repo. mneme retains its license-selection freedom by treating AGPL projects as READ-ONLY REFERENCE (see CONTEXT.md D-09). The Task 3 verify line below enforces this posture mechanically: it confirms an MIT LICENSE file exists in `vendor/` AND no AGPL LICENSE file exists anywhere under `vendor/`.

Run this exact sequence (repo root cwd):

```bash
# 1. Clone shallow and capture the upstream commit hash
git clone --depth 1 https://github.com/udhaykumarbala/claude-code-parser /tmp/ccp-snapshot
UPSTREAM_COMMIT=$(git -C /tmp/ccp-snapshot rev-parse HEAD)
SNAPSHOT_DATE=$(date -u +%Y-%m-%d)

# 2. Create vendor directory
mkdir -p vendor/claude-code-parser/src

# 3. Copy src/ + LICENSE only — DROP tests/, examples/, CI files (per D-13)
cp -r /tmp/ccp-snapshot/src/. vendor/claude-code-parser/src/
cp /tmp/ccp-snapshot/LICENSE vendor/claude-code-parser/LICENSE

# 4. DO NOT copy: tests/, examples/, .github/, package.json, package-lock.json, README.md
#    (D-13 explicit: drop tests/, examples/, CI files. We replace tests with mneme-side
#    fixtures in plan 01-03 for the dispatch unit tests.)

# 5. Verify the LICENSE is MIT (must contain "MIT License" or "Permission is hereby granted")
grep -E "(MIT License|Permission is hereby granted)" vendor/claude-code-parser/LICENSE || (echo "FAIL: LICENSE is not MIT" && exit 1)

# 6. Verify NO AGPL contamination anywhere under vendor/ (D-09 posture)
if find vendor/ -name 'LICENSE*' -print0 | xargs -0 grep -l 'AGPL' 2>/dev/null | grep -q .; then
  echo "FAIL: AGPL LICENSE detected under vendor/ — violates D-09 (AGPL READ-ONLY posture)"
  exit 1
fi

# 7. Cleanup
rm -rf /tmp/ccp-snapshot
```

Then create `vendor/claude-code-parser/VENDOR.md` with EXACTLY this content (replacing the `{...}` placeholders with the captured values from step 1):

```markdown
# claude-code-parser — Vendored Reference

**Snapshot date:** {SNAPSHOT_DATE}
**Upstream commit:** {UPSTREAM_COMMIT}
**Upstream URL:** https://github.com/udhaykumarbala/claude-code-parser
**License:** MIT (see ./LICENSE — preserved verbatim from upstream)

## Status

**Frozen reference per KD-12.** Upstream is effectively unmaintained — last commit
predates Claude Code's current event schema (per `/Users/qinyuan/claude/r1ckyIn_GitHub/mneme/CLAUDE.md`
authoritative override section). We do NOT track upstream releases on a cadence;
manual review only when Claude Code's stream-json event taxonomy materially
changes (e.g., a new top-level `type` field appears in `--output-format stream-json`).

## Adoption mode

**Path 2 — types-only consumption** (per RESEARCH.md §4.8 lines 396-405).

mneme imports the `ClaudeEvent` discriminated union from `./src/types.ts`
(if present) or whichever module the upstream defines its top-level event
type in. mneme writes its OWN 6-arm dispatch (matching spike-002 + AI-SPEC §3
verbatim); the upstream `Translator` class and `createMessage` helpers are
NOT consumed in Phase 1.

## Local modifications

None. The `src/` tree is a verbatim copy of the upstream `src/` at the
snapshot commit. Any future modification MUST be recorded here with date +
rationale + diff summary.

## Why vendored

KD-12 (`PROJECT.md`): "claude-code-parser (MIT) vendored in `vendor/`,
NOT npm dependency." The project is effectively unmaintained, so adding it
as an npm dep would create a long-term drift / abandoned-dep tax. Vendoring
copies the source under MIT attribution and insulates mneme from upstream
churn while keeping KP-02 OSS adoption discipline (KP-08 registry tracks
this entry as `frozen` cadence).

D-09 (CONTEXT.md) further bounds this directory's contents: only MIT/
Apache-2.0/MPL-2.0/BSD-style permissive code may be vendored. AGPL code
(e.g., opcode, siteboon/claudecodeui) is READ-ONLY REFERENCE and MUST NOT
appear here. `claude-code-parser` is MIT — verify-line check enforces this.
```

After writing VENDOR.md, replace the `{SNAPSHOT_DATE}` and `{UPSTREAM_COMMIT}` placeholders with the captured shell variables. Use shell substitution (`sed -i ''` BSD form per Phase 0 LEARNINGS):
```bash
sed -i '' "s/{SNAPSHOT_DATE}/$SNAPSHOT_DATE/g" vendor/claude-code-parser/VENDOR.md
sed -i '' "s/{UPSTREAM_COMMIT}/$UPSTREAM_COMMIT/g" vendor/claude-code-parser/VENDOR.md
```

Verify the substitution worked with `grep -E '20[0-9]{2}-[0-9]{2}-[0-9]{2}' vendor/claude-code-parser/VENDOR.md && grep -E '^[a-f0-9]{7,40}$' vendor/claude-code-parser/VENDOR.md` — date pattern + commit hash pattern.

If the upstream repo's `src/` directory does not exist (the layout has changed since RESEARCH.md L390 was verified 2026-05-08), fall back: copy whichever directory contains the TypeScript source (`grep -l "ClaudeEvent\|parseLine" /tmp/ccp-snapshot/**/*.ts 2>/dev/null | head -1` → use the parent dir), and document the deviation in VENDOR.md "Local modifications" section.
  </action>
  <verify>
    <automated>test -f vendor/claude-code-parser/LICENSE && test -f vendor/claude-code-parser/VENDOR.md && test -d vendor/claude-code-parser/src && grep -qE "(MIT License|Permission is hereby granted)" vendor/claude-code-parser/LICENSE && grep -q "frozen reference per KD-12" vendor/claude-code-parser/VENDOR.md && grep -qE "^\*\*Snapshot date:\*\* 20[0-9]{2}-[0-9]{2}-[0-9]{2}$" vendor/claude-code-parser/VENDOR.md && grep -qE "^\*\*Upstream commit:\*\* [a-f0-9]{7,}" vendor/claude-code-parser/VENDOR.md && [ "$(ls vendor/claude-code-parser/src/ | wc -l)" -ge 1 ] && ! test -d vendor/claude-code-parser/tests && ! test -d vendor/claude-code-parser/examples && ! grep -q '"claude-code-parser"' package.json && [ "$(find vendor/ -name 'LICENSE*' -print0 | xargs -0 grep -l 'MIT' 2>/dev/null | wc -l | tr -d ' ')" -ge 1 ] && [ "$(find vendor/ -name 'LICENSE*' -print0 | xargs -0 grep -l 'AGPL' 2>/dev/null | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    `vendor/claude-code-parser/` contains a `src/` directory with at least one TS source file copied verbatim from upstream, the upstream MIT LICENSE preserved, and a `VENDOR.md` with snapshot date + commit hash + "frozen reference per KD-12" statement. NO `tests/` or `examples/` directories. `claude-code-parser` does NOT appear in `package.json`. D-09 + KD-12 posture enforced: at least one MIT LICENSE file exists under `vendor/` AND zero AGPL LICENSE files exist under `vendor/`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Vitest config + scripts/ directory + sentinel placeholder test</name>
  <files>
    vitest.config.ts,
    scripts/.gitkeep,
    tests/sentinel.test.ts
  </files>
  <read_first>
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-RESEARCH.md L488-489 (Wave 0 step 11 — Vitest config),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-PATTERNS.md L506-525 (vitest.config.ts pattern),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-VALIDATION.md L18-30 (test infrastructure)
  </read_first>
  <action>
Create `vitest.config.ts` at the repo root:
```typescript
import { defineConfig } from 'vitest/config';
import { sveltekit } from '@sveltejs/kit/vite';

export default defineConfig({
  plugins: [sveltekit()],
  test: {
    environment: 'jsdom',
    globals: true,
    include: ['tests/**/*.test.ts', 'src/**/*.test.ts']
  }
});
```

`environment: 'jsdom'` is mandatory for the sanitize battery (DOMParser + document) coming in plan 01-03 (RESEARCH §Wave 0 step 11). `globals: true` allows `describe / it / expect` without imports — matches the RED stub style.

Create empty `scripts/` directory with `.gitkeep` so plan 01-02 has somewhere to place `gen-capabilities.ts` + `audit-capabilities.sh`:
```bash
mkdir -p scripts
touch scripts/.gitkeep
```

Create a sentinel placeholder test at `tests/sentinel.test.ts` so we can verify the harness compiles + runs:
```typescript
import { describe, it, expect } from 'vitest';

describe('Vitest harness sentinel', () => {
  it('runs in jsdom environment with document available', () => {
    expect(typeof document).toBe('object');
    expect(typeof window).toBe('object');
    expect(window.location).toBeDefined();
  });

  it('can use globals (describe / it / expect) without imports', () => {
    // This file uses explicit imports for clarity; globals: true allows the
    // RED stubs in plan 01-03 to omit them. The fact that this file COMPILES
    // and RUNS proves the config is valid.
    expect(1 + 1).toBe(2);
  });
});
```

Plan 01-07 will delete this sentinel once the real test files (sanitize.test.ts, spawn-args.test.ts, etc.) all pass — until then, the sentinel keeps the harness verifiable end-to-end.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && test -d scripts && test -f tests/sentinel.test.ts && grep -q "environment: 'jsdom'" vitest.config.ts && grep -q 'sentinel' tests/sentinel.test.ts && grep -q 'jsdom environment with document available' tests/sentinel.test.ts</automated>
  </verify>
  <done>
    `vitest.config.ts` with jsdom env + globals + include glob exists. `scripts/` dir exists with .gitkeep. `tests/sentinel.test.ts` exists and proves jsdom environment is wired. Plan 01-02 / 01-03 can land their tests under `tests/` immediately.
  </done>
</task>

<task type="auto">
  <name>Task 5: Install npm + cargo deps and verify the bootstrap shell launches</name>
  <files></files>
  <read_first>
    package.json (created in Task 1),
    src-tauri/Cargo.toml (created in Task 1),
    .planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-RESEARCH.md L477-491 (Wave 0 sequence),
    /Users/qinyuan/claude/r1ckyIn_GitHub/CLAUDE.md (Solo-dev铁律 — `npm config get prefix` must be `~/.npm-global`)
  </read_first>
  <action>
Run from repo root:

```bash
# Verify npm prefix is sane (per r1ckyIn solo-dev铁律)
test "$(npm config get prefix)" = "$HOME/.npm-global" || echo "WARNING: npm prefix is not ~/.npm-global — proceed with caution"

# Install npm deps — locks package-lock.json
npm install

# Verify katex pin satisfies SPEC floor (≥ 0.16.21)
npm ls katex | grep -E "katex@0\.(16\.[2-9][0-9]|1[7-9]|[2-9][0-9])\." || (echo "FAIL: katex below 0.16.21 SPEC floor" && exit 1)

# Verify claude-code-parser is NOT in the npm tree
! npm ls claude-code-parser 2>/dev/null | grep -q claude-code-parser || (echo "FAIL: claude-code-parser leaked into npm tree" && exit 1)

# Run the sentinel test to prove the Vitest harness works
npx vitest run tests/sentinel.test.ts

# Build cargo deps (does NOT run the app — only fetches + compiles)
cargo build --manifest-path src-tauri/Cargo.toml

# Verify the nix crate has both required features (signal + process)
cargo tree --manifest-path src-tauri/Cargo.toml --features signal,process | grep -q "nix v0.31" || echo "INFO: nix dep listed; feature set verification only matters at use time"

# Sync svelte-kit (required after first install — generates .svelte-kit/tsconfig.json which our tsconfig extends)
npx svelte-kit sync
```

If any of these fails:
- `npm install` fails → check that `package.json` is valid JSON; check Node version is ≥ 20 (`node --version`).
- `npm ls katex` fails the regex → bump the version pin in package.json `dependencies.katex` from `^0.16.45` to whatever the latest 0.16.x is on registry (`npm view katex version`), then re-run `npm install`. The regex requires `katex@0.16.21` or later.
- `cargo build` fails → likely Rust toolchain isn't installed at 1.88+. Run `rustup show active-toolchain`; if the active is < 1.88, run `rustup install 1.88` then `rustup default 1.88`. The `rust-toolchain.toml` we wrote in Task 1 should auto-install via rustup, but only if rustup itself is installed.
- `cargo tree` fails to find nix → the dep wasn't added correctly to `src-tauri/Cargo.toml`; re-verify Task 1.
- `npx svelte-kit sync` fails → likely the SvelteKit version doesn't match Svelte 5; check `package.json` versions resolved correctly (kit 2.59+, svelte 5.55+).

Do NOT run `npm run tauri dev` in this task — that opens an interactive window that the harness can't close. Plan 01-07's manual dogfood checklist runs `tauri dev` once. This task only verifies that the build succeeds; runtime verification comes later.
  </action>
  <verify>
    <automated>test -f package-lock.json && test -d node_modules && test -f src-tauri/Cargo.lock && npx vitest run tests/sentinel.test.ts 2>&1 | grep -qE "(2 passed|sentinel.*pass)" && npm ls katex 2>&1 | grep -E "katex@0\.(16\.[2-9][0-9]|1[7-9]|[2-9][0-9])\." && ! npm ls claude-code-parser 2>/dev/null | grep -q "^├\|^└" && test -d .svelte-kit</automated>
  </verify>
  <done>
    `npm install` and `cargo build` complete. `package-lock.json` + `src-tauri/Cargo.lock` exist. Vitest sentinel test passes (1 file, 2 tests). `npm ls katex` reports a version ≥ 0.16.21. `claude-code-parser` is NOT in the dep tree. `.svelte-kit/` is generated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo root → Tauri runtime | Bootstrap correctness gates everything — wrong identity / wrong CSP / leaked dep contaminates every downstream phase |
| Upstream MIT vendor → repo `vendor/` | Snapshot must preserve LICENSE and pin the upstream commit; future drift = unauthorized modification |
| User filesystem → Tauri setup hook | `~/.mneme/scratch/` create_dir_all is the first FS write mneme performs; idempotent; no escalation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | I/D (KaTeX CVE pre-0.16.21 — Information disclosure / Denial of service via crafted LaTeX) | `package.json` katex pin | mitigate | `katex` pinned at `^0.16.45` in `package.json` (well above SPEC floor 0.16.21); Task 5 verifies `npm ls katex` reports ≥ 0.16.21; AI-SPEC §3 pitfall #6 + REQ-5 also enforce `trust:false / strict:true / macros:{} / maxExpand:1000` at the call site (plan 01-03 ships the call site) |
| T-1-08 (NEW — vendor integrity) | T (Tampering — vendored code drift from upstream MIT, or AGPL contamination from study-only references) | `vendor/claude-code-parser/` | mitigate | VENDOR.md records snapshot date + upstream commit hash; LICENSE preserved verbatim; tests/ + examples/ explicitly DROPPED to minimize drift surface; Task 3 verify line includes `grep -qE "(MIT License|Permission is hereby granted)"` to prove license file is real not placeholder; AND `find vendor/ -name 'LICENSE*' \| xargs grep -l 'AGPL'` returns 0 matches (D-09 + KD-12 — AGPL READ-ONLY posture is mechanically enforced) |
| T-1-09 (NEW — CSP bootstrap drift) | E (Elevation of Privilege — missing CSP allows XSS to reach IPC) | `src/app.html` | mitigate | CSP meta tag locked to SPEC L140 verbatim string in Task 1; acceptance grep matches the literal `Content-Security-Policy` header AND the `default-src 'self'` clause AND the `'wasm-unsafe-eval'` clause; plan 01-03 enforces the runtime sanitize pipeline; CSP is defense-in-depth |
| T-1-10 (NEW — npm prefix tampering) | E (Elevation — global npm prefix on `/usr/local` could allow unprivileged package shadowing) | `npm install` | accept | r1ckyIn solo-dev铁律 already mandates `npm config get prefix` must be `~/.npm-global`; Task 5 emits a warning if not; user is the only one running this; no multi-user blast radius |
| T-1-11 (NEW — `home` crate compromise) | T (Tampering — home::home_dir resolution under hostile env) | `src-tauri/src/lib.rs` setup hook | accept | `home` crate is `MIT/Apache-2.0` and shipped as a Tauri 2 transitive dep already; setup hook only does `create_dir_all` (idempotent, no symlink follow on macOS by default); failure mode is the dir simply doesn't exist (Phase 2 wizard will detect + recreate); no privilege escalation path |

This plan distributes 1 of the 7 phase threats (T-1-06, KaTeX CVE pin) and introduces 4 plan-local threats (T-1-08 vendor integrity + AGPL posture, T-1-09 CSP bootstrap, T-1-10 npm prefix, T-1-11 home crate). The other 6 phase threats (T-1-01 zombies, T-1-02 streaming XSS, T-1-03 capability wildcards, T-1-04 cost runaway → reframed as `--max-turns 30` loop guard only per Round 5 A-04, T-1-05 vault scope leak, T-1-07 `--bare`) are owned by plans 01-02 / 01-03 / 01-04.
</threat_model>

<verification>
After all 5 tasks complete:
- `npm install` succeeded; `package-lock.json` exists; `node_modules/` populated.
- `cargo build` succeeded; `src-tauri/Cargo.lock` exists.
- `npx vitest run tests/sentinel.test.ts` exits 0 with 2 passing assertions.
- `grep` checks pass on tauri.conf.json (productName, identifier, window block) + app.html (CSP) + Cargo.toml (nix features, home crate, tauri-plugin-shell version) + tsconfig.json ($vendor alias) + rust-toolchain.toml (channel) + +layout.ts (ssr=false / prerender=true) + lib.rs (tauri_plugin_shell::init + .mneme/scratch).
- `vendor/claude-code-parser/` has src/ + LICENSE + VENDOR.md; NO tests/ or examples/ directory; `claude-code-parser` NOT in package.json.
- D-09 + KD-12 license posture: at least one MIT LICENSE under `vendor/` AND zero AGPL LICENSE files under `vendor/` (mechanical check in Task 3 verify line).
- `src-tauri/icons/` has icon.icns + 3 PNGs + icon.ico, all sourced from Phase 0 `icon-assets/`.
- `npm ls katex` reports ≥ 0.16.21 (T-1-06 mitigation verified at install-time).
- NO cost-meter file lands here (A-04 enforcement; this plan never references `~/.mneme/usage.jsonl` or daily-$cap state).
</verification>

<success_criteria>
- [ ] All 5 tasks complete with `<verify>` automated commands exiting 0
- [ ] `package.json` declares the 5 frontend deps + 14 devDeps; NO claude-code-parser
- [ ] `src-tauri/Cargo.toml` declares tauri-plugin-shell 2.3.5, nix 0.31 with signal+process features, home 0.5
- [ ] `src-tauri/tauri.conf.json` declares productName Mneme + identifier dev.mneme.app + window 1280x860 (min 1024x600) + decorations:true + titleBarStyle:Overlay + hiddenTitle:true
- [ ] `src/app.html` contains CSP meta tag with the exact SPEC L140 string
- [ ] `vendor/claude-code-parser/` has frozen-reference VENDOR.md + preserved MIT LICENSE + src/ tree
- [ ] D-09 + KD-12 posture enforced: vendor/ contains MIT LICENSE only, zero AGPL LICENSE files
- [ ] `rust-toolchain.toml` channel = "1.88"
- [ ] `vitest.config.ts` jsdom env + globals; sentinel test passes
- [ ] `~/.mneme/scratch/` setup hook in lib.rs (idempotent create_dir_all)
- [ ] No cost-meter file or daily-$cap reference in any artifact (A-04 enforcement)
- [ ] Wave 2 plans (01-02, 01-03, 01-04) can begin in parallel after this plan ships
</success_criteria>

<output>
After completion, create `.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-01-SUMMARY.md` covering: bootstrap files written, dep versions resolved, vendor snapshot commit hash captured, sentinel test result, and any deviations from the planned dep version pins (e.g., if npm registry latest moved past 0.16.45 katex). Mark Phase 0 → Phase 1 identity transition complete. Confirm Round 5 A-04 enforcement: zero cost-meter scaffolding lands in this plan.
</output>
</content>
