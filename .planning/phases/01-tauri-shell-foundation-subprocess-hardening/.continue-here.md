---
context: phase
phase: 01-tauri-shell-foundation-subprocess-hardening
task: 4
total_tasks: 5
status: paused_for_exploration
last_updated: 2026-05-11T09:46:39Z
---

# Phase 1 — Continue Here (paused for /gsd-explore test-automation)

> Paused 2026-05-11 ~09:46 UTC mid-dogfood. User hit fatigue on the 47-row
> manual visual checklist after the 17/18 AUTO half completed. Decided to
> step out into `/gsd-explore test-automation` Socratic session before
> grinding through the rest manually. Phase 1 is 88% done; only dogfood +
> SUMMARY writing block ship.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| skip-planner-on-debug-fix | Plan 01-10 H4 fix was applied via /gsd-debug investigation in main thread, bypassing planner→executor sequence. Retroactive PLAN.md preserved trail but caused process debt. | advisory | Next gap-closure (01-11 CSP) MUST go via `/gsd-execute-phase 1 --gaps-only` so planner agent writes plan BEFORE executor edits files. |
| Tauri-2-macOS-CDP-attach | Attempting to connect chrome-devtools or playwright MCP to Tauri 2 WKWebView via remote-debug-port. Walked this path mid-session. | blocking | macOS WKWebView exposes Webkit Inspector Protocol, NOT Chrome DevTools Protocol. The two are incompatible — no flag or port config bridges them. Skip this entire avenue; use @testing-library/svelte for components + Playwright on vite-dev-server (5173) for E2E layer (no Tauri runtime, but covers C/H/I sections), and accept that real Tauri webview cases (window drag / Cmd+Q / native chrome) stay manual until tauri-driver macOS matures. |
| Socratic-auto-skip | Auto-skipping `/gsd-explore` / `/gsd-spec-phase` / `/gsd-discuss-phase` interactive interview when session-level "no clarifying questions" is set. | blocking | Memory `feedback_socratic_no_auto_skip.md` is explicit: Socratic commands override session-level no-clarifying directive. Walk the 6 rounds in full. |
| dogfood-checklist-drift | dogfood-checklist.md A-06/A-07 row text drifted from actual components (MindMapBar 2-line structure, Lecture/PDF Phase numbers inverted) since plan 01-09 Task 11 only synced A-09 + A-34. Caught during this session's pre-dogfood review. | advisory | Every plan that modifies a Phase 1 component MUST verify dogfood-checklist.md row text still matches that component's rendered output, not just commit acceptance. Treat dogfood-checklist.md as a documentation surface co-owned with each visual component. |

## Required Reading (in order)

1. `.planning/HANDOFF.json` — full structured state (machine-readable parallel of this file)
2. `.planning/phases/01-tauri-shell-foundation-subprocess-hardening/01-10-PLAN.md` — H4 capability fix retroactive plan; explains why core:window:allow-start-dragging was missing + how SSOT generator path was followed
3. `tests/manual/dogfood-checklist.md` — current state, AUTO rows have `[AUTO 2026-05-10 ✅]` markers + evidence
4. `~/Downloads/mneme-dogfood-visual-2026-05-10.html` — the 47 visual rows; localStorage persists tick state across browser restarts (key: `mneme-dogfood-visual-2026-05-10`)
5. `.planning/CLAUDE.md` (project-level) — Anthropic/Claude aesthetic family (KP-09 / KD-13) — relevant if test-automation explore proposes screenshot-diff tooling

<current_state>
Phase 1 plan 01-07 Task 4 (dogfood walkthrough) is in-progress, split into:
- **AUTO half** ✅ done — 17 of 18 AUTO rows verified by Claude on 2026-05-10. Evidence written as `[AUTO 2026-05-10 ✅ <result>]` markers on each row in `tests/manual/dogfood-checklist.md`. The 18th (E-01 preflight) is HYBRID — false-positive in active Claude Code session; explicitly deferred to user with mitigation guidance.
- **VISUAL half** ⏳ in-progress — user partway through 47 rows in `~/Downloads/mneme-dogfood-visual-2026-05-10.html` (KP-09 styled, localStorage-backed). Section A-13/A-14 (REQ-3 console verification) was the last visible row at pause.

User triggered `/gsd-explore test-automation` because manual dogfood burden (repeated dev-server restarts, manual ⌘⌥I console inspections, manual ⌘Q lifecycle observations) felt disproportionate vs the goal. Phase 1 ship gate is now:

dogfood approval (visual + section E) + 01-10-SUMMARY.md + 01-07 Task 5 (frontmatter flip) → Phase 1 done.

The exploration's outcome decides whether we grind the remaining ~40 manual rows to ship NOW (path a — recommended) or invest 1-2 days in a test-foundation plan first (path b/c — Phase 2+ benefit).
</current_state>

<completed_work>

This session (2026-05-10/11):
- H1 devtools probe (`window.__TAURI_INTERNALS__` + `getCurrentWindow().startDragging()`) — identified H4 (capability missing) as root cause for window-drag T-1-49
- Plan 01-10 capability fix applied:
  - `scripts/gen-capabilities.ts` — added `"core:window:allow-start-dragging"` permission
  - `src-tauri/capabilities/default.json` — regenerated
  - `src/routes/+page.svelte` — removed misleading onMount JS startDragging fallback (30 lines + wrong comment) + 4 lines of `cursor: grab/grabbing` CSS
- Plan 01-10-PLAN.md written retroactively (`retroactive: true` frontmatter — preserves trail despite process violation)
- Audit PASS + cargo auto-rebuild 12.07s + HMR-verified pure-native data-tauri-drag-region path works
- Commit `fcd939a` — fix(phase-01): add window-drag capability permission
- 17 AUTO dogfood rows verified by Claude with evidence written into `tests/manual/dogfood-checklist.md`
- A-06 / A-07 row text corrected to match plan 01-09 reality (MindMapBar 2-line structure, prototype Mneme.html L1261/L1281 Phase 4/6 inversion)
- A-19 target file updated (src/app.html → svelte.config.js, per plan 01-08 ownership migration)
- HTML visual checklist generated at `~/Downloads/mneme-dogfood-visual-2026-05-10.html` (47 rows, KP-09 styled, sticky progress bar, localStorage persistence, sign-off blob generator)
</completed_work>

<remaining_work>

- **Next action**: `/gsd-explore test-automation` (Socratic 6-round interview)
- After explore: either
  - **Path a** — grind manual VISUAL dogfood (40 remaining rows + Section E) → 01-10-SUMMARY.md → 01-07 Task 5 → Phase 1 ship
  - **Path b/c** — plan 01-13 test-foundation (vitest + @testing-library/svelte + Playwright on vite-dev-server) → re-run dogfood with most rows now automated → Phase 1 ship
- Plan 01-11 CSP `connect-src` `ipc:` gap closure — independent of dogfood, can run anytime via `/gsd-execute-phase 1 --gaps-only`
- Plan 01-10-SUMMARY.md (5 sections covering H4 fix + audit pass + cleanup verification + dogfood A-04 unblocking)
- Plan 01-07 Task 5 — flip `01-VALIDATION.md` frontmatter `wave_0_complete:true` / `nyquist_compliant:true` once dogfood signs off
- Commit pending: `tests/manual/dogfood-checklist.md` (AUTO markers + A-06/A-07 fix) — bundled into this WIP commit
</remaining_work>

<decisions_made>

1. **Plan 01-10 written retroactively** — main thread skipped planner→executor on H4 fix; acknowledged as process violation. Plan trail preserved via `retroactive: true` frontmatter. Future gap-closures MUST go via `/gsd-execute-phase 1 --gaps-only`.
2. **Dogfood split AUTO + VISUAL** — Claude runs 18 AUTO rows (file/grep/test); user runs 47 VISUAL rows. AUTO marker convention: `[AUTO 2026-05-10 ✅ <evidence>]` in `tests/manual/dogfood-checklist.md`.
3. **A-19 target migrated** — src/app.html → svelte.config.js (plan 01-08 moved CSP ownership; original row was stale).
4. **A-06 / A-07 row text re-aligned to prototype** — dogfood-checklist.md had drifted from plan 01-09 component implementations.
5. **Tauri WKWebView remote-debug-attach (D-path) abandoned** — verified WKWebView ≠ Chromium at protocol level. Test infrastructure must use a different stack.
6. **STATE.md not touched by main thread** — orchestrator commands (`/gsd-resume-work` / `/gsd-progress`) own STATE.md sync per existing anti-pattern.
</decisions_made>

<blockers>

- **Manual dogfood fatigue**: 47 visual rows requiring repeated dev-server restart + console inspection felt disproportionate; triggered `/gsd-explore test-automation` exploration. Mitigation: explore session in progress.
- **E-01 false-positive in active Claude Code session**: session's ripgrep + MCP servers produce ~50+ rows under preflight grep. Mitigation: narrow grep to `[c]laude --print` OR run E-01 in a non-Claude-Code terminal. Documented in checklist.
- **Plan 01-08 CSP gap (deferred)**: `connect-src` missing `ipc:` causes 2 console errors at every launch (Tauri IPC falls back to postMessage). Not blocking ship but should be plan 01-11 before declaring Phase 1 fully done.
</blockers>

## Infrastructure State

- **Tauri dev server**: shell `bqukkjdk8` background, Mneme window open (Vite ready 1.19s + cargo finished 0.96s). User SHOULD ⌘Q Mneme before `/clear` so the subprocess exits clean and stdout flushes to `/tmp/mneme-tauri-dev.log`.
- **Working tree**: `tests/manual/dogfood-checklist.md` modified, will be bundled into this WIP commit.
- **HEAD**: `fcd939a` (plan 01-10 capability fix).
- **Branch**: `main` (project `branching_strategy:none`).
- **Visual checklist HTML**: `~/Downloads/mneme-dogfood-visual-2026-05-10.html` — opened in default browser; localStorage key `mneme-dogfood-visual-2026-05-10` holds tick state.

## Pre-Execution Critique Required

Not applicable — we are not pausing between a design artifact and its execution. Plan 01-10 is fully landed + committed. Plan 01-11 (CSP) and plan 01-13 (test-foundation) are both pre-planning; they will go through their own planner→executor cycles when started.

<context>
The exploration is the right move IF the resuming session has any appetite for Phase 1 schedule slip. The risk is grinding 40 manual rows under fatigue and either making errors or just stopping. The exploration session should produce a small set of concrete options (15-30 min decision; not 2 hours of planning) and lead back to either path a or path b/c quickly.

If `/gsd-explore` produces "definitely do test-foundation now" — open plan 01-13 with /gsd-plan-phase or /gsd-execute-phase --gaps-only routing (NOT in main thread).

If `/gsd-explore` produces "just ship Phase 1 manually" — come back to this checkpoint, ⌘⏎ resume dogfood from where localStorage saved, finish 40 rows, write 01-10-SUMMARY + 01-07 Task 5 frontmatter flip + commit, declare Phase 1 done.

The user's strongest implicit ask: don't waste their time. Both paths above honor that — path b takes 1-2 days but saves 30-40 future dogfood hours; path a takes 1-3 hours but inherits the same dogfood burden every Phase 2-10.
</context>

<next_action>
Start with: `/clear` then `/gsd-explore test-automation`

Before /clear: ⌘Q the Mneme window so Tauri dev server background shell `bqukkjdk8` exits clean. (Not strictly required — shell will auto-clean when terminal closes — but cleaner state.)

After /gsd-explore concludes: read this file again, read .planning/HANDOFF.json, decide path a or b/c, route accordingly.
</next_action>
