---
phase: 00-identity-branding-lock
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - .planning/phases/00-identity-branding-lock/scripts/build-icon.sh
  - .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh
  - icon-assets/1024x1024.png
  - icon-assets/icon.svg  # optional — only if Claude Design SVG refinement succeeds
  - icon-assets/icon.icns
  - icon-assets/iconset.iconset/icon_16x16.png
  - icon-assets/iconset.iconset/icon_16x16@2x.png
  - icon-assets/iconset.iconset/icon_32x32.png
  - icon-assets/iconset.iconset/icon_32x32@2x.png
  - icon-assets/iconset.iconset/icon_128x128.png
  - icon-assets/iconset.iconset/icon_128x128@2x.png
  - icon-assets/iconset.iconset/icon_256x256.png
  - icon-assets/iconset.iconset/icon_256x256@2x.png
  - icon-assets/iconset.iconset/icon_512x512.png
  - icon-assets/iconset.iconset/icon_512x512@2x.png
autonomous: false  # gpt-image-1 generation + sketch pick are user checkpoints (CD-03)
success_criteria_covered: [SC-2]
tdd_applicable: false
tdd_rationale: "Asset production via external vendor (gpt-image-1) + Apple CLI tooling (sips + iconutil). The shell scripts ARE the verification (verify-icon.sh runs `file` + `iconutil -V`); no logic to test."
validation_map: [V-02]
requirements: [SC-2]
user_setup:
  - service: openai-chatgpt
    why: "gpt-image-1 (ChatGPT image2.0) generates icon concept sketches per D-06"
    env_vars: []
    dashboard_config:
      - task: "Confirm ChatGPT Plus or higher subscription tier"
        location: "https://chatgpt.com/#settings → Subscription"
        rationale: "Free tier rate-limits image generation to ~3/day; D-06 requires 6-8 sketches in one session"
  - service: claude-design
    why: "Optional D-06 stage 4 — SVG refinement of chosen sketch"
    env_vars: []
    dashboard_config:
      - task: "Confirm Claude Pro/Max access (artifacts feature required)"
        location: "https://claude.ai/settings → Plan"
        rationale: "Claude Design generates SVG inside HTML artifacts (RESEARCH.md D-09); requires Pro/Max tier"

must_haves:
  truths:
    - "Wave 0 verification scripts (build-icon.sh + verify-icon.sh) exist and are executable"
    - "User has selected one icon sketch (or refined SVG) as the master 1024×1024 source"
    - "icon-assets/icon.icns is a valid Mac OS X icon containing 10 size variants"
    - "Master 1024×1024 PNG and the iconset folder are kept at repo root for future re-runs"
  artifacts:
    - path: ".planning/phases/00-identity-branding-lock/scripts/build-icon.sh"
      provides: "Reproducible PNG → ICNS pipeline (sips × 10 + iconutil)"
      contains: "iconutil -c icns"
    - path: ".planning/phases/00-identity-branding-lock/scripts/verify-icon.sh"
      provides: "V-02 automated verification (file format + 10-variant count)"
      contains: "Mac OS X icon"
    - path: "icon-assets/icon.icns"
      provides: "Final macOS Dock icon for Phase 1 src-tauri/icons/"
      min_size_bytes: 10000
    - path: "icon-assets/1024x1024.png"
      provides: "Master raster source — re-runnable input to build-icon.sh"
      contains: "PNG image data"
  key_links:
    - from: "icon-assets/1024x1024.png"
      to: "icon-assets/iconset.iconset/*.png"
      via: "sips -z resize commands in build-icon.sh"
      pattern: "sips -z [0-9]+ +[0-9]+"
    - from: "icon-assets/iconset.iconset/"
      to: "icon-assets/icon.icns"
      via: "iconutil -c icns invocation"
      pattern: "iconutil -c icns"
---

<objective>
Produce the macOS Dock icon (`icon-assets/icon.icns` + 10 PNG variants + 1024×1024 master) for the locked app name, using the D-06 pipeline: gpt-image-1 sketches → user picks → optional Claude Design SVG refinement → `sips` resize → `iconutil` packaging. Author and commit the reproducible `build-icon.sh` and `verify-icon.sh` scripts so future-self can regenerate the iconset from a new master without rediscovery.

Purpose: SC-2 demands the ICNS exists in the repo before Plan 04's pre-publish gate. The icon is also a Phase 1 input — Phase 1's `src-tauri/icons/` will copy the ICNS produced here. The graph-node + monogram concept (D-05) reflects the project's KG dual-layer architecture (REQ-07).

Output: `icon-assets/` directory at repo root containing master PNG + iconset folder + ICNS, plus two committed shell scripts in `.planning/phases/00-identity-branding-lock/scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-identity-branding-lock/00-CONTEXT.md
@.planning/phases/00-identity-branding-lock/00-RESEARCH.md
@.planning/phases/00-identity-branding-lock/00-VALIDATION.md
@.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md

<interfaces>
<!-- Wave 0 verification scripts are AUTHORED in this plan (Task 1) and used in Task 4. -->
<!-- Plan 03 also calls verify-icon.sh as part of its pre-commit gate. -->

build-icon.sh contract (from RESEARCH.md "Code Examples → Example 2"):
  - Input: $1 = source 1024×1024 PNG path (default: icon-assets/1024x1024.png)
  - Output: icon-assets/icon.icns + icon-assets/iconset.iconset/*.png (10 files)
  - Behavior: idempotent — safe to re-run; overwrites existing iconset/ICNS

verify-icon.sh contract (V-02 from VALIDATION.md):
  - Input: none (uses fixed path icon-assets/icon.icns)
  - Exit code: 0 if ICNS valid + ≥10 image-format entries; non-zero otherwise
  - Behavior: pure read-only verification

Apple iconset spec (RESEARCH.md "Apple HIG-compliant sizes for ICNS"):
  Filename                  → Pixel dimensions
  icon_16x16.png            → 16 × 16
  icon_16x16@2x.png         → 32 × 32
  icon_32x32.png            → 32 × 32
  icon_32x32@2x.png         → 64 × 64
  icon_128x128.png          → 128 × 128
  icon_128x128@2x.png       → 256 × 256
  icon_256x256.png          → 256 × 256
  icon_256x256@2x.png       → 512 × 512
  icon_512x512.png          → 512 × 512
  icon_512x512@2x.png       → 1024 × 1024
</interfaces>

<environment_check>
Verified available (RESEARCH.md "Environment Availability"):
  - /usr/bin/iconutil (built-in)
  - /usr/bin/sips (sips-316, built-in)
  - /usr/local/bin/gh 2.83.2

NOT available (fallbacks documented):
  - librsvg / rsvg-convert — fallback: sips handles SVG → PNG natively (with caveats for complex SVGs)
  - imagemagick / magick — fallback: macOS Preview app for alpha-channel hand-fix

BLOCKED (cannot use, no fallback within Phase 0 time-box):
  - Apple Icon Composer — requires Xcode 26+ → macOS Sequoia 15.6+; user is on Ventura 13.7.8 (RESEARCH.md verified via sw_vers)
</environment_check>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Author build-icon.sh + verify-icon.sh + load NAME-DECISION</name>
  <files>
    .planning/phases/00-identity-branding-lock/scripts/build-icon.sh
    .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh
  </files>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md (extract `final_name` and `final_name_display` from frontmatter)
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (sections "Pattern 3: PNG → ICNS" lines 291-328 + "Code Examples → Example 2" lines 1003-1044 + "Common Pitfalls → Pitfall 4" lines 873-898)
    - .planning/phases/00-identity-branding-lock/00-VALIDATION.md (V-02 row + Wave 0 Requirements section)
  </read_first>
  <action>
    Extract `<finalname>` from NAME-DECISION.md frontmatter (e.g., `FINAL=$(grep '^final_name:' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md | awk '{print $2}')`). Hold this in a session variable for use across tasks.

    **1a. Create `.planning/phases/00-identity-branding-lock/scripts/build-icon.sh`** (chmod +x):

    Use the canonical pipeline from RESEARCH.md "Code Examples → Example 2" verbatim, with these adjustments:
    - Set `set -euo pipefail` at top
    - SRC default: `icon-assets/1024x1024.png` (relative to repo root — script is called from repo root)
    - ICONSET default: `icon-assets/iconset.iconset` (extension `.iconset` is REQUIRED — Pitfall 4)
    - OUT_ICNS default: `icon-assets/icon.icns`
    - Pre-validate: `test -s "$SRC"` AND `sips -g pixelWidth -g pixelHeight "$SRC"` returns "1024 1024"
    - Generate exactly 10 PNG variants via 10 sips calls (mapping per `<interfaces>` block above)
    - Pre-iconutil sanity check: loop over the 10 expected filenames; abort if any is missing or zero-byte (Pitfall 4 mitigation):
      ```bash
      EXPECTED=(icon_16x16.png icon_16x16@2x.png icon_32x32.png icon_32x32@2x.png \
                icon_128x128.png icon_128x128@2x.png icon_256x256.png icon_256x256@2x.png \
                icon_512x512.png icon_512x512@2x.png)
      for f in "${EXPECTED[@]}"; do
        test -s "$ICONSET/$f" || { echo "MISSING: $f"; exit 1; }
      done
      ```
    - Pack: `iconutil -c icns "$ICONSET" -o "$OUT_ICNS"`
    - Post-verify: `file "$OUT_ICNS" | grep -q "Mac OS X icon"` else exit 1
    - Echo final size: `echo "ok — $OUT_ICNS ($(stat -f%z "$OUT_ICNS") bytes)"`

    Use English-only code comments per L1 CLAUDE.md. Header comment at top:
    ```bash
    #!/usr/bin/env bash
    # build-icon.sh — Apple-canonical PNG → ICNS pipeline.
    # Source: .planning/phases/00-identity-branding-lock/00-RESEARCH.md "Pattern 3"
    # Idempotent — safe to re-run after master PNG is regenerated.
    # Run from repo root.
    ```

    **1b. Create `.planning/phases/00-identity-branding-lock/scripts/verify-icon.sh`** (chmod +x):

    Implement V-02 from VALIDATION.md exactly:
    ```bash
    #!/usr/bin/env bash
    # verify-icon.sh — V-02 automated check (VALIDATION.md row 2).
    # Returns 0 if icon-assets/icon.icns is a valid Mac OS X icon with ≥ 10 size variants.
    set -euo pipefail
    ICNS=icon-assets/icon.icns
    test -f "$ICNS" || { echo "FAIL: $ICNS missing"; exit 1; }
    file "$ICNS" | grep -q 'Mac OS X icon' || { echo "FAIL: not Mac OS X icon format"; exit 1; }
    VARIANT_COUNT=$(iconutil -V "$ICNS" 2>&1 | grep -c 'image format' || true)
    [ "$VARIANT_COUNT" -ge 10 ] || { echo "FAIL: only $VARIANT_COUNT variants (need ≥ 10)"; exit 1; }
    echo "ok — $ICNS ($VARIANT_COUNT variants, $(stat -f%z "$ICNS") bytes)"
    ```

    **1c. Make both scripts executable:**
    ```bash
    chmod +x .planning/phases/00-identity-branding-lock/scripts/build-icon.sh
    chmod +x .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh
    ```

    **1d. Sanity-check scripts with `bash -n`** (parse-only check; no execution because no master PNG exists yet):
    ```bash
    bash -n .planning/phases/00-identity-branding-lock/scripts/build-icon.sh
    bash -n .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh
    ```
  </action>
  <verify>
    <automated>test -x .planning/phases/00-identity-branding-lock/scripts/build-icon.sh && test -x .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh && bash -n .planning/phases/00-identity-branding-lock/scripts/build-icon.sh && bash -n .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh && grep -c 'sips -z' .planning/phases/00-identity-branding-lock/scripts/build-icon.sh | xargs -I{} test {} -ge 10 && grep -q 'iconutil -c icns' .planning/phases/00-identity-branding-lock/scripts/build-icon.sh && grep -q 'Mac OS X icon' .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts exist at the exact paths under `.planning/phases/00-identity-branding-lock/scripts/`
    - Both scripts are executable (`-x` test passes)
    - `bash -n` parse-check passes for both (no syntax errors)
    - build-icon.sh contains exactly 10 `sips -z` invocations (one per Apple iconset filename)
    - build-icon.sh contains the literal string `iconutil -c icns`
    - build-icon.sh contains the pre-iconutil EXPECTED-array sanity check (Pitfall 4 mitigation)
    - verify-icon.sh contains the literal string `Mac OS X icon`
    - All code comments are English-only (`! grep -q '中文\|文件\|图标' .planning/phases/00-identity-branding-lock/scripts/build-icon.sh`)
  </acceptance_criteria>
  <done>
    Reproducible build pipeline scripts are committed-ready. Future-self can regenerate the iconset from a new master PNG via `bash .planning/phases/00-identity-branding-lock/scripts/build-icon.sh`.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Generate 8 sketches via gpt-image-1 (ChatGPT image2.0)</name>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-CONTEXT.md (D-05 graph-node + monogram concept anchor + D-06 pipeline + CD-03 prompt drafted by Claude)
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (sections "Pattern 1: ChatGPT image2.0 → gpt-image-1" lines 256-275 + "Common Pitfalls → Pitfall 3 transparency holes" lines 859-871 + "Code Examples → Example 1" lines 970-1000)
    - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md (read `final_name_display` for monogram letter)
  </read_first>
  <what-built>
    Claude has drafted the gpt-image-1 prompt below per CD-03. User runs ChatGPT image2.0 with this prompt to generate 8 concept sketches.
  </what-built>
  <how-to-verify>
    **Step A — Claude drafts the prompt (CD-03):**

    Read `final_name_display` from NAME-DECISION.md frontmatter. Let `MONOGRAM` be the first letter capitalized (e.g., Mneme → "M", Theoria → "T", Scholea → "S").

    Output the following prompt to the user verbatim, embedding `MONOGRAM`:

    ```
    Minimalist macOS app icon, 1024×1024, square canvas, no rounded corners
    (macOS will apply the squircle mask automatically — DO NOT pre-round).
    Concept: a small graph of 5-7 nodes connected by curved edges, with the
    letter "<MONOGRAM>" embedded as the focal monogram in the central node.
    Solid colored shapes only — NO white interior fills (transparency mode
    hollows out white areas, which would create unwanted holes).
    Modern flat design, soft drop shadow on the central node, single accent
    color (suggested: deep teal #0D6E5E or warm amber #C4622D — pick one).
    Off-white or transparent background. Style: scholarly and timeless,
    NOT 2020s-AI-startup-derivative. References: Greek geometry textbook
    diagrams, Heptabase node aesthetic.
    Generate 4 distinct variations.
    ```

    Tell user: "Run this prompt twice in ChatGPT image2.0 (Plus/Pro tier required) → 4 + 4 = 8 sketches total. Save all 8 PNG outputs to `icon-assets/sketches/sketch-01.png` ... `sketch-08.png` (create the directory first: `mkdir -p icon-assets/sketches`)."

    **Step B — User executes:**
    1. User opens ChatGPT image2.0 (https://chatgpt.com — image generation flow with model selector "image2.0" / gpt-image-1)
    2. User pastes prompt verbatim, runs once → 4 PNGs
    3. User runs prompt a second time (variation seed) → 4 more PNGs
    4. User saves all 8 to `icon-assets/sketches/sketch-01.png` through `sketch-08.png`
    5. User confirms transparency: `open icon-assets/sketches/sketch-01.png` in Preview, toggle macOS dark mode, look for unintended black holes (Pitfall 3 — gpt-image-1 transparency mode hollows white areas)

    **Step C — Resume signal:**
    - User reports back with: count of usable sketches (no holes, on-prompt) AND list of sketch numbers preferred
    - If <2 usable: Claude generates 4 more variations (the prompt with explicit "Use solid filled shapes; if you must show white, use a light-cream off-white #F5F1E8 instead — never pure transparent"); user re-runs.
  </how-to-verify>
  <resume-signal>
    User replies with one of:
    - "8 sketches saved, all usable. My picks for refinement: sketch-NN, sketch-MM" — proceed to Task 3
    - "Only N usable; need more — please redraft prompt" — Claude redrafts; user re-runs gpt-image-1
    - "Skip refinement — pick this one as master: sketch-NN" — proceed to Task 4 with chosen sketch as master (skips Task 3 entirely; valid path per RESEARCH.md "locked pipeline → Option D3")
  </resume-signal>
  <acceptance_criteria>
    - `icon-assets/sketches/` directory exists with 8 PNG files (sketch-01.png through sketch-08.png)
    - Each PNG is non-zero bytes (`find icon-assets/sketches -name '*.png' -size +0c | wc -l` returns 8)
    - User has identified 0 / 1-2 / fallback-pick from the resume-signal options above
  </acceptance_criteria>
  <done>
    User has selected 1-2 sketches for refinement (Task 3) OR explicitly chose to skip refinement and use a sketch directly (proceed to Task 4 with `cp icon-assets/sketches/sketch-NN.png icon-assets/1024x1024.png`).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (OPTIONAL): Claude Design SVG refinement of chosen sketch(es)</name>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (sections "Pattern 2: Claude Design for SVG icon refinement" lines 277-289 + "Icon Pipeline Resolution (D-09 answer)" lines 437-522)
    - .planning/phases/00-identity-branding-lock/00-CONTEXT.md (D-09 — Claude Design substitution authorization)
  </read_first>
  <what-built>
    Refine 1-2 user-selected sketches into a clean SVG via Claude Design (or Claude.ai artifact), then convert to a 1024×1024 PNG master that becomes the input to Task 4's build-icon.sh.

    SKIP THIS TASK ENTIRELY if user chose "Skip refinement" in Task 2 resume-signal — proceed directly to Task 4 with the chosen sketch as master.
  </what-built>
  <how-to-verify>
    **Step A — User opens Claude Design (or Claude.ai artifact in Pro/Max tier):**

    User uploads the chosen sketch(es) as input image(s) and prompts:

    ```
    Convert this concept sketch into a clean, production-ready SVG icon.
    Constraints:
    - Square canvas, 1024×1024 viewBox, NO pre-rounded corners
      (macOS will apply the squircle mask automatically)
    - Maintain the graph-node-with-monogram concept
    - Solid colored shapes only — no white interior fills
      (avoids gpt-image-1 transparency holes when re-rasterized)
    - Crisp vector paths suitable for scaling 16×16 to 1024×1024
    - Output format: standalone <svg>...</svg> in an HTML artifact
      (so I can copy the SVG markup out manually)
    ```

    **Step B — User extracts SVG:**

    1. Right-click the rendered artifact → "View source" or "Copy SVG"
    2. Save as `icon-assets/icon.svg` at repo root

    **Step C — User converts SVG to 1024×1024 PNG master:**

    Try Option D1 first (sips, no install required):
    ```bash
    sips -s format png icon-assets/icon.svg --out icon-assets/1024x1024.png
    sips -g pixelWidth -g pixelHeight icon-assets/1024x1024.png  # confirm 1024×1024
    ```

    If sips output looks fuzzy at full size (zoom in via Preview), fallback to Option D2:
    ```bash
    brew install librsvg  # ~5MB, one-time
    rsvg-convert -w 1024 -h 1024 icon-assets/icon.svg -o icon-assets/1024x1024.png
    ```

    **Step D — User visually verifies the 1024×1024 master:**

    1. `open icon-assets/1024x1024.png` in Preview
    2. Toggle macOS dark mode — confirm no transparency holes (Pitfall 3)
    3. Visually compare against the original sketch — refinement is faithful, not regressed

    **D-09 fallback authorization:** If Claude Design's SVG output is not usable (poor quality, broken paths), user is authorized per D-09 to:
    - Hand-edit the SVG in any text editor and tweak paths, OR
    - Skip Stage C entirely and use the raw gpt-image-1 sketch as master: `cp icon-assets/sketches/sketch-NN.png icon-assets/1024x1024.png` (Option D3)
    - Document the deviation in PR body when Plan 04 opens the PR
  </how-to-verify>
  <resume-signal>
    User replies with one of:
    - "Master ready at icon-assets/1024x1024.png" — proceed to Task 4
    - "SVG refinement failed; using raw sketch as master (Option D3)" — proceed to Task 4 (note in Plan 04 PR body)
    - "SVG good but sips output fuzzy; installing librsvg" — user runs `brew install librsvg`, re-runs rsvg-convert, then signals "Master ready"
  </resume-signal>
  <acceptance_criteria>
    - `icon-assets/1024x1024.png` exists and is exactly 1024 × 1024 pixels (`sips -g pixelWidth -g pixelHeight icon-assets/1024x1024.png` reports both 1024)
    - File is non-zero bytes
    - User has visually confirmed no transparency holes against macOS dark-mode background
    - `icon-assets/icon.svg` MAY exist (optional — only if Stage C completed); if absent, deviation noted for PR body
  </acceptance_criteria>
  <done>
    Master 1024×1024 PNG sits at `icon-assets/1024x1024.png` and is ready as input to build-icon.sh.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run build-icon.sh + verify-icon.sh; commit iconset and ICNS</name>
  <files>
    icon-assets/iconset.iconset/icon_16x16.png
    icon-assets/iconset.iconset/icon_16x16@2x.png
    icon-assets/iconset.iconset/icon_32x32.png
    icon-assets/iconset.iconset/icon_32x32@2x.png
    icon-assets/iconset.iconset/icon_128x128.png
    icon-assets/iconset.iconset/icon_128x128@2x.png
    icon-assets/iconset.iconset/icon_256x256.png
    icon-assets/iconset.iconset/icon_256x256@2x.png
    icon-assets/iconset.iconset/icon_512x512.png
    icon-assets/iconset.iconset/icon_512x512@2x.png
    icon-assets/icon.icns
  </files>
  <read_first>
    - .planning/phases/00-identity-branding-lock/scripts/build-icon.sh (the script Task 1 authored)
    - .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh (the script Task 1 authored)
    - .planning/phases/00-identity-branding-lock/00-VALIDATION.md (V-02 row + "Manual-Only Verifications" first row — Dock-rendered squircle visual check)
  </read_first>
  <action>
    Run from repo root:

    **4a. Run the build pipeline:**
    ```bash
    bash .planning/phases/00-identity-branding-lock/scripts/build-icon.sh
    ```

    Expected output:
    ```
    ok — icon-assets/icon.icns (NNNNN bytes)
    ```

    If build-icon.sh exits non-zero:
    - "Source PNG missing or empty" → master PNG was not produced; return to Task 3
    - "Source must be 1024×1024" → master is wrong dimensions; re-run sips/rsvg-convert from Task 3 step C
    - "MISSING: icon_NNxNN.png" → an sips invocation silently failed; check sips error stream

    **4b. Run V-02 verification:**
    ```bash
    bash .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh
    ```

    Expected: `ok — icon-assets/icon.icns (10 variants, NNNNN bytes)` AND exit 0.

    **4c. Visual squircle check (manual, but document outcome):**

    Per VALIDATION.md "Manual-Only Verifications" row 1, copy `icon-assets/icon.icns` to `~/Desktop/`, run `open ~/Desktop/icon.icns` then `Cmd+I` (Get Info) on it in Finder. Confirm:
    - Dock-rendered preview at 32×32 reads cleanly (squircle mask applied automatically)
    - Monogram letter is legible
    - No double-rounded corners (Pitfall 2)
    - No transparency holes visible against the Finder white background

    Document the visual-check outcome in the SUMMARY.md output.
  </action>
  <verify>
    <automated>bash .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh && test -f icon-assets/icon.icns && file icon-assets/icon.icns | grep -q 'Mac OS X icon' && [ "$(ls icon-assets/iconset.iconset/*.png 2>/dev/null | wc -l | tr -d ' ')" = "10" ]</automated>
  </verify>
  <acceptance_criteria>
    - V-02 passes: `verify-icon.sh` exits 0
    - `icon-assets/icon.icns` exists, is recognized as `Mac OS X icon` by `file`, and contains ≥ 10 size variants per `iconutil -V`
    - `icon-assets/iconset.iconset/` contains exactly 10 PNG files (the Apple-spec naming set)
    - `icon-assets/1024x1024.png` is preserved (master is the source-of-truth for re-runs)
    - Manual squircle / monogram-legibility / no-double-rounding visual check completed and recorded in SUMMARY.md
  </acceptance_criteria>
  <done>
    SC-2 satisfied: app icon committed-ready in `icon-assets/`. Phase 1 can copy `icon.icns` into `src-tauri/icons/` without further work.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → ChatGPT (Task 2) | gpt-image-1 prompt is sent to OpenAI; output PNG is downloaded to local disk |
| Claude.ai artifact → user clipboard (Task 3) | SVG markup is extracted from rendered HTML artifact; no automated pipe |
| Local PNG → sips/iconutil (Task 4) | macOS system binaries; trusted toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ICON-01 | Tampering | Master PNG dimensions ≠ 1024×1024 → sips silently up/downscales producing soft results | mitigate | build-icon.sh pre-validates with `sips -g pixelWidth -g pixelHeight` and aborts if not exactly "1024 1024" (RESEARCH.md Code Example 2 line 1019) |
| T-ICON-02 | Tampering | Pre-rounded squircle in master PNG → double-mask on macOS Dock | mitigate | gpt-image-1 prompt explicitly says "no rounded corners (macOS will apply the squircle mask automatically — DO NOT pre-round)"; visual check in Task 4 step C catches drift |
| T-ICON-03 | Tampering | gpt-image-1 transparency holes (Pitfall 3, ~80% bug rate) | mitigate | prompt forbids white interior fills; user dark-mode visual check in Task 2 step C catches holes; fallback to ImageMagick alpha cleanup if minor |
| T-ICON-04 | Repudiation | build-icon.sh non-idempotent → re-run produces inconsistent output | mitigate | script overwrites iconset/ICNS deterministically; same master PNG → same output |
| T-ICON-05 | Information Disclosure | gpt-image-1 prompt leaks project details to OpenAI | accept | Prompt only mentions a generic learning-app concept; no proprietary code / data; user already trusts OpenAI for ChatGPT subscription |

</threat_model>

<verification>
Phase-level checks owned by this plan:
- V-02 (SC-2): Automated via verify-icon.sh in Task 4. PASS criteria: `icon-assets/icon.icns` is `Mac OS X icon` format AND `iconutil -V` reports ≥ 10 image-format entries.
- Visual squircle check (VALIDATION.md "Manual-Only Verifications" row 1): documented outcome in SUMMARY.md; user-judgment, not blocking automation.
</verification>

<success_criteria>
- [ ] Task 1: build-icon.sh + verify-icon.sh exist, executable, parse-clean, and meet line-count + content checks.
- [ ] Task 2: 8 sketches saved at `icon-assets/sketches/sketch-NN.png`; user has identified picks (or chosen direct-skip path).
- [ ] Task 3 (optional): `icon-assets/1024x1024.png` master exists at exactly 1024×1024 and visually clean.
- [ ] Task 4: V-02 passes via verify-icon.sh; `icon.icns` exists; iconset folder has 10 PNGs.
- [ ] Manual squircle / monogram / no-double-rounding visual check recorded in SUMMARY.md.
- [ ] No leakage: `! find icon-assets/ -name '*.tmp' -o -name '.DS_Store'` (no stray junk).
</success_criteria>

<output>
After completion, create `.planning/phases/00-identity-branding-lock/00-02-SUMMARY.md` recording:
- gpt-image-1 sketch count generated + count usable
- Whether Stage C (Claude Design SVG refinement) was used or skipped (and which option per RESEARCH.md "locked pipeline → Option D1/D2/D3")
- Final ICNS byte size
- Manual squircle visual check verdict (pass / fail with photo / re-do)
- librsvg / imagemagick installations performed (if any) — Plan 04 will reference for PR body documentation
- Hand-off to Plan 04: `icon-assets/` is committed-ready; Plan 04's atomic-commit step adds it.
</output>
