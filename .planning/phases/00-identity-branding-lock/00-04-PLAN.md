---
phase: 00-identity-branding-lock
plan: 04
type: execute
wave: 3
depends_on: ["00-02", "00-03"]
files_modified:
  - .planning/STATE.md  # post-merge timestamp + Phase 0 retired flag
autonomous: false  # secrets audit + atomic commit + gh repo create + local-path move all involve user-confirmation moments
success_criteria_covered: [SC-1, SC-2, SC-3, SC-4]  # final gate verifying all four
tdd_applicable: false
tdd_rationale: "Pre-publish secrets audit + git commit + gh repo create + visual ICNS verification + local-path mv. The verification scripts authored in Plans 02 and 03 ARE the tests; this plan invokes them as the gate."
validation_map: [V-01, V-02, V-03, V-04, V-05, V-06, V-07, V-08, V-09]  # consolidated gate run
requirements: [SC-1, SC-2, SC-3, SC-4]
user_setup:
  - service: github
    why: "Publish r1ckyIn/<finalname> public repo for the first time (D-11 atomic + RESEARCH.md no GitHub-side rename — fresh creation)"
    env_vars: []
    dashboard_config:
      - task: "Confirm gh CLI is authenticated as r1ckyIn"
        location: "Run `gh auth status` — must show logged in as r1ckyIn before Task 3"
        rationale: "`gh repo create --public` requires authenticated user; if not logged in, Task 3 will halt"

must_haves:
  truths:
    - "All 9 V-IDs (V-01..V-09 except deferred V-05) green when consolidated gate runs"
    - "Single atomic commit covers all rename + asset + README/LICENSE work (D-12)"
    - "Repo r1ckyIn/<finalname> created public on GitHub via `gh repo create --source=. --public --push`"
    - "Local working directory moved from /Users/qinyuan/claude/r1ckyIn_GitHub/learn-os to /Users/qinyuan/claude/r1ckyIn_GitHub/<finalname>"
    - "STATE.md updated to reflect Phase 0 completion + new project name"
  artifacts:
    - path: "git log: single commit `feat(0): retire codename learn-os, lock <finalname> identity`"
      provides: "Atomic Phase 0 history record"
      contains: "feat(0):"
    - path: "github.com/r1ckyIn/<finalname> repository"
      provides: "Public-visible portfolio piece (OOS-01 amended)"
      contains: "README.md, LICENSE, icon-assets/"
  key_links:
    - from: "verify-rename.sh + verify-icon.sh both green"
      to: "git commit"
      via: "Pre-commit gate — Task 2 will not commit if either script exits non-zero"
      pattern: "ALL GATES PASS"
    - from: "git commit (still on local main)"
      to: "gh repo create --source=. --push"
      via: "First push of branch + remote init in single gh invocation"
      pattern: "Pushing main → r1ckyIn/<finalname>"
    - from: "Local working dir move"
      to: "Claude Code session cache regeneration under new path"
      via: "User opens new path in next session — Claude Code auto-creates ~/.claude/projects/-Users-...-<finalname>/"
      pattern: "regenerated under new path"
---

<objective>
Run the consolidated pre-publish gate (verify-rename.sh + verify-icon.sh + secrets audit V-09 + spike-001 captures sensitivity audit), commit the entire Phase 0 work as a single atomic commit per D-12, create the GitHub repo `r1ckyIn/<finalname>` for the first time via `gh repo create --source=. --public --push` (no rename needed — RESEARCH.md verified no existing remote), perform the local working-directory move, then perform a final visual squircle/Dock-rendered icon verification.

Purpose: This is the ship-and-publish plan. It enforces the security gate (T-PUB-01 + T-SEC-01) before going public, lands SC-1 + SC-2 + SC-3 + SC-4 simultaneously in one atomic commit (D-12 solo-dev rule), and operationalizes the OOS-01 amendment (codebase becomes a portfolio piece under MIT). Without this plan, Plans 01-03 produce a renamed but unpublished local repo — half the value.

Output: A public GitHub repo at `https://github.com/r1ckyIn/<finalname>` with the renamed codebase, all icon assets, README.md + LICENSE; local working directory moved to the new path; STATE.md updated to reflect Phase 0 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-identity-branding-lock/00-CONTEXT.md
@.planning/phases/00-identity-branding-lock/00-RESEARCH.md
@.planning/phases/00-identity-branding-lock/00-VALIDATION.md
@.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
@.planning/phases/00-identity-branding-lock/00-01-SUMMARY.md
@.planning/phases/00-identity-branding-lock/00-02-SUMMARY.md
@.planning/phases/00-identity-branding-lock/00-03-SUMMARY.md

<interfaces>
<!-- Pre-conditions for Task 1 (gate run) -->

When this plan starts:
- `verify-rename.sh "$FINAL"` exists and is executable (authored by Plan 03 Task 1)
- `verify-icon.sh` exists and is executable (authored by Plan 02 Task 1)
- `icon-assets/icon.icns` exists (Plan 02 Task 4)
- `README.md`, `LICENSE` exist at repo root (Plan 03 Task 2)
- All `learn-os` references are flipped in non-skip-listed files (Plan 03 Task 3)
- Skill is renamed: `.claude/skills/spike-findings-${FINAL}/` (Plan 03 Task 4)
- Spike 002 `tauri.conf.json` identifier is `dev.${FINAL}.spike` (Plan 03 Task 3)

This plan does NOT modify the renames or assets — it only runs gates, commits, publishes, moves.

<!-- gh repo create command (RESEARCH.md "GitHub repo creation (NOT a rename)" lines 676-705) -->

```bash
cd /Users/qinyuan/claude/r1ckyIn_GitHub/<finalname>  # POST-mv path

gh repo create r1ckyIn/<finalname> \
  --source=. \
  --public \
  --description="Personal AI-native desktop learning app — Tauri 2 shell wrapping local Claude Code, three-pane UI (course files / video / chat), AI-native knowledge graph + FSRS-6 review. macOS-only, single-user, MIT-licensed." \
  --push

# Optional follow-up to add topics (RESEARCH.md Pitfall 10)
gh repo edit r1ckyIn/<finalname> \
  --add-topic tauri \
  --add-topic sveltekit \
  --add-topic claude-code \
  --add-topic learning-app \
  --add-topic personal-knowledge-management \
  --add-topic ai-native \
  --add-topic macos
```

<!-- Local-path move semantics (RESEARCH.md "Local-path move + Claude Code session cache" lines 649-674) -->

The move is just `mv`:
```bash
cd /Users/qinyuan/claude/r1ckyIn_GitHub/
mv learn-os <finalname>
cd <finalname>
```

Side effects (per RESEARCH.md):
- Claude Code session cache at `~/.claude/projects/-Users-qinyuan-claude-r1ckyIn-GitHub-learn-os/` becomes orphaned. Auto-regenerated under new path on next session start. **Recommended action: leave as-is** (option A) — keeps pre-rename audit trail navigable.
- Shell history (`~/.zsh_history`) contains old `cd` paths — no mitigation needed.
- VS Code / iTerm2 saved workspaces: user opens new path manually in next session.

<!-- guard-branch.sh hook reminder -->

The solo-dev rule (r1ckyIn L2 CLAUDE.md):
- one phase = one branch = one PR
- The branch should be `gsd/phase-0-identity-branding-lock` (per phase_branch_template)
- guard-branch.sh hook enforces — pre-commit on a non-feature branch will FAIL

Verify branch state at start of Task 2:
```bash
git branch --show-current
# Expected: gsd/phase-0-identity-branding-lock
```

If currently on `main`, this plan's executor MUST first create the feature branch before commit (Task 2 sub-step).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-publish consolidated gate (verify-rename.sh + verify-icon.sh + spike-001 captures audit + secrets audit)</name>
  <files>
    .planning/phases/00-identity-branding-lock/scripts/audit-spike-001-captures.sh  # NEW one-shot audit script
  </files>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md (extract `final_name`)
    - .planning/phases/00-identity-branding-lock/00-VALIDATION.md (sections "Sampling Rate" + "Manual-Only Verifications" + V-09 row)
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (Security Domain section lines 1216-1247 — spike 001 captures content audit recommendation)
    - .planning/spikes/001-stream-json-recon/captures/01-simple-text.jsonl (read first 30 lines to assess sensitivity)
    - .planning/spikes/001-stream-json-recon/captures/02-markdown-code.jsonl (read first 30 lines)
    - .planning/spikes/001-stream-json-recon/captures/03-tool-use.jsonl (read first 30 lines)
  </read_first>
  <action>
    **1a. Run consolidated rename gate:**
    ```bash
    NAME_DECISION=.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
    FINAL=$(grep '^final_name:' "$NAME_DECISION" | awk '{print $2}')
    bash .planning/phases/00-identity-branding-lock/scripts/verify-rename.sh "$FINAL"
    ```
    Expected: `ALL GATES PASS — rename verified for finalname=$FINAL` AND exit 0.

    **1b. Run icon gate:**
    ```bash
    bash .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh
    ```
    Expected: `ok — icon-assets/icon.icns (10 variants, NNNN bytes)` AND exit 0.

    **1c. Spike 001 captures sensitivity audit (RESEARCH.md Security Domain T-SEC-01):**

    Author one-shot script `.planning/phases/00-identity-branding-lock/scripts/audit-spike-001-captures.sh` (chmod +x):

    ```bash
    #!/usr/bin/env bash
    # audit-spike-001-captures.sh
    # One-shot pre-publish audit of spike 001 stream-json captures for sensitive content.
    # Per RESEARCH.md "Security Domain → T-SEC-01": these JSONL files may contain user-message
    # text from real claude sessions; confirm content is trivial test prompts before public publish.
    set -euo pipefail
    CAPTURES=(
      .planning/spikes/001-stream-json-recon/captures/01-simple-text.jsonl
      .planning/spikes/001-stream-json-recon/captures/02-markdown-code.jsonl
      .planning/spikes/001-stream-json-recon/captures/03-tool-use.jsonl
    )

    SENSITIVE_PATTERNS=(
      'password' 'secret' 'token' 'sk-ant' 'sk-proj' 'api_key' 'api-key'
      'bearer' 'private_key' 'rsa private' 'BEGIN RSA' 'BEGIN PRIVATE'
      'oauth' 'session_id'
    )

    EXIT=0
    for f in "${CAPTURES[@]}"; do
      [ -f "$f" ] || { echo "MISSING: $f"; EXIT=1; continue; }
      echo "=== $f ($(wc -l < "$f") lines, $(stat -f%z "$f") bytes) ==="
      for pat in "${SENSITIVE_PATTERNS[@]}"; do
        # Case-insensitive grep; show only file:linecount summary
        local hits
        hits=$(grep -c -i "$pat" "$f" 2>/dev/null || echo 0)
        if [ "$hits" -gt 0 ]; then
          echo "  WARN: '$pat' matched $hits time(s) — manual review required"
          EXIT=1
        fi
      done
      # Heuristic: any user message containing personal info / real coursework content
      if grep -i -E 'COMP[0-9]{4}|MATH[0-9]{4}|usyd|university of sydney|qinyuan|yqin0800' "$f" > /dev/null 2>&1; then
        echo "  WARN: file mentions identifying personal/academic markers — manual review required"
        EXIT=1
      fi
    done
    [ "$EXIT" = "0" ] && echo "OK — spike 001 captures contain no flagged sensitive patterns"
    exit "$EXIT"
    ```

    Note: BSD-bash uses `local` only inside functions; the loop above uses subshell-scoped variables — adjust:

    Use simpler, non-`local` form:
    ```bash
    for f in "${CAPTURES[@]}"; do
      [ -f "$f" ] || { echo "MISSING: $f"; EXIT=1; continue; }
      echo "=== $f ($(wc -l < "$f") lines, $(stat -f%z "$f") bytes) ==="
      for pat in "${SENSITIVE_PATTERNS[@]}"; do
        HITS=$(grep -c -i "$pat" "$f" 2>/dev/null || echo 0)
        if [ "$HITS" -gt 0 ]; then
          echo "  WARN: '$pat' matched $HITS time(s) — manual review required"
          EXIT=1
        fi
      done
      ...
    done
    ```

    Then run it:
    ```bash
    chmod +x .planning/phases/00-identity-branding-lock/scripts/audit-spike-001-captures.sh
    bash .planning/phases/00-identity-branding-lock/scripts/audit-spike-001-captures.sh
    ```

    If exit non-zero → STOP and report findings to user via CHECKPOINT REACHED. Do not proceed to commit until user confirms either:
    - "redact those lines manually before commit" (user edits the JSONL files using Edit tool — but this contradicts D-15 "preserve as-is"; pragma: redact only if HIGH-confidence sensitive),
    - OR "those are trivial test prompts, proceed" (user-explicit acknowledgment),
    - OR "add captures/ to .gitignore + git rm --cached" (last-resort; preferable to commit-amending history)

    **1d. Re-run V-09 secrets audit explicitly** (covered by verify-rename.sh gate_v09 but re-run for paranoia at the publish gate):
    ```bash
    git ls-files | xargs grep -l -i 'sk-ant\|sk-proj\|api[_-]key\|token\|password\|secret' 2>/dev/null | wc -l | tr -d ' '
    ```
    Expected: `0`. If non-zero → halt + manual review.

    **1e. .gitignore sanity check** (RESEARCH.md Security Domain — T-PUB-01 mitigation):
    ```bash
    # CLAUDE.md must be gitignored (project-level instructions are private)
    git check-ignore CLAUDE.md && echo "CLAUDE.md gitignored OK" \
      || { echo "FAIL: CLAUDE.md NOT gitignored — would leak to public repo"; exit 1; }
    # .env* must be gitignored
    echo ".env" | xargs -I{} bash -c 'touch /tmp/{}; git check-ignore /tmp/{} > /dev/null && echo "{} would be gitignored" || echo "FAIL: {} NOT in gitignore"'
    rm -f /tmp/.env
    ```

    Document outcomes of all 5 sub-checks in SUMMARY.md.
  </action>
  <verify>
    <automated>NAME_DECISION=.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && FINAL=$(grep '^final_name:' "$NAME_DECISION" | awk '{print $2}') && bash .planning/phases/00-identity-branding-lock/scripts/verify-rename.sh "$FINAL" && bash .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh && bash .planning/phases/00-identity-branding-lock/scripts/audit-spike-001-captures.sh && [ "$(git ls-files | xargs grep -l -i 'sk-ant\|sk-proj\|api[_-]key\|token\|password\|secret' 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && git check-ignore CLAUDE.md > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `verify-rename.sh "$FINAL"` exits 0 with `ALL GATES PASS`
    - `verify-icon.sh` exits 0
    - `audit-spike-001-captures.sh` exits 0 OR user has explicitly acknowledged findings (with documented decision)
    - V-09 standalone re-run: zero matches for secret patterns in tracked files
    - `git check-ignore CLAUDE.md` succeeds (CLAUDE.md is private; gitignore protects it)
    - Audit script is committed-ready in `.planning/phases/00-identity-branding-lock/scripts/`
  </acceptance_criteria>
  <done>
    All 9 V-IDs (V-01 through V-09 except V-05 deferred to Phase 1) are GREEN. Repo is publish-safe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create feature branch + atomic commit (D-12)</name>
  <files>
    (no new files — git operations only; STATE.md update happens in Task 4)
  </files>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md (extract `final_name` and `final_name_display`)
    - /Users/qinyuan/claude/r1ckyIn_GitHub/CLAUDE.md (§"GSD 工作流(v1.40)" + §"Solo-dev 铁律" — guard-branch.sh enforcement)
    - .planning/phases/00-identity-branding-lock/00-CONTEXT.md (D-12 single atomic commit; D-15 codename retirement date)
  </read_first>
  <action>
    **2a. Confirm working tree state:**
    ```bash
    git status --short  # should show many M/A entries from Plans 02 + 03 work
    git branch --show-current  # current branch
    ```

    **2b. Switch to / create feature branch** (if not already on it):
    ```bash
    BRANCH="gsd/phase-0-identity-branding-lock"
    if [ "$(git branch --show-current)" != "$BRANCH" ]; then
      git checkout -b "$BRANCH" 2>/dev/null || git checkout "$BRANCH"
    fi
    ```

    **2c. Stage all Phase 0 output files explicitly** (avoid `git add -A` to prevent accidental .env / sensitive-file capture per L1 GSD safety rules):

    ```bash
    NAME_DECISION=.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
    FINAL=$(grep '^final_name:' "$NAME_DECISION" | awk '{print $2}')
    SKILL_DIR=$(grep '^skill_dir:' "$NAME_DECISION" | awk '{print $2}')

    # New files at repo root
    git add README.md
    git add LICENSE

    # Icon assets directory (entire tree)
    git add icon-assets/

    # Phase 0 scripts + decision file + summaries
    git add .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
    git add .planning/phases/00-identity-branding-lock/scripts/
    git add .planning/phases/00-identity-branding-lock/00-01-SUMMARY.md
    git add .planning/phases/00-identity-branding-lock/00-02-SUMMARY.md
    git add .planning/phases/00-identity-branding-lock/00-03-SUMMARY.md
    git add .planning/phases/00-identity-branding-lock/00-01-PLAN.md
    git add .planning/phases/00-identity-branding-lock/00-02-PLAN.md
    git add .planning/phases/00-identity-branding-lock/00-03-PLAN.md
    git add .planning/phases/00-identity-branding-lock/00-04-PLAN.md

    # In-place rename targets
    git add .planning/PROJECT.md
    git add .planning/ROADMAP.md
    git add .planning/REQUIREMENTS.md
    git add .planning/STATE.md
    git add .planning/research/
    git add .planning/notes/foundation-decisions.md
    git add .planning/spikes/WRAP-UP-SUMMARY.md
    git add .planning/spikes/002-tauri-claude-shell/

    # Skill rename — git tracks the directory rename via git mv (already done in Plan 03 Task 4a)
    git add ".claude/skills/${SKILL_DIR}/"

    # Note: CLAUDE.md should NOT be staged — it's gitignored (verified in Task 1e). Confirm:
    git status --short CLAUDE.md  # expected: empty output (file modified but ignored)
    ```

    **2d. Pre-commit sanity:**
    ```bash
    git status --short
    # Visual review of staged changes
    git diff --cached --stat | head -30
    ```

    Expected staged-files breakdown:
    - 1 new README.md
    - 1 new LICENSE
    - icon-assets/* (12+ files: 10 iconset PNGs + master + ICNS + optional SVG + sketches/)
    - 4 new scripts (build-icon.sh, verify-icon.sh, verify-rename.sh, audit-spike-001-captures.sh)
    - 1 new NAME-DECISION.md + 4 PLAN.md files + 3 SUMMARY.md files
    - ~15 in-place .md edits (PROJECT, ROADMAP, REQUIREMENTS, STATE, research/*, notes/*, spikes/*)
    - 1 tauri.conf.json (bundle id flip)
    - 1 spike 002 +page.svelte
    - skill directory rename (counted as R, not D+A)

    **2e. Atomic commit (D-12):**

    Use HEREDOC to ensure correct formatting (per L1 GSD commit-message rules):

    ```bash
    # Read final name for commit message
    FINAL_DISPLAY=$(grep '^final_name_display:' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md | awk '{print $2}')

    git commit -m "$(cat <<EOF
feat(0): retire codename \`learn-os\`, lock ${FINAL_DISPLAY} identity

Phase 0 deliverables (atomic per D-12, single commit per solo-dev rule):

- Final app name locked: ${FINAL_DISPLAY} (per D-04 user pick from researcher
  finalists Mneme / Theoria / Scholea; see 00-NAME-DECISION.md for rationale)
- App icon committed: icon-assets/icon.icns (Mac OS X icon, 10 size variants)
  + master 1024×1024 PNG + iconset folder (graph-node + monogram concept per D-05)
- Bundle id flipped: spike 002 \`dev.learn-os.spike\` -> \`dev.${FINAL}.spike\`
  (production .app reserved for Phase 1 per D-10 + RESEARCH.md Q4)
- Atomic content rename across 23 files: \`learn-os\` -> \`${FINAL}\` and
  \`learnos\` -> \`${FINAL}\` (REQ-17 .learnos/rules/ lockstep per Pitfall 6)
- README.md (bilingual EN+ZH, flat-square badges, MIT, codename history footer
  per D-13 + D-15) + LICENSE (MIT) authored at repo root
- Project skill renamed: spike-findings-learn-os/ -> spike-findings-${FINAL}/
  (directory + frontmatter name: + CLAUDE.md skill-table; content unchanged)
- PROJECT.md OOS-01 amended: distribution-as-OSS-portfolio allowed
  (multi-user/collaboration/commercialization remain excluded)
- Wave 0 verification scripts authored:
  - scripts/build-icon.sh (PNG -> ICNS pipeline)
  - scripts/verify-icon.sh (V-02)
  - scripts/verify-rename.sh (V-01 V-03 V-04 V-06 V-07 V-08 V-09 bundled)
  - scripts/audit-spike-001-captures.sh (T-SEC-01 sensitivity audit)

Validation: all 9 V-IDs green per VALIDATION.md (V-05 deferred to Phase 1).
Skip-list integrity: 00-{CONTEXT,DISCUSSION-LOG,RESEARCH,VALIDATION}.md and
spike 001 captures preserved as-is per D-15.

Codename history: project was developed under codename \`learn-os\` until
2026-05-07; preserved in git log + README footer (no filter-repo per D-15).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
    ```

    **2f. Verify commit landed:**
    ```bash
    git log -1 --stat
    git log --oneline -3  # confirm new commit on top
    ```

    If guard-branch.sh hook fails on commit: investigate (current branch wrong? wrong file staged?) — DO NOT bypass with `--no-verify`. Per L1 CLAUDE.md "NEVER skip hooks unless user explicitly requests".
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -q '^feat(0):' && git log -1 --pretty=format:'%s' | grep -qF 'retire codename' && git branch --show-current | grep -q 'gsd/phase-0-identity-branding-lock' && [ "$(git status --short | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - Currently on branch `gsd/phase-0-identity-branding-lock`
    - Latest commit message starts with `feat(0): retire codename` and includes the locked `<finalname>`
    - `git status --short` is empty (working tree is clean — all changes committed)
    - Commit body includes `Co-Authored-By: Claude Opus 4.7 (1M context)`
    - Commit body lists at least 5 of the 7 Phase 0 deliverable bullets
    - guard-branch.sh hook did NOT need to be bypassed (no `--no-verify`)
    - `git diff HEAD~1 HEAD --stat` shows: 1 new README.md, 1 new LICENSE, ≥ 12 icon-assets/* files, 4 new scripts, ~15+ modified .md files
    - Skip-listed files NOT in `git diff HEAD~1 HEAD --name-only` (verify: `git diff HEAD~1 HEAD --name-only | grep -E '00-(CONTEXT|DISCUSSION-LOG|RESEARCH|VALIDATION).md|001-stream-json-recon/captures' | wc -l` returns 0)
  </acceptance_criteria>
  <done>
    Single atomic commit on the feature branch; ready for `gh repo create --push`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Local-path move + GitHub repo creation + push</name>
  <files>
    (no in-repo file changes — filesystem rename + remote network operation)
  </files>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md (extract `final_name`)
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (sections "GitHub repo creation (NOT a rename)" lines 676-705 + "Local-path move + Claude Code session cache" lines 649-674 + Pitfall 10 — gh repo create flags)
  </read_first>
  <action>
    **3a. Pre-flight: confirm gh authenticated as r1ckyIn:**
    ```bash
    gh auth status
    ```
    If not logged in or wrong user → halt; user runs `gh auth login` then signals to resume.

    **3b. Confirm GitHub-side state (no existing remote):**
    ```bash
    git remote -v  # must be empty per RESEARCH.md verification
    gh repo view r1ckyIn/$(grep '^final_name:' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md | awk '{print $2}') --json name 2>&1 \
      | grep -q 'Could not resolve to a Repository' \
      || { echo "WARN: r1ckyIn/<finalname> already exists on GitHub — manual review needed"; exit 1; }
    ```

    **3c. Local-path move** (executed BEFORE `gh repo create` so the new directory name is the source for repo creation):

    Per RESEARCH.md "Suggested execution order" Step 8: local move happens AFTER commit so git's internal state is clean.

    ```bash
    NAME_DECISION_OLD=/Users/qinyuan/claude/r1ckyIn_GitHub/learn-os/.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
    FINAL=$(grep '^final_name:' "$NAME_DECISION_OLD" | awk '{print $2}')
    OLD_PATH=/Users/qinyuan/claude/r1ckyIn_GitHub/learn-os
    NEW_PATH=/Users/qinyuan/claude/r1ckyIn_GitHub/${FINAL}

    # Confirm we're not in OLD_PATH currently (mv from inside fails on macOS)
    pwd
    cd /Users/qinyuan/claude/r1ckyIn_GitHub/

    mv "$OLD_PATH" "$NEW_PATH"
    cd "$NEW_PATH"
    pwd  # confirm we're now in the new path

    # Sanity: git is still happy under the new path
    git status --short
    git log -1 --pretty=format:'%s'
    ```

    **NOTE:** Per RESEARCH.md "Local-path move + Claude Code session cache implications", Claude Code's session cache at `~/.claude/projects/-Users-qinyuan-claude-r1ckyIn-GitHub-learn-os/` (and 2 sub-paths) becomes orphaned. **Recommended action: leave as-is (option A)** — they don't take much space, history.jsonl is global, regeneration happens automatically on next session start. **Do NOT attempt to migrate them** — fragile and offers no real benefit.

    **3d. Create GitHub repo + push** (RESEARCH.md "GitHub repo creation"):

    ```bash
    cd "$NEW_PATH"

    gh repo create r1ckyIn/${FINAL} \
      --source=. \
      --public \
      --description="Personal AI-native desktop learning app — Tauri 2 shell wrapping local Claude Code, three-pane UI (course files / video / chat), AI-native knowledge graph + FSRS-6 review. macOS-only, single-user, MIT-licensed." \
      --push
    ```

    This single command:
    - creates the GitHub repo `r1ckyIn/${FINAL}` (public)
    - sets the local `origin` remote
    - pushes the current branch (`gsd/phase-0-identity-branding-lock`) to origin

    **3e. Add topics** (Pitfall 10 — RESEARCH.md):
    ```bash
    gh repo edit r1ckyIn/${FINAL} \
      --add-topic tauri \
      --add-topic sveltekit \
      --add-topic claude-code \
      --add-topic learning-app \
      --add-topic personal-knowledge-management \
      --add-topic ai-native \
      --add-topic macos
    ```

    **3f. Verify remote state:**
    ```bash
    git remote -v
    # Expected:
    #   origin  https://github.com/r1ckyIn/${FINAL}.git (fetch)
    #   origin  https://github.com/r1ckyIn/${FINAL}.git (push)

    gh repo view r1ckyIn/${FINAL} --json name,visibility,description,isPrivate
    # Expected: name=${FINAL}, visibility=PUBLIC, description=correct, isPrivate=false
    ```

    **3g. Manual visual verification** (VALIDATION.md "Manual-Only Verifications" row 4):

    Output to user: "Open `https://github.com/r1ckyIn/${FINAL}` in browser. Verify:
    1. README renders correctly (badges visible, English + Chinese sections balanced, codename history footer factual)
    2. LICENSE detected by GitHub (badge shows 'MIT' on right sidebar)
    3. No unexpected files (CLAUDE.md should NOT be visible — gitignored; .env* should NOT be visible)
    4. Topics applied (tauri, sveltekit, claude-code, etc. — sidebar)

    Reply 'GitHub repo verified' to proceed."
  </action>
  <verify>
    <automated>NAME_DECISION=.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && FINAL=$(grep '^final_name:' "$NAME_DECISION" | awk '{print $2}') && [ "$(pwd)" = "/Users/qinyuan/claude/r1ckyIn_GitHub/${FINAL}" ] && git remote -v | grep -q "origin.*github.com[/:]r1ckyIn/${FINAL}" && gh repo view "r1ckyIn/${FINAL}" --json visibility --jq '.visibility' | grep -q 'PUBLIC'</automated>
  </verify>
  <acceptance_criteria>
    - Local working directory is now `/Users/qinyuan/claude/r1ckyIn_GitHub/${FINAL}` (NOT `learn-os`)
    - `git remote -v` shows `origin` pointing to `https://github.com/r1ckyIn/${FINAL}.git` for both fetch and push
    - `gh repo view r1ckyIn/${FINAL}` returns the repo metadata (visibility=PUBLIC, isPrivate=false)
    - Branch `gsd/phase-0-identity-branding-lock` is pushed to origin (`git ls-remote origin gsd/phase-0-identity-branding-lock` returns the commit SHA)
    - At least 5 topics applied (`gh repo view r1ckyIn/${FINAL} --json repositoryTopics`)
    - Manual visual check completed and recorded in SUMMARY.md
    - Old path `/Users/qinyuan/claude/r1ckyIn_GitHub/learn-os` no longer exists (`! test -d /Users/qinyuan/claude/r1ckyIn_GitHub/learn-os`)
  </acceptance_criteria>
  <done>
    Public repo exists at `https://github.com/r1ckyIn/${FINAL}`; codebase pushed; local path moved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual icon verification + STATE.md update + Phase 0 retirement</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md (extract `final_name`, `final_name_display`, `codename_retirement_date`)
    - .planning/phases/00-identity-branding-lock/00-VALIDATION.md (Manual-Only Verifications rows 1, 3 — visual ICNS Dock check + README readability)
  </read_first>
  <what-built>
    Plans 01-04 produced + committed + published the entire Phase 0 atomic deliverable set. This task is the final user-facing visual check + STATE.md update to mark Phase 0 retired.
  </what-built>
  <how-to-verify>
    **4a. Visual ICNS / Dock-rendered squircle check (VALIDATION.md Manual-Only row 1):**

    User runs:
    ```bash
    cp icon-assets/icon.icns ~/Desktop/${FINAL}.icns
    open ~/Desktop/${FINAL}.icns  # opens in Preview, shows the icon
    ```
    Then in Finder:
    1. Cmd+I (Get Info) on `${FINAL}.icns` on Desktop
    2. Confirm the Dock-rendered preview at 32×32 reads cleanly:
       - Squircle mask applied automatically by macOS (corners rounded, but NOT double-rounded — Pitfall 2)
       - Monogram letter is legible at small size
       - No transparency holes visible (Pitfall 3)
       - Color/contrast looks intentional, not muddy
    3. Drag the icon into the Dock briefly (it will not stick — that's fine; just visual sanity)

    User reports: "Icon visual check pass" or "Icon visual check fail: <reason>".

    **4b. README readability check (VALIDATION.md Manual-Only row 3):**

    User opens `https://github.com/r1ckyIn/${FINAL}` in browser:
    1. README renders correctly (badges visible at top)
    2. English section reads naturally to a stranger landing on the GitHub page
    3. Chinese section is a faithful mirror, not a translation artifact
    4. Codename history footer is factually accurate (2026-05-07 retirement date)
    5. LICENSE detected by GitHub (right sidebar shows MIT)

    User reports: "README readable" or "README issue: <reason>".

    **4c. Update STATE.md** (after both visual checks pass):

    Use Edit tool to update `.planning/STATE.md`:

    Find:
    ```
    **Phase**: 0 — Identity & Branding Lock (context gathered, awaiting plan)
    **Plan**: none yet — next is `/gsd-plan-phase 0`
    **Status**: phase-0-context-ready
    **Progress**: 0/11 phases complete
    ```

    Replace with:
    ```
    **Phase**: 1 — Tauri Shell Foundation + Subprocess Hardening (Phase 0 retired ${codename_retirement_date})
    **Plan**: none yet — next is `/gsd-discuss-phase 1` or `/gsd-plan-phase 1`
    **Status**: phase-0-complete
    **Progress**: 1/11 phases complete
    ```

    Find:
    ```
    [░░░░░░░░░░░░░░░░░░░░] 0% (0/11 phases)
    ```
    Replace with:
    ```
    [██░░░░░░░░░░░░░░░░░░] 9% (1/11 phases)
    ```

    Find the `## Project Reference` section "Name (codename)" line:
    ```
    **Name (codename)**: learn-os (final name TBD in Phase 0)
    ```
    Replace with:
    ```
    **Name**: ${FINAL_DISPLAY} (codename `learn-os` retired ${codename_retirement_date})
    ```

    Find the `progress:` block in frontmatter and update:
    - `completed_phases: 0` → `completed_phases: 1`
    - `total_plans: 0` → `total_plans: 4`
    - `completed_plans: 0` → `completed_plans: 4`
    - `last_updated:` → today's ISO date

    Find ROADMAP.md Phase 0 entry status line and update from `[pending]` to `[complete]`.

    **4d. Commit STATE.md update:**
    ```bash
    git add .planning/STATE.md .planning/ROADMAP.md
    git commit -m "$(cat <<'EOF'
chore(state): mark Phase 0 complete; advance to Phase 1

Phase 0 (Identity & Branding Lock) retired 2026-05-07.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
    git push origin gsd/phase-0-identity-branding-lock
    ```

    Wait — this is a SECOND commit on the feature branch, which violates D-12 atomic-commit-per-phase. Reconsider:

    **REVISED 4c + 4d:** STATE.md was already in the Plan 03 Task 3 Stage 1 sed pass (it contained `learn-os` references). Its `Phase`, `Status`, `Progress` fields are descriptive of pre-Phase-0 state and would benefit from being updated. But D-12 says single atomic commit — so either:

    (i) Update STATE.md in Plan 03's Stage 7 explicitly (adding it to that plan's responsibility) and the atomic commit in Plan 04 Task 2 captures it. **Preferred path** — but Plan 03 is already authored.

    (ii) Make Plan 04 Task 2's commit also include STATE.md update at staging time. **This is what we should do** — fold the STATE.md update into Task 2's commit, NOT a separate commit here.

    **Decision:** STATE.md update happens in Task 2 staging step (re-add `git add .planning/STATE.md` after editing it). Move sub-step 4c to BEFORE Task 2 commit:

    Refactor: this Task 4 should be split — the STATE.md update belongs in Task 2 (immediately before `git commit`). The visual checks (4a, 4b) remain here as the post-publish gate.

    **REVISED Task 4 final action set:**
    - 4a (visual ICNS check) — user-facing, no file changes
    - 4b (README readability check) — user-facing, no file changes
    - 4c (STATE.md update) — **MOVED to Task 2 sub-step 2c.5** (after staging in-place rename targets, before commit). This task only verifies STATE.md is correctly updated.
    - 4d (commit + push) — already done in Task 2 + Task 3

    User-facing report:
    ```
    Phase 0 ship summary:
    - Final name: ${FINAL_DISPLAY}
    - GitHub: https://github.com/r1ckyIn/${FINAL}
    - Local path: /Users/qinyuan/claude/r1ckyIn_GitHub/${FINAL}
    - Atomic commit SHA: $(git log -1 --pretty=format:%H)
    - Visual icon check: <pass / fail>
    - README readability: <pass / fail>
    - STATE.md updated to reflect Phase 0 retired
    ```
  </how-to-verify>
  <resume-signal>
    User replies with one of:
    - "Both pass — Phase 0 complete" — proceed to extract-learnings + Phase 1 entry
    - "Icon fail: &lt;reason&gt;" — return to Plan 02 to regenerate icon (likely Pitfall 2 double-round or Pitfall 3 transparency hole)
    - "README fail: &lt;reason&gt;" — return to Plan 03 Task 2 to fix README content; commit-amend (single-PR rule allows amend if no merge has happened yet)
  </resume-signal>
  <acceptance_criteria>
    - User has typed exactly one of the resume-signal forms
    - If "Both pass": STATE.md `Phase` field is "1 — Tauri Shell Foundation"; `Status` is "phase-0-complete"; ROADMAP.md Phase 0 line shows `[complete]`
    - Icon visual check (4a) and README check (4b) both recorded in SUMMARY.md with verdict
    - Local path verified as `/Users/qinyuan/claude/r1ckyIn_GitHub/${FINAL}` (not `learn-os`)
  </acceptance_criteria>
  <done>
    User-confirmed visual verification complete. Phase 0 retired. STATE.md reflects new project name + Phase 1 as next.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → public GitHub (Task 3) | First public exposure of the codebase; secrets / sensitive content leakage is irreversible |
| Spike 001 captures content → public visibility | JSONL files contain user-message text from real claude sessions per RESEARCH.md Security Domain |
| `gh repo create --public` | One-shot; no easy rollback to private without GitHub API call (technically possible but disruptive) |
| Local filesystem `mv` | Single command, atomic at filesystem level; Claude Code session cache becomes orphaned but recoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PUB-01 | Information Disclosure | Secret patterns (sk-ant, sk-proj, api_key, token, password) leak via tracked files | mitigate | V-09 grep gate runs at 3 separate points: Plan 03 Task 1 verify-rename.sh gate_v09; Plan 04 Task 1d explicit re-run; final pre-`gh repo create` confirmation |
| T-PUB-02 | Spoofing | Brand confusion / namespace squatting on `r1ckyIn/<finalname>` | mitigate | Researcher pre-vetted finalists (RESEARCH.md "Naming Due-Diligence Results"); Task 3 sub-step 3b verifies repo doesn't already exist on GitHub before creation |
| T-SEC-01 | Information Disclosure | Spike 001 captures may contain real user-message content (trivial test prompts vs personal coursework) | mitigate | Task 1c authors and runs `audit-spike-001-captures.sh` with regex for sensitive patterns + identifying markers (COMP/MATH course codes, user name); halt + manual review on flag |
| T-PUB-03 | Information Disclosure | CLAUDE.md (project-level) accidentally tracked despite gitignore | mitigate | Task 1e explicit `git check-ignore CLAUDE.md` test; Task 2 staging uses explicit file-list (NOT `git add -A`) |
| T-CFG-02 | Tampering | guard-branch.sh hook bypass via `--no-verify` introduces commit on wrong branch | reject | L1 CLAUDE.md "NEVER skip hooks unless user explicitly requests"; Task 2 specifies "DO NOT bypass with --no-verify" |
| T-CACHE-01 | Repudiation | Claude Code session cache orphan after path move causes loss of conversation history | accept | RESEARCH.md verified: history.jsonl is global (path-independent); cache regenerates under new path; recommended action is "leave as-is" (option A) |
| T-PUB-04 | Information Disclosure | Future portfolio-release brand confusion (commercialization claim implied by public repo) | mitigate | README.md OOS-01 amendment note in BOTH language sections (Plan 03 Task 2) explicitly states no commercialization, no SaaS, no multi-user |
| T-FOSSIL-02 | Repudiation | First-time GitHub repo creation re-uses bundle id `dev.<finalname>.spike` confusing future reader (spike vs production) | accept | Phase 1 will scaffold production tauri.conf.json with `dev.<finalname>.app` (D-10 reserves this); README clarifies; spike 002 README has "Historical note" footnote per Plan 03 Pitfall 9 |

</threat_model>

<verification>
Phase-level checks owned by this plan (consolidated gate):
- V-01..V-04, V-06..V-09: All bundled in `verify-rename.sh "$FINAL"` — Task 1a runs the consolidated gate
- V-02: `verify-icon.sh` — Task 1b
- V-05 (window/Dock contract): Deferred to Phase 1 verify per VALIDATION.md row 5; Phase 0 only locks the contract-text in CONTEXT.md D-14
- T-SEC-01 audit: Task 1c runs `audit-spike-001-captures.sh`
- Visual checks (Task 4): VALIDATION.md "Manual-Only Verifications" rows 1, 3 — recorded in SUMMARY.md, not blocking automation

Single command summarizes all automated gates:
```bash
bash .planning/phases/00-identity-branding-lock/scripts/verify-rename.sh "$FINAL" \
  && bash .planning/phases/00-identity-branding-lock/scripts/verify-icon.sh \
  && bash .planning/phases/00-identity-branding-lock/scripts/audit-spike-001-captures.sh
```
All three must exit 0 before Task 2 commits.
</verification>

<success_criteria>
- [ ] Task 1: All 3 gate scripts (verify-rename.sh, verify-icon.sh, audit-spike-001-captures.sh) green; `git check-ignore CLAUDE.md` passes; V-09 standalone re-run returns 0 hits.
- [ ] Task 2: Single atomic commit on `gsd/phase-0-identity-branding-lock` branch with comprehensive bullet-list message; STATE.md update folded into the same commit; guard-branch.sh hook NOT bypassed.
- [ ] Task 3: Local path moved to `/Users/qinyuan/claude/r1ckyIn_GitHub/${FINAL}`; GitHub repo created at `r1ckyIn/${FINAL}` (public, with description, with topics); branch pushed.
- [ ] Task 4: User confirms visual icon check pass + README readability pass; STATE.md correctly reflects Phase 0 complete + Phase 1 as next.
- [ ] Final integration check: `pwd` returns the new path; `git remote -v` shows origin under new name; `gh repo view r1ckyIn/${FINAL} --json visibility` returns PUBLIC.
- [ ] No checkpoint bypass: all human-action / human-verify checkpoints completed via resume-signal mechanism.
</success_criteria>

<output>
After completion, create `.planning/phases/00-identity-branding-lock/00-04-SUMMARY.md` recording:
- Final atomic commit SHA + message excerpt
- GitHub repo URL + visibility status + applied topics
- Old → new path migration confirmation
- audit-spike-001-captures.sh output (all clean / which patterns flagged + how user resolved)
- Task 4 visual checks: icon Dock-render verdict + README GitHub-render verdict (verbatim user replies)
- Claude Code session cache decision (option A leave-as-is / option B migrate)
- Any deviations from RESEARCH.md "locked pipeline" (Stage C skipped → Option D3 documentation, etc.)
- Hand-off pointer: STATE.md Phase 1 ready for `/gsd-discuss-phase 1` or `/gsd-plan-phase 1`; codename `learn-os` permanently retired.
- Phase 0 wall-clock duration vs D-08 ≤1-day time-box budget.
</output>
