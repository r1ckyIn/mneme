---
phase: 00-identity-branding-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
autonomous: false  # D-04 user-pick checkpoint
success_criteria_covered: [SC-1]
tdd_applicable: false
tdd_rationale: "No business logic, API, or algorithm — task is finalist presentation + user pick + decision hand-off file authoring. Verifiable via grep + file existence, not unit test."
validation_map: [V-01]
requirements: [SC-1]  # Phase 0 has no v1/v1.x REQs; using ROADMAP success criteria as equivalent
user_setup: []

must_haves:
  truths:
    - "User has selected one final app name from researcher-validated finalists (Mneme / Theoria / Scholea)"
    - "Locked name <finalname> is recorded in NAME-DECISION.md hand-off file with rationale"
    - "Plans 02 + 03 + 04 can read <finalname> from a single canonical location"
  artifacts:
    - path: ".planning/phases/00-identity-branding-lock/00-NAME-DECISION.md"
      provides: "Locked final name + rationale + bundle-id pattern + skill-rename target"
      contains: "final_name:"
  key_links:
    - from: "00-NAME-DECISION.md"
      to: "Plans 02, 03, 04"
      via: "frontmatter `final_name` field; downstream plans read this file's frontmatter to substitute <finalname>"
      pattern: "final_name: [a-z]+"
---

<objective>
Present the three researcher-validated naming finalists (Mneme, Theoria, Scholea) to the user with conflict-cleanliness × D-02 fit ranking, capture the user's decision, and lock the chosen name in a single canonical hand-off file (`00-NAME-DECISION.md`) that Plans 02, 03, and 04 read for `<finalname>` substitution.

Purpose: Per D-04, the user — not Claude — selects the final app name. This plan is the bottleneck checkpoint between research and execution; downstream icon production, atomic rename, and GitHub publish all depend on a locked name. Without this, Plan 02 cannot draft a gpt-image-1 prompt embedding the focal monogram letter, and Plan 03 cannot run the bulk-replace pipeline.

Output: One Markdown hand-off file with YAML frontmatter (`final_name:`, `bundle_id_app:`, `bundle_id_spike:`, `skill_dir:`, `directory_convention:`) plus prose rationale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-identity-branding-lock/00-CONTEXT.md
@.planning/phases/00-identity-branding-lock/00-RESEARCH.md
@.planning/phases/00-identity-branding-lock/00-VALIDATION.md

<interfaces>
<!-- Downstream consumers: how Plans 02-04 read the locked name -->

Plans 02, 03, 04 each read `.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md`
and parse the YAML frontmatter:

```yaml
---
final_name: <name>          # e.g. "mneme", lowercase, ASCII-safe for paths
final_name_display: <Name>  # e.g. "Mneme", title-case for README + window-title
bundle_id_app: dev.<name>.app      # Phase 1 production target (locked here, used in Phase 1)
bundle_id_spike: dev.<name>.spike  # Phase 0 spike-002 flip target (used in Plan 03)
skill_dir: spike-findings-<name>   # New skill directory name
directory_convention: .<name>      # REQ-17 per-course rules dir (was .learnos)
decision_date: 2026-05-07
---
```

Reading pattern in downstream plans (Bash):
```bash
NAME_DECISION=.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md
FINAL=$(grep '^final_name:' "$NAME_DECISION" | awk '{print $2}')
BUNDLE_SPIKE=$(grep '^bundle_id_spike:' "$NAME_DECISION" | awk '{print $2}')
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Present finalists table and capture user decision</name>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (sections "Naming Due-Diligence Results" lines 371-436 — verdicts table for D-03 starting candidates + D-03 backup pool + expansion candidates + Top 3 Finalists)
    - .planning/phases/00-identity-branding-lock/00-CONTEXT.md (D-01..D-04 — naming criteria + decision authority)
  </read_first>
  <decision>
    User selects ONE final app name from the three researcher-vetted finalists (or supplies an alternative).
  </decision>
  <context>
    All three D-03 starting candidates and all five D-03 backup-pool candidates were rejected by the researcher (extreme namespace conflicts in dev-tools / EdTech / AI niche).

    Three vetted finalists (RESEARCH.md "Top 3 Finalists" + "Honourable mentions" + Q1 Sketch volume note + A1 Mneme footprint note):

    | Rank | Candidate | Pronunciation | Mandarin | Syllables | D-02 Fit | Conflicts (HIGH-confidence) | Risk |
    |------|-----------|---------------|----------|-----------|----------|------------------------------|------|
    | #1   | **Mneme** | /ˈniːmi/      | 尼-米    | 2         | EXCELLENT (theme + syllable) | App Store: "Mneme: Memory journal", "Mneme AI - Local AI Notes" (direct competitor — small footprint per A1) | Direct competitor exists in our exact niche; no popularity ratings noted but unverified |
    | #2   | **Theoria**| /θiːˈɔːriə/   | 西奥利亚 | 4         | THEME-STRONG, SYLLABLE-VIOLATION | None — npm FREE, GitHub effectively empty, no software product | 4 syllables hard violates D-02 ≤2 rule |
    | #3   | **Scholea**| /ˈskoʊliə/    | 斯科利亚 | 3         | THEME-STRONG, SYLLABLE-BORDERLINE | None — npm FREE, GitHub empty, no app conflicts | 3 syllables borderline; less recognizable; misspelling risk (Scholia/Scholae/Scholee) |

    Honourable mentions (drop-in replacements if user dislikes finalists):
    - **Stele** (/ˈstiːliː/, 2 syllables, "engraved memory stone") — clean GitHub, small npm placeholder
    - **Anaxis** (3 syllables) — clean availability but thin meaning

    User's stated aesthetic (CONTEXT.md `<specifics>`): Greek/Latin scholarly roots, timeless feel, NOT 2020s-AI-startup derivative.
  </context>
  <options>
    <option id="mneme">
      <name>Mneme</name>
      <pros>Best D-03 survivor; 2 syllables (D-02 strict win); Greek goddess-of-memory gravitas; bilingual-friendly (尼-米); ICNS monogram letter "M" works graphically</pros>
      <cons>Two App Store competitors (one a direct AI-notes competitor); user should know they exist before committing</cons>
    </option>
    <option id="theoria">
      <name>Theoria</name>
      <pros>Cleanest namespace (npm FREE, no app conflicts); strong philosophical theme (Plato's contemplative knowing); ICNS monogram letter "T" works graphically</pros>
      <cons>4 syllables — explicit hard violation of D-02 ≤2 rule; Mandarin (西奥利亚) is 4 syllables too</cons>
    </option>
    <option id="scholea">
      <name>Scholea</name>
      <pros>Clean namespace (npm FREE, GitHub empty); literal "school" etymology; ICNS monogram letter "S" works graphically</pros>
      <cons>3 syllables borderline; misspelling risk; less recognizable Greek root than Mneme/Theoria</cons>
    </option>
    <option id="other">
      <name>Other (user supplies)</name>
      <pros>Stele or Anaxis from honourable mentions; or user has fresh idea</pros>
      <cons>Bypasses researcher due-diligence — user accepts conflict-check responsibility</cons>
    </option>
  </options>
  <resume-signal>
    Reply with one of:
    - "mneme" / "theoria" / "scholea" — pick a finalist
    - "other: &lt;name&gt;" — supply alternative (Claude will perform a quick GitHub + npm conflict scan before locking)
  </resume-signal>
  <acceptance_criteria>
    - User has typed a clear selection (one of the four resume-signal forms)
    - Selected name is ASCII-only, lowercase-coercible, ≤8 characters
    - If "other" supplied: Claude performed a quick `gh api search/repositories` + `curl https://registry.npmjs.org/&lt;name&gt;` conflict scan and reported findings; user re-confirmed
  </acceptance_criteria>
  <done>
    User has confirmed exactly one final name. Variable `&lt;finalname&gt;` is now resolvable for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author NAME-DECISION.md hand-off file</name>
  <files>.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md</files>
  <read_first>
    - .planning/phases/00-identity-branding-lock/00-CONTEXT.md (D-10 bundle-id pattern, D-11 rename scope, D-15 codename retirement date)
    - .planning/phases/00-identity-branding-lock/00-RESEARCH.md (Q4 — bundle id `.spike` vs `.app` rationale)
  </read_first>
  <action>
    Write `.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md` with the following exact frontmatter shape (substitute &lt;name&gt; with Task 1's locked name, lowercased; substitute &lt;Name&gt; with title-case):

    ```yaml
    ---
    phase: 00-identity-branding-lock
    type: name-decision
    final_name: <name>
    final_name_display: <Name>
    bundle_id_app: dev.<name>.app
    bundle_id_spike: dev.<name>.spike
    skill_dir: spike-findings-<name>
    directory_convention: .<name>
    codename_retired: learn-os
    codename_retirement_date: 2026-05-07
    decision_date: 2026-05-07
    decision_authority: user (per D-04)
    ---
    ```

    Body sections (Markdown, ≤ 60 lines total):

    1. `## Locked Name` — single-line statement: "Final app name: **&lt;Name&gt;**" + IPA pronunciation + Mandarin transliteration.

    2. `## Why This Name` — 2-3 bullets summarizing user's rationale (Claude paraphrases user's reply from Task 1; if user picked silently, Claude writes "User picked &lt;Name&gt;; rationale: best D-02 fit per researcher's verdict table" and references RESEARCH.md).

    3. `## Bundle Identifier Pattern` — table:
       | Domain | Pattern | Used In |
       |--------|---------|---------|
       | Production app (Phase 1+) | `dev.<name>.app` | New `tauri.conf.json` scaffolded in Phase 1 |
       | Spike artifact (frozen) | `dev.<name>.spike` | Existing `.planning/spikes/002-tauri-claude-shell/app/src-tauri/tauri.conf.json` (Plan 03 flips this) |

       (Per CONTEXT.md D-11 + RESEARCH.md Q4: spike keeps `.spike` suffix to tag it as historical/non-production; production gets `.app` reserved for Phase 1.)

    4. `## Rename Targets` — bulleted summary of what Plans 02-04 will use this name for:
       - Repo dir + GitHub repo: `r1ckyIn/&lt;name&gt;`
       - All `.planning/*.md` references: `learn-os` → `&lt;name&gt;`
       - Directory convention (REQ-17): `.learnos/rules/` → `.&lt;name&gt;/rules/`
       - Skill: `.claude/skills/spike-findings-learn-os/` → `.claude/skills/spike-findings-&lt;name&gt;/`
       - Bundle id (spike 002): `dev.learn-os.spike` → `dev.&lt;name&gt;.spike`

    5. `## Conflicts Acknowledged` — list any HIGH-confidence conflicts the user accepted (e.g., for Mneme: "App Store competitor 'Mneme AI - Local AI Notes' exists; user accepts; D-04 decision is final per CONTEXT.md").

    6. `## Hand-off` — single sentence: "Plans 02, 03, 04 read this file's YAML frontmatter to substitute &lt;finalname&gt; throughout their tasks."

    Use English-only content (machine-consumed by downstream plan executors). Do NOT translate to Chinese here — orchestrator-facing summaries can be Chinese, but file content is parsed by Plans 02-04.
  </action>
  <verify>
    <automated>test -f .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && grep -E '^final_name: [a-z]+$' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && grep -E '^bundle_id_app: dev\.[a-z]+\.app$' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && grep -E '^bundle_id_spike: dev\.[a-z]+\.spike$' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && grep -E '^skill_dir: spike-findings-[a-z]+$' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md && grep -E '^directory_convention: \.[a-z]+$' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `.planning/phases/00-identity-branding-lock/00-NAME-DECISION.md`
    - Frontmatter contains all 11 required fields (phase, type, final_name, final_name_display, bundle_id_app, bundle_id_spike, skill_dir, directory_convention, codename_retired, codename_retirement_date, decision_date, decision_authority)
    - `final_name` is lowercase ASCII (`[a-z]+`)
    - `bundle_id_app` matches `dev.&lt;final_name&gt;.app` exactly
    - `bundle_id_spike` matches `dev.&lt;final_name&gt;.spike` exactly (NOT `.app` — per D-11 / RESEARCH.md Q4)
    - `skill_dir` matches `spike-findings-&lt;final_name&gt;`
    - `directory_convention` is `.&lt;final_name&gt;` (with leading dot, no `/rules/` suffix — Plans 03 will append `/rules/` where needed)
    - All 6 body sections present (Locked Name, Why This Name, Bundle Identifier Pattern, Rename Targets, Conflicts Acknowledged, Hand-off)
    - File is English-only (no `中文` heading; verify: `! grep -q '中文' .planning/phases/00-identity-branding-lock/00-NAME-DECISION.md`)
  </acceptance_criteria>
  <done>
    NAME-DECISION.md exists with valid frontmatter; downstream plans (02, 03, 04) can extract `<finalname>` deterministically via `grep '^final_name:' | awk '{print $2}'`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Claude (Task 1) | User-supplied alternative name (option "other") crosses into Claude's automation surface; if malformed (Unicode, spaces, special chars) downstream sed/path operations break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PUB-02 | Spoofing | Brand confusion / namespace squatting on `r1ckyIn/<finalname>` | mitigate | Researcher pre-vetted all 3 finalists for GitHub + npm conflicts (RESEARCH.md "Naming Due-Diligence Results"); for option "other" Task 1 acceptance criteria force a quick conflict scan before locking |
| T-CFG-01 | Tampering | Invalid `<finalname>` characters break downstream sed/iconutil/git-mv | mitigate | Task 2 acceptance criteria enforce `[a-z]+` regex on `final_name`; ASCII-lowercase-only; ≤ 8 chars |
| T-RNM-01 | Information Disclosure | Codename `learn-os` survives in NAME-DECISION.md (the audit trail is fine but the frontmatter `codename_retired` field documents intent) | accept | NAME-DECISION.md is itself audit-record; Plan 03's V-07 grep gate skip-lists `.planning/phases/00-identity-branding-lock/` so this file's codename references are intentional traceability, not leakage |

</threat_model>

<verification>
Phase-level checks owned by this plan (V-01):
- V-01 (SC-1): Validates after Plan 03 completes the rename. THIS plan provides the input (`final_name` field of NAME-DECISION.md frontmatter) that Plan 03's V-01 grep gate matches against `.planning/PROJECT.md`.
- This plan does NOT yet update PROJECT.md — that happens in Plan 03 Stage 3b.
</verification>

<success_criteria>
- [ ] Task 1: User has selected exactly one final name (mneme / theoria / scholea / other).
- [ ] Task 2: NAME-DECISION.md exists with valid YAML frontmatter parseable via `grep '^final_name:'`.
- [ ] Plans 02-04 can deterministically extract `<finalname>` from the hand-off file.
- [ ] No circular dependency: this plan does NOT touch repo content beyond creating the new hand-off file.
</success_criteria>

<output>
After completion, create `.planning/phases/00-identity-branding-lock/00-01-SUMMARY.md` recording:
- Locked `<finalname>` value
- User's stated rationale (1-2 sentences)
- Any conflicts user explicitly accepted (link to NAME-DECISION.md `## Conflicts Acknowledged` section)
- Hand-off pointer for Plan 02 (gpt-image-1 monogram letter is `<finalname>`'s first letter, capitalized)
</output>
