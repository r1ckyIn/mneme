---
phase: 03-multi-session-command-palette-editor
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/session-machine.ts
  - src/lib/editor-markdown.ts
  - src/lib/transcript-reader.ts
  - tests/session-machine.test.ts
  - tests/editor-markdown.test.ts
  - tests/transcript-reader.test.ts
  - tests/fixtures/markdown-roundtrip/frontmatter.md
  - tests/fixtures/markdown-roundtrip/code-and-math.md
  - tests/fixtures/markdown-roundtrip/nested-lists-wikilinks.md
  - tests/fixtures/transcript/sample.jsonl
autonomous: true
requirements: [REQ-12, KD-09]
must_haves:
  truths:
    - "The session state machine is a pure `(state, event) -> state` reducer: create/switch/rename/close transitions are applied; illegal transitions are rejected (not silently absorbed) — REQ-12."
    - "Markdown round-trip is byte-stable across `split frontmatter -> body -> rejoin` for a fixture corpus incl. YAML frontmatter, fenced code, KaTeX math, nested lists, and `[[wikilinks]]` — KD-09 verified round-trip."
    - "A Claude Code `.jsonl` transcript (incl. an `isCompactSummary` record) parses with vendored claude-code-parser types and feeds stream-dispatch to reconstruct rendered history (D-06); malformed lines are skipped, not thrown."
  artifacts:
    - path: "src/lib/session-machine.ts"
      provides: "pure session reducer + SessionState/SessionEvent types (analog: stream-dispatch pure dispatcher)"
      contains: "export function reduceSession"
    - path: "src/lib/editor-markdown.ts"
      provides: "gray-matter splitForEditor/joinForSave (frontmatter split/rejoin around the Tiptap body) — KD-09 round-trip core"
      contains: "splitForEditor"
    - path: "src/lib/transcript-reader.ts"
      provides: "transcriptToState(jsonlText) JSONL->DispatchState adapter reusing freshState/dispatchEvent + vendored ClaudeEvent types (KD-12)"
      contains: "transcriptToState"
  key_links:
    - from: "src/lib/transcript-reader.ts"
      to: "src/lib/stream-dispatch.ts"
      via: "import { freshState, dispatchEvent } reused unchanged"
      pattern: "from \"\\$lib/stream-dispatch\""
    - from: "src/lib/editor-markdown.ts"
      to: "gray-matter"
      via: "matter() split + matter.stringify() rejoin"
      pattern: "gray-matter"
---

<objective>
Build the three pure-logic TypeScript modules Phase 3 needs, each a clean TDD target with deterministic I/O and no UI: (1) `session-machine.ts` — the pure session-thread reducer (create/switch/rename/close, illegal transitions rejected); (2) `editor-markdown.ts` — gray-matter frontmatter split/rejoin around the Tiptap body, the KD-09 round-trip core; (3) `transcript-reader.ts` — the opt-in JSONL→DispatchState adapter (D-06) reusing `stream-dispatch` + vendored `ClaudeEvent` types.

Purpose: these are the VALIDATION.md proofs for the session state machine (REQ-12), the markdown round-trip (KD-09), and the JSONL transcript parse (D-06) — all unit-testable in isolation before any component consumes them. They have zero file-ownership overlap with the other Wave-1 plans (different files) so they run in parallel.
Output: three `src/lib/` modules + three vitest test files + the markdown round-trip fixture corpus + a transcript fixture. No Svelte components, no Rust, no spawn-args (those are other plans). Wave 2 consumes these: `session-machine.ts` → `sessions.svelte.ts` (03-05), `editor-markdown.ts` → `TiptapEditor.svelte` (03-07), `transcript-reader.ts` → opt-in full-restore (03-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md
@.planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md
@.planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md
@.planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md
@src/lib/stream-dispatch.ts
@vendor/claude-code-parser/src/types/protocol.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: session-machine.ts pure reducer (REQ-12)</name>
  <files>src/lib/session-machine.ts, tests/session-machine.test.ts</files>
  <read_first>
    - src/lib/stream-dispatch.ts (analog: pure `(event, state) -> void` dispatcher discipline, the no-module-state rule, defensive narrowing helpers `readString`/`typeof`/`in` guards)
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md row 1 (session state machine — illegal transitions rejected, not absorbed) + Reference Dataset "Session-state test vectors"
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "palette-actions.ts (pure data)" note on no module-scope mutable state
    - .planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md D-08 (lazy: a thread exists as metadata with no process until a prompt is sent)
  </read_first>
  <behavior>
    - RED first: write `tests/session-machine.test.ts` enumerating legal AND illegal `(state, event)` pairs. Define the model: a thread is `{ id, title, status }` where `status` ∈ `"idle" | "active" | "generating" | "closed"`; `SessionState = { threads: SessionThread[], activeId: string | null }`. Legal events: `{type:"create", id, title}` adds an idle thread + sets activeId; `{type:"switch", id}` sets activeId to an existing thread; `{type:"rename", id, title}` renames an existing thread; `{type:"close", id}` marks a thread closed (NOT deleted — D-06 transcript persists) + clears activeId if it was active; `{type:"begin-generate", id}` idle/active→generating; `{type:"end-generate", id}` generating→active. Illegal cases asserted to be REJECTED (state returned unchanged, NOT mutated): switch/rename/close to a non-existent id; begin-generate on a closed thread; a 3rd begin-generate while 2 are already generating (the reducer reports it can't, leaving the toast to the caller — mirror D-10 but the authoritative gate is Rust). Run `npx vitest run tests/session-machine.test.ts` → FAIL (module absent). Capture RED.
    - GREEN: write `src/lib/session-machine.ts` exporting `SessionThread`, `SessionState`, `SessionEvent` types and `export function reduceSession(state: SessionState, event: SessionEvent): SessionState` — a PURE reducer returning a NEW state object (immutable; never mutate the input, per CLAUDE.md immutability rule). On an illegal event, return the input `state` unchanged (or a structurally-equal new object) — never throw, never partially apply. No module-scope mutable state.
    - REFACTOR: confirm `reduceSession` does not mutate `state.threads` in place (use spread/map) and has no module-level `let`. Header cites `// REQ-12` + `// analog: stream-dispatch.ts pure dispatcher`.
  </behavior>
  <action>Define `status` as the closed set `"idle" | "active" | "generating" | "closed"` (string-literal union, matching the codebase convention over enums). `reduceSession` returns a new `SessionState` for every call; illegal transitions return the unchanged state (caller decides UX). Do NOT couple the reducer to Rust or to the ceiling — the reducer can model "would exceed 2 generating" by returning unchanged + the caller surfaces the toast; the authoritative D-10 gate stays in `session.rs` (03-01). No `$state` here (this is plain `.ts`, a pure reducer — NOT a `.svelte.ts` store; the reactive store `sessions.svelte.ts` in 03-05 wraps this reducer).</action>
  <verify>
    <automated>npx vitest run tests/session-machine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export function reduceSession' src/lib/session-machine.ts` == 1; `grep -c 'export.*\\$state' src/lib/session-machine.ts` == 0 (no runes — pure module).
    - Behavior assertion: vitest passes all legal transitions AND every illegal case returns state unchanged (a test deep-equals the output to the input for each illegal event).
    - Source assertion (immutability): the reducer does not call `.push(`/`.splice(`/`.sort(` on `state.threads` — `grep -Ec '\\.push\\(|\\.splice\\(|state\\.threads\\.sort' src/lib/session-machine.ts` == 0.
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>reduceSession applies all legal transitions immutably and rejects every illegal one by returning state unchanged; no module state; no runes.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: editor-markdown.ts frontmatter round-trip (KD-09)</name>
  <files>src/lib/editor-markdown.ts, tests/editor-markdown.test.ts, tests/fixtures/markdown-roundtrip/frontmatter.md, tests/fixtures/markdown-roundtrip/code-and-math.md, tests/fixtures/markdown-roundtrip/nested-lists-wikilinks.md</files>
  <read_first>
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "editor-markdown.ts (net-new, gray-matter API analog)" (the `splitForEditor`/`joinForSave` code at L271-279) + tests/gray-matter.test.ts (existing gray-matter API usage)
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Pitfall 5 (Tiptap markdown ext does NOT model YAML frontmatter — split with gray-matter first) + Pitfall 3 (round-trip lossy on edge cases — assert AST or byte equality deliberately)
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md "Markdown round-trip fidelity incl. frontmatter" row + Reference Dataset "Markdown round-trip corpus"
  </read_first>
  <behavior>
    - RED first: create the three fixtures under `tests/fixtures/markdown-roundtrip/`: `frontmatter.md` (YAML frontmatter block + a heading + a paragraph), `code-and-math.md` (a fenced code block with a language tag + an inline `$x^2$` + a `$$` display-math block), `nested-lists-wikilinks.md` (a 2-level nested bulleted+numbered list + a `[[wikilink]]`). Write `tests/editor-markdown.test.ts`: for each fixture, `const { frontmatter, body } = splitForEditor(raw)`; assert the frontmatter block was stripped from `body` (the `body` must NOT start with `---`); then `joinForSave(body, frontmatter)` must reproduce the ORIGINAL frontmatter block verbatim (the frontmatter half is byte-stable — gray-matter's job). For the body half, assert the split `body` equals gray-matter's `content` exactly (the Tiptap serializer round-trip itself is deferred to the Wave-0-style spike noted in PATTERNS; THIS plan pins only the frontmatter split/rejoin contract, which is the load-bearing data-loss risk). Run `npx vitest run tests/editor-markdown.test.ts` → FAIL (module absent). Capture RED.
    - GREEN: write `src/lib/editor-markdown.ts` exporting `export function splitForEditor(fileText: string): { frontmatter: Record<string, unknown>; body: string }` using `matter(fileText)` (gray-matter, already a dep) → `{ frontmatter: data, body: content }`; and `export function joinForSave(body: string, frontmatter: Record<string, unknown>): string` → `Object.keys(frontmatter).length ? matter.stringify(body, frontmatter) : body`. Validate input at the boundary (CLAUDE.md): if `fileText` is not a string, throw with the `[editor-markdown]` prefix.
    - REFACTOR: confirm `joinForSave(splitForEditor(raw).body, splitForEditor(raw).frontmatter)` reproduces the original frontmatter for every fixture (round-trip property test). Header cites `// KD-09` + `// Pitfall 5 (frontmatter not modeled by tiptap-markdown)`.
  </behavior>
  <action>Use `gray-matter` (already a dep — do NOT add a dependency; do NOT hand-roll a `---\n...\n---` regex split per RESEARCH Don't-Hand-Roll). `splitForEditor` returns `{ frontmatter, body }`; `joinForSave` rejoins. This module is the SINGLE owner of the official-Tiptap-markdown-extension serialize/parse config too (a `// TODO(03-07 spike): pin official md ext serialize API` comment marks where 03-07 adds it — but THIS plan ships only the gray-matter split/rejoin + its tests, which is the data-loss-critical half). Body-level Tiptap serializer round-trip fixtures are exercised by 03-07 when the editor mounts; this plan pins frontmatter byte-stability now so the editor can never silently drop a YAML block.</action>
  <verify>
    <automated>npx vitest run tests/editor-markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export function splitForEditor' src/lib/editor-markdown.ts` == 1 AND `grep -c 'export function joinForSave' src/lib/editor-markdown.ts` == 1; `grep -c 'gray-matter' src/lib/editor-markdown.ts` >= 1.
    - Behavior assertion: vitest passes for all three fixtures — `body` never starts with `---`, and `joinForSave` reproduces the original frontmatter block verbatim.
    - Artifact assertion: `ls tests/fixtures/markdown-roundtrip/*.md | wc -l` >= 3.
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>splitForEditor/joinForSave round-trip the YAML frontmatter byte-stably for the fixture corpus using gray-matter; the editor body is cleanly separated from frontmatter; no hand-rolled regex.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: transcript-reader.ts JSONL -> DispatchState (D-06)</name>
  <files>src/lib/transcript-reader.ts, tests/transcript-reader.test.ts, tests/fixtures/transcript/sample.jsonl</files>
  <read_first>
    - src/lib/stream-dispatch.ts (`freshState()` + `dispatchEvent(evt, state)` reused unchanged; defensive narrowing `readString`/`typeof`/`in` guards are the precedent for forward-compat JSONL fields)
    - vendor/claude-code-parser/src/types/protocol.ts (the `ClaudeEvent` type — type-only import per KD-12; do NOT import runtime code from vendor)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "transcript-reader.ts (net-new adapter)" (the `transcriptToState` code at L289-301 + the `isRenderableRecord` adapter note) + No/Weak-Analog row
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md D-05a Resolution (JSONL record census: `assistant`/`user`/`ai-title`/`isCompactSummary`; the compaction-summary record shape) + Anti-Pattern "Reading the whole 7.4MB JSONL synchronously" (this is the OPT-IN full-restore path only)
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md "JSONL transcript parse -> history render" row
  </read_first>
  <behavior>
    - RED first: create `tests/fixtures/transcript/sample.jsonl` — a small hand-authored fixture (a few lines) modeling the real record shapes from RESEARCH D-05a: an `assistant` record, a `user` record, an `ai-title` record, one `isCompactSummary: true` user record (the D-05a compaction-summary record), a blank line, and one malformed (non-JSON) line. Write `tests/transcript-reader.test.ts`: `transcriptToState(raw)` returns a `DispatchState` whose `messages` reconstruct the renderable conversation; the malformed line is SKIPPED (no throw); the blank line is skipped; the `isCompactSummary` record is identifiable (assert the adapter recognizes it as the resume-summary source per D-05a — e.g. `extractCompactSummary(raw)` returns its text, or the parsed message is tagged). Run `npx vitest run tests/transcript-reader.test.ts` → FAIL (module absent). Capture RED.
    - GREEN: write `src/lib/transcript-reader.ts` importing `{ freshState, dispatchEvent }` and `type { ClaudeEvent }` from `$lib/stream-dispatch` (or the vendored types per KD-12). Export `transcriptToState(jsonlText: string): DispatchState`: split on `\n`, skip empty/whitespace lines, `JSON.parse` each in a try/catch that `continue`s on failure (append-only file may have a torn last line), and for renderable records call `dispatchEvent`. Add `isRenderableRecord(rec: unknown): boolean` doing defensive narrowing (mirror stream-dispatch's `typeof`/`in` guards) — only `assistant`/`user` records feed the renderer; `ai-title`/`file-history-snapshot`/`system/*` are recognized but not rendered as chat turns. Also export `extractCompactSummary(jsonlText: string): string | null` returning the `isCompactSummary` record's text if present (the D-05a opportunistic summary source), else null.
    - REFACTOR: confirm NO vendored RUNTIME code is imported (types only, KD-12) — `grep` the import for a `type` keyword on the vendor import. Header cites `// D-06 D-05a KD-12` + the Anti-Pattern note that this is the OPT-IN full-restore path (the default Recents preview never calls this).
  </behavior>
  <action>Reuse `freshState`/`dispatchEvent` from `stream-dispatch.ts` UNCHANGED (do NOT fork the dispatcher). Import `ClaudeEvent` as a TYPE only (KD-12: vendored parser is type-only). The per-line JSONL record shape differs from the stream-json NDJSON envelope (RESEARCH A8) — `isRenderableRecord` is the thin adapter that maps/filters; unknown forward-compat fields are tolerated, never thrown on. Malformed lines `continue`. `extractCompactSummary` is the D-05a best-effort summary source (returns null when absent — the dependency-free floor is the mneme-generated summary, computed in 03-08 on `chat.result`, NOT here). This module is the OPT-IN full-transcript path; document that the default Recents flow does not invoke it (Anti-Pattern: never read the 7.4MB file on a normal thread switch).</action>
  <verify>
    <automated>npx vitest run tests/transcript-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export function transcriptToState' src/lib/transcript-reader.ts` == 1 AND `grep -c 'from "\$lib/stream-dispatch"' src/lib/transcript-reader.ts` >= 1.
    - Source assertion (type-only vendor import): any import from `vendor/claude-code-parser` or `$vendor/` uses `import type` — `grep -E 'import .*vendor|import .*\$vendor' src/lib/transcript-reader.ts | grep -vc 'import type'` == 0.
    - Behavior assertion: vitest passes — renderable records reconstruct messages, malformed + blank lines are skipped (no throw), and the `isCompactSummary` record is extractable via `extractCompactSummary`.
    - Artifact assertion: `tests/fixtures/transcript/sample.jsonl` exists and contains an `isCompactSummary` line (`grep -c isCompactSummary tests/fixtures/transcript/sample.jsonl` >= 1).
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>transcriptToState parses a real-shaped JSONL into DispatchState reusing stream-dispatch, skips malformed/blank lines without throwing, extracts the D-05a compaction summary when present, and imports vendored types type-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code JSONL file -> mneme parser | the `.jsonl` is written by the `claude` subprocess; mneme reads it but must never execute its content |
| vault `.md` file -> editor split | file content is user/Claude-authored markdown; parsed, never evaluated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-10 | Tampering | transcript JSONL content | mitigate | type-only parse (KD-12); `JSON.parse` in try/catch, malformed lines skipped; NO eval / NO dynamic import of transcript content; render path still goes through `sanitize.ts` (Wave 2) |
| T-3-11 | Information Disclosure | reading arbitrary JSONL paths | accept | the path is `~/.claude/projects/<encoded-cwd>/<id>.jsonl` derived from the persisted row in Wave 3; this module takes already-read TEXT, not a path |
| T-3-12 | Tampering | frontmatter YAML parse | mitigate | gray-matter (vetted, already a dep) parses YAML; no `eval`; output is a plain object stored via bound params in Rust |
</threat_model>

<verification>
- `npx vitest run tests/session-machine.test.ts tests/editor-markdown.test.ts tests/transcript-reader.test.ts` — all green.
- `npm run check` (svelte-check + tsc strict) shows no new errors for the three new modules.
</verification>

<success_criteria>
- All three VALIDATION.md proofs (session state machine, markdown frontmatter round-trip, JSONL parse) pass.
- Zero file overlap with 03-01/03-02/03-04 (parallel Wave 1).
- Modules are pure/adapters consumed by Wave 2 (`sessions.svelte.ts`, `TiptapEditor.svelte`).
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-command-palette-editor/03-03-SUMMARY.md`. Include RED proofs and document the exports Wave 2 consumes: `session-machine.reduceSession` (→ 03-05 `sessions.svelte.ts`), `editor-markdown.{splitForEditor,joinForSave}` (→ 03-07 `TiptapEditor.svelte`), `transcript-reader.{transcriptToState,extractCompactSummary}` (→ 03-07 opt-in full restore). Note the deferred official-Tiptap-markdown serialize-API pin that 03-07 must resolve in its editor mount.
</output>
