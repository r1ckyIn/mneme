---
phase: 03-multi-session-command-palette-editor
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/editor_lock.rs
  - scripts/editor-lock-hook.mjs
  - tests/editor-lock-lifecycle.test.ts
  - scripts/__tests__/editor-lock-hook.test.mjs
  - src-tauri/tests/editor_lock_io.rs
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "The node PreToolUse hook exits 2 (blocks Claude's Edit/Write) when the stdin JSON `.tool_input.file_path` is in the lockfile; exits 0 when unlocked — the human editor is never blocked (D-09 / D-09a)."
    - "mneme writes/clears lock entries atomically (temp+rename) to `~/.mneme/editor-locks.json`; lockfile write failure never crashes the caller (D-09b)."
    - "The lock lifecycle resolves: focus -> locked, blur -> cleared, and the mtime<5s post-write rule resolves to locked/unlocked at boundary times (D-09b)."
  artifacts:
    - path: "src-tauri/src/editor_lock.rs"
      provides: "atomic write/clear of ~/.mneme/editor-locks.json + idempotent vault .claude/settings.json hook declaration (analog: config.rs atomic temp+rename)"
      contains: "editor-locks.json"
    - path: "scripts/editor-lock-hook.mjs"
      provides: "node PreToolUse hook: stdin JSON .tool_input.file_path vs lockfile -> exit 2 / exit 0 (no jq)"
      contains: "tool_input"
    - path: "tests/editor-lock-lifecycle.test.ts"
      provides: "lock lifecycle proof (focus/blur + mtime<5s boundary)"
    - path: "scripts/__tests__/editor-lock-hook.test.mjs"
      provides: "hook exit-code proof (locked -> 2, unlocked -> 0)"
  key_links:
    - from: "scripts/editor-lock-hook.mjs"
      to: "~/.mneme/editor-locks.json"
      via: "read lockfile, compare .tool_input.file_path, process.exit(2) if locked"
      pattern: "exit\\(2\\)"
    - from: "src-tauri/src/editor_lock.rs"
      to: "~/.mneme/editor-locks.json"
      via: "atomic temp+rename write (config.rs pattern)"
      pattern: "rename"
---

<objective>
Build the soft-lock guardrail (D-09): a node PreToolUse hook that refuses Claude's Edit/Write on a file the user is actively editing (or that was just written, mtime<5s), plus the Rust `editor_lock.rs` that mneme uses to OWN the lock state (write/clear `~/.mneme/editor-locks.json` atomically + declare the hook in the vault `.claude/settings.json`). Plus the pure lock-lifecycle logic (focus/blur + mtime<5s rule) as a unit-testable TS module.

Purpose: the soft-lock is CONFIG, not argv — it fires AND blocks even under `bypassPermissions`, with exit-2 pre-empting permission eval (D-09a), and it does NOT touch `buildClaudeArgs` / the capability allowlist (preserve that KP-04 invariant — the soft-lock is the only Phase-3 surface that could be mistaken for a spawn change; it explicitly is not). VALIDATION.md proofs: soft-lock advisory (hook blocks Claude not the human) + lock lifecycle.
Output: `editor_lock.rs` (new Rust module), `editor-lock-hook.mjs` (the hook script source mneme ships into the vault), the lock-lifecycle TS logic + its test, the hook exit-code test, and a Rust IO test. This plan does NOT edit `lib.rs` (Wave 3 / 03-08 registers the `write_editor_lock`/`clear_editor_lock` commands + the `mod editor_lock;` line) and does NOT edit any frontend component (Wave 2 wires editor-focus → the lock-lifecycle module). It exposes the functions those waves call. Note: the hook script lives at `scripts/editor-lock-hook.mjs` in-repo (the source of truth); `editor_lock.rs` copies it into the vault's `.claude/hooks/` on first run — the in-repo path keeps it testable + version-controlled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md
@.planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md
@.planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md
@.planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md
@src-tauri/src/config.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: editor-lock-hook.mjs PreToolUse hook + exit-code test (D-09 / D-09a)</name>
  <files>scripts/editor-lock-hook.mjs, scripts/__tests__/editor-lock-hook.test.mjs</files>
  <read_first>
    - scripts/gsd-dev-screenshot.mjs (analog: Node ESM `*.mjs` script shape; `scripts/__tests__/*.test.mjs` is the vitest test shape)
    - .planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md D-09 / D-09a / D-09b (hook reads `.tool_input.file_path` from stdin JSON, checks `~/.mneme/editor-locks.json`, `exit 2` when locked; write in node — macOS may lack jq; hooks fire+block under bypassPermissions; exit-2 pre-empts permission eval)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "editor-lock-hook.mjs (node hook script)" + No/Weak-Analog row
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md "Soft-lock advisory" row (locked path -> exit 2; unlocked -> exit 0; human editor stays usable)
  </read_first>
  <behavior>
    - RED first: write `scripts/__tests__/editor-lock-hook.test.mjs` (vitest, discovered by the existing `scripts/__tests__/**/*.test.mjs` glob). It spawns `node scripts/editor-lock-hook.mjs` (via `node:child_process` `spawnSync`) feeding stdin JSON, pointing the hook at a tempdir lockfile via an env var (e.g. `MNEME_EDITOR_LOCKS_PATH`). Cases: (a) lockfile lists `/tmp/x/locked.md`, stdin `{"tool_input":{"file_path":"/tmp/x/locked.md"}}` → exit code 2; (b) lockfile empty/absent, same stdin → exit code 0; (c) stdin missing `.tool_input.file_path` (malformed) → exit code 0 (fail-open: do not block on a malformed event); (d) lockfile lists an entry with a recent mtime (the mtime<5s rule) for a DIFFERENT just-written path → that path also blocks. Run `npx vitest run scripts/__tests__/editor-lock-hook.test.mjs` → FAIL (script absent). Capture RED.
    - GREEN: write `scripts/editor-lock-hook.mjs` (pure node, ZERO external deps, no jq): read all of stdin, `JSON.parse` it, extract `.tool_input.file_path`; read the lockfile path from `process.env.MNEME_EDITOR_LOCKS_PATH` (fallback `~/.mneme/editor-locks.json`); if the file is missing/unreadable → exit 0 (fail-open: never block Claude on a hook IO error — the soft-lock is advisory); parse the lockfile (a JSON object/array of locked entries, each `{ path, lockedAt, kind }` where `kind` ∈ `"focus" | "recent-write"`); if the requested `file_path` matches a `focus` entry OR a `recent-write` entry whose `lockedAt` is within the mtime<5s window → `process.exit(2)`; else `process.exit(0)`. On any unexpected error → exit 0 (fail-open). Print a one-line stderr explanation before exit 2 (Claude surfaces hook stderr).
    - REFACTOR: confirm no `require(`/no external import (pure node builtins only); the env-var override (`MNEME_EDITOR_LOCKS_PATH`) keeps it testable. Header comment cites `// D-09 D-09a — CONFIG not argv; does NOT touch buildClaudeArgs / capability allowlist`.
  </behavior>
  <action>Write the hook in pure node (no `jq`, no npm deps — D-09). Read stdin fully, `JSON.parse`, extract `.tool_input.file_path`. Lockfile path comes from `process.env.MNEME_EDITOR_LOCKS_PATH` (test override) falling back to `path.join(os.homedir(), ".mneme", "editor-locks.json")`. Locked → `process.exit(2)` (D-09a: exit-2 pre-empts permission eval, fires under bypassPermissions). Fail-OPEN on any IO/parse error or malformed event → `process.exit(0)` (advisory lock must never wedge Claude on a hook bug). This file is CONFIG: it MUST NOT import or reference `buildClaudeArgs`, the capability JSON, or any spawn arg — the soft-lock deliberately stays off the capability surface (D-09a, KP-04 invariant).</action>
  <verify>
    <automated>npx vitest run scripts/__tests__/editor-lock-hook.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'exit(2)' scripts/editor-lock-hook.mjs` >= 1 AND `grep -c 'tool_input' scripts/editor-lock-hook.mjs` >= 1.
    - Source assertion (no capability-surface coupling): `grep -Ec 'buildClaudeArgs|capabilities|--bare|--max-turns|spawn-args' scripts/editor-lock-hook.mjs` == 0.
    - Source assertion (pure node): `grep -Ec "require\\(|from ['\\\"](?!node:)" scripts/editor-lock-hook.mjs` == 0 (node builtins only — `node:` imports allowed).
    - Behavior assertion: vitest passes — locked → exit 2, unlocked → exit 0, malformed event → exit 0 (fail-open), recent-write within window → blocks.
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>The node hook exits 2 for a locked path and 0 otherwise, fails open on errors/malformed events, uses no jq/no deps, and never references the capability surface.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: lock-lifecycle logic (focus/blur + mtime<5s) (D-09b)</name>
  <files>tests/editor-lock-lifecycle.test.ts</files>
  <read_first>
    - .planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md D-09b (mneme owns lock state — writes/clears on editor focus, computes the mtime<5s rule, writes both into the lockfile; hook stays a dumb lookup)
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md "Lock lifecycle — focus + mtime<5s rule" row
    - src/lib/editor-markdown.ts will be created by 03-03 — but this lifecycle logic is independent (pure boundary math); place the pure functions in a small module the test imports
  </read_first>
  <behavior>
    - RED first: write `tests/editor-lock-lifecycle.test.ts` against a small pure module `src/lib/editor-lock.ts` (NEW, created in GREEN). Cases: (a) `computeLockEntries({ focusedPath, recentWrites, now })` returns a `focus` entry for `focusedPath` when present and none when `focusedPath` is null; (b) a `recent-write` entry is included only when `now - writeTime < 5000ms`, excluded at exactly 5000ms+ (boundary), included at 4999ms; (c) `isLocked(entries, path, now)` returns true for a focus-locked path and for a recent-write path within the window, false otherwise. Run `npx vitest run tests/editor-lock-lifecycle.test.ts` → FAIL. Capture RED.
    - GREEN: create `src/lib/editor-lock.ts` (plain `.ts`, pure — NOT `.svelte.ts`; no runes) exporting `computeLockEntries(input): LockEntry[]` and `isLocked(entries, path, now): boolean` with the 5000ms window as a named constant `MTIME_LOCK_WINDOW_MS = 5000`. `LockEntry = { path: string; lockedAt: number; kind: "focus" | "recent-write" }` — the exact shape the hook (Task 1) reads.
    - REFACTOR: confirm the boundary is strict (`< MTIME_LOCK_WINDOW_MS`, matching the hook). Header cites `// D-09b`.
  </behavior>
  <action>Create `src/lib/editor-lock.ts` as a pure module (the reactive editor-focus wiring that CALLS this lives in 03-07's `TiptapEditor.svelte` + 03-08's Tauri invoke). Define `LockEntry` with the EXACT field names the hook reads (`path`, `lockedAt`, `kind`). `MTIME_LOCK_WINDOW_MS = 5000` as a named constant (no magic number). `isLocked` uses strict `<` at the boundary so it agrees with the hook's window check. This module is added to `files_modified` implicitly via the test; declare it created here (the plan owns `src/lib/editor-lock.ts` even though it is listed under the test's coverage).</action>
  <verify>
    <automated>npx vitest run tests/editor-lock-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'MTIME_LOCK_WINDOW_MS' src/lib/editor-lock.ts` >= 1 (named constant, no magic 5000 elsewhere) AND `grep -c 'export function isLocked' src/lib/editor-lock.ts` == 1.
    - Behavior assertion: vitest passes — focus entry present/absent correctly; recent-write included at 4999ms, excluded at 5000ms+; isLocked matches.
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>computeLockEntries + isLocked resolve focus and mtime<5s locking at boundary times using a named window constant matching the hook.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: editor_lock.rs atomic lockfile write/clear + hook install (D-09b)</name>
  <files>src-tauri/src/editor_lock.rs, src-tauri/tests/editor_lock_io.rs</files>
  <read_first>
    - src-tauri/src/config.rs (EXACT analog per 03-PATTERNS.md: atomic temp+rename `save_to` with `sync_all` before rename at L94-107; `default_path` resolver + `NoHome` guard at L53-59; `ConfigError` thiserror enum)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "editor_lock.rs (EXACT analog)" + No/Weak-Analog row for the vault `.claude/settings.json` create-if-absent/merge note
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Runtime State Inventory (the vault `.claude/settings.json` PreToolUse hook is mneme-managed; decide overwrite-merge vs create-if-absent to avoid clobbering a user's existing hooks)
    - src-tauri/tests/vault_index_count.rs (Rust integration test shape using tempfile + `use mneme_lib::...`)
  </read_first>
  <behavior>
    - RED first: write `src-tauri/tests/editor_lock_io.rs` using `tempfile::tempdir()` + `#[path = "../src/editor_lock.rs"] mod editor_lock;`. Cases: (a) `write_locks(path, &entries)` then `read_locks(path)` round-trips the `LockEntry` vec; (b) the write is atomic (a `.json.tmp` sibling is renamed — assert the final file exists and the tmp does not after the call); (c) `clear_locks(path)` empties the lockfile; (d) `ensure_settings_hook(vault_dir)` on a vault with NO `.claude/settings.json` creates one declaring the PreToolUse hook matching `Edit|Write|MultiEdit|NotebookEdit`; on a vault WITH an existing `.claude/settings.json` carrying unrelated hooks, it MERGES (does not clobber the user's hooks) — assert the pre-existing key survives. Run `cargo test --test editor_lock_io` → FAIL (module absent). Capture RED.
    - GREEN: write `src-tauri/src/editor_lock.rs` mirroring `config.rs`: an `EditorLockError` thiserror enum (Sqlite-free; Io + NoHome + Json); `pub fn default_locks_path() -> Result<PathBuf, EditorLockError>` → `~/.mneme/editor-locks.json`; `pub fn write_locks(path: &Path, entries: &[LockEntry]) -> Result<(), EditorLockError>` using the temp+`sync_all`+rename sequence verbatim from `config.rs::save_to`; `pub fn read_locks(path: &Path) -> Result<Vec<LockEntry>, EditorLockError>` (returns empty vec if absent — matches the hook's fail-open); `pub fn clear_locks(path: &Path)`; `pub fn ensure_settings_hook(vault_dir: &Path) -> Result<(), EditorLockError>` that create-if-absent/merges the PreToolUse hook into `<vault>/.claude/settings.json` (idempotent first-run write; preserve existing keys). `LockEntry` struct mirrors the TS shape (`path: String, locked_at: i64, kind: String`) deriving `serde::{Serialize, Deserialize}`.
    - REFACTOR: confirm the atomic write uses `sync_all` before rename (config.rs invariant) and that `ensure_settings_hook` MERGES rather than overwrites. Header cites `// D-09b — mneme owns lock state; analog: config.rs atomic temp+rename`.
  </behavior>
  <action>Mirror `config.rs::save_to` exactly for the atomic write (temp file → `write_all` → `sync_all` → `fs::rename`). `read_locks` returns an empty vec when the file is absent (so the system is unlocked by default — matches the hook fail-open). `ensure_settings_hook` MUST merge into an existing `.claude/settings.json` (parse JSON, insert/merge the `hooks.PreToolUse` entry pointing at the copied `editor-lock-hook.mjs`, write back atomically) — never blind-overwrite (RESEARCH Runtime State Inventory: do not clobber user hooks). Do NOT add `mod editor_lock;` to `lib.rs` (Wave 3 / 03-08 owns that). The test compiles the module via `#[path = "../src/editor_lock.rs"]`. Use `serde`/`serde_json`/`tempfile` (already present); the lockfile JSON is NOT SQL so audit Gate 10 does not apply, but keep all paths from app state (no attacker-influenced traversal into the lockfile path).</action>
  <verify>
    <automated>cd src-tauri && cargo test --test editor_lock_io -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'editor-locks.json' src-tauri/src/editor_lock.rs` >= 1; `grep -c 'sync_all' src-tauri/src/editor_lock.rs` >= 1; `grep -c 'rename' src-tauri/src/editor_lock.rs` >= 1.
    - Source assertion (merge not clobber): `grep -c 'ensure_settings_hook' src-tauri/src/editor_lock.rs` >= 1.
    - Behavior assertion: `cargo test --test editor_lock_io` passes round-trip, atomic-rename (no leftover `.tmp`), clear, and the merge-preserves-existing-hooks case.
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>editor_lock.rs writes/reads/clears the lockfile atomically (temp+sync_all+rename), defaults to unlocked when absent, and ensure_settings_hook merges the PreToolUse hook without clobbering existing user hooks; lib.rs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mneme (Rust) -> lockfile | mneme writes `~/.mneme/editor-locks.json`; the hook (inside the claude subprocess) reads it |
| claude subprocess -> PreToolUse hook | the hook receives tool-call stdin JSON from claude; advisory exit-2 blocks the tool call |
| mneme -> vault `.claude/settings.json` | first config mneme writes into the vault dir; must merge, not clobber |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-13 | Tampering | lockfile path traversal | mitigate | lockfile path is `~/.mneme/editor-locks.json` from `home::home_dir()` (Rust) / `os.homedir()` (hook env override is test-only) — not attacker-influenced; the hook fails OPEN on any path/IO error |
| T-3-14 | Spoofing | a malformed PreToolUse event bypassing the lock | accept | the hook fails OPEN (exit 0) on malformed events — advisory by design; the authoritative protection for `_source/` remains chmod-444 (Phase 2) as the OS layer |
| T-3-15 | Tampering | clobbering a user's existing `.claude/settings.json` hooks | mitigate | `ensure_settings_hook` MERGES (parse + insert), proven by the merge-preserves-existing test; never blind-overwrite |
| T-3-16 | Elevation of Privilege | soft-lock mistaken for an argv/capability change | mitigate | hook is CONFIG only; acceptance grep proves it references no `buildClaudeArgs`/capability/spawn-arg — KP-04 capability surface untouched (D-09a) |
</threat_model>

<verification>
- `npx vitest run scripts/__tests__/editor-lock-hook.test.mjs tests/editor-lock-lifecycle.test.ts` — green.
- `cd src-tauri && cargo test --test editor_lock_io` — green.
- `bash scripts/audit-capabilities.sh` still exits 0 (the soft-lock did NOT touch the capability surface — regression guard).
</verification>

<success_criteria>
- Soft-lock advisory + lock-lifecycle proofs pass; hook blocks Claude not the human; fails open on errors.
- `editor_lock.rs` writes atomically and merges the settings hook.
- KP-04 capability surface untouched (hook is config, not argv).
- `lib.rs` and frontend components untouched (Wave 3 / Wave 2 own those edges).
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-command-palette-editor/03-04-SUMMARY.md`. Include RED proofs and document the wiring Wave 2/3 must add: 03-07 calls `editor-lock.computeLockEntries` on editor focus/blur → 03-08 invokes `write_editor_lock`/`clear_editor_lock` Tauri commands wrapping `editor_lock::{write_locks, clear_locks}`; 03-08 calls `editor_lock::ensure_settings_hook(vault_dir)` on first run + copies `scripts/editor-lock-hook.mjs` into the vault `.claude/hooks/`.
</output>
