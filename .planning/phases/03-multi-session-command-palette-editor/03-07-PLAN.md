---
phase: 03-multi-session-command-palette-editor
plan: 07
type: execute
wave: 2
depends_on: ["03-03", "03-04"]
files_modified:
  - src/lib/components/TiptapEditor.svelte
  - src/lib/components/SegmentedToggle.svelte
  - src/lib/components/MiddlePaneRouter.svelte
  - src/lib/components/SoftLockPill.svelte
  - src/lib/components/CeilingToast.svelte
autonomous: true
requirements: [KD-09]
must_haves:
  truths:
    - "Opening a `.md` file shows the Tiptap editor in the middle pane with a Preview|Edit segmented toggle; PDF keeps FilePreview, video keeps LectureVideo (D-02 type-routing) — KD-09."
    - "The Tiptap editor mounts a ProseMirror instance via $effect and destroys it on cleanup + onDestroy (no leak / no double-mount under HMR)."
    - "Preview mode renders via the existing marked->DOMPurify->KaTeX sanitize.ts pipeline (REQ-5 CSP-clean — no unsafe-inline)."
    - "The soft-lock pill appears in the editor header when Claude holds the lock; the editor stays usable by the human (advisory only)."
  artifacts:
    - path: "src/lib/components/TiptapEditor.svelte"
      provides: "ProseMirror mount/destroy in $effect+onDestroy; gray-matter frontmatter split via editor-markdown.ts; Preview via sanitize.ts; slash menu; save dot"
      contains: "@tiptap/core"
    - path: "src/lib/components/MiddlePaneRouter.svelte"
      provides: "D-02 type-router (.md->editor, pdf->FilePreview, video->LectureVideo) + Preview|Edit header toggle"
      contains: "FilePreview"
    - path: "src/lib/components/SoftLockPill.svelte"
      provides: "dark 'Claude is editing — locked' advisory pill (D-09)"
      contains: "Claude is editing"
    - path: "src/lib/components/CeilingToast.svelte"
      provides: "D-10 resource-ceiling warning toast (informational, --color-error rule)"
      contains: "Two generations already running"
  key_links:
    - from: "src/lib/components/TiptapEditor.svelte"
      to: "src/lib/editor-markdown.ts"
      via: "splitForEditor on load, joinForSave on save (frontmatter preserved)"
      pattern: "editor-markdown"
    - from: "src/lib/components/TiptapEditor.svelte"
      to: "src/lib/sanitize.ts"
      via: "Preview pane uses sanitizeMarkdown + renderKatexInDom"
      pattern: "sanitize"
---

<objective>
Build the Tiptap markdown editor (KD-09, 03-UI-SPEC Surface 3) and the two guardrail indicator components: `TiptapEditor.svelte` (ProseMirror mount/destroy via $effect + frontmatter split via 03-03's `editor-markdown.ts` + Preview via `sanitize.ts` + slash menu), `SegmentedToggle.svelte` (Preview|Edit), `MiddlePaneRouter.svelte` (D-02 type-routing the middle-bottom slot), `SoftLockPill.svelte` (D-09 advisory pill), and `CeilingToast.svelte` (D-10 warning toast).

Purpose: KD-09's visible surface (Tiptap block editor, markdown round-trip via the 03-03 splitter), plus the D-09 soft-lock + D-10 ceiling indicators the Wave-3 integration wires to backend state. These are execute (UI/glue) — the round-trip LOGIC was already TDD-proven in 03-03; this plan mounts the editor against it and validates behaviorally via the dev-feedback-loop vs 03-UI-SPEC.
Output: five `.svelte` components. This plan does NOT edit `+page.svelte` (single-owner = 03-09 routes `.md` into MiddlePaneRouter at the middle-bottom slot), `ChatPanel.svelte` (03-08 mounts SoftLockPill/CeilingToast contexts), or `lib.rs`. It installs the editor + indicators as self-contained components that 03-08/03-09 wire. New deps (@tiptap/* v3, tiptap official markdown ext) are registered in dependencies.md by 03-08; the packages are installed here so the editor compiles (note in SUMMARY for 03-08 to register). All net-new components carry `// Visual SSOT: 03-UI-SPEC.md §Surface 3`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md
@.planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md
@.planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md
@.planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md
@src/lib/components/AssistantMessage.svelte
@src/lib/components/FilePreview.svelte
@src/lib/editor-markdown.ts
@src/lib/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TiptapEditor.svelte (ProseMirror mount/destroy + frontmatter split + Preview + slash menu)</name>
  <files>src/lib/components/TiptapEditor.svelte</files>
  <read_first>
    - src/lib/components/AssistantMessage.svelte (the `bind:this` host + `$effect` DOM-lifecycle + rAF pattern at L32-57/L60; `{@html sanitizedMarkdown}` at L65 + the `:global(...)` CSS scoping for {@html} content — the Preview-pane precedent)
    - src/lib/editor-markdown.ts (03-03 — `splitForEditor`/`joinForSave`; plus the deferred `// TODO(03-07 spike)` marker for the official Tiptap markdown serialize API this plan must resolve)
    - src/lib/sanitize.ts (`sanitizeMarkdown` + `renderKatexInDom` — Preview pane reuse)
    - src/lib/dev/console-forwarder.ts L119-124 (the `globalThis.__mneme*Installed` singleton HMR double-init guard to mirror)
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Pattern 3 (the Editor mount/destroy code L254-287; the OPEN official-markdown-ext serialize API to pin in a Wave-0-style spike) + Standard Stack correction (use Tiptap OFFICIAL markdown ext >=3.7.0, NOT aguingand) + Pitfall 4 (destroy on cleanup + onDestroy) + Pitfall 5 (frontmatter split first)
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md Surface 3 (editor states: empty/editing/preview/save-indicator/soft-lock; slash menu bounded set + visual; segmented toggle; save dot a11y) + 03-PATTERNS.md "TiptapEditor.svelte"
  </read_first>
  <action>Create `src/lib/components/TiptapEditor.svelte`. Props: `interface Props { initialMarkdown: string; filePath: string; mode: "preview" | "edit"; locked: boolean; onsave: (markdown: string) => void; onfocus: () => void; onblur: () => void }`. EDIT mode: a `bind:this={host}` div; in `$effect`, when `host && !editor`, on first load call `splitForEditor(initialMarkdown)` (03-03) to strip the YAML frontmatter, feed only the `body` to `new Editor({ element: host, extensions: [StarterKit, /* official Tiptap markdown ext >=3.7.0 */, Suggestion-based slash menu], content: body })`; the `$effect` MUST `return () => { editor?.destroy(); editor = undefined; }` AND `onDestroy(() => editor?.destroy())` (belt-and-suspenders, Pitfall 4). On `onUpdate`, serialize the body via the official markdown ext, rejoin the frontmatter via `joinForSave(body, frontmatter)` (03-03), set a dirty flag (orange save dot), and on save call `onsave(rejoined)`. Wrap the install in the `globalThis.__mnemeTiptapInstalled`-style guard (HMR — mirror console-forwarder). PREVIEW mode: render `{@html sanitizeMarkdown(initialMarkdown)}` + run `renderKatexInDom` in an `$effect` (exactly the AssistantMessage pattern — REQ-5 CSP-clean, no `{@html}` of un-sanitized content). Slash menu: the bounded set from 03-UI-SPEC (`Heading 1/2/3`, `Bulleted list`, `Numbered list`, `Code block`, `Math block`, `Callout`, `Wikilink`), each round-tripping to markdown; `/` at an empty block opens it anchored at caret; ↑↓/Enter/Esc. Fire `onfocus`/`onblur` so 03-08 can write/clear the editor lock (D-09b). Save indicator dot a11y per 03-UI-SPEC (`aria-label="Unsaved changes"`). Reference ONLY existing tokens. Header: `// Visual SSOT: 03-UI-SPEC.md §Surface 3`. **Wave-0-style spike inside this task:** pin the official markdown ext's serialize/parse method names against a round-trip of `tests/fixtures/markdown-roundtrip/*.md` (03-03's corpus) BEFORE finalizing `onUpdate` — document the exact API in SUMMARY (RESEARCH Pattern 3 flags it OPEN). Install `@tiptap/core@3.23.6 @tiptap/starter-kit@3.23.6 @tiptap/extension-suggestion@3.23.6` + the official markdown ext; flag in SUMMARY for 03-08 to register in dependencies.md (KP-08) + flip the stale aguingand row.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -8 && npx vitest run tests/editor-markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '@tiptap/core' src/lib/components/TiptapEditor.svelte` >= 1 AND `grep -c 'onDestroy' src/lib/components/TiptapEditor.svelte` >= 1 (cleanup) AND `grep -c 'editor.destroy\|editor?.destroy' src/lib/components/TiptapEditor.svelte` >= 2 ($effect cleanup + onDestroy).
    - Source assertion (frontmatter split + preview reuse): `grep -c 'editor-markdown' src/lib/components/TiptapEditor.svelte` >= 1 AND `grep -c 'sanitize' src/lib/components/TiptapEditor.svelte` >= 1.
    - Source assertion (HMR guard, no new tokens, CSP-clean): `grep -c '__mneme.*Installed\|globalThis' src/lib/components/TiptapEditor.svelte` >= 1; `grep -Ec '#[0-9a-fA-F]{3,6}' src/lib/components/TiptapEditor.svelte` == 0; `grep -Ec 'unsafe-inline|eval\\(' src/lib/components/TiptapEditor.svelte` == 0.
    - Behavior assertion: `npm run check` reports no new errors; `tests/editor-markdown.test.ts` (03-03 round-trip) stays green; the official-markdown-ext serialize API is documented in SUMMARY.
  </acceptance_criteria>
  <done>TiptapEditor mounts/destroys ProseMirror cleanly (no leak, HMR-guarded), splits frontmatter via editor-markdown.ts and rejoins on save, renders Preview via sanitize.ts, ships the bounded slash menu, fires focus/blur for the lock, no raw hex, CSP-clean.</done>
</task>

<task type="auto">
  <name>Task 2: SegmentedToggle.svelte + MiddlePaneRouter.svelte (D-02 type-routing)</name>
  <files>src/lib/components/SegmentedToggle.svelte, src/lib/components/MiddlePaneRouter.svelte</files>
  <read_first>
    - src/lib/components/FilePreview.svelte (the PDF branch the router routes to — lives in the Splitter middleBottom snippet) + src/lib/components/LectureVideo.svelte (the video branch)
    - src/lib/components/settings/ rail-item button pattern (the SegmentedToggle partial analog per 03-PATTERNS.md) — segmented control styling precedent
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md Surface 3 "Mode toggle" (segmented Preview|Edit, inactive `--ink-soft`/transparent, active `--color-orange` + `--orange-soft` fill, `--radius-md` track, `:focus-visible` `--orange-ring`) + the editor placement (D-02 middle-pane type-routing; PDF/video keep existing)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "MiddlePaneRouter.svelte" + "SegmentedToggle.svelte"
  </read_first>
  <action>Create `src/lib/components/SegmentedToggle.svelte`: a 2-segment Preview|Edit control. Props `interface Props { value: "preview" | "edit"; onchange: (v: "preview" | "edit") => void }`. Two buttons, `--fs-meta` labels, `--radius-md` track; inactive = `--ink-soft` text transparent fill, active = `--color-orange` text + `--orange-soft` fill; `:focus-visible` → `--orange-ring`; press `active:scale(0.96)`. Create `src/lib/components/MiddlePaneRouter.svelte`: D-02 type-router. Props `interface Props { filePath: string | null; fileExt: string | null }` (or derive ext from filePath). Branch on extension: `.md` → mount `TiptapEditor` (Task 1) with a `SegmentedToggle` + `SoftLockPill` (Task 3) in a middle-pane header; `.pdf` → existing `FilePreview`; video ext → existing `LectureVideo`; no file → the existing FilePreview placeholder. The router REPLACES the `middleBottom` snippet content (03-09 wires it into `+page.svelte`); the middle-pane header carries the SegmentedToggle (Preview|Edit) + the save indicator + the SoftLockPill. Reference ONLY existing tokens. Headers: `// Visual SSOT: 03-UI-SPEC.md §Surface 3`.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (type-routing): `grep -c 'FilePreview' src/lib/components/MiddlePaneRouter.svelte` >= 1 AND `grep -c 'TiptapEditor' src/lib/components/MiddlePaneRouter.svelte` >= 1 AND `grep -c '\\.md' src/lib/components/MiddlePaneRouter.svelte` >= 1.
    - Source assertion (segmented active state): `grep -c 'color-orange' src/lib/components/SegmentedToggle.svelte` >= 1 AND `grep -c 'orange-soft' src/lib/components/SegmentedToggle.svelte` >= 1; `grep -Ec '#[0-9a-fA-F]{3,6}' src/lib/components/SegmentedToggle.svelte` == 0.
    - Source assertion: both files carry `Visual SSOT: 03-UI-SPEC.md`.
    - Behavior assertion: `npm run check` reports no new errors.
  </acceptance_criteria>
  <done>SegmentedToggle renders Preview|Edit with the orange active segment + focus ring; MiddlePaneRouter routes `.md`→editor / pdf→FilePreview / video→LectureVideo with the toggle in the header; no raw hex; Visual-SSOT headers present.</done>
</task>

<task type="auto">
  <name>Task 3: SoftLockPill.svelte (D-09) + CeilingToast.svelte (D-10)</name>
  <files>src/lib/components/SoftLockPill.svelte, src/lib/components/CeilingToast.svelte</files>
  <read_first>
    - src/lib/components/ImportStatusPill.svelte (the pill-chip pattern analog for SoftLockPill per 03-PATTERNS.md)
    - src/lib/components/ReconciliationOverlay.svelte (the template-root mount + onDone pattern analog for CeilingToast per 03-PATTERNS.md)
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md Surface 3 soft-lock row (dark pill `--color-warm-surface` bg + `--color-cream` text + `--fs-meta` + lock SVG, advisory not blocking) + "Also-Spec — Resource-Ceiling Warning" (bottom-center toast, `--color-cream` surface, 2px `--color-error` left rule + warning SVG, `Dismiss` ghost link, translateY(8px→0)+opacity in 200ms `--duration-base` / out 320ms `--duration-slow`, auto-dismiss ~5s) + Copywriting (soft-lock `Claude is editing — locked` + hover-title; ceiling `Two generations already running` / body / `Dismiss`)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "SoftLockPill.svelte" + "CeilingToast.svelte"
  </read_first>
  <action>Create `src/lib/components/SoftLockPill.svelte`: props `interface Props { visible: boolean }`. When visible, render a dark pill: `--color-warm-surface` background, `--color-cream` text, `--fs-meta`, a small lock inline SVG (`aria-hidden="true"`), copy `Claude is editing — locked`, `title="Claude has a write lock on this file. Your edits are unaffected."`. Fade-in `opacity` over `--duration-fast` (160ms). Advisory only — never disables the editor. Create `src/lib/components/CeilingToast.svelte`: props `interface Props { visible: boolean; ondismiss: () => void }`. Bottom-center toast (mounts at template root via 03-09): `--color-cream` surface, `--radius-lg`, `--shadow-2`, 2px `--color-error` left rule + a small warning inline SVG in `--color-error`; heading `Two generations already running`, body `mneme runs at most two Claude generations at once on this Mac. Wait for one to finish, then try again.`, a quiet `Dismiss` ghost link (`--ink-mute`) calling `ondismiss`; enter `transform: translateY(8px→0)` + opacity over `--duration-base`, exit over `--duration-slow`; auto-dismiss after ~5s (setTimeout cleared on unmount). This is a WARNING, not destructive — `--color-error` is the attention rule/icon only, never a filled-red alarm. Reference ONLY existing tokens. Headers: `// Visual SSOT: 03-UI-SPEC.md §Surface 3` (pill) / `§Also-Spec — Resource-Ceiling Warning` (toast). Note for 03-08: ChatPanel shows CeilingToast when `try_register_if_below` returns the ceiling error; 03-07 only builds the component.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (soft-lock copy + dark surface): `grep -c 'Claude is editing' src/lib/components/SoftLockPill.svelte` >= 1 AND `grep -c 'warm-surface' src/lib/components/SoftLockPill.svelte` >= 1.
    - Source assertion (ceiling copy + warning rule): `grep -c 'Two generations already running' src/lib/components/CeilingToast.svelte` >= 1 AND `grep -c 'color-error' src/lib/components/CeilingToast.svelte` >= 1 AND `grep -c 'Dismiss' src/lib/components/CeilingToast.svelte` >= 1.
    - Source assertion (no raw hex): `grep -Ec '#[0-9a-fA-F]{3,6}' src/lib/components/SoftLockPill.svelte src/lib/components/CeilingToast.svelte` == 0.
    - Behavior assertion: `npm run check` reports no new errors.
  </acceptance_criteria>
  <done>SoftLockPill renders the dark advisory chip with exact copy; CeilingToast renders the bottom-center warning with the error left-rule + Dismiss + auto-dismiss; both reference only existing tokens with Visual-SSOT headers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault `.md` content -> editor | file content is user/Claude-authored markdown; ProseMirror parses it, Preview sanitizes it |
| lock-state -> SoftLockPill | the pill reflects backend lock state; purely visual (no enforcement here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-22 | Tampering (XSS) | editor Preview {@html} | mitigate | Preview renders ONLY `sanitizeMarkdown(...)` output (marked->DOMPurify->KaTeX); ProseMirror Edit mode is DOM-direct (no {@html} of raw input) — REQ-5 preserved |
| T-3-23 | EoP | CSP regression via Tiptap/ProseMirror | mitigate | ProseMirror manipulates DOM via document.createElement, no inline scripts (RESEARCH REQ-5); acceptance greps `unsafe-inline`/`eval(` == 0; `script-src` unchanged |
| T-3-24 | Information Disclosure | frontmatter dropped on save (silent data loss) | mitigate | `joinForSave` (03-03, byte-stable-proven) rejoins the YAML block; the editor never serializes the frontmatter through the markdown ext (Pitfall 5) |
| T-3-25 | DoS | undestroyed ProseMirror leaking across file/thread switches | mitigate | `$effect` cleanup + `onDestroy` both `editor.destroy()`; HMR singleton guard (Pitfall 4) |
</threat_model>

<verification>
- `npm run check` — no new svelte-check/tsc errors across the five components.
- `npx vitest run tests/editor-markdown.test.ts` — 03-03 round-trip stays green.
- dev-feedback-loop (Wave 3 after routing): `gsd-dev-screenshot` editor Edit/Preview toggle + save dot + soft-lock pill match 03-UI-SPEC Surface 3; ceiling toast matches Also-Spec.
</verification>

<success_criteria>
- KD-09 editor mounts/destroys cleanly, frontmatter round-trips via 03-03, Preview reuses sanitize.ts; slash menu present.
- D-02 type-routing works (.md→editor, pdf→FilePreview, video→LectureVideo); D-09 pill + D-10 toast components built per 03-UI-SPEC.
- CSP-clean; no new tokens; no edits to +page / ChatPanel / lib.rs (single-owner waves preserved).
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-command-palette-editor/03-07-SUMMARY.md`. Document for 03-08 (dependencies.md): register `@tiptap/core@3.23.6` + `@tiptap/starter-kit@3.23.6` + `@tiptap/extension-suggestion@3.23.6` + the official Tiptap markdown ext (>=3.7.0) (KP-08); flip the stale `@tiptap/starter-kit@3.22.5` + `aguingand/tiptap-markdown` rows. Record the official-markdown-ext serialize/parse API resolved in the Task-1 spike. Document for 03-09: mount `MiddlePaneRouter` in the `+page.svelte` `middleBottom` snippet (type-routed on the selected file); `TiptapEditor` `onfocus`/`onblur` → 03-08's `write_editor_lock`/`clear_editor_lock`; `CeilingToast` mounts at `+page.svelte` template root driven by ChatPanel's ceiling error.
</output>
