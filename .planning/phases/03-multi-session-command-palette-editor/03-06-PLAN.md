---
phase: 03-multi-session-command-palette-editor
plan: 06
type: tdd
wave: 2
depends_on: []
files_modified:
  - src/lib/palette-actions.ts
  - src/lib/keybindings.svelte.ts
  - src/lib/components/CommandPalette.svelte
  - tests/palette-ranking.test.ts
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "fuzzysort ranks a command/file-path fixture in the expected order for representative queries (REQ-11 fuzzy match)."
    - "The command palette is a centered modal + dim scrim built on Bits UI Command, with the keyboard model up/down (wrap) / Enter (run+dismiss) / Esc (dismiss, restore focus) and a type-to-filter input — load-bearing for the >=80%-no-mouse goal (REQ-11)."
    - "Three entry modes (Cmd+P file / Cmd+O course / Cmd+Shift+P action) share one modal chrome with mode-specific data + placeholder; Cmd+Shift+V is RESERVED (not bound)."
    - "Fuzzy-matched characters render --fw-semibold + --color-orange (no background highlight)."
  artifacts:
    - path: "src/lib/palette-actions.ts"
      provides: "bounded Cmd+Shift+P action set (pure data + run() handlers, no module $state)"
      contains: "New markdown file"
    - path: "src/lib/keybindings.svelte.ts"
      provides: "single global window keydown registrar (install-once) dispatching Cmd+P/O/Shift+P; Cmd+Shift+V reserved; collision-safe with Cmd+,/Cmd+I/Cmd+Q"
      contains: "Cmd+Shift+V"
    - path: "src/lib/components/CommandPalette.svelte"
      provides: "Bits UI Command + fuzzysort scorer, centered modal + scrim, 3 entry modes, fuzzy highlight"
      contains: "bits-ui"
    - path: "tests/palette-ranking.test.ts"
      provides: "fuzzysort expected-rank proof over a command/file-path fixture (REQ-11)"
  key_links:
    - from: "src/lib/components/CommandPalette.svelte"
      to: "fuzzysort"
      via: "fuzzysort.go(search, items, { key, limit }) with shouldFilter={false}"
      pattern: "fuzzysort"
    - from: "src/lib/keybindings.svelte.ts"
      to: "src/lib/components/CommandPalette.svelte"
      via: "Cmd+P/O/Shift+P openPalette(mode)"
      pattern: "openPalette"
---

<objective>
Build the command palette (REQ-11, 03-UI-SPEC Surface 2): the bounded action set (`palette-actions.ts`), the single global keybinding registrar (`keybindings.svelte.ts`), and the `CommandPalette.svelte` modal (Bits UI `Command` + fuzzysort scorer, centered + dim scrim, three entry modes). The fuzzysort ranking is the one TDD target (REQ-11 fuzzy match proof); the modal chrome + keybindings are execute (UI/glue, validated behaviorally via the dev-feedback-loop).

Purpose: REQ-11 — Cmd+P fuzzy file search, Cmd+O course/concept jump, Cmd+Shift+P universal action, ≥80% of navigation reachable without mouse. Bits UI `Command` provides the accessible focus-trap + ↑↓/Enter/Esc model the no-mouse goal depends on (do NOT hand-roll — RESEARCH Don't-Hand-Roll). This plan owns ONLY palette files; it does NOT edit `+layout.svelte` (single-owner = Wave 3 / 03-09 installs the registrar at layout root) — instead `keybindings.svelte.ts` exports an `installKeybindings()` that 03-09 calls. It does NOT edit `+page.svelte` (03-09 mounts the palette at template root). The palette renders against an `items` prop + an `onrun` callback; 03-09/03-08 supply the file/course data sources.
Output: `palette-actions.ts` + `keybindings.svelte.ts` + `CommandPalette.svelte` + the fuzzysort ranking test. bits-ui + fuzzysort are registered in dependencies.md by 03-08 (KP-08). Net-new components carry `// Visual SSOT: 03-UI-SPEC.md §Surface 2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md
@.planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md
@.planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md
@.planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md
@src/lib/components/SettingsPanel.svelte
@src/lib/stream-dispatch.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: palette-actions.ts + fuzzysort ranking proof (REQ-11)</name>
  <files>src/lib/palette-actions.ts, tests/palette-ranking.test.ts</files>
  <read_first>
    - src/lib/stream-dispatch.ts (helper region — pure functions + data, NO module-scope mutable state; the no-state-leak discipline is the precedent for palette-actions)
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md Surface 2 "Recommended Cmd+Shift+P action set" (New session, New markdown file, Toggle session sidebar, Open settings (Cmd+,), Switch to last session, Sync vault now, Toggle preview/edit)
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Pattern 1 (fuzzysort.go returns {obj, score, indexes}; indexes drive the highlight) + Standard Stack (fuzzysort@3.1.0)
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md "Command-palette fuzzy match" row + Reference Dataset "Command-registry fixture"
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "palette-actions.ts (pure data)"
  </read_first>
  <behavior>
    - RED first: write `tests/palette-ranking.test.ts` importing `fuzzysort` + the `PALETTE_ACTIONS` array from `palette-actions.ts`. Build a fixture of command labels + file paths (e.g. `lecture-01.md`, `CS2024/notes/intro.md`, `New markdown file`, `New session`, `Toggle preview/edit`). Assert representative queries rank as expected: query `"lec01"` ranks `lecture-01.md` first; query `"newmd"` ranks `New markdown file` above `New session`; query `"tgpe"` (subsequence) matches `Toggle preview/edit`; a no-match query (`"zzzz"`) returns an empty `fuzzysort.go` result. Use `fuzzysort.go(query, items, { key: "searchText", limit: 50 })` and assert `results[0].obj.id`. Run `npx vitest run tests/palette-ranking.test.ts` → FAIL (palette-actions absent OR fuzzysort not installed). If fuzzysort is not yet a dep, the RED is the install/import failure; install `fuzzysort@3.1.0` (dependencies.md registration is 03-08's job, but the package install happens here so the test can run — note this in SUMMARY for 03-08 to register). Capture RED.
    - GREEN: write `src/lib/palette-actions.ts` exporting `interface PaletteAction { id: string; label: string; glyph: string; searchText: string; run: () => void }` and `export const PALETTE_ACTIONS: PaletteAction[]` = the bounded set from 03-UI-SPEC (each `run()` dispatches an existing window CustomEvent like `mneme:open-settings`, or calls a sessions-store setter — NO module-level `$state`; pure data + handlers). Each action's `searchText` is the lowercased label for ranking.
    - REFACTOR: confirm no module-scope mutable state (`grep` for `$state`/`let ` at module scope is empty for mutable data). Header cites `// REQ-11` + `// Visual SSOT: n/a (pure data)`.
  </behavior>
  <action>Define `PALETTE_ACTIONS` as the EXACT bounded set from 03-UI-SPEC Surface 2 (`New session`, `New markdown file`, `Toggle session sidebar`, `Open settings (Cmd+,)`, `Switch to last session`, `Sync vault now`, `Toggle preview/edit`). Each `run()` triggers an existing window CustomEvent (e.g. `window.dispatchEvent(new CustomEvent("mneme:open-settings"))` — the precedent SettingsPanel uses) or a sessions-store setter; NO module `$state`. Use `fuzzysort@3.1.0` (`fuzzysort.go(search, items, { key, limit })` returning `{obj, score, indexes}`). Install `fuzzysort@3.1.0` so the test runs; flag in SUMMARY that 03-08 must register it (+ bits-ui) in dependencies.md (KP-08) and flip the stale `cmdk` row. The ranking test is the REQ-11 fuzzy-match proof.</action>
  <verify>
    <automated>npx vitest run tests/palette-ranking.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'New markdown file' src/lib/palette-actions.ts` >= 1 AND `grep -c 'export const PALETTE_ACTIONS' src/lib/palette-actions.ts` == 1.
    - Source assertion (no module state): `grep -Ec '^let |^const .*\\$state|export let' src/lib/palette-actions.ts` == 0 (pure data; no mutable module scope).
    - Behavior assertion: vitest passes — `lec01`→`lecture-01.md` first, `newmd`→`New markdown file` first, `tgpe` subsequence matches `Toggle preview/edit`, `zzzz`→empty.
    - RED proof recorded in SUMMARY (palette-actions/fuzzysort import failure before GREEN).
  </acceptance_criteria>
  <done>palette-actions.ts is a pure bounded action set; fuzzysort ranks the command/path fixture in the expected order; the ranking proof passes; fuzzysort installed (03-08 to register in dependencies.md).</done>
</task>

<task type="auto">
  <name>Task 2: keybindings.svelte.ts global registrar (collision-safe, Cmd+Shift+V reserved)</name>
  <files>src/lib/keybindings.svelte.ts</files>
  <read_first>
    - src/routes/+layout.svelte (the install-once + onDestroy-unlisten pattern at L94-98/L196-199 — DEV-forwarder dynamic install precedent)
    - src/lib/components/SettingsPanel.svelte (the existing Cmd+, listener that dispatches a window CustomEvent rather than handling inline — the dispatch-then-single-listener contract)
    - src/lib/dev/console-forwarder.ts (the `globalThis.__mnemeForwarderInstalled` singleton flag at L119-124 — reuse this HMR double-install guard)
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Pattern 5 (single global keydown, normalize with `.toLowerCase()` + read `e.shiftKey` separately; never match `e.key === "P"`; collision set Cmd+, / Cmd+I / Cmd+Q; Cmd+Shift+V reserved) + 03-UI-SPEC Keyboard-Nav Contract
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "keybindings.svelte.ts (event registrar)" + "Shared Pattern: HMR double-init guard"
  </read_first>
  <action>Create `src/lib/keybindings.svelte.ts` (`.svelte.ts` because it holds the reactive palette open-state). Export module-scope `$state` `paletteState = $state<{ open: boolean; mode: "file" | "course" | "action" }>({ open: false, mode: "file" })` + `openPalette(mode)` / `closePalette()`. Export `installKeybindings(): () => void` (returns an unlisten fn) that registers ONE `window` keydown via `addEventListener` (CSP-safe — not eval), guarded by a `globalThis.__mnemeKeybindingsInstalled` singleton flag (HMR double-register guard, mirror console-forwarder). Dispatch: `meta && !shift && key.toLowerCase()==="p"` → `openPalette("file")` + `preventDefault`; `meta && key.toLowerCase()==="o"` → `openPalette("course")`; `meta && shift && key.toLowerCase()==="p"` → `openPalette("action")`. Normalize with `.toLowerCase()` + read `e.shiftKey` separately (never match `"P"`). Cmd+Shift+V: explicitly `preventDefault` + no-op (RESERVED for REQ-19 voice — the slot is taken so a future binding cannot collide, per VALIDATION.md gap note) with a comment `// RESERVED REQ-19 — do NOT bind to anything`. Do NOT re-handle Cmd+, / Cmd+I / Cmd+Q (owned by native menu / existing listeners — collision awareness only). Header cites `// REQ-11` + `// Visual SSOT: n/a (registrar)`. Note for 03-09: call `installKeybindings()` in `+layout.svelte`'s install block + the returned unlisten in `onDestroy`.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (reserved binding): `grep -c 'Shift+V' src/lib/keybindings.svelte.ts` >= 1 AND a comment marks it RESERVED (`grep -ci 'RESERVED' src/lib/keybindings.svelte.ts` >= 1).
    - Source assertion (normalize, not literal P): `grep -c 'toLowerCase' src/lib/keybindings.svelte.ts` >= 1 AND `grep -c 'shiftKey' src/lib/keybindings.svelte.ts` >= 1.
    - Source assertion (HMR guard + svelte.ts): the file is `keybindings.svelte.ts` AND `grep -c '__mnemeKeybindingsInstalled' src/lib/keybindings.svelte.ts` >= 1.
    - Source assertion (no collision re-handle): `grep -Ec 'open-settings|cmd.*I.*import' src/lib/keybindings.svelte.ts` reflects awareness only — Cmd+, / Cmd+I are NOT re-dispatched as palette opens.
    - Behavior assertion: `npm run check` reports no new errors.
  </acceptance_criteria>
  <done>keybindings.svelte.ts installs ONE collision-safe window keydown (HMR-guarded), opens the palette in file/course/action mode, normalizes keys via toLowerCase + shiftKey, reserves Cmd+Shift+V with a no-op, and exposes installKeybindings()/unlisten for 03-09.</done>
</task>

<task type="auto">
  <name>Task 3: CommandPalette.svelte (Bits UI Command + fuzzysort, centered modal + scrim)</name>
  <files>src/lib/components/CommandPalette.svelte</files>
  <read_first>
    - src/lib/components/SettingsPanel.svelte (modal CHROME analog: backdrop scrim + `role="dialog"` + Esc/backdrop close at L131-140/L156-170 — for the scrim + close behavior; Bits UI Command provides the INTERNAL focus-trap + ↑↓/Enter/Esc — do NOT re-implement those)
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Pattern 1 (the Bits UI Command + fuzzysort body with `shouldFilter={false}` + `$derived` ranking at L211-237; verified bits-ui@2.18.1 API facts) + Standard Stack (bits-ui@2.18.1)
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md Surface 2 (modal geometry max-width 560px / max-height 60vh / `--radius-lg` / `--shadow-2`; scrim `--color-scrim` + `--blur-soft`; result-row anatomy icon+primary label+context line; fuzzy highlight `--fw-semibold` + `--color-orange`; selected-row 2px orange bar + cream-deep fill; three entry modes + placeholders; empty-state copy; keyboard model) + Motion (open 160ms `--duration-fast`, scale 0.98→1)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "CommandPalette.svelte" + keybindings.svelte.ts (Task 2 — `paletteState`)
  </read_first>
  <action>Create `src/lib/components/CommandPalette.svelte` using `import { Command } from "bits-ui"` + `import fuzzysort from "fuzzysort"`. Props: `interface Props { items: PaletteItem[]; mode: "file" | "course" | "action"; open: boolean; onrun: (item: PaletteItem) => void; onclose: () => void }`. Use `<Command.Root shouldFilter={false}>` + own `const ranked = $derived(search ? fuzzysort.go(search, items, { key: "searchText", limit: 50 }) : items.map(obj => ({ obj, indexes: [] })))` so match `indexes` drive the per-char highlight. Render `Command.Input` (placeholder per mode: `Search files…` / `Jump to course or concept…` / `Run a command…`), `Command.List`, `Command.Empty` (mode-specific empty copy from 03-UI-SPEC), `Command.Item` per ranked result with: 16px inline-SVG glyph (`aria-hidden="true"`, mode-specific), primary label `--fs-body` serif `--ink` with fuzzy-matched chars wrapped `--fw-semibold` + `--color-orange` (drive from `indexes`), secondary context line `--fs-meta` `--font-mono` `--ink-mute`. Modal geometry: max-width 560px, max-height 60vh, `--radius-lg`, `--shadow-2`, surface `--color-cream`, 1px `--border-soft`; centered slightly above middle; backdrop scrim `--color-scrim` + `--blur-soft` (the SettingsPanel/DropzoneOverlay shared veil). Open/close motion 160ms `--duration-fast` (opacity + `translateY(-4px→0) scale(0.98→1)`). Keyboard: rely on Bits UI Command for ↑↓ (wrap via `loop`) / Enter (`onSelect` → `onrun` + `onclose`) / Esc (close, restore focus) + focus-trap — do NOT hand-roll. Backdrop click + Esc → `onclose`. Reference ONLY existing tokens. Header: `// Visual SSOT: 03-UI-SPEC.md §Surface 2`. Note for 03-09: mount at `+page.svelte` template root (outside `.window` overflow:hidden), bind `open`/`mode` from `keybindings.svelte.ts` `paletteState`, supply `items` per mode (file = vault index, course = course/concept index, action = `PALETTE_ACTIONS`).</action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'bits-ui' src/lib/components/CommandPalette.svelte` >= 1 AND `grep -c 'fuzzysort' src/lib/components/CommandPalette.svelte` >= 1 AND `grep -c 'shouldFilter' src/lib/components/CommandPalette.svelte` >= 1.
    - Source assertion (scrim + geometry): `grep -c 'color-scrim' src/lib/components/CommandPalette.svelte` >= 1 AND `grep -c 'blur-soft' src/lib/components/CommandPalette.svelte` >= 1 AND `grep -c '560' src/lib/components/CommandPalette.svelte` >= 1.
    - Source assertion (fuzzy highlight + a11y): `grep -c 'color-orange' src/lib/components/CommandPalette.svelte` >= 1 AND `grep -c 'aria-hidden' src/lib/components/CommandPalette.svelte` >= 1; `grep -Ec '#[0-9a-fA-F]{3,6}' src/lib/components/CommandPalette.svelte` == 0 (no new tokens).
    - Source assertion (CSP-clean): `grep -Ec 'unsafe-inline|eval\\(' src/lib/components/CommandPalette.svelte` == 0 (REQ-5).
    - Behavior assertion: `npm run check` reports no new errors.
  </acceptance_criteria>
  <done>CommandPalette renders a Bits-UI-Command + fuzzysort centered modal with dim scrim, three entry modes, per-char orange fuzzy highlight, the ↑↓/Enter/Esc keyboard model from Bits UI (not hand-rolled), no raw hex, CSP-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes -> palette filter | search text drives fuzzysort ranking only; never evaluated |
| vault index items -> palette rows | file paths / labels (untrusted) rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-19 | Tampering (XSS) | palette result labels / paths | mitigate | labels render as Svelte text content (auto-escaped); fuzzy-highlight wraps matched chars in `<span>` with text only, NOT `{@html}` |
| T-3-20 | EoP | CSP regression via Bits UI / fuzzysort | mitigate | both use standard Svelte reactivity / no `eval` (RESEARCH REQ-5 verification); acceptance greps for `unsafe-inline`/`eval(` == 0; `script-src` unchanged |
| T-3-21 | DoS | huge vault list ranking on every keystroke | accept | `fuzzysort.go` `limit: 50` caps rendered rows; single-user desktop posture |
</threat_model>

<verification>
- `npx vitest run tests/palette-ranking.test.ts` — green.
- `npm run check` — no new errors across the three palette files.
- dev-feedback-loop (Wave 3 after mount): `gsd-dev-screenshot` palette open (centered, dim scrim, orange fuzzy highlight) matches 03-UI-SPEC Surface 2.
</verification>

<success_criteria>
- fuzzysort ranking proof passes (REQ-11 fuzzy match).
- Palette is a Bits UI Command modal with the up/down/Enter/Esc model + dim scrim + three modes + orange per-char highlight; Cmd+Shift+V reserved.
- CSP-clean; no new tokens; no edits to +layout / +page (Wave 3 owns those).
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-command-palette-editor/03-06-SUMMARY.md`. Document for 03-08 (dependencies.md): register `bits-ui@2.18.1` + `fuzzysort@3.1.0` (KP-08), flip the stale `cmdk (or kbar)` row. Document for 03-09: call `installKeybindings()` in `+layout.svelte`; mount `CommandPalette` at `+page.svelte` template root bound to `paletteState`; supply per-mode `items` (file=vault index, course=course/concept index, action=`PALETTE_ACTIONS`).
</output>
