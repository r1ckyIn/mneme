---
phase: 03-multi-session-command-palette-editor
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/sessions_store.rs
  - src-tauri/src/session.rs
  - src-tauri/tests/sessions_store.rs
  - src-tauri/tests/session_ceiling.rs
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "A session row persists to the Phase-2 WAL DB and survives a connection drop+reopen (insert -> reopen -> row present) — REQ-12 'sessions survive app restart'."
    - "The sessions schema is idempotent: opening on a fresh DB AND on an existing vault-index.db both succeed (CREATE TABLE IF NOT EXISTS)."
    - "Recents order is last_active DESC."
    - "The 3rd concurrent live generation is refused at the SessionRegistry under a single lock (D-10 ceiling = 2)."
  artifacts:
    - path: "src-tauri/src/sessions_store.rs"
      provides: "rusqlite sessions CRUD in the existing vault-index.db (D-04): init/upsert/get/list, all params![]"
      contains: "CREATE TABLE IF NOT EXISTS sessions"
    - path: "src-tauri/src/session.rs"
      provides: "SessionRegistry.count() + try_register_if_below(id, handle, ceiling) refusing >= D-10 ceiling under one lock; ChildHandle extended"
      contains: "try_register_if_below"
    - path: "src-tauri/tests/sessions_store.rs"
      provides: "P-03 persistence + idempotent-schema proof (second Connection::open introspection)"
    - path: "src-tauri/tests/session_ceiling.rs"
      provides: "P-02 ceiling proof"
  key_links:
    - from: "src-tauri/src/sessions_store.rs"
      to: "vault-index.db (rusqlite WAL)"
      via: "execute_batch(CREATE TABLE IF NOT EXISTS sessions ...) + params![] queries"
      pattern: "CREATE TABLE IF NOT EXISTS sessions"
    - from: "src-tauri/src/session.rs::try_register_if_below"
      to: "D-10 ceiling constant"
      via: "map.len() >= ceiling -> false, under the existing locked() guard (no TOCTOU)"
      pattern: "len\\(\\)\\s*>="
---

<objective>
Build the two Rust backend primitives multi-session needs, both pure-logic TDD targets with deterministic I/O: (1) `sessions_store.rs` — a rusqlite `sessions` table added to the EXISTING Phase-2 `vault-index.db` (D-04), mirroring `vault_index.rs` verbatim; (2) the D-10 concurrency ceiling on the Phase-1 `SessionRegistry` (refuse the 3rd live `ChildHandle` under a single lock).

Purpose: REQ-12 requires durable session records (so the Recents list lists past sessions across launches) and a hard concurrency limit (so two `claude` subprocesses never become three on this Intel Mac). These are the VALIDATION.md proofs P-02 (ceiling) and P-03 (persistence + idempotent schema).
Output: `sessions_store.rs` (new module), `session.rs` extended with `count()` + `try_register_if_below()` + an extended `ChildHandle`, and two Rust integration test files. This plan does NOT touch `lib.rs` — Wave 3 plan 03-08 owns the `mod sessions_store;` declaration + invoke-handler registration + the D-10 wire-up at the spawn call site. This plan only exposes the constructors/methods 03-08 calls. The tests compile the modules directly via `use mneme_lib::...` (the lib crate is `mneme_lib` per Cargo.toml `[lib] name`), so they need `pub mod` — see <action> for how that stays a Wave-3-owned single edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md
@.planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md
@.planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md
@.planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md
@src-tauri/src/vault_index.rs
@src-tauri/src/session.rs
@src-tauri/tests/vault_index_count.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: sessions_store.rs rusqlite persistence + idempotent schema (P-03)</name>
  <files>src-tauri/src/sessions_store.rs, src-tauri/tests/sessions_store.rs</files>
  <read_first>
    - src-tauri/src/vault_index.rs (EXACT analog per 03-PATTERNS.md: `VaultIndex { conn: Mutex<Connection> }`, `VaultIndexError` thiserror enum, `with_conn` poisoned-mutex helper at L124-138, `init()` WAL pragma stack order at L88-122, `params![]` insert/get/list-ordered at L142-216)
    - src-tauri/tests/vault_index_count.rs (analog: integration test that opens a SECOND `rusqlite::Connection::open(&db_path)` for read-only introspection; `use mneme_lib::vault_index::...`)
    - .planning/phases/03-multi-session-command-palette-editor/03-RESEARCH.md Pattern 4 (the SESSIONS_SCHEMA verbatim, L296-306) + Architectural Responsibility Map (D-04 row)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "sessions_store.rs (EXACT analog)"
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md P-03 row
  </read_first>
  <behavior>
    - RED first: write `src-tauri/tests/sessions_store.rs` using `tempfile::tempdir()` for an isolated `vault-index.db`. Test `persists_and_reopens`: open a `SessionsStore` on `dir/vault-index.db`, `upsert_session` a row (`session_id` = `"3f2504e0-4f89-41d3-9a0c-0305e82c3301"`, `title` = `"hello"`, `cwd` = `"/Users/me/StudyVault"`, `summary` = `"first prompt"`), drop the store, reopen on the same path, assert `get_session(id)` returns id/title/session_id/cwd/summary intact. Test `schema_idempotent_on_existing_db`: `SessionsStore::init` the same path twice in a row → both `Ok` (proves `CREATE TABLE IF NOT EXISTS` coexists with an already-populated `vault_files` table). Test `list_orders_by_last_active_desc`: insert 3 rows with ascending `last_active`, assert `list_sessions()` returns them newest-first. Run `cargo test --test sessions_store` and confirm it FAILS to compile (module absent) — capture the exact error as the RED proof.
    - GREEN: create `src-tauri/src/sessions_store.rs` mirroring `vault_index.rs`. Public surface: `pub struct SessionsStore { conn: Mutex<Connection> }`; `pub fn init(db_path: &Path) -> Result<Self, SessionsStoreError>` that creates the parent dir, opens, applies the SAME pragma stack IN THE SAME ORDER as `vault_index.rs::init` (`journal_mode=WAL`, `synchronous=NORMAL`, `busy_timeout=5000`, `wal_autocheckpoint=1000`), then `execute_batch` the `sessions` schema verbatim from RESEARCH Pattern 4: `CREATE TABLE IF NOT EXISTS sessions (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT, title TEXT, cwd TEXT NOT NULL, summary TEXT, last_active TEXT NOT NULL); CREATE INDEX IF NOT EXISTS sessions_last_active_idx ON sessions(last_active DESC);`. Reuse the `with_conn` poisoned-mutex helper verbatim (rename prefix to `[sessions_store]`). Methods, all via `params![]`: `upsert_session(&self, row: &SessionRow) -> Result<i64, SessionsStoreError>` (INSERT, return rowid); `get_session(&self, id: i64) -> Result<Option<SessionRow>, _>`; `list_sessions(&self) -> Result<Vec<SessionRow>, _>` ordered `last_active DESC`; `touch_session(&self, id: i64, last_active: &str, summary: Option<&str>) -> Result<(), _>` (UPDATE for the D-05a chat.result summary bump); `delete_session(&self, id: i64) -> Result<(), _>`. Define `pub struct SessionRow { pub id: Option<i64>, pub session_id: Option<String>, pub title: Option<String>, pub cwd: String, pub summary: Option<String>, pub last_active: String }` deriving `serde::Serialize, serde::Deserialize, Clone` (Wave-3 Tauri command needs Serialize). Define a `SessionsStoreError` thiserror enum mirroring `VaultIndexError` minus the `Walk` variant.
    - REFACTOR: confirm EVERY statement uses `params![]` — no `format!()`/string-concatenated SQL (audit Gate 10 forbids `format!()` with a SQL keyword anywhere under `src-tauri/src/`). Module header comment cites `// D-04` + `// analog: vault_index.rs` + `// Visual SSOT: n/a (backend)`.
  </behavior>
  <action>Mirror `vault_index.rs` exactly: same `Mutex<Connection>` field, same pragma order in `init`, same `with_conn` helper, same `params![]` discipline. The table goes in the EXISTING `vault-index.db` — do NOT create a second DB file (D-04 / RESEARCH Pattern 4 L310). Do NOT add `mod sessions_store;` to `lib.rs` here (Wave 3 / 03-08 owns that single `lib.rs` edit). To let `cargo test --test sessions_store` compile this Wave-1-only module without a `lib.rs` edit, the integration test file declares `#[path = "../src/sessions_store.rs"] mod sessions_store;` and uses `use sessions_store::{SessionsStore, SessionRow};` (same `#[path]` include technique the wave-1 ceiling test uses for `session.rs`). Column names are exactly `id, session_id, title, cwd, summary, last_active`. Use the `rusqlite`, `serde`, `serde_json` crates already in Cargo.toml; `tempfile` is already a dev-dep. Derive ISO-8601 timestamps as caller-supplied `&str` (do NOT hard-code a clock — the caller passes `last_active`), matching `vault_index.rs` which takes timestamps as parameters.</action>
  <verify>
    <automated>cd src-tauri && cargo test --test sessions_store -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'CREATE TABLE IF NOT EXISTS sessions' src-tauri/src/sessions_store.rs` == 1.
    - Source assertion (parameterized-only): `grep -v '^[[:space:]]*//' src-tauri/src/sessions_store.rs | grep -Ec 'format!\([^)]*(SELECT|INSERT|UPDATE|DELETE)'` == 0, AND `grep -c 'params!' src-tauri/src/sessions_store.rs` >= 4.
    - Behavior assertion: `cargo test --test sessions_store` passes `persists_and_reopens`, `schema_idempotent_on_existing_db`, and `list_orders_by_last_active_desc`.
    - RED proof recorded in SUMMARY: the pre-implementation `cargo test --test sessions_store` compile-failure output.
  </acceptance_criteria>
  <done>sessions_store persists across reopen, the schema is idempotent on an existing DB, list is last_active DESC, every statement is parameterized, and the module compiles via the test's `#[path]` include without any `lib.rs` edit.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: SessionRegistry concurrency ceiling + ChildHandle extension (P-02)</name>
  <files>src-tauri/src/session.rs, src-tauri/tests/session_ceiling.rs</files>
  <read_first>
    - src-tauri/src/session.rs (existing `SessionRegistry`, the `locked()` poisoned-mutex helper at L41-46, `register`/`drain_one`/`drain_all`/`kill_all`, and the `ChildHandle` extension comment at L16-20)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "session.rs (model, extend)" (the `try_register_if_below` code at L139-148 + ChildHandle extension at L150-157)
    - .planning/phases/03-multi-session-command-palette-editor/03-VALIDATION.md P-02 row
    - .planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md D-10
  </read_first>
  <behavior>
    - RED first: write `src-tauri/tests/session_ceiling.rs` declaring `#[path = "../src/session.rs"] mod session;` and `use session::{SessionRegistry, ChildHandle};`. Test `refuses_third_live_generation`: build a `SessionRegistry`, `try_register_if_below(1, ChildHandle{ pid: 1001, .. }, 2)` -> true, `try_register_if_below(2, ChildHandle{ pid: 1002, .. }, 2)` -> true, assert `count() == 2`, then `try_register_if_below(3, ChildHandle{ pid: 1003, .. }, 2)` -> false, and assert `count()` is STILL 2 (the refused handle was not inserted). Test `drain_frees_a_slot`: after two registers, `drain_one(1)`, then a 3rd `try_register_if_below(.., 2)` -> true. Run `cargo test --test session_ceiling` and confirm it FAILS (no `try_register_if_below`/`count` yet) — capture the RED proof.
    - GREEN: add to `session.rs`: `pub fn count(&self) -> usize { self.locked().len() }`; `pub fn try_register_if_below(&self, id: SessionId, handle: ChildHandle, ceiling: usize) -> bool` that takes the lock ONCE via the existing `locked()` helper, checks `if map.len() >= ceiling { return false; }`, inserts, returns true — single guard across check-and-insert (no TOCTOU). Extend `ChildHandle` at the documented L16-20 site with `pub resume_token: Option<String>` and `pub spawned_at: std::time::Instant` (the schema the L19 comment defers to plan-phase). Keep the existing `register` for Phase-1 callers (do NOT delete it); `register` may set `resume_token: None, spawned_at: Instant::now()` so existing call sites still compile.
    - REFACTOR: confirm `try_register_if_below` does NOT call `count()` internally (that would take the lock twice → race); it must hold the single `locked()` guard across `len()` and `insert`. Comment the method `// D-10 — atomic check-and-insert under one lock`.
  </behavior>
  <action>Add `count()`, `try_register_if_below()`, and the two `ChildHandle` fields (`resume_token: Option<String>`, `spawned_at: Instant`). The ceiling is passed as a parameter (`ceiling: usize`) — Wave 3 / 03-08 calls `try_register_if_below(id, handle, 2)` at the spawn site; do NOT hard-code a module const. The check-and-insert MUST hold one `locked()` guard (no internal `count()` call). Update the existing `register` (and any existing call sites within `session.rs`) so `ChildHandle` literals include the two new fields. Do NOT modify any `#[tauri::command]` fn or `lib.rs` — Wave 3 (03-08) rewrites the command layer to allocate dynamic `SessionId`s and call `try_register_if_below`. `try_register_if_below` returning `false` is the sentinel Wave 3 maps to the D-10 `CeilingToast`.</action>
  <verify>
    <automated>cd src-tauri && cargo test --test session_ceiling -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'fn try_register_if_below' src-tauri/src/session.rs` == 1 AND `grep -c 'fn count' src-tauri/src/session.rs` >= 1.
    - Source assertion (single-lock, no TOCTOU): within `try_register_if_below`, `count(` does not appear — verify `sed -n '/fn try_register_if_below/,/^    }/p' src-tauri/src/session.rs | grep -c 'count('` == 0.
    - Source assertion: `grep -c 'resume_token' src-tauri/src/session.rs` >= 1 AND `grep -c 'spawned_at' src-tauri/src/session.rs` >= 1.
    - Behavior assertion: `cargo test --test session_ceiling` passes `refuses_third_live_generation` (3rd -> false, count stays 2) and `drain_frees_a_slot`.
    - RED proof recorded in SUMMARY.
  </acceptance_criteria>
  <done>The registry refuses the 3rd registration under a single lock, frees a slot on drain, ChildHandle carries resume_token + spawned_at, existing `register` call sites still compile, and no command fn / lib.rs is touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-state -> SQLite | session `cwd`/`session_id` come from app state; a user-derived `title`/`summary` (first prompt text) is untrusted text stored in TEXT columns |
| JS -> Rust registry | Wave 3 commands call `try_register_if_below`; this plan only defines the safe primitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (SQL injection) | sessions_store.rs SQL | mitigate | All statements use rusqlite `params![]`; no `format!()`-built SQL — acceptance criteria greps prove it; audit Gate 10 enforces across `src-tauri/src/` |
| T-3-02 | Injection | session title/summary (untrusted user text) | mitigate | Stored only via bound parameter; never interpolated into SQL; render-side escaping is Wave-2's `sanitize.ts` concern |
| T-3-03 | Denial of Service | unbounded concurrent subprocesses | mitigate | `try_register_if_below` enforces the D-10 ceiling=2 with a single-lock check-and-insert (no TOCTOU) |
| T-3-04 | Tampering | DB file path | accept | Path is `~/.mneme/vault-index.db`, fixed in Wave-3 setup from `home::home_dir()`, not attacker-influenced |
</threat_model>

<verification>
- `cd src-tauri && cargo test --test sessions_store --test session_ceiling` — both green.
- `grep -v '^[[:space:]]*//' src-tauri/src/sessions_store.rs | grep -c 'params!'` >= 4.
- `cargo build -p mneme_lib` still compiles with the extended `ChildHandle` (existing `register` call sites updated).
</verification>

<success_criteria>
- P-02 and P-03 proofs pass.
- `lib.rs` is NOT modified by this plan (Wave 3 ownership preserved).
- New module + registry methods are self-contained, consumed only via the tests' `#[path]` includes until Wave 3 wires `mod sessions_store;` + the D-10 spawn-site call.
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-command-palette-editor/03-01-SUMMARY.md` when done. Include the RED failing-test output for both tasks and the exports Wave 3 (03-08) must wire: `sessions_store::SessionsStore::{init, upsert_session, get_session, list_sessions, touch_session, delete_session}`, `sessions_store::SessionRow`, `session::{count, try_register_if_below}`, and the extended `ChildHandle` fields.
</output>
