---
phase: 03-multi-session-command-palette-editor
plan: 05
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/lib/sessions.svelte.ts
  - src/lib/components/SessionSidebar.svelte
  - src/lib/components/SessionRailItem.svelte
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "The session sidebar defaults to COLLAPSED (a single 28px toggle at the top of the right chat pane) and PUSH-expands to a 240px rail (New session -> Recents -> footer) — REQ-12 / 03-UI-SPEC Surface 1."
    - "Recents lists session titles ordered last_active DESC; the active thread shows a 2px --color-orange left bar + --fw-semibold title."
    - "Collapse state persists across launches via localStorage key mneme.sessions.collapsed (default true)."
    - "The footer reads the EXISTING connection-state.svelte singleton (no duplicate status store)."
  artifacts:
    - path: "src/lib/sessions.svelte.ts"
      provides: "module-scope $state reactive singleton wrapping session-machine.reduceSession: activeId + recents + setters (MANDATORY .svelte.ts)"
      contains: "$state"
    - path: "src/lib/components/SessionSidebar.svelte"
      provides: "28px toggle <-> 240px PUSH rail via right-pane inner grid-template-columns transition; New session CTA + Recents + footer"
      contains: "mneme.sessions.collapsed"
    - path: "src/lib/components/SessionRailItem.svelte"
      provides: "Recents row with default/hover/active/loading/empty states per 03-UI-SPEC"
      contains: "Visual SSOT: 03-UI-SPEC.md"
  key_links:
    - from: "src/lib/components/SessionSidebar.svelte"
      to: "src/lib/sessions.svelte.ts"
      via: "reads recents + activeId from the store; New session / switch / close call store setters"
      pattern: "sessions"
    - from: "src/lib/components/SessionSidebar.svelte"
      to: "src/lib/connection-state.svelte.ts"
      via: "footer imports the existing connection singleton"
      pattern: "connection-state"
---

<objective>
Build the multi-session sidebar (REQ-12, 03-UI-SPEC Surface 1): the reactive session store `sessions.svelte.ts` (wraps the pure `reduceSession` from 03-03), the `SessionSidebar.svelte` rail (28px collapsed toggle ↔ 240px PUSH-expand, New session → Recents → footer), and the `SessionRailItem.svelte` Recents row. Dock is RESOLVED (03-UI-SPEC): embedded INSIDE the right ChatPanel, NOT a left overlay, NOT a 4th Splitter column.

Purpose: REQ-12's visible surface. This plan builds the sidebar components + store; Wave 3 (03-08) embeds `SessionSidebar` into `ChatPanel.svelte` and wires the store to the Rust `sessions_store` CRUD commands. This plan does NOT edit ChatPanel (single-owner = 03-08), does NOT edit `lib.rs`, and does NOT call Tauri commands directly — it exposes store setters and renders against in-store data; 03-08 connects the store to the backend.
Output: `sessions.svelte.ts` + `SessionSidebar.svelte` + `SessionRailItem.svelte`. Validated behaviorally against 03-UI-SPEC via the dev-feedback-loop (gsd-dev-screenshot / gsd-dev-snapshot), NOT Playwright (per VALIDATION.md). All net-new components carry a `// Visual SSOT: 03-UI-SPEC.md §Surface 1` header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-session-command-palette-editor/03-CONTEXT.md
@.planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md
@.planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md
@src/lib/connection-state.svelte.ts
@src/lib/components/Splitter.svelte
@src/lib/session-machine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sessions.svelte.ts reactive store</name>
  <files>src/lib/sessions.svelte.ts</files>
  <read_first>
    - src/lib/session-machine.ts (the pure `reduceSession` + `SessionState`/`SessionEvent` types from 03-03 — this store wraps it)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "sessions.svelte.ts (EXACT analog)" (vault-state.svelte.ts shape L186-202) + "Shared Pattern: .svelte.ts suffix for module-scope $state"
    - src/lib/connection-state.svelte.ts (the canonical `.svelte.ts` module-scope `$state` note — do NOT duplicate connection status into this store; the footer reads connection-state directly)
  </read_first>
  <action>Create `src/lib/sessions.svelte.ts` (the `.svelte.ts` suffix is MANDATORY — a plain `.ts` silently degrades module-scope `$state` to a non-reactive object per connection-state.svelte.ts). Declare `const state = $state<SessionState>({ threads: [], activeId: null })` (the `SessionState` type imported from `session-machine.ts`). Export `getSessions(): SessionState` and event-dispatching setters that route through the pure reducer: `dispatch(event: SessionEvent): void { Object.assign(state, reduceSession(state, event)); }` plus thin wrappers `newSession(id, title)`, `switchSession(id)`, `renameSession(id, title)`, `closeSession(id)`, `setRecents(rows)` (replace the threads list from a backend `list_sessions` result, mapped to `SessionThread`), and `resetForTest()`. Track a separate `loadingId: string | null` field in the store for the Recents "loading-while-resuming" state (03-UI-SPEC). Do NOT add connection status here (footer reads `connection-state.svelte`). Header cites `// REQ-12` + `// Visual SSOT: n/a (store)` + the `.svelte.ts` mandatory note.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the file is named `sessions.svelte.ts` (not `.ts`) AND `grep -c '\$state' src/lib/sessions.svelte.ts` >= 1.
    - Source assertion: `grep -c 'reduceSession' src/lib/sessions.svelte.ts` >= 1 (routes through the pure reducer) AND `grep -c 'connection' src/lib/sessions.svelte.ts` == 0 (no duplicate status store).
    - Behavior assertion: `npm run check` (svelte-check + tsc strict) reports no new errors for this module.
  </acceptance_criteria>
  <done>sessions.svelte.ts is a reactive `.svelte.ts` singleton wrapping reduceSession with newSession/switchSession/renameSession/closeSession/setRecents/resetForTest + a loadingId, no duplicated connection status, type-clean.</done>
</task>

<task type="auto">
  <name>Task 2: SessionRailItem.svelte Recents row</name>
  <files>src/lib/components/SessionRailItem.svelte</files>
  <read_first>
    - src/lib/components/UserBubble.svelte (the minimal `Props`-interface component precedent — inline `interface Props` + `$props()`)
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md "Recents item states" table (default/hover/active/loading-while-resuming/empty) + Color section (active = 2px `--color-orange` left bar + `--fw-semibold`; inactive = `--ink-soft`) + Motion (Recents item hover 160ms `--duration-fast` fade to `--color-cream-deep`)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "SessionRailItem" note (small Props component, last_active DESC rows)
  </read_first>
  <action>Create `src/lib/components/SessionRailItem.svelte` with inline `interface Props { id: string; title: string; active: boolean; loading: boolean; onselect: (id: string) => void; onclose: (id: string) => void; }` and `let { ... }: Props = $props()`. Render a row: title at `--fs-meta` (14px), one line, ellipsis-truncate, padding `--space-2` vertical / `--space-3` horizontal, `--radius-md` corner on hover/active fill. States exactly per 03-UI-SPEC: default = `--ink-soft` `--fw-regular`, no bar; hover = `--color-cream-deep` fill, `--ink`, 160ms `--duration-fast` `background-color` fade; active = 2px `--color-orange` left bar + `--ink` `--fw-semibold` + `--color-cream-deep` fill; loading (resuming) = active styling + a small `--color-orange` pulsing dot trailing the title (reuse `--pulse-period` 1.2s) and the row non-interactive until cleared. `:focus-visible` → `--orange-ring` 3px. Close affordance per 03-UI-SPEC Copywriting (inline confirm popover, `Close session` text in `--color-error` ghost — keep the popover minimal here; full confirm copy is `Close this session? Its summary and history stay on disk...`). Reference ONLY existing `tokens.css` tokens (no new tokens). Header: `// Visual SSOT: 03-UI-SPEC.md §Surface 1 — Recents item states`.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'Visual SSOT: 03-UI-SPEC.md' src/lib/components/SessionRailItem.svelte` >= 1.
    - Source assertion (active-state contract): `grep -c 'color-orange' src/lib/components/SessionRailItem.svelte` >= 1 AND `grep -c 'fw-semibold' src/lib/components/SessionRailItem.svelte` >= 1 AND `grep -c 'pulse-period' src/lib/components/SessionRailItem.svelte` >= 1.
    - Source assertion (no new tokens): no raw hex color — `grep -Ec '#[0-9a-fA-F]{3,6}' src/lib/components/SessionRailItem.svelte` == 0.
    - Behavior assertion: `npm run check` reports no new errors.
  </acceptance_criteria>
  <done>SessionRailItem renders default/hover/active/loading states with the orange active bar + semibold title + pulsing resume dot, focus-visible ring, no raw hex, Visual-SSOT header present.</done>
</task>

<task type="auto">
  <name>Task 3: SessionSidebar.svelte (28px toggle <-> 240px PUSH rail)</name>
  <files>src/lib/components/SessionSidebar.svelte</files>
  <read_first>
    - src/lib/components/Splitter.svelte (localStorage restore+persist try/catch pattern at L40/L54-75/L133-146; the `style:grid-template-columns` grid-driven layout at L157 — the PUSH-expand precedent)
    - src/lib/connection-state.svelte.ts (the footer status singleton — import it, do NOT recreate)
    - src/lib/components/SessionRailItem.svelte (Task 2 — the Recents row this rail renders)
    - src/lib/sessions.svelte.ts (Task 1 — recents + activeId + setters)
    - .planning/phases/03-multi-session-command-palette-editor/03-UI-SPEC.md Surface 1 (collapsed 28px `--btn-send` toggle with state-flipping `aria-label`/`title`; expanded 240px PUSH rail via right-pane inner grid `grid-template-columns` transition 200ms `--duration-base` `--ease-out`; rail content New session ghost CTA → Recents `--fs-h` group label → Recents list → footer; rail surface `--color-cream`, divider 1px `--color-cream-edge`) + Copywriting (`New session`, empty-Recents `No sessions yet` / `Start a new session to begin a thread.`)
    - .planning/phases/03-multi-session-command-palette-editor/03-PATTERNS.md "SessionSidebar.svelte + SessionRailItem.svelte"
  </read_first>
  <action>Create `src/lib/components/SessionSidebar.svelte`. Collapsed (DEFAULT): a single 28px-square toggle button (= `--btn-send` diameter) at the top of the right pane, glyph = 16px hamburger/panel-left inline SVG (stroke `--ink-mute`, hover `--ink-soft`, `aria-hidden="true"`), with state-flipping `aria-label`+`title` (`Show sessions` collapsed / `Hide sessions` expanded), `:focus-visible` → `--orange-ring` 3px. Expanded: PUSH the rail to 240px via a `grid-template-columns` transition on the right-pane inner grid (animate grid columns, NOT `left`/`margin` — mirror Splitter L157), 200ms `--duration-base` `--ease-out`. Rail content top→bottom: (1) `New session` ghost button (accent text `--color-orange`, `--orange-soft` hover fill, leading `+` SVG) calling `sessions.newSession`; (2) `Recents` group label `--fs-h` `--fw-semibold` `--ink-soft`, `--space-6` above; (3) the Recents list rendering `SessionRailItem` for each `getSessions().threads` row (already last_active DESC from the backend via `setRecents`), wiring `onselect`→`switchSession`, `onclose`→`closeSession`, passing `active`/`loading` from the store; empty list → `No sessions yet` / `Start a new session to begin a thread.` centered `--ink-mute` `--fs-meta`; (4) footer (pinned bottom) reading user + connection status from `connection-state.svelte` (`--fs-meta` `--ink-mute`, `--border-soft` top hairline). Persist collapse state to localStorage key `mneme.sessions.collapsed` (default `true`) with the Splitter try/catch restore+persist. Reference ONLY existing tokens. Header: `// Visual SSOT: 03-UI-SPEC.md §Surface 1`. Note for 03-08: this component renders the toggle + rail; 03-08 places it at the top of ChatPanel and wires the store to `list_sessions`/`session_create`/`session_close` Tauri commands + the resume round-trip. Expose the expanded/collapsed width contract (28px / 240px) so ChatPanel's inner grid math matches.</action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'mneme.sessions.collapsed' src/lib/components/SessionSidebar.svelte` >= 1.
    - Source assertion (PUSH via grid, not margin/left): `grep -c 'grid-template-columns' src/lib/components/SessionSidebar.svelte` >= 1.
    - Source assertion (a11y on icon-only toggle): `grep -c 'aria-label' src/lib/components/SessionSidebar.svelte` >= 1 AND `grep -c 'aria-hidden' src/lib/components/SessionSidebar.svelte` >= 1.
    - Source assertion (footer reuses singleton): `grep -c 'connection-state' src/lib/components/SessionSidebar.svelte` >= 1 AND `grep -Ec '#[0-9a-fA-F]{3,6}' src/lib/components/SessionSidebar.svelte` == 0 (no new tokens).
    - Behavior assertion: `npm run check` reports no new errors; default render is collapsed (the localStorage default is `true`).
  </acceptance_criteria>
  <done>SessionSidebar defaults collapsed (28px toggle), PUSH-expands to a 240px grid-driven rail with New session CTA + last_active-DESC Recents + connection-state footer, persists collapse to mneme.sessions.collapsed, icon-only a11y labels present, no raw hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend rows -> sidebar render | session titles/summaries (untrusted user text) are rendered as Recents labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-17 | Tampering (XSS) | session title rendered in Recents | mitigate | titles render as plain text content (Svelte auto-escapes `{title}`), NOT via `{@html}`; no innerHTML; only the chat body uses the sanitize.ts pipeline |
| T-3-18 | Information Disclosure | connection status duplicated/stale | accept | footer reads the single `connection-state.svelte` source of truth; no second store to drift |
</threat_model>

<verification>
- `npm run check` — no new svelte-check/tsc errors across the three files.
- dev-feedback-loop: `gsd-dev-screenshot` collapsed (28px toggle only) + expanded (240px rail) states match 03-UI-SPEC Surface 1 (validated in Wave 3 after ChatPanel embed).
</verification>

<success_criteria>
- Sidebar defaults collapsed, PUSH-expands to 240px, Recents last_active DESC, active-thread orange bar, footer reuses connection-state, collapse persisted.
- No raw hex / no new tokens; icon-only a11y labels present.
- No edits to ChatPanel / lib.rs / +page / +layout (single-owner waves preserved).
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-command-palette-editor/03-05-SUMMARY.md`. Document for 03-08: embed `SessionSidebar` at the top of ChatPanel; wire `sessions.setRecents` ← `list_sessions` Tauri command, `newSession`→`session_create`, `closeSession`→`session_close`, and the Recents-click → set active `{session_id, cwd}` → resume round-trip (cwd MUST match the persisted row per D-07). The collapsed/expanded widths are 28px / 240px (ChatPanel inner grid must match).
</output>
