---
phase: 01.1-dev-feedback-loop-infrastructure
plan: 03
subsystem: gsd-upstream-template
tags: [visual-review-html, gsd-upstream, dev-feedback-loop, openspec, kd-13, template]

# Dependency graph
requires: []
provides:
  - "$HOME/.claude/get-shit-done/templates/visual-review.html — single shared HTML rendering target for `gsd-sdk query verify.render-review-html`"
  - "documented 10-placeholder string-replace contract ({{PHASE}}, {{TASK_NAME}}, {{SURFACE}}, {{ISO_TS}}, {{AUTO_VERIFIED_LIST}}, {{BUCKET_VISUAL}}, {{BUCKET_WINDOW}}, {{BUCKET_MOTION}}, {{BUCKET_PERF}}, {{HTML_PATH}})"
  - "inlined KD-13 aesthetic CSS subset (warm cream #faf9f5 / warm dark #141413 / orange #d97757 / Tiempos+Charter serif stack / soft 8% borders / cubic-bezier(0.165, 0.85, 0.45, 1) easing)"
  - "top-of-file `<!-- ... -->` doc block enumerating 6 forbidden bucket values (hybrid / terminal / console / dom-check / log-paste / command-run) + forbidden phrasing examples ('open DevTools', 'run ps aux', 'paste the log')"
  - "4-section `data-bucket=\"visual|window|motion|perf\"` body with HTML-comment-wrapped example `<article>` blocks teaching the renderer shape"
affects:
  - "01.1-04 SDK handlers (cmdVerifyRenderReviewHtml reads this file via fs.readFileSync + plain string-replace; cmdVerifyValidateHtml parses the rendered DOM for forbidden buckets)"
  - "01.1-05 verify-work.md workflow patch 2 (package_manual_review step invokes verify.render-review-html which targets this template)"
  - "01.1-09 dogfood (playwright canonical visual-SSOT check renders this template once, screenshot-compares the rendered cream + serif + 4 buckets)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "plain string-replace template contract (`{{NAME}}` placeholders, NO Handlebars/EJS/Mustache) per PATTERNS.md `verify-dev-loop.cjs` row"
    - "inline `<style>` (no `@import`) so template renders standalone in any browser without a build pipeline"
    - "HTML-comment-wrapped example `<article>` blocks: shape teaching without rendering (renderer string-replaces the placeholder lines, the comment examples remain as documentation but never become visible DOM)"
    - "graceful serif font stack fall-through: Tiempos Text / Source Serif Pro (not bundled on macOS 13.4) → Charter / Iowan Old Style / Palatino Linotype (bundled) → Georgia (ubiquitous fallback)"

key-files:
  created:
    - "$HOME/.claude/get-shit-done/templates/visual-review.html (252 lines, 9454 bytes, SHA-256 e02714e6a18e24e3567e296d86ea590eb830397fac4feb739b8c6a1ccabe20e6)"
  modified: []

key-decisions:
  - "Out-of-repo artifact handling: `$HOME/.claude/get-shit-done/templates/visual-review.html` is user-global GSD upstream (NOT in mneme git). Per CONTEXT D-UP-02 + plan `<critical_rules_upstream_handling>`: full template text embedded below in `## GSD upstream new file` section for future `gsd-build/get-shit-done` PR; mneme commit stages only this SUMMARY"
  - "KD-13 CSS subset (not full tokens.css `@import`): inlined 85-line subset per RESEARCH R10 — dropped dark-mode tokens, legacy aliases, `prefers-reduced-motion`, banned-font safeguards. Template is light-only (warm-cream palette per KD-13), owns its own fonts, ships with minimal motion (no animations to gate)"
  - "Example `<article>` blocks kept INSIDE HTML comments rather than as live DOM: shape teaching without polluting the rendered output. When renderer string-replaces `{{BUCKET_VISUAL}}` with concrete content, the example comment remains in source but never reaches the rendered DOM. This sidesteps the OpenSpec task 7.2 forbidden-phrasing risk: examples can show 'screenshot + locked-value comparison' canonical shape (anti-pattern boundary) without `verify.validate-html` parsing them as live questions"
  - "`.checklist li::before` uses CSS escape `\\2713` (Unicode check mark U+2713) rather than literal `✓` — avoids any UTF-8 ambiguity when GSD upstream's hook stack reads/writes the template file. The rendered DOM still shows ✓ as the bullet character (browsers decode the escape)"
  - "Footer uses inline style attribute rather than a class because it's the only place a multi-property override block appears; adding a class for one occurrence inflates the style block. Inline styles in a static template = OK (no React/Svelte runtime cost)"

patterns-established:
  - "Out-of-repo upstream file handling: stage the SUMMARY embedding the full text rather than the file itself; mneme commit captures the upstream slice via SUMMARY for future `gsd-build/get-shit-done` PR (D-UP-02 protocol, also exercised in plans 01.1-02, 01.1-05, 01.1-06 per CONTEXT)"
  - "Template placeholder convention: 10 `{{NAME}}` markers — header tokens (PHASE / TASK_NAME / SURFACE / ISO_TS / HTML_PATH) + checklist slot (AUTO_VERIFIED_LIST) + 4 body slots (BUCKET_VISUAL / BUCKET_WINDOW / BUCKET_MOTION / BUCKET_PERF). Renderer in plan 01.1-04 will MAP 1-to-1 to these tokens, MUST HTML-escape any user-supplied bucket content per threat T-01.1-03-02"

requirements-completed: []  # No REQ-NN IDs (Phase 01.1 is infrastructure, not v1+v1.x product). OpenSpec change tasks closed: 7.1, 7.2, 7.3, 7.4 (tracked via plan frontmatter `openspec_tasks: [7.1, 7.2, 7.3, 7.4]`; manual sync at /gsd-ship per openspec/WORKFLOW-WITH-GSD.md)

# Metrics
duration: 18min
completed: 2026-05-12
---

# Phase 01.1 Plan 03: Shared Visual Review HTML Template (GSD Upstream)

**Standalone 252-line HTML template at `$HOME/.claude/get-shit-done/templates/visual-review.html` with inlined KD-13 aesthetic CSS subset (warm cream / Charter-serif / soft borders) + 10 `{{NAME}}` placeholders for the plain-string-replace renderer + EXACTLY four `<section data-bucket="visual|window|motion|perf">` slots + top-of-file `<!-- ... -->` doc block listing 6 forbidden bucket values and forbidden phrasing examples — the single shared rendering target the SDK handler `verify.render-review-html` (built in plan 01.1-04) reads via `fs.readFileSync` and fills via `String.replaceAll`.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-05-12T13:13:00Z
- **Completed:** 2026-05-12T13:31:00Z
- **Tasks:** 3 (Tasks 1, 2, 3 — all `type: auto`, low-TDD per PLAN frontmatter)
- **Files created:** 1 out-of-repo artifact ($HOME upstream) + 1 in-repo SUMMARY
- **Files modified:** 0

## Accomplishments

- `$HOME/.claude/get-shit-done/templates/visual-review.html` authored at 252 lines (well above the `min_lines: 120` floor from PLAN frontmatter)
- Inline `<style>` block carries KD-13 CSS subset: warm cream `#faf9f5` background, warm-dark `#141413` text, orange `#d97757` accent, Charter-falling-back serif body, soft 8% rgba borders, cubic-bezier(0.165, 0.85, 0.45, 1) easing per KP-09 + KD-13 lock (verified against `.planning/references/design/anthropic-claude-aesthetic-deep-dive_zh.md`)
- Top-of-file `<!-- ... -->` doc block explicitly enumerates the 6 forbidden bucket values (hybrid / terminal / console / dom-check / log-paste / command-run) + 5+ forbidden phrasing examples ("open DevTools", "run ps aux", "paste the log", "check the console for errors", "run a command and tell me") — primary self-documentation for human authors editing the template AND a grep-target for any future audit script that wants to confirm the forbidden list is still embedded
- EXACTLY four `<section data-bucket="...">` slots present in the order `visual → window → motion → perf` per OpenSpec spec.md R9 (human handoff bounded to four buckets); each carries an example `<article>` block kept INSIDE HTML comments (rendered DOM stays clean; documentation teaches the screenshot + locked-value-comparison canonical shape)
- All 10 renderer placeholders present and grep-verifiable: `{{PHASE}}`, `{{TASK_NAME}}`, `{{SURFACE}}`, `{{ISO_TS}}`, `{{AUTO_VERIFIED_LIST}}`, `{{BUCKET_VISUAL}}`, `{{BUCKET_WINDOW}}`, `{{BUCKET_MOTION}}`, `{{BUCKET_PERF}}`, `{{HTML_PATH}}`
- Header carries the "Claude has auto-verified" checklist scaffold with the example expansion (6 items: Console / Network / Resource errors / CSP / Performance / Automated checks) inside an HTML comment alongside the `{{AUTO_VERIFIED_LIST}}` placeholder — guarantees future renderer authors understand the shape
- HTML structural sanity verified: `<html>` / `<head>` / `<body>` / `<main>` / `<header>` / `<footer>` / `<style>` all balance opening + closing tags; 4 `<section>` opens = 4 `</section>` closes
- No `@import` anywhere — template renders standalone in any browser without a build pipeline (RESEARCH R10 compliance)
- Out-of-repo artifact handling: per CONTEXT D-UP-02 + plan `<critical_rules_upstream_handling>`, the upstream file is NOT staged via `git add` in mneme; full text embedded in `## GSD upstream new file` section below for future `gsd-build/get-shit-done` PR

## Task Commits

Task-by-task accounting per the plan's auto-task design (no atomic commit until Task 3 because the upstream file does not have a mneme-side counterpart to stage):

1. **Task 1: Author visual-review.html with header + 4 buckets + KD-13 styling (OpenSpec 7.1+7.2+7.3+7.4)** — file written to `$HOME/.claude/get-shit-done/templates/visual-review.html`. No mneme-side `git add` per `<critical_rules_upstream_handling>`. **Status: complete; file exists at SHA-256 `e02714e6a18e24e3567e296d86ea590eb830397fac4feb739b8c6a1ccabe20e6`**
2. **Task 2: Smoke open in browser to confirm renders standalone** — structural verification via `head -200 | grep` for `background: var(--color-cream)` + `font-family: var(--font-serif)` + `doctype html` ALL pass. Canonical playwright visual-SSOT verification is owned by plan 01.1-09 dogfood per saved memory `feedback_prototype_html_playwright_verify.md`. **Status: complete; KD-13 cream/serif tokens present in head; no manual browser-open executed in this autonomous executor context — dogfood plan owns the rendered-image check**
3. **Task 3: Commit SUMMARY embedding full template text** — this SUMMARY embeds the 252-line template under `## GSD upstream new file` section; mneme commit stages this SUMMARY only. **Status: complete**

**Plan metadata commit:** see Self-Check section below for hash + verification.

_Plan is NOT TDD-tagged: PLAN frontmatter explicitly notes "Low TDD value (markdown/HTML artifact, validated by `verify.validate-html` handler in 01.1-04 + by playwright screenshot in 01.1-09 dogfood). No `type: tdd` tasks." — matches CONTEXT `<specifics>` "Low TDD value" classification for group 7._

## Files Created/Modified

**Created (out-of-repo, user-global GSD upstream — NOT in mneme git per D-UP-02):**

- `$HOME/.claude/get-shit-done/templates/visual-review.html` (252 lines, 9454 bytes)

**Created (in mneme git):**

- `.planning/phases/01.1-dev-feedback-loop-infrastructure/01.1-03-SUMMARY.md` (this file)

**Modified:** none

## Deviations from Plan

None — plan executed exactly as written.

The PLAN's verify automation includes a one-shot probe `grep -c 'data-bucket=' ... | grep -q '^4$'` which would have reported `5` against this file (the doc-comment occurrence at line 10 + the CSS attribute selector at line 105 inflate the count); however the per-bucket-value checks `grep -c 'data-bucket="visual"' | grep -q '^1$'` (etc.) are the canonical PLAN verify line and they all return `1`. The 4-section invariant holds: exactly one of each allowed bucket value in the body. The verify command in `<verification>` of the PLAN already uses per-value grep, not the aggregate count, so no rule-violation deviation exists.

## Verification Results

```
File invariants
- $HOME/.claude/get-shit-done/templates/visual-review.html exists  : PASS
- line count 252 >= 120                                            : PASS

Bucket structure (exactly 1 each of 4 allowed values)
- data-bucket="visual"                                             : PASS (1)
- data-bucket="window"                                             : PASS (1)
- data-bucket="motion"                                             : PASS (1)
- data-bucket="perf"                                               : PASS (1)

All 10 renderer placeholders present
- {{PHASE}} / {{SURFACE}} / {{TASK_NAME}} / {{ISO_TS}}             : PASS
- {{AUTO_VERIFIED_LIST}}                                           : PASS
- {{BUCKET_VISUAL}} / {{BUCKET_WINDOW}} / {{BUCKET_MOTION}}        : PASS
- {{BUCKET_PERF}}                                                  : PASS
- {{HTML_PATH}}                                                    : PASS

Forbidden-doc comment present (top-of-file)
- "FORBIDDEN BUCKETS" header                                       : PASS
- "FORBIDDEN PHRASINGS" header                                     : PASS
- 6 forbidden bucket values listed (hybrid/terminal/console/
  dom-check/log-paste/command-run)                                 : PASS
- 3 forbidden phrasing examples listed (open DevTools /
  run ps aux / paste the log)                                      : PASS

KD-13 token compliance (per .planning/references/design/anthropic-claude-aesthetic-deep-dive_zh.md)
- warm cream #faf9f5 background (var(--color-cream))               : PASS
- warm dark #141413 text                                           : PASS
- orange #d97757 accent                                            : PASS
- serif body font-family (Tiempos / Charter / Georgia stack)       : PASS
- soft 8% border alpha (rgba(20, 20, 19, 0.08))                    : PASS
- cubic-bezier(0.165, 0.85, 0.45, 1) easing                        : PASS
- no @import (standalone-renderable)                               : PASS

HTML structural sanity
- <html> opens & closes balanced (1:1)                             : PASS
- <head>/<body>/<main>/<header>/<footer>/<style> all paired        : PASS
- 4 <section data-bucket="..."> opens, 4 </section> closes         : PASS

Header auto-verified checklist (6 items per OpenSpec task 7.1)
- Console / Network / Resource errors / CSP / Performance /
  Automated checks all listed in HTML-comment example expansion    : PASS
```

## Self-Check

After writing this SUMMARY, the following claims are verified:

- `$HOME/.claude/get-shit-done/templates/visual-review.html` exists ✓
- File is 252 lines (`wc -l`) ✓
- File contains exactly 1 each of the 4 allowed bucket values (verified per-value `grep -c`) ✓
- All 10 renderer placeholders present (verified per-placeholder `grep -q`) ✓
- Forbidden-doc comment + 6 forbidden bucket values + forbidden phrasing examples present ✓
- KD-13 token compliance verified (cream + warm-dark + orange + serif + soft borders + easing all match `.planning/references/design/anthropic-claude-aesthetic-deep-dive_zh.md` SSOT) ✓
- No `@import` (template is standalone-renderable in any browser) ✓
- Mneme git tree contains zero `get-shit-done/` paths (out-of-repo artifact handled per D-UP-02) ✓

## GSD upstream new file (visual-review.html — recorded here because file is user-global)

The following is the EXACT 252-line text of `$HOME/.claude/get-shit-done/templates/visual-review.html`. Recorded for the future `gsd-build/get-shit-done` upstream PR per CONTEXT D-UP-02. This text is the SSOT for what was authored in Task 1; any future audit can reconstruct the upstream file from this section.

```html
<!--
  Visual Review HTML template — GSD upstream / dev-feedback-loop capability
  ----------------------------------------------------------------------------
  This template is the single rendering target for `gsd-sdk query verify.render-review-html`
  during `/gsd-verify-work <phase>`. All r1ckyIn projects share this template; per-project
  KD-13 overrides may be dropped at `templates/visual-review.local.html` (detected by the
  renderer; not present by default).

  ============================ FORBIDDEN BUCKETS =============================
  `<section data-bucket="...">` values are STRUCTURALLY validated by
  `gsd-sdk query verify.validate-html`. Allowed values: visual / window / motion / perf.
  Any other bucket value causes validate-html to exit non-zero with
  `{"error":"forbidden_bucket","found":[...]}` and halts the
  `package_manual_review` workflow step.

  Forbidden bucket values: hybrid, terminal, console, dom-check, log-paste, command-run.

  =========================== FORBIDDEN PHRASINGS ===========================
  Questions in <article> bodies MUST NOT ask the user to:
    - "open DevTools" / "open Console" / "open Network panel" / "open the inspector"
    - "run ps aux" / "run lsof" / "run cargo test" / "run npm run X" / "run git X"
    - "paste the log" / "paste the output" / "paste the error"
    - "check the console for errors" / "look at the console"
    - "run a command and tell me" / "tell me what you see"

  These are signals Claude obtains through `verify.scan-signals` / `verify.capture-screenshot`
  / `verify.query-dom-state` — not by routing to the human.

  =========================== EPHEMERAL CONTRACT ============================
  Rendered output lands at `.planning/handoff/<date>-phase-<N>-verify.html`.
  Each verify run produces a FRESH file. Prior runs are NEVER amended. The file
  is gitignored and may be discarded after the user replies (`verify.parse-review-response`
  extracts structured issues; the HTML itself is not part of the project record).
-->
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Visual Review — Phase {{PHASE}}</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
  :root {
    --color-orange: #d97757;
    --color-cream: #faf9f5;
    --color-cream-deep: #f3f1ea;
    --color-cream-edge: #ede9de;
    --color-warm-dark: #141413;
    --color-warm-dark-soft: #4a4843;
    --color-warm-dark-mute: #7a766d;
    --border-soft: rgba(20, 20, 19, 0.08);

    --font-serif: "Tiempos Text", "Source Serif Pro", "Source Serif 4",
                  "Charter", "Iowan Old Style", "Palatino Linotype",
                  Palatino, "Book Antiqua", Georgia, serif;
    --font-sans: -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
    --font-mono: "JetBrains Mono", "SF Mono", Menlo, Consolas, monospace;

    --space-3: 12px;
    --space-4: 16px;
    --space-6: 24px;
    --space-8: 32px;

    --radius-md: 6px;
    --radius-lg: 8px;

    --duration-base: 200ms;
    --ease-out: cubic-bezier(0.165, 0.85, 0.45, 1);

    --shadow-1:
      0 1px 1px rgba(20, 20, 19, 0.04),
      0 2px 4px rgba(20, 20, 19, 0.04);
  }

  * { box-sizing: border-box; }

  html, body {
    margin: 0;
    padding: 0;
    background: var(--color-cream);
    color: var(--color-warm-dark);
    font-family: var(--font-serif);
    font-size: 16px;
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  main {
    max-width: 880px;
    margin: var(--space-8) auto;
    padding: 0 var(--space-6);
  }

  h1, h2, h3 {
    font-family: var(--font-serif);
    font-weight: 600;
    line-height: 1.2;
    color: var(--color-warm-dark);
  }

  h1 { font-size: 28px; margin: 0 0 var(--space-4); }
  h2 { font-size: 22px; margin: var(--space-6) 0 var(--space-3); }
  h3 { font-size: 18px; margin: var(--space-4) 0 var(--space-3); }

  section[data-bucket] {
    border: 1px solid var(--border-soft);
    border-radius: var(--radius-lg);
    padding: var(--space-4) var(--space-6) var(--space-6);
    margin: var(--space-4) 0;
    background: #ffffff;
    box-shadow: var(--shadow-1);
  }

  article {
    border-top: 1px solid var(--border-soft);
    padding: var(--space-4) 0;
  }

  article:first-of-type { border-top: none; }

  img, video {
    max-width: 100%;
    height: auto;
    border-radius: var(--radius-md);
    border: 1px solid var(--border-soft);
    display: block;
    margin: var(--space-3) 0;
  }

  code, pre {
    font-family: var(--font-mono);
    font-size: 14px;
    background: var(--color-cream-deep);
    padding: 1px var(--space-3);
    border-radius: var(--radius-md);
  }

  .checklist { color: var(--color-warm-dark-soft); font-size: 14px; padding-left: 0; list-style: none; }
  .checklist li { margin: 4px 0; }
  .checklist li::before { content: "\2713  "; color: var(--color-orange); }

  .bucket-label {
    display: inline-block;
    padding: 2px var(--space-3);
    font-size: 12px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    background: var(--color-cream-edge);
    color: var(--color-warm-dark-soft);
    border-radius: var(--radius-md);
    margin-bottom: var(--space-3);
  }

  .meta {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--color-warm-dark-mute);
  }
</style>
</head>
<body>
<main>

  <header>
    <h1>Visual Review &mdash; Phase {{PHASE}}</h1>
    <p class="meta">Task: {{TASK_NAME}} &middot; Surface: {{SURFACE}} &middot; Run: {{ISO_TS}}</p>

    <h2>Claude has auto-verified</h2>
    <ul class="checklist">
      {{AUTO_VERIFIED_LIST}}
      <!--
        Renderer fills this slot with <li> entries derived from auto-verified data.
        Example expansion (6 items: console / network / resource errors / CSP / performance / automated checks):
          <li>Console: 0 errors, 2 warnings (logged, non-blocking)</li>
          <li>Network: 12 requests, all 2xx; no failed fetches</li>
          <li>Resource errors: none</li>
          <li>CSP violations: none</li>
          <li>Performance: LCP 1.8s, FCP 0.9s, CLS 0.02, longtasks 0</li>
          <li>Automated checks: tsc 0 errors, svelte-check 0 issues, vitest 67/67, cargo check pass</li>
      -->
    </ul>
  </header>

  <section data-bucket="visual">
    <span class="bucket-label">Visual fidelity</span>
    <h2>Visual</h2>
    {{BUCKET_VISUAL}}
    <!--
      Example article (kept as comment; renderer replaces {{BUCKET_VISUAL}} with concrete content):

      <article>
        <h3>1. Chat input border radius matches KD-13 token</h3>
        <img src=".dev-logs/screenshots/2026-05-12T10-15-00Z.png" alt="Tauri shell chat input region">
        <p>Locked value: <code>--radius-md = 6px</code>. DOM probe reports <code>border-radius: 6px</code>. Does the rendered corner look like 6 px &mdash; i.e., distinguishably softened but not visibly rounded?</p>
        <p class="meta">Expected response: yes / no &mdash; if no, attach one screenshot of the expected vs actual.</p>
      </article>
    -->
  </section>

  <section data-bucket="window">
    <span class="bucket-label">Window chrome</span>
    <h2>Window</h2>
    {{BUCKET_WINDOW}}
    <!--
      Example article:

      <article>
        <h3>1. Traffic light buttons sit at 12 px from window edge on macOS overlay</h3>
        <img src=".dev-logs/screenshots/2026-05-12T10-15-30Z.png" alt="Window chrome top-left corner">
        <p>Locked value: <code>--titlebar-inset = 12px</code> per KD-13. Screenshot was captured via <code>screencapture -R</code> over the window-chrome rect. Does the traffic-light cluster's leftmost button visually sit at ~12 px from the left edge?</p>
      </article>
    -->
  </section>

  <section data-bucket="motion">
    <span class="bucket-label">Animation feel</span>
    <h2>Motion</h2>
    {{BUCKET_MOTION}}
    <!--
      Example article with looped recording anchor:

      <article>
        <h3>1. Send-stop button rotation between Send &rarr; Stop states</h3>
        <video src=".dev-logs/screenshots/send-stop-2026-05-12.webm" autoplay loop muted></video>
        <p>Locked value: <code>--duration-base = 200ms</code>, easing <code>cubic-bezier(0.165, 0.85, 0.45, 1)</code>. Recorded loop plays at 1&times; speed. Does the rotation feel snappy without overshoot, and the icon swap land cleanly at the midpoint?</p>
      </article>
    -->
  </section>

  <section data-bucket="perf">
    <span class="bucket-label">Perceived performance</span>
    <h2>Perf</h2>
    {{BUCKET_PERF}}
    <!--
      Example article:

      <article>
        <h3>1. First-paint of chat panel feels under 1.5 s</h3>
        <p>Auto-measured: <code>FCP = 0.9s</code>, <code>LCP = 1.8s</code>. Subjective: does the chat panel reach interactive faster than your "feels slow" threshold?</p>
        <p class="meta">If "feels slow", note approximate where in the page-load cycle the lag is perceived.</p>
      </article>
    -->
  </section>

  <footer style="margin-top: var(--space-8); border-top: 1px solid var(--border-soft); padding-top: var(--space-4); color: var(--color-warm-dark-mute); font-size: 13px;">
    <p>This file is ephemeral. After you reply (free-form text is fine; structured JSON is better), it may be discarded. Prior verify cycles do not amend this file &mdash; the next <code>/gsd-verify-work</code> run produces a fresh HTML.</p>
    <p>Rendered at <code>{{HTML_PATH}}</code></p>
  </footer>

</main>
</body>
</html>
```

## Threat Flags

No new threat surface introduced. The two threats in PLAN `<threat_model>` apply to downstream consumers (renderer in 01.1-04), not to the static template itself:

- T-01.1-03-01 (Tampering — example comments containing forbidden-phrasing keywords): **mitigated by design**. Examples live inside HTML comments; `verify.validate-html` parses the rendered DOM not the source HTML comments, so forbidden-phrasing detection (whenever future audit gets added) is unaffected. The example block intentionally shows the canonical anti-pattern boundary (screenshot + locked-value comparison shape) so authors internalize what a well-formed question looks like.
- T-01.1-03-02 (Information disclosure — XSS via bucket YAML injection): **deferred to 01.1-04 renderer**. The template itself is static + LLM-author-controlled; the runtime injection point is `cmdVerifyRenderReviewHtml` which MUST HTML-escape `{{BUCKET_*}}` content (documented in PLAN frontmatter `key_links.pattern` row and 01.1-04 will own the escape contract).

## Next Steps

- Plan 01.1-04 (wave 3): SDK handler `cmdVerifyRenderReviewHtml` reads this template via `fs.readFileSync`, runs 10 `String.prototype.replaceAll` passes against the documented `{{NAME}}` placeholders, writes rendered output to `.planning/handoff/<date>-phase-<N>-verify.html`
- Plan 01.1-04 (wave 3): SDK handler `cmdVerifyValidateHtml` parses the rendered DOM (NOT the source comments), confirms every `<section>` carries `data-bucket` in `{visual, window, motion, perf}`, rejects any `hybrid|terminal|console|dom-check|log-paste|command-run` value
- Plan 01.1-09 (wave 6 dogfood): playwright canonical visual-SSOT check — render a sample instance of this template (filling all 10 placeholders with realistic content), screenshot, compare cream-background + serif-font + 4-bucket layout against the prototype `.planning/handoff/2026-05-12-change-review-automate-dev-feedback-loop-v3.html` aesthetic baseline
