---
phase: 01.1-dev-feedback-loop-infrastructure
plan: 02
subsystem: gsd-upstream-workflow
tags: [verify-work, critical_rules, package_manual_review, tauri-branch, dev-feedback-loop, gsd-upstream]

# Dependency graph
requires:
  - phase: 01.1
    plan: 01
    provides: "Wave 0 — OpenSpec change `automate-dev-feedback-loop` validated and shipped (4 artifacts: proposal / design / tasks / spec)"
provides:
  - "verify-work.md `<critical_rules>` block at workflow top (5 rules: no-DevTools-asking / bucket whitelist / anchored questions / auto-verified header / fresh HTML each run)"
  - "verify-work.md Tauri-shell branch inside `<step name=automated_ui_verification>` (probes `npm run gsd-dev-screenshot --dry-run`; on hit calls verify.start-dev-loop + scan-signals + capture-screenshot --surface tauri + query-dom-state --surface tauri)"
  - "verify-work.md new `<step name=package_manual_review>` between automated_ui_verification and find_summaries (groups queued items by 4-bucket → render-review-html → validate-html → present path → parse-review-response)"
  - "`$HOME/.claude/get-shit-done/workflows/verify-work.md.bak` rollback snapshot (746 lines, pristine pre-patch)"
affects: [01.1-03, 01.1-04, 01.1-05, 01.1-06, 01.1-07, 01.1-08, 01.1-09, 01.1-10, all future GSD workflow patches]

# Tech tracking
tech-stack:
  added: []   # no new code; markdown prose patches only
  patterns:
    - "GSD upstream user-global file patching protocol: cp <file>{,.bak} → Edit → diff -u <file>{,.bak} verification → embed diff in mneme SUMMARY.md (since upstream files are NOT in mneme git history)"
    - "<critical_rules> block convention (mirrors spec-phase.md L242-251) for verify-work — first XML block of that name in this workflow file"
    - "Step ordering convention: package_manual_review = additive batched path between automated_ui_verification and find_summaries; never removes present_test fallback"
    - "Prose references to critical rules use `critical rule (X)` plain text, NOT `<critical_rules>` tag-shaped citations — preserves single-instance count of the opening tag for structural grep checks"

key-files:
  created:
    - "$HOME/.claude/get-shit-done/workflows/verify-work.md.bak (user-global; not in mneme git)"
    - ".planning/phases/01.1-dev-feedback-loop-infrastructure/01.1-02-SUMMARY.md (this file)"
  modified:
    - "$HOME/.claude/get-shit-done/workflows/verify-work.md (user-global; 3 patches, 746 → 830 lines)"

key-decisions:
  - "D-PR-01: prose citations of critical rules use `critical rule (X)` plain text, not `<critical_rules>` tag-shaped strings — required to keep grep -c '<critical_rules>' = 1 while still cross-referencing rule letters by name. Plan task 4 originally wrote `<critical_rules> (b)` style; I substituted `critical rule (b)` style to satisfy the plan's own structural check at task 5."
  - "D-PR-02: tauri-shell branch ordering — branch lives BEFORE the `If automated verification is not available` fallback line, as a peer IF condition to the Playwright-MCP branch. This preserves the existing fallback semantics while making the Tauri path runnable in parallel with Chromium (per plan task 3 'peer IF condition' requirement)."
  - "D-PR-03: mneme-side commit captures only the SUMMARY (per plan task 6 explicit design). The 3 upstream patches live at user-global path `$HOME/.claude/get-shit-done/workflows/` and are NOT tracked in mneme git. The full diff is embedded in this SUMMARY for the future `gsd-build/get-shit-done` upstream PR (deferred per CONTEXT D-DF)."

patterns-established:
  - "GSD upstream backup-before-patch (cp <file>{,.bak}): existence-check first (don't overwrite a known-good .bak from a prior run); fall through to diff -q if .bak already exists; STOP if mismatch."
  - "Diff containment audit: after patching, capture `diff -u <file>{,.bak} > /tmp/...diff`; count hunks via `grep -c '^@@'` (expected count = number of intended patches); reject if extra hunks appear."
  - "Out-of-repo artifact documentation: when patching files outside the mneme git tree, the SUMMARY embeds the full unified diff verbatim so the change is auditable from mneme git history alone (no need to read the user-global file to know what changed)."

requirements-completed: []   # Phase 01.1 has no v1+v1.x REQ-NN; OpenSpec change `automate-dev-feedback-loop` tracks fulfilled tasks (5.0-5.4) and requirements (R5) instead.

# OpenSpec traceability (this plan satisfies)
openspec_change: automate-dev-feedback-loop
openspec_tasks_satisfied: [5.0, 5.1, 5.2, 5.3, 5.4]
openspec_requirements_satisfied: [R5]

# Metrics
duration: 4min
completed: 2026-05-12
---

# Phase 01.1 Plan 02: verify-work.md three-patch landing Summary

**Three discrete patches applied to `$HOME/.claude/get-shit-done/workflows/verify-work.md` per OpenSpec group 5 + design D-UP-01: top-of-file `<critical_rules>` block (5 rules), Tauri-shell branch inside `<step name=automated_ui_verification>`, and new `<step name=package_manual_review>` between automated_ui_verification and find_summaries. Backup `.bak` snapshot retained for rollback. File grew 746 → 830 lines with exactly 3 diff hunks.**

## Performance

- **Duration:** 4 min (274 s)
- **Started:** 2026-05-12T11:08:58Z
- **Completed:** 2026-05-12T11:13:32Z
- **Tasks:** 6 (5 file-system patches + 1 mneme-side commit)
- **Files modified:** 2 (1 user-global patched, 1 user-global backed-up)
- **Files created in mneme git:** 1 (this SUMMARY)

## Accomplishments

- **Patch 3 (`<critical_rules>` block, applied first)** — 5 anti-asking rules placed between `</template>` (line 25) and `<process>` (line 36 post-patch). Rules cover: (a) no DevTools/console/terminal/log-paste/DOM-inspection asking, (b) data-bucket whitelist `visual/window/motion/perf` with forbidden values enumerated, (c) every HTML question requires concrete anchor (screenshot/GIF/selector/locked-token), (d) HTML header requires "Claude has auto-verified" reassurance checklist, (e) fresh HTML each run (never amend prior file).
- **Patch 1 (Tauri-shell branch in `<step name=automated_ui_verification>`)** — new IF condition added as peer to the existing Playwright-MCP branch, with `npm run gsd-dev-screenshot --silent --dry-run` exit-0 probe, `TAURI_CAPABLE` flag, and 5-step SDK call sequence (verify.start-dev-loop → scan-signals → capture-screenshot --surface tauri → query-dom-state --surface tauri → queue-for-manual). Failures route to `data-bucket="window"` and continue (do-not-abort semantics).
- **Patch 2 (new `<step name=package_manual_review>`)** — 50-line step inserted between automated_ui_verification (closing at line 155) and find_summaries (now at line 209). 7-step procedure: group-by-4-bucket → build YAML inputs → render-review-html → validate-html (HALT on forbidden_bucket/forbidden_tag) → present path to user → wait → parse-review-response → never re-render. Original `present_test` step explicitly preserved as fallback for 0-queued-items / no-UI-surface / MCP-unavailable / single-test edge cases.
- **Backup discipline** — `.bak` snapshot created BEFORE first edit per D-UP-02 contract. Rollback path: `cp ~/.claude/get-shit-done/workflows/verify-work.md{.bak,}` restores 746-line pristine state.

## Task Commits

Per plan task 6 explicit design, **only ONE mneme-side commit** is made (the SUMMARY).  Tasks 1-5 are file-system operations on user-global paths outside the mneme git tree; they have no atomic-commit boundary in mneme git history. The plan acknowledged this caveat upfront ("the per-task git commits will mostly contain SUMMARY.md and minimal in-repo state").

1. **Task 1: Backup verify-work.md (OpenSpec 5.0)** — `$HOME/.claude/get-shit-done/workflows/verify-work.md.bak` created via `cp`, 746 lines preserved. Existence-check guard in place (skips overwrite if a known-good .bak already exists from prior run; would STOP if mismatch detected).
2. **Task 2: Patch 3 (`<critical_rules>` block)** — single Edit; 8-line addition (5 rules + open + close + trailing blank). Diff confirmed 1 hunk, +8/-0.
3. **Task 3: Patch 1 (Tauri-shell branch)** — single Edit; 24-line addition inside automated_ui_verification step body. Diff confirmed 2 hunks total at this point (both intentional).
4. **Task 4: Patch 2 (`<step name=package_manual_review>`)** — single Edit; 52-line new step block. Initial run had `grep -c '<step name=' = 18` due to one prose tag-shaped reference; followup 4-line Edit replaced 4 `<critical_rules>`-shaped prose citations and 1 `<step name=present_test>`-shaped prose citation with plain-text equivalents to keep structural greps clean.
5. **Task 5: Final structural check + diff capture** — all 4 plan-stated checks PASS (line count 830 > 800 ✓, step count 17 ✓, critical_rules open/close = 1/1 ✓, mneme git tree clean ✓). Diff containment verified at exactly 3 hunks via `grep -c '^@@' /tmp/verify-work-patches.diff = 3`.
6. **Task 6: mneme-side atomic commit** — single `docs(01.1-02): ...` commit landing this SUMMARY only. See `## Plan metadata` below for hash.

**Plan metadata commit:** `b5bde92` (docs(01.1-02): record verify-work.md upstream patches - 3 patches landed)

## Files Created/Modified

### User-global (NOT in mneme git tree)

- `$HOME/.claude/get-shit-done/workflows/verify-work.md` — patched in place; 746 → 830 lines; 3 discrete patches per D-UP-01.
- `$HOME/.claude/get-shit-done/workflows/verify-work.md.bak` — pristine pre-patch snapshot; 746 lines.

### In mneme git tree (this commit)

- `.planning/phases/01.1-dev-feedback-loop-infrastructure/01.1-02-SUMMARY.md` — this file. Embeds the full upstream diff for future `gsd-build/get-shit-done` upstream PR.

## Decisions Made

- **D-PR-01: Prose citations of rules use plain text.** Task 4's inserted step body cites `<critical_rules>` (b) / (c) / (e) and `<step name=present_test>` in prose. These bracket-tag-shaped strings break the plan's own structural greps (`grep -c '<critical_rules>' = 1`, `grep -c '<step name=' = 17`). I rewrote the prose citations as `critical rule (X)` and `present_test step` so the structural check is unambiguous. The semantic cross-reference is preserved.
- **D-PR-02: Tauri-shell branch is a peer IF, not nested.** Branch lives between the Playwright list end (line 120) and the fallback line ("If automated verification is not available…"). It runs before the fallback so Tauri-only phases (no Playwright-MCP) still hit the auto-verification path. Plan task 3 specified "peer IF condition" and "After Tauri-branch completion (regardless of TAURI_CAPABLE), if Playwright-MCP is also available, run the existing Chromium branch as a peer" — both honored.
- **D-PR-03: Single mneme-side commit by plan design.** Plan task 6 explicitly designs a single commit (SUMMARY only). Mneme git history does NOT track user-global workflow patches — that's the D-UP-02 contract. The SUMMARY embeds the full diff so the future `gsd-build/get-shit-done` upstream PR can be drafted from mneme git history alone.

## Deviations from Plan

**Total deviations: 1 — Rule 3 (blocking) auto-fix during Task 4.**

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Prose tag-shaped references inflated structural greps**
- **Found during:** Task 4 verification (post-Edit) — `grep -c '<step name=' = 18` (expected 17) and `grep -c '<critical_rules>' = 5` (expected 1)
- **Issue:** The plan's task 4 action block specifies new-step body content that includes 4 prose citations of `<critical_rules>` (a/b/c/e) and 1 prose citation of `<step name="present_test">`. Those bracket-tag-shaped strings get matched by the plan's own structural greps at task 5, causing them to fail. The plan also acknowledges (task 4 body, end) that the new step body "references the `<critical_rules>` constraints by letter" — i.e., the planner accepted this trade-off as natural; the failure to match the structural check at task 5 is an internal inconsistency in the plan.
- **Fix:** 5 targeted Edits replaced bracket-tag-shaped citations with plain-text equivalents — `<critical_rules>` (X) → `critical rule (X)` (4 occurrences in package_manual_review body) and `<step name="present_test">` → `present_test step` (1 occurrence). Semantic cross-references preserved.
- **Files modified:** `$HOME/.claude/get-shit-done/workflows/verify-work.md` (user-global)
- **Verification:** Post-fix structural greps: `<step name=` = 17 PASS, `<critical_rules>` open = 1 close = 1 PASS, diff hunk count = 3 PASS
- **Committed in:** (no commit; user-global file change, captured in SUMMARY diff embedment)

---

**Impact on plan:** Single deviation is mechanical (clean greppability) and content-preserving. Future upstream PR to `gsd-build/get-shit-done` will use the plain-text citation form, which is also slightly more readable than bracket-tagged citations.

## Issues Encountered

None. All structural checks PASS on first or second iteration. The 1 deviation above was caught + fixed via the plan's own task-5 structural check before mneme-side commit.

## User Setup Required

None. This plan modifies user-global GSD workflow files at `$HOME/.claude/get-shit-done/workflows/`. No external services, no environment variables, no dashboard config.

## Verification Outcomes

```bash
$ wc -l ~/.claude/get-shit-done/workflows/verify-work.md
830 /Users/qinyuan/.claude/get-shit-done/workflows/verify-work.md

$ grep -c '<step name=' ~/.claude/get-shit-done/workflows/verify-work.md
17

$ grep -c '<critical_rules>' ~/.claude/get-shit-done/workflows/verify-work.md
1
$ grep -c '</critical_rules>' ~/.claude/get-shit-done/workflows/verify-work.md
1

$ grep -c '<step name="package_manual_review">' ~/.claude/get-shit-done/workflows/verify-work.md
1

$ test -f ~/.claude/get-shit-done/workflows/verify-work.md.bak && echo "backup present"
backup present

$ git status --porcelain | grep "get-shit-done/" | wc -l
0
(no upstream files leaked into mneme git tree)

$ grep -c '^@@' /tmp/verify-work-patches.diff
3
(exactly 3 hunks = exactly 3 intentional patches)
```

## Next Phase Readiness

- **Plan 01.1-03 (HTML template, wave 2 sibling)** can land in parallel with this plan — no shared file conflicts.
- **Plan 01.1-04 (SDK 8 handlers, wave 3 dependency)** can now build the handlers referenced by these patches:
  - `verify.start-dev-loop` (called in Tauri branch step 1)
  - `verify.scan-signals` (Tauri branch step 2)
  - `verify.capture-screenshot` (Tauri branch step 3 + package_manual_review step 2 input)
  - `verify.query-dom-state` (Tauri branch step 4 + package_manual_review step 2 input)
  - `verify.render-review-html` (package_manual_review step 3)
  - `verify.validate-html` (package_manual_review step 4)
  - `verify.parse-review-response` (package_manual_review step 6)
  - `verify.stop-dev-loop` (not yet referenced by patches, but per design D-UP-03 forms the matched pair to start-dev-loop)
- **All future plans in waves 4-7** (mneme-side Tier D + E) can now self-validate against the patched workflow when `/gsd-verify-work 01.1` runs.
- **Backup retention:** `.bak` stays on disk through phase ship. Rollback path is `cp ~/.claude/get-shit-done/workflows/verify-work.md{.bak,}`. Don't delete until Plan 01.1-09 (dogfood) confirms the patched workflow works end-to-end against the very phase that patched it.
- **Future upstream PR:** the diff section below is the SSOT for the eventual `gsd-build/get-shit-done` upstream PR (deferred per CONTEXT D-DF; not Phase 01.1 completion criterion).

## GSD upstream diff (verify-work.md — recorded here because file is user-global)

This is the verbatim `diff -u .bak file` output captured at task 5. It is the single source of truth for the upstream PR. Mneme git history alone is sufficient to reconstruct the patches; no need to read the user-global file directly.

```diff
--- /Users/qinyuan/.claude/get-shit-done/workflows/verify-work.md.bak	2026-05-12 21:09:39
+++ /Users/qinyuan/.claude/get-shit-done/workflows/verify-work.md	2026-05-12 21:12:50
@@ -24,6 +24,14 @@
 @$HOME/.claude/get-shit-done/templates/UAT.md
 </template>
 
+<critical_rules>
+- (a) Claude MUST NOT ask the user to open DevTools, read the console, run a terminal command, paste a log file, run `ps aux` / `lsof` / `cargo` / `npm` / `git` / etc., or perform any DOM inspection during verify-work. Signals MUST be obtained via the SDK `verify.*` handlers (see step `automated_ui_verification` for the canonical entry points).
+- (b) Visual Review HTML `<section data-bucket="…">` values MUST be in the whitelist `visual / window / motion / perf`. Forbidden bucket values: `hybrid`, `terminal`, `console`, `dom-check`, `log-paste`, `command-run`. `verify.validate-html` enforces this structurally; do not bypass.
+- (c) Each question in the rendered HTML MUST have a concrete anchor (screenshot path, GIF path, CSS selector, locked-token-value comparison). Questions of the form "does this look right?" without an anchor are forbidden.
+- (d) The HTML header MUST include a "Claude has auto-verified" reassurance checklist so the user sees what was already validated before any manual judgment is requested.
+- (e) Each `/gsd-verify-work <N>` run produces a fresh HTML at `.planning/handoff/<date>-phase-<N>-verify.html`. NEVER amend a prior file.
+</critical_rules>
+
 <process>
 
 <step name="initialize" priority="first">
@@ -111,6 +119,30 @@
 5. Flag items that require human judgment (subjective aesthetics, content accuracy)
    and present only those as manual UAT questions.
 
+**Tauri-shell branch — applies when the active phase touched window chrome / IPC / `claude` subprocess / Echo360 webview surfaces (detected via SUMMARY.md path patterns) AND the project exposes the dev-feedback-loop npm-script bridge:**
+
+```bash
+# Probe for mneme-style npm-script bridge (R1 of the dev-feedback-loop convention).
+# Exit 0 → bridge present; non-zero → unavailable (skip the branch).
+if npm run gsd-dev-screenshot --silent --dry-run >/dev/null 2>&1; then
+  TAURI_CAPABLE=1
+else
+  TAURI_CAPABLE=0
+fi
+```
+
+If `TAURI_CAPABLE=1`, call the SDK in order:
+
+1. `gsd-sdk query verify.start-dev-loop "${PHASE}" --surface tauri` — start the Tauri dev shell, write PID/port to `.dev-logs/`.
+2. `gsd-sdk query verify.scan-signals --since "${VERIFY_RUN_START_TS}"` — pull structured issues from `.dev-logs/{console,network,perf}.log`.
+3. `gsd-sdk query verify.capture-screenshot --surface tauri` — record a window capture at `.dev-logs/screenshots/<iso-ts>.png`.
+4. `gsd-sdk query verify.query-dom-state --surface tauri` — pull the `__mnemeDevSnapshot__` JSON for KD-13 token verification.
+5. Each call's output gets appended to the running auto-verified report; items requiring human judgment are queued for `package_manual_review`.
+
+On any step's failure, append the structured error to the queued-for-manual list with `data-bucket="window"` and continue — do NOT abort the entire verification, since other branches may still produce useful signal.
+
+After Tauri-branch completion (regardless of `TAURI_CAPABLE`), if Playwright-MCP is also available, run the Chromium branch above as a peer. Both branches MAY contribute auto-verified rows + queued-for-manual items.
+
 If automated verification is not available, fall back to the standard manual
 checkpoint questions defined in this workflow unchanged. This step is entirely
 conditional: if Playwright-MCP is not configured, behavior is unchanged from today.
@@ -119,7 +151,59 @@
 ```
 UI checkpoints: {N} auto-verified, {M} queued for manual review
 ```
+
+</step>
+
+<step name="package_manual_review">
+**Package queued-for-manual items into a single Visual Review HTML for batched human judgment.**
+
+This step is the canonical replacement for the conversational one-at-a-time question loop. Triggered when `automated_ui_verification` (above) produced one or more "queued for manual review" items.
+
+**Skip-condition:** if total queued count is 0, OR no UI surface was touched in this phase, OR `verify.render-review-html` SDK handler is not registered (legacy SDK pre-Phase-01.1) — fall through to the existing `present_test` step (below) which handles the single-test-at-a-time path.
+
+Otherwise, execute in order:
 
+1. **Group queued items by 4-bucket** — every item carries a tentative `data-bucket` attribute from the upstream branch. Buckets MUST be in `visual / window / motion / perf` (per critical rule (b)). Items with any other bucket value are routing errors — return them to `automated_ui_verification` for re-classification before proceeding.
+
+2. **Build inputs for the HTML renderer:**
+   ```bash
+   AUTO_VERIFIED_YAML=$(...)   # serialized "Claude has auto-verified" checklist
+   BUCKETS_YAML=$(...)          # serialized 4-bucket question array
+   ```
+   Source: combine outputs of `verify.scan-signals` + `verify.capture-screenshot` + `verify.query-dom-state` + Playwright auto-checks.
+
+3. **Render the HTML:**
+   ```bash
+   HTML_PATH=$(gsd-sdk query verify.render-review-html \
+     --phase "${PHASE}" \
+     --auto-verified "${AUTO_VERIFIED_YAML}" \
+     --buckets "${BUCKETS_YAML}")
+   ```
+   Output target: `.planning/handoff/<date>-phase-<PHASE>-verify.html`.
+
+4. **Structurally validate the HTML:**
+   ```bash
+   gsd-sdk query verify.validate-html --path "${HTML_PATH}"
+   ```
+   If validate returns non-zero with a `forbidden_bucket` / `forbidden_tag` error, HALT this step and require Claude to rewrite the offending sections (per critical rule (b)). Validation enforces the 4-bucket whitelist + forbidden-tag blacklist.
+
+5. **Present the path to the user, one short message:**
+   ```
+   Visual review packaged at <HTML_PATH>.
+   Reply when done. Free-form text is fine; structured JSON is better.
+   The file is ephemeral — discarded after your response.
+   ```
+   Do NOT ask the user to read the console, run a terminal command, paste a log, or inspect the DOM (per critical rule (a)). The HTML carries every anchor (screenshot / GIF / selector / locked-value comparison) that the user needs to answer (per critical rule (c)).
+
+6. **Wait for response. On receipt:**
+   ```bash
+   STRUCTURED_RESPONSE=$(gsd-sdk query verify.parse-review-response --path "${HTML_PATH}")
+   ```
+   Route the parsed issues into the UAT.md `## Gaps` section using the same structure used by `present_test`.
+
+7. **Do NOT re-render or amend the HTML.** Per critical rule (e), if a second cycle is needed (e.g., user requested fixes), the next `/gsd-verify-work` run produces a fresh HTML — never edit the prior file.
+
+The original `present_test` step (below) remains the canonical fallback for: phases with 0 queued-for-manual items, phases with no UI surface, MCP-unavailable degradation, and single-test edge cases. `package_manual_review` is the additive batched path; it does NOT remove `present_test`.
 </step>
 
 <step name="find_summaries">
```

## Self-Check

After writing this SUMMARY, run the verification suite from `<self_check>` in execute-plan.md.

- [x] `~/.claude/get-shit-done/workflows/verify-work.md` exists at 830 lines
- [x] `~/.claude/get-shit-done/workflows/verify-work.md.bak` exists at 746 lines
- [x] `grep -c '<critical_rules>' verify-work.md` = 1
- [x] `grep -c '</critical_rules>' verify-work.md` = 1
- [x] `grep -c '<step name=' verify-work.md` = 17 (was 16 pristine)
- [x] `grep -c '^</step>$' verify-work.md` = 17
- [x] `grep -c '<step name="package_manual_review">' verify-work.md` = 1
- [x] `grep -c '^@@' /tmp/verify-work-patches.diff` = 3 (3 hunks = 3 intentional patches)
- [x] `git status --porcelain | grep 'get-shit-done/' | wc -l` = 0 (no leakage)
- [x] All structural checks PASS

## Self-Check: PASSED

---
*Phase: 01.1-dev-feedback-loop-infrastructure*
*Plan: 02 (verify-work.md three-patch landing)*
*Wave: 2*
*Completed: 2026-05-12*
*Closes OpenSpec tasks 5.0, 5.1, 5.2, 5.3, 5.4 (group 5 — GSD upstream verify-work patches).*
