---
phase: 01.1-dev-feedback-loop-infrastructure
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/dependencies.md
  - CLAUDE.md
autonomous: true
requirements: []
openspec_change: automate-dev-feedback-loop
openspec_tasks: [8.1, 8.2]
openspec_requirements: []
errata_honored: []

must_haves:
  truths:
    - ".planning/dependencies.md gains a new group 'External dev tools (MCP servers + macOS CLIs)' listing chrome-devtools-mcp / playwright MCP / screencapture / lsof / tee (minimum macOS Ventura 13.4) and cross-references the openspec change name automate-dev-feedback-loop."
    - "mneme CLAUDE.md gets ONE row pointer entry under ## Patterns (NOT ## Skills per D8 OpenSpec deferral — no .claude/skills/dev-feedback-loop/ skill is created in this phase). The entry references GSD upstream workflow + mneme-side files + openspec change."
  artifacts:
    - path: ".planning/dependencies.md"
      provides: "External dev tools group registered + cross-reference to openspec change"
      contains: "dev-feedback-loop"
    - path: "CLAUDE.md"
      provides: "one-row pointer entry referencing GSD upstream + mneme-side capability + openspec change"
      contains: "dev-feedback-loop"
  key_links: []
---

<objective>
Register the new external dev-tool dependencies introduced by Phase 01.1 (chrome-devtools-mcp / playwright MCP / macOS CLIs `screencapture`, `lsof`, `tee`) in `.planning/dependencies.md` per D-DEP-01, and add a one-row pointer entry to mneme `CLAUDE.md` under `## Patterns` per D-DEP-02 + D8 deferral (NOT a new project skill — the rule layer lives in GSD upstream).

Wave 1 — fully independent of all other plans (pure documentation update). Parallel-safe with plan 01.1-01. Lowest TDD value of the 10 plans; no `type: tdd` tasks.

Purpose: discoverability — future-self looking at CLAUDE.md should see "dev-feedback-loop" and know where the rule layer + capability live without reverse-engineering.
Output: 2 file modifications in 1 atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-dev-feedback-loop-infrastructure/01.1-CONTEXT.md

# Current dependencies registry — find the right section to extend
@.planning/dependencies.md

# Current CLAUDE.md — find ## Patterns section (or equivalent) — NOT ## Skills
@CLAUDE.md

<interfaces>
<!-- Per CONTEXT D-DEP-01 the new group is named: -->
//   "External dev tools (MCP servers + macOS CLIs)"
//
//   listing: chrome-devtools-mcp, playwright MCP, screencapture, lsof, tee
//   minimum platform: macOS Ventura 13.4 Intel

<!-- Per CONTEXT D-DEP-02 the CLAUDE.md entry: -->
//   ONE row under ## Patterns (NOT ## Skills — D8 OpenSpec deferral)
//   References: GSD upstream workflow + mneme-side files + openspec change
//
//   Wording (one of):
//   "dev-feedback-loop — verify-work-anchored UI dev loop. Rule layer in GSD
//    upstream (~/.claude/get-shit-done/workflows/verify-work.md); mneme-side
//    capability via src/lib/dev/console-forwarder.ts + src-tauri/src/dev.rs +
//    gsd-dev-* npm scripts. See openspec/changes/automate-dev-feedback-loop/
//    for full spec."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append dev-feedback-loop group to .planning/dependencies.md (OpenSpec 8.1)</name>
  <files>.planning/dependencies.md</files>
  <action>
First read `.planning/dependencies.md` to find:
- The existing group structure (likely a markdown table or section-per-group)
- A natural insertion point (end of file or a section labeled like "Other tools" / "Development tooling")
- Whether the file uses YAML frontmatter

Mirror the prevailing style. Append a NEW group at the end (or in the appropriate position by category) titled exactly per D-DEP-01:

```markdown
## External dev tools (MCP servers + macOS CLIs)

Introduced by Phase 01.1 (`automate-dev-feedback-loop` OpenSpec change). These
are environment-provided tools the verify-work workflow + SDK handlers depend on
during `/gsd-verify-work`. None are runtime dependencies of the mneme binary
itself; all are dev-only.

| Tool | Source | Required version | Purpose |
|------|--------|------------------|---------|
| `chrome-devtools-mcp` | MCP server (environment-provided) | latest | Chromium-surface signals: `list_console_messages`, `list_network_requests`, `take_screenshot`. Used by `verify.scan-signals` / `verify.capture-screenshot --surface chromium` |
| `playwright` MCP | MCP server (environment-provided) | latest | Accessibility-tree snapshots + before/after visual diff when the phase warrants. Used by `automated_ui_verification` Playwright branch |
| `screencapture` | macOS system tool (`/usr/sbin/screencapture`) | macOS 10.2+ (Ventura 13.4 target) | Tauri-shell window capture via `screencapture -l <CGWindowID>` or `-R x,y,w,h`. Primary path per design.md v3.1 E1 errata (no `WebviewWindow::capture()` in Tauri 2). Used by `dev_capture_screenshot` and `gsd-dev-screenshot` bridge |
| `lsof` | macOS system tool | Ventura 13.4 builtin | Port-collision probe in `verify.start-dev-loop` (`lsof -i :5173` per E4 errata) |
| `tee` | POSIX standard | Ventura 13.4 builtin | `npm run dev 2>&1 \| tee .dev-logs/tauri.log` pattern for surfacing Tauri runtime output to the log scanner |

**Minimum platform:** macOS Ventura 13.4 Intel (mneme target per PROJECT.md).
Apple Silicon supported by all 5 tools but not the validated baseline.

**Cross-reference:** see `openspec/changes/automate-dev-feedback-loop/` for the change spec, design decisions (D-DEP-01 / D-DEP-02), and v3.1 errata (E1 / E4) governing tool selection.
```

If the existing file uses a different visual style (e.g. bullet lists instead of tables), adapt to match.
  </action>
  <verify>
    <automated>grep -q 'External dev tools' .planning/dependencies.md &amp;&amp; grep -q 'chrome-devtools-mcp' .planning/dependencies.md &amp;&amp; grep -q 'screencapture' .planning/dependencies.md &amp;&amp; grep -q 'automate-dev-feedback-loop' .planning/dependencies.md</automated>
  </verify>
  <done>New group appended to dependencies.md listing 5 tools (chrome-devtools-mcp / playwright / screencapture / lsof / tee) with required versions + purposes. Cross-references the openspec change name. Mirrors the prevailing file style.</done>
</task>

<task type="auto">
  <name>Task 2: Add one-row pointer to CLAUDE.md ## Patterns (OpenSpec 8.2 + D8 deferral)</name>
  <files>CLAUDE.md</files>
  <action>
First read `CLAUDE.md` to locate the `## Patterns` section (or `## Conventions`, `## Architecture`, or whichever section already serves as the lookup table for cross-cutting patterns). Per CONTEXT D-DEP-02 + D8 deferral the entry must:

1. Live in `## Patterns` (or equivalent), NOT `## Skills` (D8 OpenSpec deferral — no `.claude/skills/dev-feedback-loop/` is created).
2. Be ONE row only (not a full section).
3. Reference the three locations the future-self will need: GSD upstream workflow + mneme-side files + openspec change.

Append a row at the bottom of the `## Patterns` table (or section). Exact wording:

```markdown
- **dev-feedback-loop** — verify-work-anchored UI dev loop (no terminal-asking, no console-pasting, no DOM-inspecting during execute or verify). Rule layer in GSD upstream (`~/.claude/get-shit-done/workflows/verify-work.md` + 8 `verify.*` SDK handlers + `templates/visual-review.html`). Mneme-side capability via `src/lib/dev/console-forwarder.ts` + `src-tauri/src/dev.rs` + 3 `gsd-dev-*` npm scripts in `package.json`. See `openspec/changes/automate-dev-feedback-loop/` for the full spec (4 artifacts), `01.1-CONTEXT.md` for D-* decisions, `01.1-RESEARCH.md` for R1-R10 + v3.1 errata.
```

If the file uses a table format instead of a bullet list, format accordingly:

```markdown
| Pattern | Where it lives | When to use |
|---------|----------------|-------------|
| dev-feedback-loop | GSD upstream + `src/lib/dev/` + `src-tauri/src/dev.rs` + `scripts/gsd-dev-*.mjs` + `openspec/changes/automate-dev-feedback-loop/` | All UI-touching phases — replaces "open DevTools and tell me what you see" pattern |
```

Per D8 deferral DO NOT create `.claude/skills/dev-feedback-loop/SKILL.md`. The skill-system slot is intentionally empty so the discipline stays in GSD upstream (shared across all r1ckyIn projects) instead of mneme-local where it would drift per-project.

Verify the entry only goes in ONE place (search for "dev-feedback-loop" — should appear exactly once after edit):
```bash
grep -c 'dev-feedback-loop' CLAUDE.md   # should be 1
```
  </action>
  <verify>
    <automated>grep -q 'dev-feedback-loop' CLAUDE.md &amp;&amp; grep -c 'dev-feedback-loop' CLAUDE.md | grep -qE '^[12]$' &amp;&amp; ! test -d .claude/skills/dev-feedback-loop &amp;&amp; grep -q 'openspec/changes/automate-dev-feedback-loop' CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md gains one (or at most two if a table heading row counts) reference to `dev-feedback-loop` under the patterns section. No `.claude/skills/dev-feedback-loop/` directory was created (D8 deferral respected). Reference points to GSD upstream + mneme-side files + openspec change.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit</name>
  <files>(commit only)</files>
  <action>
Single commit per atomic-boundary rule (the 2 doc updates form one logical unit):

```bash
git add .planning/dependencies.md CLAUDE.md
git commit \
  -m "docs(01.1-08): register dev-feedback-loop deps + CLAUDE.md pointer (OpenSpec group 8)" \
  -m "Adds 'External dev tools (MCP servers + macOS CLIs)' group to .planning/dependencies.md per D-DEP-01 (chrome-devtools-mcp / playwright / screencapture / lsof / tee on macOS Ventura 13.4). Adds one-row pointer entry to CLAUDE.md under ## Patterns per D-DEP-02 referencing GSD upstream + mneme-side files + openspec change. No .claude/skills/dev-feedback-loop/ created — D8 OpenSpec deferral keeps rule layer in GSD upstream." \
  -m "Closes OpenSpec tasks 8.1 + 8.2."
```

Per saved memory — multiple `-m` flags only.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | grep -q "docs(01.1-08): register dev-feedback-loop deps" &amp;&amp; git diff HEAD~1 HEAD --stat | grep -q 'dependencies.md' &amp;&amp; git diff HEAD~1 HEAD --stat | grep -q 'CLAUDE.md'</automated>
  </verify>
  <done>Single commit lands 2 files. Subject matches conventional commit format `docs(01.1-08): ...`. No skills directory created.</done>
</task>

</tasks>

<verification>
Phase-level checks after Task 3:

```bash
# Dependencies group added
grep -c 'External dev tools' .planning/dependencies.md   # 1
grep -c 'automate-dev-feedback-loop' .planning/dependencies.md   # ≥1

# CLAUDE.md pointer added
grep -c 'dev-feedback-loop' CLAUDE.md   # 1 or 2

# D8 deferral respected — NO skill directory created
! test -d .claude/skills/dev-feedback-loop
```
</verification>

<success_criteria>
Plan 01.1-08 done when:
- 2 documentation files modified + committed atomically
- New "External dev tools" group in dependencies.md lists all 5 tools per D-DEP-01
- CLAUDE.md gains exactly one cross-cutting pointer under ## Patterns per D-DEP-02
- D8 OpenSpec deferral verified — no `.claude/skills/dev-feedback-loop/` created
</success_criteria>

<output>
Create `.planning/phases/01.1-dev-feedback-loop-infrastructure/01.1-08-SUMMARY.md` per template containing:
- Commit sha
- Snippet of the new dependencies.md group (tool list verifiable against R7+R8+R9)
- Snippet of the new CLAUDE.md row (exact wording for future-grep)
- Confirmation that D8 deferral was respected (no skills directory)
</output>
