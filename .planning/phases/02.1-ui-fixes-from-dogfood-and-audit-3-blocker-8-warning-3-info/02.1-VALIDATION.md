---
phase: "02.1"
slug: ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info
status: shipped
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-18
validated_at: 2026-05-18
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from all 13 `02.1-NN-PLAN.md` / `02.1-NN-SUMMARY.md` files plus the
> code-review fix layer (`02.1-REVIEW.md` / `02.1-REVIEW-FIX.md`) and the four
> Nyquist gap-fill test commits (`ce73cc2` / `4dba9bf` / `977ecd1`).
>
> Phase 02.1 is the UI dogfood + audit fix wave for Phase 02 (combined PR
> ship). It does not introduce new top-level requirements — only re-touches
> the REQ-03 / REQ-06 / REQ-13 / REQ-14 surfaces already validated by
> `.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md`.
> This document layers on top of the parent map and pins the 02.1-specific
> behaviors (B / W / I dogfood-found issues + post-hoc code-review fixes).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (JS/TS)** | Vitest 4.1.x (inherited from Phase 1 → Phase 02) |
| **Framework (Rust)** | `cargo test` built-in (`tokio` async tests via `#[tokio::test]`) |
| **Config files** | `vitest.config.ts` (`environment: 'jsdom'`, `conditions: ['browser']`), `src-tauri/Cargo.toml` |
| **Quick run command (JS)** | `npx vitest run --changed` (auto via Husky pre-commit) |
| **Quick run command (Rust)** | `cargo test --manifest-path src-tauri/Cargo.toml` |
| **Full suite command** | `npm test && cargo test --manifest-path src-tauri/Cargo.toml` |
| **Type / lint gate** | `npm run check` (`svelte-kit sync && svelte-check`) |
| **Capability audit gate** | `bash scripts/audit-capabilities.sh` (Husky pre-commit) |
| **E2E framework** | None — visual verification via dev-feedback-loop (`gsd-dev-snapshot.mjs` + Tauri shell screenshot) |
| **Estimated runtime** | ~35s JS suite + ~20s Rust suite |
| **Final suite state (2026-05-18)** | cargo: 80/80 pass · vitest: 310 pass / 1 pre-existing fail (`scripts/__tests__/visual-review-template.test.mjs > R7` GSD upstream cluster, declared out-of-scope per `02-VALIDATION.md` L220-222) / 1 skip · svelte-check: 0 errors / 0 warnings / 402 files |

---

## Sampling Rate

- **After every task commit:** `npx vitest run --changed` (Husky pre-commit auto) + `cargo test` for Rust changes
- **After every plan wave:** `npm test && cargo test --manifest-path src-tauri/Cargo.toml` (full suite green)
- **Before `/gsd-verify-work`:** Full suite green + `bash scripts/audit-capabilities.sh` clean + dev-snapshot visual diff against `Mneme.html` SSOT for each net-touched surface (Onboarding Step3 / Step5 / Step6 + ImportStatusPill + ImportHistoryModal + DropzoneOverlay + SettingsPanel + TitlebarMeta)
- **Max feedback latency:** ≤ 55s (full suite including Rust)

---

## Per-Task Verification Map

> Maps each 02.1 plan task to the test that pins its behavior. Wave column:
> 0 = RED bundle, 1 = blocker GREEN, 2 = warning/info polish, FIX = post-hoc
> code-review fix layer, NYQUIST = gap-fill tests added by this audit.
> Status reflects state as of `validated_at`.

### Plan-task layer (13 plans)

| Task ID | Plan | Wave | Requirement | Closes | Test Type | Automated Command | File Path | Status |
|---------|------|------|-------------|--------|-----------|-------------------|-----------|--------|
| 02.1-01-T0 | 01 | 0 | REQ-03, REQ-06, REQ-14 | RED bundle for B3 / W5 / W6 | unit (Rust + TS) | `cargo test --test inspector_keychain_detect && cargo test --test course_code_validation && npx vitest run tests/import-error-fallback.test.ts` | `src-tauri/tests/inspector_keychain_detect.rs`, `src-tauri/tests/course_code_validation.rs`, `tests/import-error-fallback.test.ts` | ✅ green (RED→GREEN gates pinned) |
| 02.1-02 | 02 | 1 | REQ-14 | B3 — `claude_auth_check` via subprocess `claude --version` (not file probe) | unit (Rust) | `cargo test --manifest-path src-tauri/Cargo.toml --test inspector_keychain_detect` | `src-tauri/tests/inspector_keychain_detect.rs` | ✅ green |
| 02.1-03 | 03 | 1 | REQ-14 | B2 — `--color-success` token leak on Move-complete confirmation swapped to `--color-warm-dark-soft` | grep gate + visual | `! grep -rn "var(--color-success)" src/lib/components/settings/VaultCategory.svelte` + dev-snapshot diff | `src/lib/components/settings/VaultCategory.svelte` | ✅ green (token-discipline contract) |
| 02.1-04 | 04 | 1 | REQ-14 | W6 — strip `^[A-Z]{4}\d{4}$` regex; minimal path-safe validator accepts USYD codes (BIOL2010S2, COMP3027L, MATH1062, cs-101, 日本語1) and rejects path traversal | unit (Rust) | `cargo test --manifest-path src-tauri/Cargo.toml --test course_code_validation` | `src-tauri/tests/course_code_validation.rs` | ✅ green |
| 02.1-05 | 05 | 1 | REQ-06, REQ-14 | W5 — `classifyImportError` appends raw `(details: <tail>)` when no structured classifier matches | unit (TS) | `npx vitest run tests/import-error-fallback.test.ts` | `tests/import-error-fallback.test.ts` | ✅ green (5/5) |
| 02.1-06 | 06 | 1 | REQ-03, REQ-13 | B1 — DropzoneOverlay dark veil + hero card per UI-SPEC §6.1 | grep gate + visual | `grep -nE 'backdrop-filter\|--color-dark-veil' src/lib/components/dropzone/DropzoneOverlay.svelte` + dev-snapshot diff | `src/lib/components/dropzone/DropzoneOverlay.svelte` | ✅ green (visual contract) |
| 02.1-07 | 07 | 2 | REQ-13 | W1 — ImportStatusPill icons + spinner + state backgrounds | grep gate + visual | `grep -nE 'aria-busy\|spinner\|--color-importing\|--color-success' src/lib/components/ImportStatusPill.svelte` + dev-snapshot diff | `src/lib/components/ImportStatusPill.svelte` | ✅ green (visual contract) |
| 02.1-08 | 08 | 2 | REQ-14 | W2 — `motion.ts` module + `prefers-reduced-motion` honoring | unit (TS) | `npx vitest run src/lib/motion.test.ts` | `src/lib/motion.test.ts`, `src/lib/motion.ts` | ✅ green (4/4) |
| 02.1-09 | 09 | 2 | REQ-14 | W3 — SettingsPanel rail focus-ring per UI-SPEC §3.2 | grep gate + visual | `grep -nE ':focus-visible\|outline:' src/lib/components/SettingsPanel.svelte` + dev-snapshot diff | `src/lib/components/SettingsPanel.svelte` | ✅ green (visual contract) |
| 02.1-10 | 10 | 2 | REQ-14 | W4 — Step3VaultPicker "Choose another folder" ghost-link | grep gate + visual | `grep -nE 'ghost-link\|Choose another folder' src/lib/components/onboarding/Step3VaultPicker.svelte` + dev-snapshot diff | `src/lib/components/onboarding/Step3VaultPicker.svelte` | ✅ green (visual contract) |
| 02.1-11 | 11 | 2 | REQ-14 | W7 — keymap doc sync (Cmd+, Cmd+I bridge documented) | doc-only | manual review against `docs/keymap.md` SSOT | `docs/keymap.md` + impl pin `tests/cmd-comma-shortcut.test.ts`, `tests/menu-bridge.test.ts`, `src-tauri/tests/menu_preferences_emits_event.rs` | 📄 manual (doc) · impl tests ✅ green (5 TS + 4 Rust) |
| 02.1-12 | 12 | 2 | REQ-14 | W8 — TitlebarMeta enables window drag from right half via `data-tauri-drag-region` | manual (declared) | OS-level WKWebView interaction — not jsdom/vitest-testable; declared in 02.1-12-SUMMARY.md L40-44 | `src/lib/components/TitlebarMeta.svelte` | 📄 manual (OS-level, declared) |
| 02.1-13 | 13 | 2 | REQ-13, REQ-14 | I2 — ImportHistoryModal copy refinements; I3 — Step6 token leak audit | grep gate + visual | `grep -nE 'var\(--color-success\)' src/lib/components/onboarding/Step6DemoImport.svelte` (should match only on `.success` semantic uses) + dev-snapshot diff | `src/lib/components/ImportHistoryModal.svelte`, `src/lib/components/onboarding/Step6DemoImport.svelte` | ✅ green (visual + token contract) |

### Code-review fix layer (7 post-hoc fixes)

| Fix ID | Wave | Requirement | Behavior Pinned | Test Type | Automated Command | File Path | Status |
|--------|------|-------------|-----------------|-----------|-------------------|-----------|--------|
| CR-001 | FIX | REQ-14 | Frontend course-code validator aligned with Rust SSOT (USYD codes accepted on Step5 onboarding AND Settings → Vault) | unit (TS) | `npx vitest run tests/course-code-regex.test.ts` | `tests/course-code-regex.test.ts`, `src/lib/onboarding-validation.ts`, `src/lib/components/settings/VaultCategory.svelte` | ✅ green |
| WR-001 | NYQUIST | REQ-03, REQ-14 | Step6 `browseFile()` double-click guard — `importing` $state short-circuits second invocation | unit (TS) | `npx vitest run tests/onboarding-step6-double-click.test.ts` (`WR-001` describe block, 3 tests) | `tests/onboarding-step6-double-click.test.ts`, `src/lib/components/onboarding/Step6DemoImport.svelte` | ✅ green (3/3) |
| WR-002 | NYQUIST | REQ-06 | `atomic_write` extensionless tmp-name shape — `Makefile → Makefile.tmp` (not `Makefile..tmp`); standard `foo.pdf → foo.pdf.tmp` still round-trips | integration (Rust) | `cargo test --manifest-path src-tauri/Cargo.toml --test atomic_write_extensionless` | `src-tauri/tests/atomic_write_extensionless.rs`, `src-tauri/src/vault_writer.rs` L244-265 | ✅ green (2/2) |
| WR-003 | NYQUIST | REQ-03 | `cancel_all` is async, non-blocking, idempotent — Tauri lifecycle handlers spawn via `tauri::async_runtime::spawn` so caller returns immediately; CancellationTokens flip before subprocess reap | integration (Rust) | `cargo test --manifest-path src-tauri/Cargo.toml --test cancel_all_nonblocking` | `src-tauri/tests/cancel_all_nonblocking.rs`, `src-tauri/src/lib.rs` L913-930 | ✅ green (3/3) |
| WR-004 | FIX | REQ-14 | Onboarding pre-mount `next()` calls queue via `pendingNext` instead of silent no-op | unit (TS) | `npx vitest run tests/onboarding-resume.test.ts` (`WR-03 fix` describe block, 3 tests) | `tests/onboarding-resume.test.ts`, `src/lib/components/onboarding/Onboarding.svelte` | ✅ green |
| WR-005 | NYQUIST | REQ-03 | Step6 multi-file count surface — 5 picked files render as "Imported: 5 files (<first>, …)"; single-file path still renders single basename | unit (TS) | `npx vitest run tests/onboarding-step6-double-click.test.ts` (`WR-005` describe block, 2 tests) | `tests/onboarding-step6-double-click.test.ts`, `src/lib/components/onboarding/Step6DemoImport.svelte` | ✅ green (2/2) |
| WR-006 | FIX | REQ-06 | `compute_dest` returns `Err(invalid-name:<reason>)` for unusable basenames (`.`, `..`, empty) instead of routing to `_inbox/invalid-source-basename` sentinel | unit (Rust inline) | `cargo test --manifest-path src-tauri/Cargo.toml --lib compute_dest` | `src-tauri/src/import_controller.rs` inline `#[cfg(test)]` (`compute_dest_rejects_unusable_basenames` + `compute_dest_happy_path_returns_expected_paths`) | ✅ green |
| WR-007 | FIX | REQ-14 | `Onboarding.onMount` MERGE (not REPLACE) — per-field merge formula preserves in-flight user mutations during load window | unit (TS) | `npx vitest run tests/onboarding-resume.test.ts` (`WR-007 fix` describe blocks, 4 runtime + 2 source-regex) | `tests/onboarding-resume.test.ts`, `src/lib/components/onboarding/Onboarding.svelte` L97-144 | ✅ green (6/6) |

---

## Wave 0 Requirements

> Phase 02.1 Wave 0 (plan 02.1-01) landed all 3 RED tests in one commit so
> Wave 1 GREEN plans (02.1-02 / 02.1-04 / 02.1-05) execute against a failing
> baseline.

- [x] `src-tauri/tests/inspector_keychain_detect.rs` — B3 RED (3 arms: subprocess Ok / NotFound / other spawn error)
- [x] `src-tauri/tests/course_code_validation.rs` — W6 RED (accepts USYD real codes; rejects path traversal / empty / non-alphanumeric / overlength)
- [x] `tests/import-error-fallback.test.ts` — W5 RED (asserts GENERIC fallback appends `(details: <tail>)` when no classifier matches)

All three transitioned to GREEN as part of Wave 1.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| W7 keymap documentation accuracy (`docs/keymap.md`) | REQ-14 | Doc-only fix; semantic accuracy reviewed by human (impl behavior IS pinned by `cmd-comma-shortcut.test.ts` + `menu-bridge.test.ts` + `menu_preferences_emits_event.rs`) | Diff `docs/keymap.md` against `Mneme.html` Settings SSOT key listings; verify Cmd+, opens Settings and Cmd+I opens ImportDialog in actual app |
| W8 titlebar drag from right half (`data-tauri-drag-region`) | REQ-14 | OS-level WKWebView attribute interpretation; not exercisable by jsdom (no Tauri drag-region semantics) or vitest. Playwright + Tauri integration harness does not exist in this repo. Declared out-of-scope per `02.1-12-SUMMARY.md` Issue 1 | Run `npm run tauri dev`, click+drag anywhere on the right half of the titlebar that is NOT a button/text field; verify window moves. Also verify clicks on `TitlebarMeta` cog still register (drag should NOT eat button clicks) |
| WR-004 + WR-007 race-window UAT (fast-click Step3 + Step5) | REQ-14 | Runtime unit tests pin the merge/queue logic deterministically, but the sub-50ms race on a real SSD requires human dogfood to confirm zero perceived UX glitch | In `npm run tauri dev`, on a fresh `~/.mneme/onboarding.json`: open Step3, immediately click "Choose vault" before the wizard finishes loading state. Verify selection persists and Step4+ shows the chosen vault path (no empty string regression). Repeat with deleted onboarding file. |
| B1 / B2 / W1 / W3 / W4 / I2 / I3 visual contracts | REQ-13, REQ-14 | UI visual fidelity per `Mneme.html` SSOT — grep gates pin token discipline + class presence; pixel-level fidelity requires dev-snapshot diff or eyeball comparison | Run `npm run gsd-dev-screenshot` (or `npm run tauri dev` + manual screenshot); diff each touched surface against `~/Downloads/Mneme 3/Mneme.html` reference. Capture in `/gsd-verify-work` 4-bucket HTML |

---

## Validation Sign-Off

- [x] All 13 plan tasks + 7 fix-layer fixes have automated verification OR explicit Manual-Only entry with test instructions
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (Manual-Only items 11 / 12 are intentional by design, not gaps)
- [x] Wave 0 covers all MISSING references at phase planning time
- [x] No watch-mode flags (`vitest run`, not `vitest --watch`)
- [x] Feedback latency ≤ 55s (full suite ~35s JS + ~20s Rust)
- [x] 4 Nyquist gap-fill tests added by this audit (`ce73cc2` WR-001+005, `4dba9bf` WR-002, `977ecd1` WR-003)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-18 — Phase 02.1 is Nyquist-compliant. All automatable behaviors pinned by regression tests; remaining Manual-Only items declared with executable verification instructions.
