---
status: complete
phase: 02.1-ui-fixes-from-dogfood-and-audit
source: [02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md, 02.1-03-SUMMARY.md, 02.1-04-SUMMARY.md, 02.1-05-SUMMARY.md, 02.1-06-SUMMARY.md, 02.1-07-SUMMARY.md, 02.1-08-SUMMARY.md, 02.1-09-SUMMARY.md, 02.1-10-SUMMARY.md, 02.1-11-SUMMARY.md, 02.1-12-SUMMARY.md, 02.1-13-SUMMARY.md, 02.1-REVIEW.md, 02.1-REVIEW-FIX.md, 02.1-VALIDATION.md]
started: 2026-05-18T01:26:44Z
updated: 2026-05-18T01:45:00Z
handoff_html: .planning/handoff/2026-05-18-phase-02.1-verify.html
auto_verified:
  cargo: "80/80 pass"
  vitest: "310 pass + 1 pre-existing R7 + 1 skip"
  svelte_check: "402 files, 0 error, 0 warning"
  audit_capabilities: "PASS"
  dev_log_scan: "0 console errors, 0 network failures, 0 perf concerns since 2026-05-18T01:26:44Z"
  bridge_limitations:
    - "gsd-dev-screenshot: blocked by AX window enumeration (Phase 01.2 UDS will fix)"
    - "gsd-dev-snapshot: dev_query_state CLI not in v1 (Phase 01.2 UDS will fix)"
---

## Current Test

[testing complete]

## Tests

### 0. DOGFOOD-DISCOVERED — ImportDialog stuck on "Importing…" after successful import
expected: clicking Submit on ImportDialog with a valid file + course + category, dialog should auto-close once Rust emits import:done
result: issue
reported: "卡在importing了 [screenshot: titlebar shows '✓ imported 1 · just now' but dialog button frozen at 'Importing…' for MATH2021_Assignment2_550654744.pdf into course=DOGFOOD101 / category=_inbox]"
severity: major
bucket: dogfood (off-checklist — found during exploratory verify)
root_cause: "Contract documented in ImportDialog.svelte:97-108 expects parent +page.svelte to listen for Tauri 'import:done' event and call closeImportDialog() when the payload's operation_id matches the active dialog opId. Rust correctly emits import:done at src-tauri/src/import_controller.rs:401. src/lib/import-state.svelte.ts:119 correctly listens and records history (which is why the titlebar pill flips to 'imported 1 just now'). But +page.svelte's onMount (L131-134) only wires mneme:open-settings + mneme:open-history CustomEvents — the Tauri import:done listener was never wired. Result: dialog stays submitting=true forever; user must click Cancel manually."
artifacts:
  - path: src/routes/+page.svelte
    issue: "onMount (L131-134) missing await listen<ImportDoneEventPayload>('import:done', ...) → closeImportDialog() when operation_id matches"
  - path: src/lib/components/ImportDialog.svelte
    issue: "L97-108 comment expects parent listener; either the parent needs to wire it OR this dialog should self-listen for its own currentOpId"
missing:
  - "Wire 'import:done' listener in +page.svelte onMount that calls closeImportDialog() when payload.operation_id matches the currently-open dialog's opId AND !cancelled"
  - "OR: lift ImportDialog to self-listen for its own currentOpId and call onClose() inline — simpler, keeps state colocated"
  - "Add regression test: tests/import-dialog-auto-close.test.ts asserting dialog closes when matching import:done fires"
debug_session: ""
phase_02_origin: true

### 1. B1 — DropzoneOverlay dark veil + hero card
expected: drag from Finder shows ~45% dark veil + cream hero card with 48×48 icon + 28px serif title; drop dismisses and file enters _inbox
bucket: visual
anchor: src/lib/components/dropzone/DropzoneOverlay.svelte (.overlay rgba 0.45 + blur 4px)
result: pass
note: "user rough-checked during dogfood — drag-drop triggered overlay successfully (the same flow that hit G-01 stuck dialog confirmed dropzone fires correctly)"

### 2. B2 — VaultCategory Move-complete uses warm-dark-soft
expected: Settings → Vault → Move vault → completion banner uses --color-warm-dark-soft, NOT --color-success green
bucket: visual
anchor: src/lib/components/settings/VaultCategory.svelte:192, 370-371
result: pass
note: "user rough-checked — no surface issues noted"

### 3. W1 — ImportStatusPill 4 states with icons
expected: trigger import; pill cycles idle (gray) → importing (peach + spinner) → complete (olive + ✓) → error (faded red + ⚠); spinner rotates smoothly
bucket: visual
anchor: src/lib/components/ImportStatusPill.svelte ([data-state="..."])
result: skipped
reason: "design changed mid-verify (2026-05-18T11:38 dogfood decision G-02) — ImportStatusPill is being removed from TitlebarMeta entirely; import progress/success/error moves to a temporary bottom toast (3-5s auto-dismiss). Spec for the new ImportToast component lives in G-02 gap entry. Original W1 contract no longer applies."

### 4. W3 — SettingsPanel rail focus-ring
expected: Cmd+, opens Settings; Tab through 8 categories shows 3px orange ring (--orange-ring) on focus, no default browser outline
bucket: visual
anchor: src/lib/components/SettingsPanel.svelte (:focus-visible)
result: pass
note: "user rough-checked — no surface issues noted"

### 5. W4 — Step3VaultPicker ghost-link
expected: Step 3 shows "Choose another folder" ghost-link directly below "Use this path" CTA; clicking opens file dialog
bucket: visual
anchor: src/lib/components/onboarding/Step3VaultPicker.svelte (button.ghost-link)
result: skipped
reason: "user did not navigate to Onboarding Step 3 during this verify session — to re-test: rm ~/.mneme/onboarding.json + restart mneme + walk to Step 3"

### 6. I2 — ImportHistoryModal row token
expected: Import History modal rows use design token for height, not hardcoded 56px; vertical rhythm matches SettingsPanel
bucket: visual
anchor: src/lib/components/ImportHistoryModal.svelte
result: pass
note: "user opened Recent imports modal during dogfood (screenshot 2026-05-18 12:20) — row rendered correctly, no token-leak symptoms"

### 7. I3 — Step1Welcome spacing
expected: Step 1 welcome spacing uses parent gap/flex, NO negative margins (margin-top: -*px) anywhere
bucket: visual
anchor: src/lib/components/onboarding/Step1Welcome.svelte
result: skipped
reason: "user did not navigate to Onboarding Step 1 (already past onboarding in dogfood vault) — to re-test: rm ~/.mneme/onboarding.json + restart mneme"

### 8. W8 — TitlebarMeta drag region (right half)
expected: pressing/dragging on titlebar right half (dot/sep/meta-text empty area) moves the window; cog click still opens Settings; pill click still opens Import History
bucket: window
anchor: src/lib/components/TitlebarMeta.svelte (data-tauri-drag-region)
result: pass
note: "user rough-checked — drag works (post G-02 pill removal will make MORE area drag-region, so this gets easier)"

### 9. W8b — cog click still opens Settings
expected: single click on settings cog opens Settings instantly; drag region carve-out (data-tauri-drag-region="false" L84) doesn't eat clicks
bucket: window
anchor: src/lib/components/TitlebarMeta.svelte:84
result: pass
note: "user rough-checked — Settings reachable via cog"

### 10. W8c — ImportStatusPill click still opens Import History
expected: single click on right-side pill opens Import History modal; no-drag-wrap (display: contents) passes click through to pill
bucket: window
anchor: src/lib/components/TitlebarMeta.svelte (.no-drag-wrap wrapper)
result: skipped
reason: "design changed mid-verify (2026-05-18T11:38 dogfood decision G-02) — pill removed from titlebar; no-drag-wrap wrapper goes with it. Import History opens via Cmd+I (mneme:open-history) which already works; new toast surface (G-02) carries no click-to-open-history affordance (entry point becomes keyboard + Settings link discoverability)."

### 11. W2 — Onboarding step transition (8px slide + 220ms fade)
expected: Step N → N+1 transitions with subtle 8px upward slide + opacity fade over 220ms (quintOut); not abrupt cut, not >400ms slow
bucket: motion
anchor: src/lib/components/onboarding/Onboarding.svelte (transition:fly)
result: skipped
reason: "Onboarding flow not traversed in this verify session (user already in main app with dogfood vault) — to re-test: rm ~/.mneme/onboarding.json + restart"

### 12. W2b — prefers-reduced-motion degrades to opacity-only
expected: with macOS Reduce Motion ON, step transitions show ONLY opacity fade (y=0); a11y compliance
bucket: motion
anchor: src/lib/motion.ts (SSR-safe matchMedia)
result: skipped
reason: "depends on Onboarding flow (W2 skipped); contract is pinned by 4 motion.test.ts unit tests so runtime trust is high — re-test alongside W2 when Onboarding next traversed"

### 13. WR-004 — Step3 fast-click race (pendingNext queue)
expected: rm ~/.mneme/onboarding.json; restart mneme; click "Choose vault" on Step3 before onMount completes (<50ms); selection persists in Step 4+
bucket: motion
anchor: src/lib/components/onboarding/Onboarding.svelte (pendingNext queue)
result: skipped
reason: "Onboarding not traversed this session; contract pinned by tests/onboarding-resume.test.ts WR-03 fix block (3 runtime tests). Re-test when next walking through Onboarding"

### 14. WR-007 — Step5 mid-load courses survive REPLACE→MERGE
expected: add courses on Step 5; immediately Next to Step 6; Cmd+Q + restart; resume should show Step 6 with courses still populated (not wiped by REPLACE)
bucket: motion
anchor: src/lib/components/onboarding/Onboarding.svelte:97-144 (onMount MERGE)
result: skipped
reason: "Onboarding Step 5 not reached this session; contract pinned by tests/onboarding-resume.test.ts WR-007 fix block (4 runtime + 2 source-regex tests). Re-test when next walking through Onboarding"

### 15. P1 — Cold start no white flicker
expected: kill mneme; fresh npm run tauri dev; window first frame is cream (#E6E3DC) immediately; no white → cream flash
bucket: perf
anchor: SSR=false + prerender=true + inline critical CSS
result: pass
note: "user rough-checked — mneme was started fresh during this verify session (PID 66793 at 11:26) and reportedly no visual issues"

### 16. P2 — Step6 multi-file count surface
expected: pick 5+ PDFs in Step 6 demo import; success shows "Imported: 5 files (lec1.pdf, …)" not just first basename
bucket: perf
anchor: src/lib/components/onboarding/Step6DemoImport.svelte:60-70
result: skipped
reason: "Step 6 not reached (Onboarding not traversed); contract pinned by tests/onboarding-step6-double-click.test.ts WR-005 block (2 tests). Re-test when next walking through Onboarding Step 6"

### 17. P3 — Step6 double-click guard
expected: rapid double-click "Browse files" only opens ONE dialog (in_flight guard short-circuits second invocation)
bucket: perf
anchor: src/lib/components/onboarding/Step6DemoImport.svelte:39-83
result: skipped
reason: "Step 6 not reached (Onboarding not traversed); contract pinned by tests/onboarding-step6-double-click.test.ts WR-001 block (3 tests). Re-test when next walking through Onboarding Step 6"

## Summary

total: 18
passed: 7          # B1, B2, W3, I2, W8, W8b, P1 — rough check by user, no surface issues
issues: 1          # G-01 ImportDialog stuck (real bug)
pending: 0
skipped: 10        # 2 design-change (W1, W8c) + 8 Onboarding-flow-not-traversed (W4, I3, W2, W2b, WR-004, WR-007, P2, P3)
blocked: 0
design_changes: 1  # G-02 ImportStatusPill → ImportToast (separate from test failures)

skip_breakdown:
  design_changed:
    - 3 (W1 ImportStatusPill — pill removed from titlebar per G-02)
    - 10 (W8c ImportStatusPill click — wrapper removed per G-02)
  onboarding_not_traversed:
    - 5 (W4 Step3 ghost-link)
    - 7 (I3 Step1 spacing)
    - 11 (W2 step transition)
    - 12 (W2b reduce-motion + step transition)
    - 13 (WR-004 Step3 race)
    - 14 (WR-007 Step5 race)
    - 16 (P2 Step6 multi-file)
    - 17 (P3 Step6 double-click)
  retest_path: "rm ~/.mneme/onboarding.json + restart mneme + walk Steps 1-6 + try multi-file pick on Step 6"

## Gaps

- truth: "ImportDialog auto-closes when its current operation_id receives a successful import:done event"
  status: failed
  reason: "User reported: dialog button stuck on 'Importing…' after MATH2021_Assignment2_550654744.pdf import; titlebar shows 'imported 1 just now' (Rust emit fires) but dialog never closes (parent +page.svelte does not listen for import:done — contract documented in ImportDialog.svelte:97-108 was never wired)"
  severity: major
  test: 0
  root_cause: "Missing window-level listen('import:done', ...) wire in src/routes/+page.svelte:131 onMount block — contract-by-comment was not enforced by a test, and ImportDialog/+page.svelte was outside the Phase 02.1 review scope (Phase 02 origin)"
  artifacts:
    - path: src/routes/+page.svelte
      line_range: 131-134
      issue: "onMount registers mneme:open-settings + mneme:open-history but NOT import:done"
    - path: src/lib/components/ImportDialog.svelte
      line_range: 97-108
      issue: "Documented contract expects parent listener; not enforced by test"
  missing:
    - "Wire import:done listener: when payload.operation_id === currentlyOpenOpId AND !payload.cancelled → closeImportDialog()"
    - "Co-locate state (lift opId to parent OR have dialog self-listen) — simpler is dialog self-listens for its own currentOpId"
    - "Regression test pinning auto-close behavior"
  debug_session: ""
  found_via: "exploratory dogfood during verify-work (off-checklist)"

- truth: "Titlebar middle section shows only claude-code status + vault path; import status moves to bottom toast"
  status: design_change
  reason: "User dogfood decision 2026-05-18T11:38 — current TitlebarMeta crowds claude-code-status + ImportStatusPill + vault into the 36px titlebar; user wants ImportStatusPill removed entirely. Import status (importing/success/error) routes to a new ImportToast component (bottom area, 3-5s auto-dismiss). Recent imports modal entry point moves to Cmd+I (already wired via mneme:open-history) + a Settings → Sync 'View import history' link for discoverability."
  severity: minor   # design preference, not bug — affects v1.x but doesn't block ship
  test: -1          # not a test failure; design change recorded for gap closure
  root_cause: "TitlebarMeta inherited a 3-element layout that crowds the 36px row; ImportStatusPill placement was an early-Phase-02 decision later contradicted by dogfood UX preference"
  artifacts:
    - path: src/lib/components/TitlebarMeta.svelte
      issue: "<ImportStatusPill> + <span class='no-drag-wrap'> wrapper must be removed"
    - path: src/lib/components/ImportStatusPill.svelte
      issue: "Component itself stays but caller changes — moves from titlebar to new ImportToast surface"
    - path: src/lib/components/ImportToast.svelte
      issue: "NEW component required — bottom-region, 3-5s auto-dismiss, reuses KD-13 4-state color palette + spinner SVG from ImportStatusPill"
    - path: src/routes/+page.svelte
      issue: "Mount ImportToast; subscribe to import-state.svelte.ts pill_state for transitions"
    - path: src/lib/components/SettingsPanel.svelte
      issue: "Sync category — add 'View import history' link that dispatches window event 'mneme:open-history'"
  missing:
    - "Strip ImportStatusPill from TitlebarMeta + remove no-drag-wrap"
    - "Create ImportToast component (3-5s auto-dismiss, bottom placement, KD-13 4-state palette)"
    - "Wire toast subscription to import-state pill_state transitions"
    - "Add Settings → Sync 'View import history' link"
    - "Regression test: titlebar middle should only contain claude-code-status + vault (no pill)"
    - "Regression test: ImportToast appears on import:progress, dismisses after 3-5s on idle"
  debug_session: ""
  found_via: "exploratory dogfood during verify-work — design preference, not bug"
  invalidates_uat_items: [3 (W1), 10 (W8c)]
