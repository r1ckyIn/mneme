---
phase: "02.1"
plan: 3
type: execute
wave: 1
depends_on: ["02.1-01"]
files_modified:
  - src/lib/components/settings/VaultCategory.svelte
autonomous: true
gap_closure: false
requirements: [REQ-14]

closes:
  blockers: [B2]

must_haves:
  truths:
    - "VaultCategory move-complete success message uses var(--color-warm-dark-soft) — NOT var(--color-success). Token discipline restored per UI-SPEC §4."
    - "The 3 onboarding-reserved sites for --color-success (Step 2 auth dot, Step 3 vault checkmark, Step 5 course validation) are UNTOUCHED — no collateral changes."
    - "grep live-code count of var(--color-success) in VaultCategory.svelte == 0 (CSS-comment lines stripped before counting)."
  artifacts:
    - path: "src/lib/components/settings/VaultCategory.svelte"
      provides: "B2 fix — .status.success uses --color-warm-dark-soft. Disposition marker `B2 fix` cites UI-SPEC §4 reserved-site contract."
      contains: "B2 fix"
  key_links:
    - from: "src/lib/components/settings/VaultCategory.svelte L370-372"
      to: "src/lib/styles/tokens.css --color-warm-dark-soft"
      via: ".status.success rule swaps token reference"
      pattern: "var\\(--color-warm-dark-soft\\)"
---

<objective>
Wave 1 GREEN for B2 (BLOCKER — token-discipline violation). Per UI-REVIEW.md L86-89 + UI-SPEC §4 lines 148-153, `--color-success` (the green `#4ea36b` token) is RESERVED for onboarding validation states only (Step 2 auth dot, Step 3 vault checkmark, Step 5 course validation). The reserved-for list is exhaustive: no other surface in Phase 2 may use this token.

VaultCategory.svelte:370-371 violates this contract — it applies `--color-success` to the "Move vault complete" status message in the Settings panel. A vault move in Settings is not an onboarding validation state. The fix: swap to `var(--color-warm-dark-soft)` (the soft warm-tone success color used for the "imported" pill state at ImportStatusPill.svelte:84) — matches the existing post-success treatment elsewhere in Phase 2 and respects the reserved-site boundary.

**Why execute (not TDD):** Pure visual token swap. No state machine, no parameterized input, no behavior change. Verification is grep + visual review. Adding a vitest pin would be circular.

**Out of scope:**
- TitlebarMeta.svelte:132 hardcoded `#4ea36b` — pre-token relic carve-out; NOT 02.1.
- ImportDialog.svelte:284 hardcoded backdrop rgba — separate INFO finding; NOT 02.1.
- ToolUseGroup.svelte:186 `#4ea36b` (Phase 1 component) — Phase 1 scope; NOT 02.1.

**Parallel-safety:** This plan touches ONE file (`VaultCategory.svelte`). No overlap with 02.1-02 (lib.rs / capabilities) / 02.1-04 (vault_writer.rs / import_controller.rs) / 02.1-05 (import-error.ts) / 02.1-06 (DropzoneOverlay.svelte). All 5 Wave 1 plans run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-CONTEXT.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-REVIEW.md

@src/lib/components/settings/VaultCategory.svelte
@src/lib/styles/tokens.css

<!-- Reserved-site analogs (DO NOT MODIFY) -->
@src/lib/components/onboarding/Step2AuthCheck.svelte
@src/lib/components/onboarding/Step3VaultPicker.svelte
@src/lib/components/onboarding/Step5AddCourse.svelte

<!-- Existing same-pattern for the replacement token -->
@src/lib/components/ImportStatusPill.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: B2 — swap --color-success to --color-warm-dark-soft in VaultCategory move-complete status</name>
  <files>src/lib/components/settings/VaultCategory.svelte</files>
  <read_first>
    - src/lib/components/settings/VaultCategory.svelte L185-200 (.status.success markup when moveStatus === "done")
    - src/lib/components/settings/VaultCategory.svelte L365-376 (.status / .status.success / .status.error CSS at the bottom of the style block)
    - src/lib/styles/tokens.css (confirm --color-warm-dark-soft token exists)
    - src/lib/components/ImportStatusPill.svelte L83-85 (analog use of --color-warm-dark-soft for soft success)
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md L148-153 (the reserved-for list for --color-success)
  </read_first>
  <action>
    Edit `src/lib/components/settings/VaultCategory.svelte`.

    STEP A — locate `.status.success { color: var(--color-success); }` rule at L370-372 within the `<style>` block. Replace `var(--color-success)` with `var(--color-warm-dark-soft)`. Do NOT touch `.status.error`.

    STEP B — add a disposition comment IMMEDIATELY ABOVE the changed rule (single-line `/* ... */` form to keep the strip-comments grep simple):
    `/* B2 fix (Phase 02.1 02.1-03): --color-success is reserved for onboarding validation per UI-SPEC §4 (Step 2 / Step 3 / Step 5). Vault-move success message is NOT an onboarding state — use --color-warm-dark-soft (same soft-success tone as ImportStatusPill imported state). */`

    STEP C — leave the markup at L192-194 unchanged. Do NOT add a checkmark prefix in this plan (would expand scope; INFO-style polish belongs to 02.1-13).

    STEP D — verify no live-code `--color-success` reference remains in this file. Run `grep -n 'color-success' src/lib/components/settings/VaultCategory.svelte`. If matches exist outside the new disposition comment, investigate; if they are inside other CSS comments referencing historical context, the strip-grep below handles them.

    Run:
    1. `npm run check` — 0 TS / svelte-check errors.
    2. `grep -c 'B2 fix' src/lib/components/settings/VaultCategory.svelte` ≥ 1.
    3. `grep -v '^\s*/\*' src/lib/components/settings/VaultCategory.svelte | grep -v '^\s*\*' | grep -c 'var(--color-success)'` == 0 (live-code count).
    4. `grep -c 'var(--color-warm-dark-soft)' src/lib/components/settings/VaultCategory.svelte` ≥ 1.
    5. `npx vitest run` — full suite green (no test references the changed class color; confirmed by file census in 02-VALIDATION).

    Commit:
    `git commit -m 'fix(02.1-03): B2 — VaultCategory move-complete uses warm-dark-soft' -m '--color-success is reserved for onboarding validation per UI-SPEC §4 (Step 2 / 3 / 5). VaultCategory.svelte move-vault success status was using it outside the reserved sites — swap to --color-warm-dark-soft (same soft-success tone as ImportStatusPill imported state).' -m 'Pure visual token swap; no behavior change. Inline B2 fix disposition marker cites UI-SPEC §4.'`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && npm run check 2>&1 | tail -3 && grep -c 'B2 fix' src/lib/components/settings/VaultCategory.svelte && grep -c 'var(--color-warm-dark-soft)' src/lib/components/settings/VaultCategory.svelte && echo "--- live code --color-success count (expect 0):" && grep -v '^\s*/\*' src/lib/components/settings/VaultCategory.svelte | grep -v '^\s*\*' | grep -c 'var(--color-success)'</automated>
  </verify>
  <done>
    npm run check 0 errors. grep 'B2 fix' ≥ 1. grep 'var(--color-warm-dark-soft)' ≥ 1. Live-code grep for var(--color-success) returns 0. Full vitest no regressions. Reserved-site files (Step2AuthCheck / Step3VaultPicker / Step5AddCourse) untouched.
  </done>
</task>

</tasks>

<verification>
After Task 1:

```bash
cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme

# Disposition + token swap pinned
test "$(grep -c 'B2 fix' src/lib/components/settings/VaultCategory.svelte)" -ge 1
test "$(grep -c 'var(--color-warm-dark-soft)' src/lib/components/settings/VaultCategory.svelte)" -ge 1

# Live-code --color-success removed (CSS block-comment lines stripped)
test "$(grep -v '^\s*/\*' src/lib/components/settings/VaultCategory.svelte | grep -v '^\s*\*' | grep -c 'var(--color-success)')" -eq 0

# No collateral: reserved-site files untouched (no diff in this commit)
git diff HEAD~1 HEAD -- src/lib/components/onboarding/Step2AuthCheck.svelte | wc -l   # MUST be 0
git diff HEAD~1 HEAD -- src/lib/components/onboarding/Step3VaultPicker.svelte | wc -l # MUST be 0
git diff HEAD~1 HEAD -- src/lib/components/onboarding/Step5AddCourse.svelte | wc -l   # MUST be 0

# Frontend regression-safe
npm run check
npx vitest run
```
</verification>

<success_criteria>
- VaultCategory.svelte .status.success rule uses `var(--color-warm-dark-soft)`.
- B2 fix disposition marker present with UI-SPEC §4 citation.
- Live-code count of `var(--color-success)` in VaultCategory.svelte is 0.
- No diff in the 3 reserved-site files in this commit.
- `npm run check` GREEN, `npx vitest run` full suite GREEN, no regressions.
</success_criteria>

<output>
Create `.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-03-SUMMARY.md` capturing:
- B2 disposition (root cause: token-reserved-site violation; fix: single token swap)
- Commit hash + grep gate outputs (B2 fix count, warm-dark-soft count, live-code success count)
- Confirmation that the 3 reserved-site files were not touched
- Cross-plan parallel-safety: file-disjoint from 02.1-02 / 04 / 05 / 06 — Wave 1 ran without merge conflicts
</output>
</content>
</invoke>