---
phase: "02.1"
plan: 1
type: tdd
wave: 0
depends_on: []
files_modified:
  - src-tauri/tests/inspector_keychain_detect.rs
  - src-tauri/tests/course_code_validation.rs
  - tests/import-error-fallback.test.ts
autonomous: true
gap_closure: false
requirements: [REQ-03, REQ-06, REQ-14]

closes:
  blockers: [B3]
  warnings: [W5, W6]
  red_for: [02.1-02, 02.1-04, 02.1-05]

must_haves:
  truths:
    - "Wave 0 lands ALL 3 RED tests in one commit so Wave 1 GREEN plans (02.1-02 / 02.1-04 / 02.1-05) execute against a failing baseline"
    - "B3 RED — inspector_keychain_detect.rs covers (a) subprocess success → found=true env_broken=false, (b) NotFound spawn error → found=false env_broken=false, (c) other spawn error → env_broken=true"
    - "W6 RED — course_code_validation.rs accepts USYD real codes (COMP3027L / BIOL2010S2 / MATH1062 / cs-101 / 日本語1) and rejects path-traversal / empty / non-alphanumeric / overlength"
    - "W5 RED — import-error-fallback.test.ts asserts the GENERIC fallback now appends raw-error tail when no classifier matches (current copy 'Something went wrong' fails the test until 02.1-05 lands)"
  artifacts:
    - path: "src-tauri/tests/inspector_keychain_detect.rs"
      provides: "B3 contract spec — claude_auth_check via std::process::Command::new('claude').arg('--version').output() — pins exit-code semantics"
      contains: "env_broken"
    - path: "src-tauri/tests/course_code_validation.rs"
      provides: "W6 contract spec — vault_writer::validate_course_code minimal path-safe validator (D-03 signature)"
      contains: "validate_course_code"
    - path: "tests/import-error-fallback.test.ts"
      provides: "W5 contract spec — GENERIC fallback surfaces raw error tail (last sentence after final ':') as a second-line surface"
      contains: "raw error tail"
  key_links:
    - from: "src-tauri/tests/inspector_keychain_detect.rs"
      to: "src-tauri/src/lib.rs::claude_auth_check"
      via: "must call new public helper to invoke subprocess (test cannot reach #[tauri::command] async fn directly — uses a thin pure helper)"
      pattern: "claude_auth_check_inner|probe_claude_binary"
    - from: "src-tauri/tests/course_code_validation.rs"
      to: "src-tauri/src/vault_writer.rs::validate_course_code"
      via: "free function signature per CONTEXT D-03"
      pattern: "validate_course_code"
    - from: "tests/import-error-fallback.test.ts"
      to: "src/lib/import-error.ts::classifyImportError"
      via: "fallback branch returns enriched copy when raw error has a colon-tail"
      pattern: "GENERIC_IMPORT_ERROR_MESSAGE|raw error tail"

closes_truths_for_wave1:
  - "02.1-02 GREEN turns inspector_keychain_detect.rs from RED → GREEN"
  - "02.1-04 GREEN turns course_code_validation.rs from RED → GREEN"
  - "02.1-05 GREEN turns import-error-fallback.test.ts from RED → GREEN"
---

<objective>
Wave 0 RED — land 3 failing tests in a single commit that pin the contracts for B3 + W6 + W5 before any production-code change. Wave 1 plans 02.1-02 / 02.1-04 / 02.1-05 then each turn one of these RED tests GREEN by writing the production code. This is a strict RED→GREEN protocol: the production code in src-tauri/src/lib.rs / vault_writer.rs and src/lib/import-error.ts MUST NOT be touched in this plan — only the three test files are created.

**Rationale per CONTEXT D-01:** All three target bugs are state-machine / parameterized-validation surfaces (TDD heuristic match). Landing all 3 RED tests in one commit means Wave 1 can start in parallel — each Wave 1 plan owns exactly one production source file (no cross-file race) and runs its target test to confirm GREEN.

**Why one commit, not three:** Wave 1 is parallel. If we landed 3 RED commits sequentially the timeline would balloon. The 3 tests live in 3 distinct files (2 Rust integration tests under `src-tauri/tests/`, 1 vitest under `tests/`), so a single commit `test(02.1-01): RED — B3 + W6 + W5 contracts` keeps the audit trail clean.

**Out of scope:**
- Production code (lib.rs / vault_writer.rs / import-error.ts) — owned by Wave 1
- `src/lib/motion.ts` (D-05 Onboarding helper) — D-05 is W2 (visual transition, not state-machine), planned in 02.1-08
- Capability changes (claude --version whitelist) — paired with B3 GREEN in 02.1-02
- audit-capabilities.sh changes — paired with B3 GREEN in 02.1-02
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-CONTEXT.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-REVIEW.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-SECURITY.md
@.planning/STATE.md

<!-- Test templates to MIRROR -->
@src-tauri/tests/course_scaffold.rs
@src-tauri/tests/inbox_basename_clash.rs
@tests/import-error-classifier.test.ts

<!-- Files the executor must NOT modify in this plan (just read for context) -->
@src-tauri/src/lib.rs
@src-tauri/src/vault_writer.rs
@src/lib/import-error.ts

<interfaces>
Public surfaces the RED tests pin (Wave 1 production code will satisfy):

src-tauri/src/lib.rs — B3 (Wave 1 02.1-02 implements):
```rust
// Currently lines 191-212:
// async fn claude_auth_check() -> Result<ClaudeAuthStatus, String>
//   - reads home::home_dir().join(".claude").join(".credentials.json").exists()
// Wave 1 02.1-02 REPLACES with:
//   pub fn probe_claude_binary() -> ClaudeAuthStatus  // SYNC pure helper for tests
//   async fn claude_auth_check() -> Result<ClaudeAuthStatus, String> { Ok(probe_claude_binary()) }
//
// ClaudeAuthStatus { found, version, env_broken } — unchanged shape from 02-13 fix.
```

src-tauri/src/vault_writer.rs — W6 (Wave 1 02.1-04 implements):
```rust
// Currently L95-96: VALID_COURSE_CODE regex Lazy<Regex>("^[A-Z]{4}\\d{4}$")
// Wave 1 02.1-04 DELETES the regex + ADDS:
//   pub fn validate_course_code(code: &str) -> Result<(), VaultWriterError>
// per CONTEXT D-03 signature.
// Both call sites (vault_writer::create_course L256 + import_controller::validate_inputs L174)
// swap to the new validator.
```

src/lib/import-error.ts — W5 (Wave 1 02.1-05 implements):
```ts
// Currently L46-51: GENERIC_IMPORT_ERROR_MESSAGE = "Something went wrong while importing. ..."
// classifyImportError returns the generic string when no classifier matches.
// Wave 1 02.1-05 CHANGES classifyImportError fallback branch to return:
//   `${GENERIC_IMPORT_ERROR_MESSAGE} (details: ${rawTail(raw)})`
// where rawTail extracts the last segment after the final ':' so users see
// e.g. "(details: invalid course code: 'DOGFOOD101')" instead of the generic alone.
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — inspector_keychain_detect.rs (B3 contract)</name>
  <files>src-tauri/tests/inspector_keychain_detect.rs</files>
  <read_first>
    - src-tauri/src/lib.rs lines 170-212 (existing claude_auth_check + ClaudeAuthStatus struct + WR-01 fix already present)
    - src-tauri/tests/inbox_basename_clash.rs (lines 1-50) — MIRROR test layout: `use mneme_lib::...`; `#[tokio::test]`; tempdir / std::env scaffolding pattern; commit-style header comment block
    - .planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-CONTEXT.md §D-02 (lines 98-124) — full subprocess contract
    - src-tauri/Cargo.toml (verify dev-deps include `tempfile` + `tokio` test-util — confirmed by 02-13 plan; no new deps needed)
  </read_first>
  <behavior>
    Test 1 (`probe_claude_binary_returns_found_when_subprocess_exits_zero`):
      - Skip on CI where `claude` is not on PATH: `if std::process::Command::new("which").arg("claude").output().map(|o| !o.status.success()).unwrap_or(true) { return; }` — annotate with comment explaining the skip is acceptable for a local-only dogfood-driven contract (this is the developer's daily-driver test, NOT a CI gate; the audit gate in 02.1-02 will fail the build if anyone removes the production code that satisfies this test).
      - Call `mneme_lib::probe_claude_binary()` (pure sync helper to be added by Wave 1).
      - Assert: `status.found == true`, `status.env_broken == false`.
      - DO NOT assert on `status.version` content (Wave 1 may parse "1.2.3" from stdout or leave as None — either is fine; spec D-02 mentions `parse_version` as nice-to-have not contract).

    Test 2 (`probe_claude_binary_handles_missing_binary_gracefully`):
      - Use `std::env::set_var("PATH", "/nonexistent")` inside the test to force NotFound (restore at end via guard or `defer` pattern — wrap in a struct with Drop impl, or use `scopeguard` if dev-dep already present; otherwise just set it back at the END of the test body and assert nothing in between that could panic).
      - Better: use the `which` crate pattern if already present, OR shell out to verify path is empty.
      - Call `mneme_lib::probe_claude_binary()`.
      - Assert: `status.found == false`, `status.env_broken == false` (NotFound is a normal "user hasn't installed claude yet" path — NOT an env break).
      - SIMPLER ALTERNATIVE: drop Test 2 entirely if PATH manipulation is fragile across `cargo test` parallelism. Instead use `#[ignore]` annotation + a doc comment explaining: "this is a manual recovery-test; run with `cargo test -- --ignored probe_claude_binary_handles_missing_binary_gracefully` on a shell with PATH=/nonexistent". The B3 GREEN production code's NotFound branch is then covered by code review + grep gate in 02.1-02 Task 3, NOT this test.
      - DECISION: use the `#[ignore]` simpler-alternative because `set_var` is process-global and races with other parallel tests. The test compiles + documents the intent without being flaky.

    Test 3 (`claude_auth_status_serde_shape_pins_env_broken_field`):
      - Pure compile-time + serde-output assertion. Construct `ClaudeAuthStatus { found: true, version: None, env_broken: false }`. Serialize via `serde_json::to_string(&status).unwrap()`. Assert the resulting JSON contains the literal substring `"env_broken":false`.
      - This pins the IPC contract — the Step2AuthCheck frontend reads `env_broken` from this exact key. A future refactor that renames the field would break the frontend; this test catches it before runtime.
      - Test PASSES on first run today (the struct already has env_broken from 02-13 WR-01 fix). It is included here to document the load-bearing serde shape.

    NOTE: only Test 1 and Test 3 contribute to the RED → GREEN cycle. Test 1 RED because `probe_claude_binary` does not yet exist (compile failure is acceptable RED for Rust — `cargo test` reports it as a build error, which is a stronger RED than a runtime assertion fail). Test 3 is invariant pinning (passes on first run; documents the contract).
  </behavior>
  <action>
    Create `src-tauri/tests/inspector_keychain_detect.rs`. Header comment block lines 1-25:
    "Phase 02.1 Plan 02.1-01 Task 1 RED — B3 contract (UI-REVIEW.md L148 / CONTEXT D-02). 
     Wave 0 RED, Wave 1 02.1-02 GREEN. probe_claude_binary() is the pure sync helper 
     wave 1 will add to src-tauri/src/lib.rs; claude_auth_check (#[tauri::command]) 
     will call it. This test pins (1) success path returns found=true env_broken=false, 
     (2) the ClaudeAuthStatus serde shape preserves env_broken (load-bearing for the 
     Step2AuthCheck frontend). Test 2 (PATH manipulation) is marked #[ignore] because 
     std::env::set_var races with parallel tests; cover NotFound via grep gate in 02.1-02."

    Use `mneme_lib::probe_claude_binary` import (Wave 1 will add the pub fn). Use `mneme_lib::ClaudeAuthStatus` import (already exported per 02-13).

    Run `cargo test --manifest-path src-tauri/Cargo.toml --test inspector_keychain_detect` — Test 1 MUST FAIL with a build error (`probe_claude_binary` does not exist) OR a link error (`unresolved import`). Test 3 PASSES if it compiles past the import error — but in practice cargo refuses to link the test binary, so the whole file fails to build. That is acceptable RED for the gate.

    Commit message (combined with Tasks 2 + 3 per Task 4): later.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && cargo test --manifest-path src-tauri/Cargo.toml --test inspector_keychain_detect 2>&1 | grep -E "error\[|FAILED|cannot find|unresolved" | head -10</automated>
  </verify>
  <done>
    File exists with 3 test functions. `cargo test` for this test binary fails to BUILD (E0425 or E0432 — unresolved `probe_claude_binary`). The build failure proves the RED contract: Wave 1 02.1-02 must add the helper to compile this test.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED — course_code_validation.rs (W6 contract)</name>
  <files>src-tauri/tests/course_code_validation.rs</files>
  <read_first>
    - src-tauri/src/vault_writer.rs lines 93-96 (existing VALID_COURSE_CODE regex) + lines 250-260 (existing call site in create_course)
    - src-tauri/src/import_controller.rs lines 160-180 (existing VALID_COURSE_RE regex + use site in validate_inputs)
    - src-tauri/tests/course_scaffold.rs (full file, ~50 lines) — MIRROR test layout: `use mneme_lib::vault_writer::...`; `#[test]` plain (NOT async); `tempdir()`; assertion patterns
    - .planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-CONTEXT.md §D-03 (lines 125-157) — full validator contract incl. accept / reject lists
  </read_first>
  <behavior>
    Test 1 (`validate_course_code_accepts_usyd_real_codes`):
      - Parameterized via plain Vec — no `rstest` dep added.
      - Accept list (each MUST return Ok(())): "COMP3027", "COMP3027L", "BIOL2010S2", "MATH1062", "INFO1110", "STAT1003", "XYZ", "cs-101", "日本語1", "a1", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA" (32 chars exactly — boundary OK).
      - Loop: `for code in cases { let r = validate_course_code(code); assert!(r.is_ok(), "expected Ok for {code:?}, got {r:?}"); }`

    Test 2 (`validate_course_code_rejects_invalid_inputs`):
      - Reject list (each MUST return Err(VaultWriterError::InvalidCourseCode(_))): "", "   ", "../etc", "/abs/path", "a/b", "a\\b", "\0nul", ".", "..", "!!!", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA" (33 chars — overlength).
      - For each: `let r = validate_course_code(code); assert!(matches!(r, Err(VaultWriterError::InvalidCourseCode(_))), "expected InvalidCourseCode for {code:?}, got {r:?}");`

    Test 3 (`validate_course_code_trims_surrounding_whitespace`):
      - Accept: "  COMP3027  " → Ok(()) (the validator trims before checking — per D-03 `code.trim()`).
      - Reject: "  /  " → Err (after trim is "/" which contains path separator).
      - Reject: "   " → Err (after trim is empty).

    Test 4 (`validate_course_code_length_boundary`):
      - 32 char: Accept.
      - 33 char: Reject with InvalidCourseCode("course code too long").
      - 1 char "A": Accept (D-03 explicitly: "needs at least one alphanumeric char" — single alnum passes).

    All tests will FAIL TO BUILD because `validate_course_code` does not exist yet — Wave 1 02.1-04 adds it.
  </behavior>
  <action>
    Create `src-tauri/tests/course_code_validation.rs`. Header comment block lines 1-25:
    "Phase 02.1 Plan 02.1-01 Task 2 RED — W6 contract (UI-REVIEW.md L150 / CONTEXT D-03). 
     Wave 0 RED, Wave 1 02.1-04 GREEN. validate_course_code() is the minimal path-safe 
     validator that REPLACES VALID_COURSE_CODE / VALID_COURSE_RE regex (both files). 
     Defense rationale: T-2-01 (path traversal) and T-2-02 (symlink) are already closed 
     by vault_writer::is_under_source two-arm canonicalize gate (path_traversal_blocked.rs + 
     symlink_canonicalize_blocked.rs — both passing). The regex was overdefense; cost of 
     rejecting real USYD codes (COMP3027L / BIOL2010S2) > benefit of redundant string-level 
     wall. See SECURITY.md follow-up note that 02.1-04 will add per CONTEXT D-03."

    Use `mneme_lib::vault_writer::{validate_course_code, VaultWriterError}` import. Plain `#[test]` (no tokio — pure function). No `tempdir()` needed.

    Run `cargo test --manifest-path src-tauri/Cargo.toml --test course_code_validation` — MUST FAIL with E0425 / E0432 (unresolved `validate_course_code`).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && cargo test --manifest-path src-tauri/Cargo.toml --test course_code_validation 2>&1 | grep -E "error\[|FAILED|cannot find|unresolved" | head -10</automated>
  </verify>
  <done>
    File exists with 4 `#[test]` functions (~30 cases total across accept/reject parameterization). Build fails on `unresolved import mneme_lib::vault_writer::validate_course_code`. The build failure proves the RED contract.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: RED — import-error-fallback.test.ts (W5 contract)</name>
  <files>tests/import-error-fallback.test.ts</files>
  <read_first>
    - src/lib/import-error.ts (full file, ~126 lines) — current generic-fallback contract that this test will REWRITE
    - tests/import-error-classifier.test.ts (full file) — MIRROR vitest layout: `import { describe, test, expect } from "vitest";`; `import { classifyImportError, ... } from "$lib/import-error";`
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-REVIEW.md §INFO Dogfood #8 (L155-158) — "invalid course code: 'DOGFOOD101' was only visible in devtools" — the W5 fix surfaces this tail
  </read_first>
  <behavior>
    Test 1 (`fallback surfaces raw error tail when input contains a colon and no classifier matches`):
      - Input: `"vault_writer error: validation failed: course code 'DOGFOOD101' rejected"`
      - Expected: classifyImportError(input) returns a string that:
        * STARTS WITH GENERIC_IMPORT_ERROR_MESSAGE (preserves the friendly headline)
        * CONTAINS the tail "course code 'DOGFOOD101' rejected" (raw detail surfaced)
        * Specifically: `expect(result).toMatch(/Something went wrong/);` AND `expect(result).toMatch(/course code 'DOGFOOD101' rejected/);`

    Test 2 (`fallback shows GENERIC alone when input has no colon — preserves the existing 'no further detail' UX`):
      - Input: `"unspecified failure"`
      - Expected: `expect(result).toBe(GENERIC_IMPORT_ERROR_MESSAGE);` — no enrichment when there is no tail to extract.

    Test 3 (`fallback does NOT enrich when the raw input matches a classifier`):
      - Input: `"dest-clash: /vault/_inbox/notes.pdf"` — this matches isDestClashShape, returns DEST_CLASH_MESSAGE. The fallback enrichment branch never runs. 
      - Expected: `expect(result).toBe(DEST_CLASH_MESSAGE);` (uses the friendly clash copy, NOT the GENERIC + tail copy).

    Test 4 (`fallback handles Error objects with colon-containing messages`):
      - Input: `new Error("io error: permission denied")` 
      - Expected: this matches isPermissionDeniedShape FIRST, returns DUPLICATE_PERMISSION_DENIED_MESSAGE. Test 4 pins that the regression assertion from 02-13 Task 3 still holds — the fallback enrichment ONLY runs when no classifier branch matched.

    Test 5 (`tail extraction takes the LAST colon segment when multiple colons present`):
      - Input: `"prefix: middle: actual error detail"`
      - Expected: result contains `"actual error detail"` (NOT `"middle: actual error detail"`). The W5 fix splits on the LAST colon so multi-level wrapping doesn't bloat the surface copy.

    All 5 tests will FAIL today because the current classifyImportError fallback returns the bare GENERIC_IMPORT_ERROR_MESSAGE with no enrichment. Wave 1 02.1-05 fixes the fallback branch.
  </behavior>
  <action>
    Create `tests/import-error-fallback.test.ts`. Header comment lines 1-15:
    "Phase 02.1 Plan 02.1-01 Task 3 RED — W5 contract (UI-REVIEW.md L155-158 / CONTEXT W5).
     Wave 0 RED, Wave 1 02.1-05 GREEN. Pins the contract: classifyImportError fallback
     branch must surface the raw error TAIL (last colon segment) as a second-line surface
     when no structured classifier (dest-clash / permission-denied) matched. Preserves the
     friendly GENERIC_IMPORT_ERROR_MESSAGE headline so the UX stays calm. Test 5 specifically
     pins last-colon (NOT first-colon) splitting so multi-level wrapped errors stay compact."

    Use the same import shape as `tests/import-error-classifier.test.ts`:
    `import { describe, test, expect } from "vitest";`
    `import { classifyImportError, GENERIC_IMPORT_ERROR_MESSAGE, DEST_CLASH_MESSAGE, DUPLICATE_PERMISSION_DENIED_MESSAGE } from "../src/lib/import-error";`

    Wrap all 5 tests in `describe("W5 fallback enrichment (raw error tail)", () => { ... })`.

    Run `npx vitest run tests/import-error-fallback.test.ts` — Tests 1, 5 MUST FAIL with assertion errors (the enrichment is not yet implemented; result is bare GENERIC_IMPORT_ERROR_MESSAGE). Tests 2, 3, 4 PASS on first run (Test 2 trivially passes because no colon means no enrichment is expected; Tests 3/4 pass because classifier branches still take precedence).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && npx vitest run tests/import-error-fallback.test.ts 2>&1 | grep -E "FAIL|passed|failed" | head -10</automated>
  </verify>
  <done>
    File `tests/import-error-fallback.test.ts` exists with 5 tests inside one describe block. Tests 1 + 5 FAIL with the GENERIC_IMPORT_ERROR_MESSAGE-only output (no tail enrichment). Tests 2, 3, 4 PASS. The vitest output prints "2 failed | 3 passed". This is the RED state Wave 1 02.1-05 will close.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit Wave 0 RED bundle</name>
  <files>src-tauri/tests/inspector_keychain_detect.rs, src-tauri/tests/course_code_validation.rs, tests/import-error-fallback.test.ts</files>
  <read_first>
    - Memory note: `/Users/qinyuan/.claude/projects/-Users-qinyuan-claude-r1ckyIn-GitHub-mneme/memory/feedback_gsd_validate_commit_heredoc.md` — use MULTIPLE `-m` flags for commit, NOT heredoc, to satisfy gsd-validate-commit hook
    - Confirm `cargo fmt` + `cargo clippy` pass on all 3 new files before commit (rust-toolchain pinned to 1.88)
  </read_first>
  <action>
    No source-code edits in this task. Sequence:

    1. Run `cargo fmt --manifest-path src-tauri/Cargo.toml` to format both new Rust test files.
    2. Run `cargo build --manifest-path src-tauri/Cargo.toml --tests --bin mneme 2>&1 | tail -15` — confirm builds for Phase 1/2 production binaries are still GREEN; the 2 new test binaries WILL fail to build (that is the RED — DO NOT panic on `error[E0425]: cannot find function 'validate_course_code'`).
    3. Run `npm run check` — frontend `tsc` MUST pass (the new `tests/import-error-fallback.test.ts` is type-correct because the import names already exist in import-error.ts).
    4. Stage exactly these 3 files: `git add src-tauri/tests/inspector_keychain_detect.rs src-tauri/tests/course_code_validation.rs tests/import-error-fallback.test.ts`.
    5. Commit with multi-`-m` form (NOT heredoc):
       `git commit -m 'test(02.1-01): RED — B3 + W6 + W5 contracts' -m 'inspector_keychain_detect.rs pins probe_claude_binary subprocess contract (Wave 1 02.1-02 GREEN).' -m 'course_code_validation.rs pins validate_course_code minimal path-safe validator (Wave 1 02.1-04 GREEN).' -m 'import-error-fallback.test.ts pins raw-error tail enrichment in GENERIC fallback (Wave 1 02.1-05 GREEN).' -m 'All 3 tests fail on current main: Rust tests fail to BUILD (unresolved fn); vitest fails with 2-of-5 assertion errors. Wave 1 plans turn each RED file GREEN.'`
    6. Run `git status` — confirm only the 3 new test files are committed, no stray edits to production code.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && git log -1 --name-only --pretty=format:'%s' | head -10 && echo "---" && git diff HEAD~1 HEAD --stat | tail -5</automated>
  </verify>
  <done>
    HEAD commit subject is `test(02.1-01): RED — B3 + W6 + W5 contracts`. `git diff HEAD~1 HEAD --stat` shows exactly 3 files added under `src-tauri/tests/` (2) and `tests/` (1). No production source changes (`src-tauri/src/**`, `src/lib/**`) appear in the diff. `npm run check` passes. Production build (`cargo build --bin mneme`) passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → production code surface | Wave 0 RED tests INTRODUCE no new trust boundary; they only PIN the contract Wave 1 will satisfy. |
| RED-state CI signal → developer attention | A RED test that fails to BUILD (Rust) vs FAIL at runtime (vitest) are both legitimate RED signals; build-error RED is stronger because clippy / rust-analyzer also surface it pre-commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-RED-1 | Tampering | Wave 0 RED tests reference a fn that doesn't exist | accept | Acceptable RED state — Rust build failure is the assertion. Wave 1 02.1-02 / 02.1-04 add the production fn; on commit the build flips GREEN. |
| T-2.1-RED-2 | Information Disclosure | inspector_keychain_detect.rs Test 2 (#[ignore]) PATH manipulation | mitigate | Marked `#[ignore]` to avoid std::env::set_var racing parallel tests. Documented as manual `cargo test -- --ignored ...` recovery path. Production code's NotFound branch covered by 02.1-02 grep gate. |
| T-2.1-RED-3 | Tampering | Wave 0 commit accidentally touches production code | mitigate | Task 4 `git status` gate + `git diff HEAD~1 HEAD --stat` review. Reviewer / executor should hard-fail if any file outside `src-tauri/tests/` or `tests/` appears in the commit. |

No new security threats introduced by this plan — pure test-file additions.
</threat_model>

<verification>
After Task 4, the following MUST hold:

```bash
# RED state confirmed (failures are EXPECTED)
cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme
cargo test --manifest-path src-tauri/Cargo.toml --test inspector_keychain_detect 2>&1 | grep -cE "error|FAILED|cannot find"  # > 0 (BUILD ERROR — RED)
cargo test --manifest-path src-tauri/Cargo.toml --test course_code_validation 2>&1 | grep -cE "error|FAILED|cannot find"     # > 0 (BUILD ERROR — RED)
npx vitest run tests/import-error-fallback.test.ts 2>&1 | grep -cE "FAIL|failed"                                                  # >= 2 (Tests 1+5 fail — RED)

# Production code NOT touched
git diff HEAD~1 HEAD -- src-tauri/src/ src/lib/ | wc -l  # MUST be 0
git diff HEAD~1 HEAD -- src-tauri/capabilities/ scripts/audit-capabilities.sh | wc -l  # MUST be 0

# Existing Phase 1 + Phase 2 tests still pass (no regressions)
cargo test --manifest-path src-tauri/Cargo.toml --test path_traversal_blocked
cargo test --manifest-path src-tauri/Cargo.toml --test symlink_canonicalize_blocked
cargo test --manifest-path src-tauri/Cargo.toml --test source_basename_clash
cargo test --manifest-path src-tauri/Cargo.toml --test course_scaffold
npx vitest run tests/import-error-classifier.test.ts
npx vitest run tests/onboarding-resume.test.ts

# Build still works
cargo build --manifest-path src-tauri/Cargo.toml --bin mneme   # Production binary GREEN
npm run check                                                  # tsc GREEN
```
</verification>

<success_criteria>
- 3 new test files committed in a single commit (`test(02.1-01): RED — B3 + W6 + W5 contracts`).
- inspector_keychain_detect.rs has 3 #[tokio::test] functions; build fails on unresolved `probe_claude_binary`.
- course_code_validation.rs has 4 #[test] functions covering ~30 accept/reject cases; build fails on unresolved `validate_course_code`.
- import-error-fallback.test.ts has 5 tests inside one describe block; vitest reports 2 failing (Test 1 + Test 5) and 3 passing (Tests 2 / 3 / 4).
- No production source files in `src-tauri/src/` or `src/lib/` touched.
- Existing Phase 1 + Phase 2 test suites (path_traversal_blocked / symlink_canonicalize_blocked / source_basename_clash / course_scaffold / inbox_basename_clash / import-error-classifier / onboarding-resume / onboarding-finish) all PASS unchanged.
- `cargo build --bin mneme` GREEN. `npm run check` GREEN.
- Wave 1 plans 02.1-02 / 02.1-04 / 02.1-05 can now execute in parallel; each turns one RED file → GREEN by editing its assigned production source file.
</success_criteria>

<output>
Create `.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-01-SUMMARY.md` capturing:
- Wave 0 RED commit hash + the 3 test files added
- Which Wave 1 plan owns the GREEN transition for each file:
  * inspector_keychain_detect.rs → 02.1-02 (B3 GREEN)
  * course_code_validation.rs → 02.1-04 (W6 GREEN)
  * import-error-fallback.test.ts → 02.1-05 (W5 GREEN)
- Confirmation that production code is untouched and existing tests stay green
- Forward reference: Wave 1 is now unblocked
</output>
</content>
</invoke>