---
phase: 02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info
plan: 11
subsystem: docs
tags: [keymap, spec-sync, deferral, ui-spec, w7, d-06, dogfood-driven]

# Dependency graph
requires:
  - phase: 02-vault-canvas-ed-sync-onboarding
    provides: "The 5 Phase 02 doc files (UI-SPEC / RESEARCH / VALIDATION / VERIFICATION / 12-SUMMARY) whose Cmd+* promises this plan retracts. Also the implementation wiring (Cmd+, bridge, Cmd+I handler, mneme:open-settings funnel) shipped by Plan 02-12 that this plan deliberately does NOT touch."
  - phase: 02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info
    provides: "CONTEXT D-06 — the user-explicit decision (2026-05-17) to withdraw keymap promises from spec rather than implement Cmd+, / Cmd+I / Cmd+P / Cmd+R as v1 contract. This plan is the doc-execution arm of that decision."
provides:
  - "5 Phase 02 doc files updated with inline `<!-- v1.x deferred per Phase 02.1 D-06 -->` markers on every Cmd+, / Cmd+I / Cmd+P / Cmd+R mention (38 markers total)"
  - "New §8.0 Keymap Status section in 02-UI-SPEC.md serving as the authoritative SSOT for v1 keymap surface (Cmd+Q only) + v1.x deferred list"
  - "Dogfood-cost note captured in §8.0 Keymap Status + cross-file deferral callouts: missing Cmd+R reload forced full app restart to re-trigger Step 2 probe during 2026-05-17 B3-fix dogfood"
  - "Validation Architecture deferral note in 02-RESEARCH.md — manual-verifications for Cmd+,/I are v1.x scope; tests stay green"
  - "Manual-Only Verifications callout in 02-VALIDATION.md — table rows annotated with v1.x deferral parenthetical"
  - "human_verification[] entries in 02-VERIFICATION.md prefixed `[v1.x deferred per Phase 02.1 D-06]` + `v1_ship_gate: false/partial` YAML fields (audit trail preserved)"
  - "Disposition footer (## Phase 02.1 W7 disposition note) in 02-12-SUMMARY.md citing D-06 + listing what stays in v1 vs what defers"
affects:
  - "v1 ship gate — keymap items no longer block Phase 02 release"
  - "v1.x cycle planning — backlog seed for keymap re-introduction with reload prioritization (Cmd+R first per dogfood-cost)"
  - "Phase 3 multi-session sidebar — first-class keyboard nav promise lives here, not in v1"
  - "Phase 02.1 W7 closure (only doc deliverable for W7; no source changes)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "v1.x deferral marker pattern: inline HTML comment `<!-- v1.x deferred per Phase 02.1 D-06 -->` preserves original spec text while withdrawing release contract. Future audits can grep for it; readers see the original promise + the deferral annotation."
    - "Disposition footer pattern: historical body untouched; a dated `## Phase 02.1 W7 disposition note` section appends the new status without rewriting history."
    - "Spec-without-implementation-revert: when a feature works on dev but is retracted from v1 spec, mark the spec rather than rip the wiring — tests still gate regression; v1.x re-activation is a doc-flip."

key-files:
  created:
    - ".planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-11-SUMMARY.md (this file)"
  modified:
    - ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md (8 inline markers + new §8.0 Keymap Status section)"
    - ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md (12 inline markers + Validation Architecture deferral callout + D-13 reversal in place)"
    - ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md (8 inline / parenthetical markers + Manual-Only Verifications callout)"
    - ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md (7 markers + 3 [v1.x deferred] prefixed entries + top-of-file callout)"
    - ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md (3 markers + ## Phase 02.1 W7 disposition note footer)"

key-decisions:
  - "Spec-only retraction. Implementation wiring + tests STAY intact. Per CONTEXT D-06 + W7 plan: a working dev path is not a v1 release contract; v1.x re-activation is a doc-flip, not a wiring re-build."
  - "Dogfood-cost note embedded inline. Captures the concrete pain point (no Cmd+R → full app restart to re-trigger Step 2 probe) so future readers understand WHY the keymap was withdrawn — not just THAT it was."
  - "Cmd+Q preserved everywhere. The literal substring `Cmd+I,P,R` grep + comma-aware Cmd+, treatment ensures Cmd+Q lines are never touched. Verified via post-edit grep: Cmd+Q count rose from 3 to 9 because the new §8.0 Keymap Status section explicitly lists Cmd+Q as the only v1 keymap (expected/correct)."
  - "Audit-trail-first deletion stance. Original spec text struck through (~~Cmd+,~~) or preserved with appended marker rather than removed. Future readers can grep for the deferral comment AND see the original promise side-by-side."
  - "v1.x prioritization order specified in the dogfood-cost note: Cmd+R reload first (dogfood-driven), then Cmd+, settings, then Cmd+I picker, then Cmd+P palette (Phase 3 candidate). Locked in §8.0 + the backlog-seed phrasing for /gsd-capture."

patterns-established:
  - "Doc-only execute plan (no TDD, no source changes): valid when the change is a spec-contract retraction without functional revert. Marker, callout, and footer patterns are reusable for future spec-vs-implementation drift fixes."
  - "Per-file marker thresholds in the plan's <verify> blocks (≥8 / ≥6 / ≥6 / ≥3 / ≥1) gave concrete grep-based gates instead of subjective 'looks done' acceptance."
  - "Cross-file consistency via shared substring (`v1.x deferred per Phase 02.1 D-06`) — single grep across the phase doc set reveals every place the contract was retracted."

requirements-completed: [REQ-14]

# Metrics
duration: 26min
completed: 2026-05-18
---

# Phase 02.1 Plan 11: W7 keymap-deferral doc sync Summary

**Withdrew Cmd+, / Cmd+I / Cmd+P / Cmd+R promises from 5 Phase 02 doc files via inline `v1.x deferred` markers + new §8.0 Keymap Status SSOT in UI-SPEC, with dogfood-cost note (missing Cmd+R forced full app restart to re-trigger Step 2 probe during 2026-05-17 B3 dogfood) captured to motivate v1.x reload-first prioritization.**

## Performance

- **Duration:** ~26 min (plan read + 5 file edits across 6 tasks + audit + commit)
- **Started:** 2026-05-18T00:41:00Z (worktree HEAD assertion at start)
- **Completed:** 2026-05-18T01:09:00Z (commit `46f28e3` landed)
- **Tasks:** 6 (5 per-file edits + 1 atomic commit + audit)
- **Files modified:** 5 (all `.planning/phases/02-vault-canvas-ed-sync-onboarding/*.md`; zero source-file modifications)

## Accomplishments

- **5/5 target doc files marked** with `v1.x deferred per Phase 02.1 D-06` inline / parenthetical / callout / prefix patterns. 38 total markers landed (plan threshold: ≥24).
- **§8.0 Keymap Status section** added to 02-UI-SPEC.md as the authoritative SSOT — lists Cmd+Q as the only v1 keymap, Cmd+,/I/P/R as v1.x deferred, with the dogfood-cost note embedded so v1.x cycle prioritizes Cmd+R reload first.
- **D-13 reversed in place** in 02-RESEARCH.md (Cmd+I no longer joins interaction-paradigm exception list; original text preserved with strikethrough + reversal annotation so audit trail recovers historical decision).
- **Audit trail preserved everywhere** — no spec promises were silently deleted; every retraction is marked + cross-referenced to §8.0 Keymap Status and CONTEXT D-06.
- **Implementation tests stay green** independently of the doc-only commit: `cmd-comma-shortcut.test.ts` + `menu-bridge.test.ts` (2 files, 5 tests) PASS via vitest; `menu_preferences_emits_event.rs` (4 tests) PASS via cargo. The Cmd+, bridge, Cmd+I handler, macOS Preferences menu, and `mneme:open-settings` CustomEvent funnel all remain wired and gated by tests.

## Task Commits

All 6 tasks landed in a single atomic doc commit (per plan: "Commit message deferred to Task 6"):

1. **Task 1: UI-SPEC.md — 8 inline markers + new §8.0 Keymap Status section** — staged for Task 6
2. **Task 2: RESEARCH.md — 12 inline markers (incl. D-13 reversal) + Validation Architecture deferral note** — staged for Task 6
3. **Task 3: VALIDATION.md — 8 inline / parenthetical markers + Manual-Only Verifications callout** — staged for Task 6
4. **Task 4: VERIFICATION.md — 7 markers incl. 3 `[v1.x deferred]` prefixed human_verification entries + top-of-file callout** — staged for Task 6
5. **Task 5: 02-12-SUMMARY.md — disposition footer (`## Phase 02.1 W7 disposition note`)** — staged for Task 6
6. **Task 6: Atomic 5-file commit + cross-file audit + implementation-test smoke proof** — `46f28e3` (`docs(02.1-11): W7 — keymap deferral marker across 5 Phase 02 doc files`)

**Plan metadata commit:** rolled into `46f28e3` (this plan is doc-only — no separate per-task commits per plan design).

## Files Created/Modified

### Modified (5 files, all `.planning/`)

- **`.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md`** — 8 inline `v1.x deferred` markers across the 9 Cmd+I/,/P/R mention lines (L179 history modal copy / L558 dropzone copy / L630 settings panel keymap section + strikethrough / L752 5-exception list rewritten to 1 v1 exception / L795 import dialog Cmd+I trigger / L920 REQ-06 maps-to clause / L974 dropzone overlay keyboard-path note / L1105 history empty body / L1515 default body copy). Cmd+Q lines untouched. New §8.0 Keymap Status section near start of §8 documenting v1 = Cmd+Q only; v1.x deferred = Cmd+, / Cmd+I / Cmd+P / Cmd+R; dogfood-cost note motivates Cmd+R-first v1.x prioritization.
- **`.planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md`** — 12 inline markers across the 10 mention lines (L11 summary / L42 D-13 reversal in place with strikethrough / L96 REQ-03 / L99 REQ-14 / L137 architectural responsibility map / L158-159 STACK rows / L253 code-flow diagram / L660 regression rule parenthetical / L1094 REQ-10 manual-verification row / L1086 REQ-06 manual-verification row / L1133 cmd-comma-shortcut.test.ts list entry). Validation Architecture deferral callout added at end of §Validation Architecture (right before §Threat Model).
- **`.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md`** — 8 markers via per-task-row Status-column parenthetical (02-07-T3 / 02-10-T1 / 02-12-T1 / 02-12-T3) + cmd-comma-shortcut.test.ts list entry + manual-row parentheticals on the Cmd+I and Cmd+, rows. Manual-Only Verifications callout block added at top of §Manual-Only Verifications.
- **`.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md`** — 7 markers via: (a) two `[v1.x deferred per Phase 02.1 D-06]` prefixed human_verification[] test fields for Cmd+I + Cmd+, with new `v1_ship_gate: false/partial` YAML fields (entry preserved for audit trail); (b) REQ-03 + REQ-14 row parentheticals in §REQ-ID Coverage; (c) top-of-file W7 deferral callout under the heading.
- **`.planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md`** — Historical body untouched; new `## Phase 02.1 W7 disposition note (added 2026-05-17)` footer appended after the closing metadata block. Footer lists what still ships in v1 from the 02-12 wave (cog click + macOS menu + banner + drag-drop import + CustomEvent funnel architecture) vs what defers (Cmd+, accelerator + Cmd+I keyboard shortcut surfaces), cites D-06 + the 5 cross-references, and captures the dogfood-cost note for v1.x backlog.

### Created (1 file)

- **`.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-11-SUMMARY.md`** (this file)

## Decisions Made

- **Spec-only retraction (per CONTEXT D-06).** The implementation is correct on dev (tests prove it). The contract is what's withdrawn from v1, not the code. v1.x re-activation will be a doc-flip — reverse the marker pattern, restore the §8.0 entries.
- **Dogfood-cost note embedded.** Plan's `<objective>` explicitly required capturing the dogfood-derived reason in UI-SPEC's withdrawal section so future readers understand WHY (not just THAT) keymap was withdrawn. The note appears in §8.0 + the RESEARCH Validation Architecture callout + the VALIDATION callout + the SUMMARY footer — cross-referenced so any entry point surfaces it.
- **Cmd+R explicitly prioritized for v1.x.** The dogfood pain point was missing reload, not missing settings or file picker. The §8.0 Keymap Status section + the `/gsd-capture --seed` phrasing in the SUMMARY footer both name the priority order: Cmd+R first, then Cmd+, settings, then Cmd+I picker, then Cmd+P palette (Phase 3 candidate).
- **No `git update-ref` / no `git clean`.** Worktree branch + per-agent namespace asserted at start. Standard `git add` of named files + multi-`-m` commit per the memory note about heredoc-vs-multi-flag.

## Deviations from Plan

None — plan executed exactly as written.

The plan's preflight grep census was accurate (9 hits in UI-SPEC after removing Cmd+Q, 10 in RESEARCH, 7 in VALIDATION, 4 in VERIFICATION, ~10 in 02-12-SUMMARY) modulo small variances (an extra cmd-comma-shortcut.test.ts entry in VALIDATION at L155 and a 1133 entry in RESEARCH at the green-list — both still treated under the same v1.x deferred marker pattern). The verify-block thresholds (≥8 / ≥6 / ≥6 / ≥3 / ≥1 totalling ≥24) were all met or exceeded (8 / 12 / 8 / 7 / 3 = 38 actual).

## Issues Encountered

- **PostToolUse hook STATE.md prompts during edits.** The hook fires on every `.planning/` modification asking whether STATE.md should be updated. Per the orchestrator instruction `Do NOT update STATE.md or ROADMAP.md`, the answer is consistently NO — the parent `/gsd:execute-phase` reconciles STATE.md after all parallel Wave 2 plans land. Acknowledged on every prompt without acting.
- **Husky pre-commit hook warnings.** Husky-9 deprecation banner + svelte-kit tsconfig.json baseUrl warning are pre-existing repo noise (unrelated to this plan). The audit-capabilities gate ran (`[audit] PASS`); vitest `--changed` found no test files to run (correct — no source changes); commit `46f28e3` landed cleanly.

## User Setup Required

None — doc-only change. No environment variables, no external services, no third-party wiring. The implementation surfaces remain wired (Cmd+, / Cmd+I keyboard shortcuts still respond on dev), so no user-facing behavior changes.

## Next Phase Readiness

- **Phase 02.1 W7 closed in doc form.** The remaining W7 task is to verify no parallel Wave 2 plan re-introduces keymap promises in other Phase 02 docs that this plan did not target (none expected — Wave 2 plans 07/08/09/10/12/13 touch source files, not the 5 doc files this plan owns).
- **No downstream plan affected.** The spec is now consistent with the v1 keymap reality (Cmd+Q only). Wave 2 source-file plans can land in any order without conflict.
- **v1.x backlog seeded.** The §8.0 Keymap Status section + the SUMMARY footer carry the exact `/gsd-capture --seed` phrasing for the v1.x keymap re-introduction backlog item: "Phase 02.2 or 03.5 — keymap re-introduction: Cmd+R reload first per dogfood-cost note, then Cmd+, settings, then Cmd+I file picker, then Cmd+P palette (Phase 3 candidate)."
- **Threat surface scan:** no new network endpoints, no new auth paths, no new schema changes at trust boundaries. This is a documentation-level retraction; the implementation surface (Tauri capability gates for `core:menu:default` / `core:event:default` / `core:event:allow-listen` covering `menu:open-settings`) is unchanged. No new threat flags.
- **Known stubs:** none. The 5 doc files now match the v1 implementation surface contract. The marker pattern preserves audit recoverability.
- **For the orchestrator (parent /gsd:execute-phase):** STATE.md update is the orchestrator's responsibility per task instructions — this plan deliberately did not touch STATE.md or ROADMAP.md. After all Wave 2 plans land, the orchestrator should reconcile STATE.md (advance-plan / update-progress / add-decision / record-session) with REQ-14 marked complete (the only requirement this plan claims).

## Self-Check: PASSED

Verified after writing SUMMARY:

1. **Files modified exist:**
   - `.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md` — FOUND (modified)
   - `.planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md` — FOUND (modified)
   - `.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md` — FOUND (modified)
   - `.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md` — FOUND (modified)
   - `.planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md` — FOUND (modified)
   - `.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-11-SUMMARY.md` — FOUND (this file, will be staged + committed in metadata commit)

2. **Commit `46f28e3` exists** in worktree branch `worktree-agent-a4233497ba212a532`: VERIFIED (`git log -1 --pretty=format:'%H' = 46f28e3...`).

3. **Final marker counts (post-commit):**
   - 02-UI-SPEC.md: 8 ≥ 8 ✓
   - 02-RESEARCH.md: 12 ≥ 6 ✓
   - 02-VALIDATION.md: 8 ≥ 6 ✓
   - 02-VERIFICATION.md: 7 ≥ 3 ✓
   - 02-12-SUMMARY.md: 3 ≥ 1 ✓
   - **TOTAL: 38 ≥ 24** ✓
   - D-06 citations total: 43 ≥ 8 ✓
   - `Keymap Status` section header present in UI-SPEC: ≥ 1 ✓
   - `## Phase 02.1` section in 02-12-SUMMARY: exactly 1 ✓

4. **No source-file modifications:** VERIFIED (`git diff --name-only HEAD~1 HEAD | grep -vE '^\.planning/' | wc -l` = 0).

5. **Implementation tests independently green** (decoupled from doc commit):
   - `npx vitest run tests/cmd-comma-shortcut.test.ts tests/menu-bridge.test.ts`: 2 files, 5 tests PASS.
   - `cargo test --manifest-path src-tauri/Cargo.toml --test menu_preferences_emits_event`: 4 tests PASS.

---

*Phase: 02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info*
*Plan: 11 (Wave 2 doc-sync — W7 keymap deferral)*
*Completed: 2026-05-18*
