---
phase: "02.1"
plan: 11
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md
  - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md
  - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md
  - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md
  - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md
autonomous: true
gap_closure: false
requirements: [REQ-14]

closes:
  warnings: [W7]

must_haves:
  truths:
    - "All Cmd+, / Cmd+I / Cmd+P / Cmd+R mentions across the 5 Phase 02 doc files are explicitly marked `<!-- v1.x deferred -->` inline OR struck out with a deferral note."
    - "02-UI-SPEC.md gains a new 'Keymap Status' section in §8.0 (or equivalent) listing Cmd+Q as the ONLY v1 keymap and naming the deferred candidates."
    - "02-VERIFICATION.md human_verification[] entries for Cmd+I / Cmd+, are replaced with deferral notes (NOT silently deleted — preserves audit trail)."
    - "02-12-SUMMARY.md gains a disposition footer noting keymap items in 02-12 scope are v1.x-deferred per Phase 02.1 D-06."
    - "NO source code change — this plan is doc-only. Source files (src/routes/+page.svelte Cmd+I handler, src-tauri/src/menu.rs Cmd+, bridge, etc.) STAY intact — the implementation has its own audit gates; only the spec promises retract."
  artifacts:
    - path: ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md"
      provides: "W7 fix — Keymap Status section + inline `v1.x deferred` markers on all Cmd+* mentions."
      contains: "v1.x deferred"
    - path: ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md"
      provides: "W7 fix — Validation Architecture section notes keymap manual-verifications are v1.x scope."
      contains: "v1.x deferred"
    - path: ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md"
      provides: "W7 fix — Cmd+, manual-verification row marked deferred to v1.x."
      contains: "v1.x deferred"
    - path: ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md"
      provides: "W7 fix — Cmd+I / Cmd+, human_verification[] entries marked deferred with audit trail preserved."
      contains: "v1.x deferred"
    - path: ".planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md"
      provides: "W7 fix — disposition footer cites Phase 02.1 D-06 for keymap deferral."
      contains: "v1.x deferred"
  key_links:
    - from: "Each doc file's Cmd+* mention"
      to: "the inline `<!-- v1.x deferred -->` HTML comment"
      via: "find-and-mark pattern — preserve original text + add deferral marker so future audits can recover historical promises"
      pattern: "v1.x deferred"
---

<objective>
Wave 2 doc-sync — W7 (WARNING): UI-SPEC promises `Cmd+,` (Settings open) / `Cmd+I` (file picker) / `Cmd+P` (command palette) / `Cmd+R` (reload) — but the implementation only wires `Cmd+Q` (system quit). Per CONTEXT D-06 (user-explicit decision 2026-05-17): WITHDRAW the keymap promises from the spec rather than implement the missing keymaps. Re-introducing keymap is v1.x work (likely paired with Phase 3 multi-session sidebar where keyboard nav becomes a first-class promise).

**Why withdraw, not implement:** scope discipline. Phase 02 ships v1; user expects only Cmd+Q (system quit). Better to have spec match implementation than carry false promises into ship.

**What "mark as deferred" means in practice:**
1. INLINE: append `<!-- v1.x deferred -->` HTML comment AT THE END of any line containing `Cmd+,` / `Cmd+I` / `Cmd+P` / `Cmd+R`. Comments are invisible in rendered markdown but visible in source — future audits can grep for them.
2. SECTION-LEVEL: where a whole paragraph promises keyboard behavior, prepend `> **Status:** v1.x deferred — only Cmd+Q ships in v1. See Phase 02.1 D-06.` as a callout above the paragraph.
3. TABLE ROW: where Cmd+* lives in a manual-verification table (02-VALIDATION.md), modify the Status column from ✓/queued to `deferred (v1.x)`.
4. DO NOT delete the original text — preserve audit trail.

**SPECIAL CASE — Cmd+Q is the ONLY v1 keymap.** Cmd+Q mentions stay intact. ONLY `Cmd+,` / `Cmd+I` / `Cmd+P` / `Cmd+R` get the deferral marker. Use precise grep (literal `Cmd+,` with comma; literal `Cmd+I` etc.) to avoid touching Cmd+Q lines.

**WHY this plan is "doc-only execute" (not TDD, not deletion):**
- No source code change — the running implementation's Cmd+I handler and Cmd+, bridge can stay (they're already wired per existing tests `cmd-comma-shortcut.test.ts` + `menu-bridge.test.ts` + `menu_preferences_emits_event.rs`). The user's choice was "withdraw the SPEC PROMISES", not "rip out the wiring". The implementation tests still pass; the spec just no longer guarantees the keymaps as v1 contract.
- This is the cleanest path: the wiring works on the developer's machine today, but the documentation no longer treats it as a release-blocker if a future change breaks the keymap. v1.x scope re-evaluates.

**Implementation tests stay green** (audit gate):
- `tests/cmd-comma-shortcut.test.ts` — still passes (the JS Cmd+, listener still fires the CustomEvent).
- `tests/menu-bridge.test.ts` — still passes.
- `src-tauri/tests/menu_preferences_emits_event.rs` — still passes.

If a future v1.x cycle reintroduces the keymap as a spec promise, these tests provide the regression suite.

**Out of scope:**
- Removing the Cmd+I global keydown handler at +page.svelte — would break dogfood; KEEP the wiring.
- Removing the macOS Preferences... menu item — same; KEEP.
- Touching any source file. ALL 5 files in `files_modified` are under `.planning/phases/02-vault-canvas-ed-sync-onboarding/`.

**Parallel-safety (Wave 2):** Touches 5 DOC files (different from all source-file Wave 2 plans). No overlap with 02.1-07 / 08 / 09 / 10 / 12 / 13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-CONTEXT.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md

<!-- Census of existing Cmd+ mentions (from preflight grep):
     UI-SPEC.md: L179 (history modal copy "press Cmd+I"), L253 (Cmd+Q drain), L298 (Cmd+Q),
                 L558 (Cmd+I), L630 (Cmd+, opens settings), L752 (5 exceptions), L795 (Cmd+I),
                 L920 (Cmd+I dialog), L974 (Cmd+I path), L1105 (history modal copy),
                 L1515 (default body) — 11 mentions, ~9 require marking (Cmd+Q lines preserved)
     RESEARCH.md: L11, L26, L42, L96, L99, L137, L158, L159, L253, L660 — ~10 mentions
     VALIDATION.md: L80, L86, L92, L94, L155, L182, L185 — ~7 mentions
     VERIFICATION.md: L61, L65, L130, L133 — ~4 mentions
     02-12-SUMMARY.md: L24, L26, L29, L31, L36, L43, L44, L49, L59, L60 — ~10 mentions -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI-SPEC.md — mark Cmd+I / Cmd+, mentions + add Keymap Status section</name>
  <files>.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md (full file — but use grep to enumerate the ~11 mention lines BEFORE editing)
    - .planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-CONTEXT.md §D-06 (the locked deferral policy)
  </read_first>
  <action>
    Preflight grep:
    `grep -n 'Cmd+[I,RP]' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md` → ~9 hits (Cmd+I + Cmd+, + Cmd+R + Cmd+P; Cmd+Q is NOT in this regex).

    For EACH match line, append `<!-- v1.x deferred -->` at end of line. For PARAGRAPHS where the keymap promise is load-bearing (e.g. L630 "Esc closes the panel. Cmd+, opens within 100ms (REQ-10 acceptance).") REPLACE the Cmd+, fragment with `~~Cmd+,~~` (markdown strikethrough) + append the deferral comment + add a parenthetical note: `(v1.x deferred per Phase 02.1 D-06 — only Cmd+Q ships in v1)`.

    For copy strings used in user-visible UI (e.g. L179 history modal empty body "Drop files on the window, press Cmd+I, or use Settings → Vault."): the COPY itself stays unchanged (the running app's empty-state still mentions Cmd+I because the handler still works — the user CAN press Cmd+I, the implementation doesn't change). Append `<!-- v1.x spec deferral: copy mentions Cmd+I which works in current implementation but spec no longer guarantees v1 contract per D-06 -->` to the line documenting the copy.

    STEP A — go through ALL ~9 hits. Per-line treatment:
    - L179: copy line — preserve copy + append deferral comment.
    - L298: Cmd+Q only (already in v1) — NO change. The phrase contains Cmd+Q.
    - L558: Cmd+I — append `<!-- v1.x deferred per Phase 02.1 D-06 -->`.
    - L630: paragraph-level — apply strikethrough + parenthetical note.
    - L752: 5-exception list (mentions Cmd+Q, Cmd+,, Cmd+I, Enter, Shift+Enter) — REPLACE "5 exceptions: Cmd+Q, Cmd+,, Cmd+I, Enter, Shift+Enter" with "1 v1 exception: Cmd+Q. v1.x deferred: Cmd+, / Cmd+I / Cmd+P / Cmd+R. Enter / Shift+Enter remain in scope (input affordances, not keymaps)."
    - L795: numbered step "Cmd+I → native file picker → ≥1 file selected." — append deferral comment.
    - L920: maps-to clause "REQ-06 (Cmd+I dialog)" — replace REQ-06 description: `REQ-06 (file picker — keyboard shortcut deferred to v1.x per Phase 02.1 D-06)`.
    - L974: "Cmd+I provides the keyboard path" — strikethrough + parenthetical.
    - L1105: copy in onboarding empty-state — same treatment as L179 (preserve copy + append deferral comment).
    - L1515: default body copy — same as L179 / L1105.

    STEP B — ADD a new "Keymap Status" section in §8.0 (or as a new top-level §8.0.1 if no §8.0 anchor exists; use the existing section structure to place it logically NEAR the start of §8 — first surface-level section). Section content:
    ```markdown
    ### Keymap Status (v1 ship gate — Phase 02.1 W7 fix)

    **In v1 scope:**
    - `Cmd+Q` — system quit. Drains subprocess group, closes window. Tested via `src-tauri/tests/kill_pgid.rs` + Phase 1 close-request hooks.

    **Deferred to v1.x:**
    - `Cmd+,` — open Settings panel.
    - `Cmd+I` — open native file picker.
    - `Cmd+P` — command palette (Phase 3 candidate, not Phase 2 promise).
    - `Cmd+R` — reload (DevTools-only in v1, not a user-facing shortcut).

    **Why deferred:** Phase 02.1 D-06 (user-explicit decision 2026-05-17). The
    implementation wiring for Cmd+, and Cmd+I is present (per
    `tests/cmd-comma-shortcut.test.ts` + `tests/menu-bridge.test.ts` +
    `src-tauri/tests/menu_preferences_emits_event.rs`), but the SPEC no longer
    guarantees v1 contract. v1.x re-evaluation is paired with Phase 3
    multi-session sidebar where keyboard nav becomes a first-class promise.

    **What changed:** spec promises, not implementation. The running app still
    responds to Cmd+, and Cmd+I; this is a documentation-level retraction so
    a future change that breaks those keybinds is not a release blocker.

    **Backlog:** `/gsd-capture --seed "Phase 02.2 or 03.5 — keymap re-introduction"`.
    ```

    Place the section near the start of §8 (just below the §8 main header). If the file uses heading anchors / TOC links, ensure the new section is added in a way that doesn't break existing TOC.

    STEP C — verify counts. After edits:
    1. `grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md` ≥ 8 (8 inline markers minimum — one per affected line).
    2. `grep -c 'Keymap Status' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md` ≥ 1 (the new section header).
    3. `grep -c 'W7 fix' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md` ≥ 1 (section disposition marker).
    4. `grep -cE 'Cmd\+Q' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md` ≥ 3 (preserved; Cmd+Q is the only v1 keymap so the count should be unchanged from baseline).

    Commit message deferred to Task 6.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md && grep -c 'Keymap Status' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md && grep -c 'W7 fix' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md</automated>
  </verify>
  <done>
    v1.x deferred count ≥ 8. Keymap Status section header present. W7 fix marker present. Cmd+Q mentions preserved (count ≥ 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: RESEARCH.md — mark Cmd+I / Cmd+, mentions + Validation Architecture note</name>
  <files>.planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md</files>
  <read_first>
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md (use grep to enumerate ~10 mention lines)
  </read_first>
  <action>
    Preflight grep: `grep -nE 'Cmd\+[I,RP]' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md` → 10 hits.

    Per-line treatment:
    - L11: paragraph mentioning "drag-drop + Cmd+I + folder batch" — append `<!-- v1.x deferred for Cmd+I per Phase 02.1 D-06 -->`.
    - L26: D-01 keymap-context line — preserve (Cmd+Q reference only) — NO change UNLESS it mentions Cmd+I.
    - L42: D-13 Cmd+I exception entry — REPLACE entirely: `**D-13** ~~Cmd+I joins interaction-paradigm exception list (5th narrow exception)~~. **Reversed in Phase 02.1 D-06**: Cmd+I deferred to v1.x; only Cmd+Q remains in v1.`
    - L96: REQ-03 line mentioning Cmd+I — append deferral comment.
    - L99: REQ-14 line mentioning "Cmd+, reachable" — append deferral comment.
    - L137: table row "Cmd+I native file picker | Rust backend (via tauri-plugin-dialog)" — append `(implementation present; v1.x spec deferred per D-06)` in the description column or as a footer note under the table.
    - L158-159: STACK entries for rusqlite (Cmd+P consumption mention) + plugin-dialog (Cmd+I) — append deferral notes; the LIBRARY entries themselves stay valid (the deps are still needed).
    - L253: code-flow diagram showing "Cmd+I →" arrow — append `<!-- v1.x deferred -->` comment.
    - L660: "Settings cog opening different modal than Cmd+,:" — this is a regression rule that still applies WHEN Cmd+, is wired. Add a parenthetical: `(applies whenever Cmd+, is implemented; spec contract deferred to v1.x per D-06)`.

    STEP B — ADD a note at the end of the "Validation Architecture" section (search for `## Validation Architecture` or equivalent):
    ```markdown
    > **Phase 02.1 W7 deferral note:** keymap manual-verifications (Cmd+,, Cmd+I)
    > are deferred to v1.x scope per D-06. Implementation tests (`cmd-comma-shortcut.test.ts`,
    > `menu-bridge.test.ts`, `menu_preferences_emits_event.rs`) remain green and
    > guard against silent regression; the SPEC no longer guarantees v1 contract.
    ```

    Verify:
    1. `grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md` ≥ 6.
    2. `grep -c 'W7' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md` ≥ 1.
    3. `grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md` ≥ 2.

    Commit deferred to Task 6.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md && grep -c 'W7' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md && grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md</automated>
  </verify>
  <done>
    v1.x deferred count ≥ 6. W7 ≥ 1. D-06 ≥ 2. D-13 entry reversed in place (preserved with strikethrough so audit trail survives).
  </done>
</task>

<task type="auto">
  <name>Task 3: VALIDATION.md — mark Cmd+, manual-verification row deferred</name>
  <files>.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md (~7 mentions)
  </read_first>
  <action>
    Preflight grep: `grep -nE 'Cmd\+[I,RP]' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md` → 7 hits.

    Per-line treatment — these are table rows / manual-verification entries:
    - L80: 02-07-T3 row (menu Preferences... Cmd+, emits event) — keep the row; CHANGE the Status column from ✅ green to `green (implementation intact; spec contract v1.x deferred per Phase 02.1 D-06)`.
    - L86: 02-10-T1 row (Cmd+, opens SettingsPanel) — same treatment.
    - L92: 02-12-T1 row (menu-bridge funneling cog / Cmd+, / native menu) — same.
    - L94: 02-12-T3 row (Cmd+I global handler) — same.
    - L155: cmd-comma-shortcut.test.ts entry — preserve test reference; append deferral note in the description column.
    - L182: native NSOpenPanel via Cmd+I — same; append parenthetical.
    - L185: Settings panel pixel-faithfulness via Cmd+, — same.

    STEP B — ADD a callout block ABOVE the "Manual Verification" table (or after the first ###-level heading in the file):
    ```markdown
    > **Phase 02.1 W7 deferral notice:** rows referencing Cmd+, / Cmd+I are marked
    > with a `v1.x deferred per D-06` parenthetical. The TESTS themselves remain
    > green and continue to gate regressions; the SPEC contract no longer
    > guarantees these keymaps in v1 release. See 02-UI-SPEC.md §Keymap Status.
    ```

    Verify:
    1. `grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md` ≥ 6.
    2. `grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md` ≥ 2.
    3. `grep -c 'W7' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md` ≥ 1.

    Commit deferred to Task 6.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md && grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md && grep -c 'W7' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md</automated>
  </verify>
  <done>
    v1.x deferred ≥ 6. D-06 ≥ 2. W7 ≥ 1. Tests stay listed (audit trail); Status notes added.
  </done>
</task>

<task type="auto">
  <name>Task 4: VERIFICATION.md — replace Cmd+I / Cmd+, human_verification[] entries with deferral notes</name>
  <files>.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md (~4 mentions at L61, L65, L130, L133)
  </read_first>
  <action>
    Preflight grep: `grep -nE 'Cmd\+[I,RP]' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md` → 4 hits.

    Per-line treatment:
    - L61: human_verification[] entry test "Press Cmd+I on focused main window" — PRESERVE the entry but PREFIX the test field: `[v1.x deferred per Phase 02.1 D-06] Press Cmd+I on focused main window`. This way the audit trail stays (we know we PROMISED this verification) but the entry will not gate Phase 02 ship.
    - L65: similar entry for Cmd+, — same treatment.
    - L130: REQ-03 row mentioning "Manual file/folder import via drag-drop + Cmd+I" — append `(Cmd+I spec contract deferred to v1.x per D-06)` to the description.
    - L133: REQ-14 row mentioning "Cmd+, hotkey" — same.

    STEP B — ADD a deferral callout near the top (after the frontmatter / metadata):
    ```markdown
    > **Phase 02.1 W7 deferral notice (2026-05-17):** human_verification[] entries
    > prefixed `[v1.x deferred per Phase 02.1 D-06]` are NOT required for Phase 02
    > ship. The implementation wiring for Cmd+I / Cmd+, is intact (per VALIDATION.md
    > test rows), but the SPEC no longer guarantees v1 contract. See 02-UI-SPEC.md
    > §Keymap Status. These entries remain for audit trail and will be re-activated
    > in the v1.x cycle when keymap re-introduction is planned.
    ```

    Verify:
    1. `grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md` ≥ 3.
    2. `grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md` ≥ 2.
    3. `grep -c '\[v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md` ≥ 2 (the prefixed test entries).

    Commit deferred to Task 6.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md && grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md && grep -c '\[v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md</automated>
  </verify>
  <done>
    v1.x deferred count ≥ 3. D-06 ≥ 2. Prefixed entries ≥ 2 (audit trail preserved). Callout block present.
  </done>
</task>

<task type="auto">
  <name>Task 5: 02-12-SUMMARY.md — disposition footer for keymap deferral</name>
  <files>.planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md (~10 mentions across L24-60)
  </read_first>
  <action>
    Preflight grep: `grep -nE 'Cmd\+[I,RP]' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md` → 10 hits.

    Per-line treatment: these are HISTORICAL summary notes (the summary documents what 02-12 DID build). The implementation IS still there. The MINIMAL change: add a SINGLE disposition footer at the END of the file capturing the deferral, rather than mark each historical line.

    STEP A — locate the END of the summary (after the last existing section). ADD:
    ```markdown
    ---

    ## Phase 02.1 W7 disposition note (added 2026-05-17)

    All keymap items in the 02-12 scope (`Cmd+I` global handler, `Cmd+,` bridge,
    macOS native Preferences menu wiring, `mneme:open-settings` CustomEvent funnel)
    are marked **v1.x deferred per Phase 02.1 D-06** — the running implementation
    remains intact (no code reverted; tests still gate regressions), but the SPEC
    contract no longer guarantees these keymaps in v1 release.

    See:
    - 02-UI-SPEC.md §Keymap Status
    - 02-VERIFICATION.md deferral callout
    - 02-VALIDATION.md deferral callout
    - Phase 02.1 CONTEXT D-06

    Backlog candidate: keymap re-introduction in Phase 02.2 or 03.5
    (`/gsd-capture --seed`).
    ```

    Do NOT modify the historical body of the SUMMARY — it accurately describes what 02-12 SHIPPED. The footer is the disposition update.

    Verify:
    1. `grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md` ≥ 1 (the new footer; original body untouched).
    2. `grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md` ≥ 1.
    3. `grep -c 'W7 disposition' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md` ≥ 1.
    4. `grep -c '## Phase 02.1' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md` == 1.

    Commit deferred to Task 6.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md && grep -c 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md && grep -c '## Phase 02.1' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md</automated>
  </verify>
  <done>
    v1.x deferred ≥ 1 in 02-12-SUMMARY.md (the new footer). D-06 ≥ 1. Phase 02.1 section header == 1. Historical body untouched.
  </done>
</task>

<task type="auto">
  <name>Task 6: Atomic commit for all 5 doc files + cross-file grep audit</name>
  <files>.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md, .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md, .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md, .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md, .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md</files>
  <read_first>
    - Memory note: `/Users/qinyuan/.claude/projects/-Users-qinyuan-claude-r1ckyIn-GitHub-mneme/memory/feedback_gsd_validate_commit_heredoc.md` — multi-`-m` commit (not heredoc)
  </read_first>
  <action>
    Atomic landing — confirm all 5 doc files modified by Tasks 1-5; commit together.

    STEP A — `git status` shows exactly 5 files modified (the 5 .planning/phases/02-vault-canvas-ed-sync-onboarding/*.md files). No source code modifications.

    STEP B — final cross-file grep audit:
    1. `grep -rcE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-{UI-SPEC,RESEARCH,VALIDATION,VERIFICATION,12-SUMMARY}.md | awk -F: '{s+=$2} END {print s}'` ≥ 24 (sum across 5 files; threshold = 8 + 6 + 6 + 3 + 1).
    2. `grep -rc 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-{UI-SPEC,RESEARCH,VALIDATION,VERIFICATION,12-SUMMARY}.md | awk -F: '{s+=$2} END {print s}'` ≥ 8.
    3. Confirm NO source file modifications: `git diff --name-only HEAD | grep -vE '^\.planning/' | wc -l` == 0 (all changes in .planning/).

    STEP C — confirm implementation tests STILL pass:
    - `npx vitest run tests/cmd-comma-shortcut.test.ts` — PASS.
    - `npx vitest run tests/menu-bridge.test.ts` — PASS.
    - `cargo test --manifest-path src-tauri/Cargo.toml --test menu_preferences_emits_event` — PASS.

    STEP D — stage exactly the 5 files: `git add .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md`

    Commit with multi-`-m`:
    `git commit -m 'docs(02.1-11): W7 — keymap deferral marker across 5 Phase 02 doc files' -m 'Per Phase 02.1 D-06 (user-explicit decision 2026-05-17), Cmd+, / Cmd+I / Cmd+P / Cmd+R promises are withdrawn from the spec. Only Cmd+Q ships in v1.' -m 'Marker pattern: inline <!-- v1.x deferred --> comments preserve original text; UI-SPEC gains a new §Keymap Status section; VERIFICATION + VALIDATION human_verification entries prefixed [v1.x deferred per D-06]; 02-12-SUMMARY gains a disposition footer.' -m 'Implementation wiring (Cmd+, bridge, Cmd+I handler) and tests (cmd-comma-shortcut.test.ts, menu-bridge.test.ts, menu_preferences_emits_event.rs) STAY intact and continue to gate regressions. SPEC contract no longer guarantees v1; v1.x re-evaluation paired with Phase 3 multi-session sidebar.'`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme && git diff --name-only HEAD~1 HEAD | wc -l && echo "---5 files expected---" && git log -1 --name-only --pretty=format:'' | sort -u && echo "---" && grep -rcE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md | awk -F: '{s+=$2} END {print "total deferred markers:", s}'</automated>
  </verify>
  <done>
    Commit shows exactly 5 files (the 5 .planning/ docs). No source-file diffs. Total `v1.x deferred` marker count across 5 files ≥ 24. Implementation tests (cmd-comma-shortcut / menu-bridge / menu_preferences_emits_event) all still PASS — proven independently of the doc-only commit.
  </done>
</task>

</tasks>

<verification>
After Task 6:

```bash
cd /Users/qinyuan/claude/r1ckyIn_GitHub/mneme

# Cross-file marker counts
test "$(grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md)" -ge 8
test "$(grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-RESEARCH.md)" -ge 6
test "$(grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VALIDATION.md)" -ge 6
test "$(grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md)" -ge 3
test "$(grep -cE 'v1\.x deferred' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md)" -ge 1

# Implementation untouched
test "$(git diff --name-only HEAD~1 HEAD | grep -vE '^\.planning/' | wc -l)" -eq 0

# Implementation tests still pass (decoupled from doc changes)
npx vitest run tests/cmd-comma-shortcut.test.ts tests/menu-bridge.test.ts
cargo test --manifest-path src-tauri/Cargo.toml --test menu_preferences_emits_event

# Cross-doc D-06 citation
test "$(grep -rcE 'D-06' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-{UI-SPEC,RESEARCH,VALIDATION,VERIFICATION,12-SUMMARY}.md | awk -F: '{s+=$2} END {print s}')" -ge 8

# Keymap Status section landed
test "$(grep -c 'Keymap Status' .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md)" -ge 1
```
</verification>

<success_criteria>
- All 5 Phase 02 doc files mark Cmd+, / Cmd+I / Cmd+P / Cmd+R mentions with `v1.x deferred per Phase 02.1 D-06` inline OR via callout / prefix.
- UI-SPEC.md gains §Keymap Status section listing Cmd+Q as the only v1 keymap.
- VERIFICATION.md human_verification[] entries prefixed `[v1.x deferred ...]` — audit trail preserved.
- 02-12-SUMMARY.md gains disposition footer (historical body untouched).
- NO source file modifications — implementation wiring + tests untouched.
- All 5 implementation tests (cmd-comma-shortcut / menu-bridge / menu_preferences_emits_event / any other Cmd+*-related test) still PASS — independently of doc commit.
- Total `v1.x deferred` markers ≥ 24 across 5 files; D-06 citations ≥ 8.
- Single atomic commit with multi-`-m` message; git diff shows exactly the 5 doc files.
</success_criteria>

<output>
Create `.planning/phases/02.1-ui-fixes-from-dogfood-and-audit-3-blocker-8-warning-3-info/02.1-11-SUMMARY.md` capturing:
- W7 root cause (spec promises Cmd+* but only Cmd+Q ships in v1)
- D-06 disposition (withdraw promises, not implementation)
- 5 file-level marker counts + the new Keymap Status section
- Cross-test smoke proof (implementation tests stay green)
- Backlog item: keymap re-introduction in Phase 02.2 or 03.5
- Forward reference: NO downstream plan affected; the spec is now consistent with the v1 keymap reality
</output>
</content>
</invoke>