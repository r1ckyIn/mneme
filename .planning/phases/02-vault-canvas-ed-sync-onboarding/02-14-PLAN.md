---
phase: 02
plan: 14
type: execute
wave: 2
depends_on: [02-13]  # 02-15 enforced via wave topology (no file overlap)
files_modified:
  - src/lib/components/settings/VaultCategory.svelte
  - src/lib/components/ReconciliationOverlay.svelte
  - src-tauri/src/lib.rs
  - src-tauri/src/vault_index.rs
  - src/routes/+layout.svelte
  - tests/vault-move-flow.test.ts
  - src-tauri/tests/reconcile_emits_progress.rs
autonomous: true
gap_closure: true
requirements: [REQ-14, REQ-06, REQ-13]

closes:
  blockers: [CR-01, CR-04]
  warnings: [WR-08]

must_haves:
  truths:
    - "User can browse vault location from Settings → Vault without triggering a move (CR-01 closed — Browse button reveals path; only Move triggers confirm overlay)"
    - "Startup reconciliation surface reports actual file count progress as documented in UI-SPEC §8.9 (CR-04 closed — reconcile:progress and reconcile:done events are emitted by Rust + consumed by overlay)"
    - "+layout.svelte hydration runs reconcile BEFORE list_courses, so the first read returns the canonical course set (WR-08 closed)"
    - "tests/vault-move-flow.test.ts asserts the Settings panel renders a single Move CTA — duplicate Browse-as-Move pattern cannot re-emerge"
    - "src-tauri/tests/reconcile_emits_progress.rs asserts VaultIndex::reconcile_with_progress invokes the per-file progress callback at least once for each file scanned + once for completion"
  artifacts:
    - path: "src/lib/components/settings/VaultCategory.svelte"
      provides: "CR-01 fix — Browse button bound to revealInFinder (opens picker, no move staging); Move button keeps browseAndMove"
      contains: "revealInFinder"
    - path: "src/lib/components/ReconciliationOverlay.svelte"
      provides: "CR-04 fix (frontend half) — preserves existing listeners (now Rust emits them); spec-aligned N/M counter populates"
      contains: "reconcile:progress"
    - path: "src-tauri/src/lib.rs"
      provides: "CR-04 fix (backend half) — reconcile_vault_index emits reconcile:progress per file + reconcile:done on completion"
      contains: "reconcile:progress"
    - path: "src-tauri/src/vault_index.rs"
      provides: "VaultIndex::reconcile_with_progress<F> variant accepting per-file progress closure; base reconcile() unchanged for test back-compat"
      contains: "reconcile_with_progress"
    - path: "src/routes/+layout.svelte"
      provides: "WR-08 fix — pre-reconcile list_courses removed; only post-reconcile list_courses remains"
      contains: "WR-08 fix"
    - path: "tests/vault-move-flow.test.ts"
      provides: "Extended — asserts Browse handler does NOT call move_vault + fix-marker present in source"
      contains: "CR-01"
    - path: "src-tauri/tests/reconcile_emits_progress.rs"
      provides: "Pins per-file emit invocation contract on reconcile_with_progress"
      contains: "reconcile_with_progress"
  key_links:
    - from: "src-tauri/src/lib.rs reconcile_vault_index"
      to: "VaultIndex::reconcile_with_progress + AppHandle::emit"
      via: "command builds closure that calls app.emit('reconcile:progress', {current, total}) per file + emits reconcile:done on Ok"
      pattern: "reconcile:progress"
    - from: "src/lib/components/ReconciliationOverlay.svelte"
      to: "Tauri listen('reconcile:progress')"
      via: "Existing listener unchanged; now fires because Rust emits"
      pattern: "reconcile:progress"
    - from: "src/lib/components/settings/VaultCategory.svelte"
      to: "open({directory:true})"
      via: "browseAndMove (Move) AND revealInFinder (Browse) BOTH call plugin-dialog open; Browse emits NO move state change"
      pattern: "revealInFinder"
    - from: "src/routes/+layout.svelte"
      to: "vault-state.svelte setCourseList"
      via: "ONLY post-reconcile list_courses invocation remains"
      pattern: "setCourseList"
---

<objective>
Gap closure plan — Reconciliation contract + UI fixes (Wave 2, after 02-13).

Closes 3 findings from 02-REVIEW.md whose root causes span Rust IPC emit + Svelte component + layout race:

1. CR-01 BLOCKER (UX bug — REQ-14 primary surface) — VaultCategory.svelte L166-167 binds both Browse and Move buttons to the same browseAndMove handler. Every click on either button stages the move-confirmation overlay; clicking Cancel is the only escape. Headline REQ-14 vault-move CTA is functionally unreachable as a discoverable action; users who want to merely inspect path are forced into the move flow. Fix Option B (recommended per CR-01 fix block): Browse becomes path-inspection only (plugin-dialog folder picker that discards the selection — no state mutation). Move keeps browseAndMove (stages overlay). Two visually distinct buttons, two intent paths.

2. CR-04 BLOCKER (UI dead code — UI-SPEC §8.9 contract violation) — ReconciliationOverlay.svelte L21-34 subscribes to reconcile:progress and reconcile:done events. grep -rn 'reconcile:progress|reconcile:done' src-tauri/src/ returns ZERO matches. UI-SPEC §8.9 explicitly promises an N/M counter. Overlay still dismisses via +layout.svelte finally block, but the documented progress UX is broken. Fix Option (a) — spec-aligned: add a reconcile_with_progress<F> variant on VaultIndex that takes a per-file callback; reconcile_vault_index Tauri command wraps it with a closure that calls app.emit("reconcile:progress", {current, total}) per scanned file and app.emit("reconcile:done", ()) on completion. Frontend listener block UNCHANGED — it just starts receiving events.

3. WR-08 WARNING (hydration race) — +layout.svelte L143-184 calls list_courses then setCourseList BEFORE reconcile populates the DB. On fresh install the first list_courses returns []. Any UI reading vault-state.course_list between hydration steps sees empty list. Fix: drop the pre-reconcile list_courses call; the existing post-reconcile call (still present after 02-12) becomes the single source of truth.

Wave-serialized after 02-13: this plan modifies src-tauri/src/lib.rs (CR-04 emit logic inside reconcile_vault_index), which 02-13 also modifies (CR-03 canon paths + WR-01 env_broken). Running 02-14 after 02-13 lets both edits land cleanly without rebase conflict.

Wave topology vs 02-15: 02-15 (also Wave 1, parallel with 02-13) touches vault_writer.rs / onboarding/Onboarding.svelte / onboarding/Step5AddCourse.svelte / audit-capabilities.sh — fully disjoint from this plan's files_modified set. No explicit depends_on edge is needed because the wave topology already serializes 02-14 after the entire Wave 1 (including 02-15). The `depends_on: [02-13]` line above documents the LIB.RS conflict that requires explicit serialization; 02-15 separation is enforced by file-disjointness within Wave 1 + the Wave 1 → Wave 2 boundary.

Out of scope for this plan:
- CR-02 (_inbox clash) + CR-03 (canon paths) + WR-01 (env_broken) — owned by 02-13
- WR-02/03/06/11 (hardening cluster) — owned by 02-15
- WR-04/05/07/09/10 — backlog triage per orchestrator gap_scope short list

Visual contract (D-17 + D-18 + UI-SPEC §8.9): ReconciliationOverlay markup + CSS UNCHANGED. The {#if total > 0} gate around the counter already exists; it simply starts evaluating to true because total is no longer perpetually 0. Visual SSOT header preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-CONTEXT.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-VERIFICATION.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-REVIEW.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-PATTERNS.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-SPEC.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-12-SUMMARY.md
@.planning/phases/02-vault-canvas-ed-sync-onboarding/02-13-PLAN.md
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/skills/spike-findings-mneme/SKILL.md

@src/lib/components/settings/VaultCategory.svelte
@src/lib/components/ReconciliationOverlay.svelte
@src-tauri/src/lib.rs
@src-tauri/src/vault_index.rs
@src/routes/+layout.svelte
@tests/vault-move-flow.test.ts

Interfaces touched (lifted from codebase so executor needs no exploration):

src-tauri/src/vault_index.rs — ADD `pub fn reconcile_with_progress<F>(&self, vault_root: &Path, on_progress: F) -> Result<ReconcileSummary, VaultIndexError> where F: Fn(usize, usize)`. The closure receives (current_scanned, total). Refactor existing `reconcile()` to delegate to `reconcile_with_progress(vault_root, |_, _| {})` so back-compat is automatic.

src-tauri/src/lib.rs — `reconcile_vault_index` command gains an `app: tauri::AppHandle` parameter (Tauri injects automatically). Body builds an emit closure that calls `Emitter::emit(&app, "reconcile:progress", json!({"current": c, "total": t}))` per file + `Emitter::emit(&app, "reconcile:done", ())` after Ok. No capability change needed for emit.

src/lib/components/settings/VaultCategory.svelte — ADD `revealInFinder` async handler; Browse button binds to it. Move button keeps `browseAndMove`. revealInFinder opens picker (read-only inspection) and discards the result.

src/lib/components/ReconciliationOverlay.svelte — listener block UNCHANGED (L21-34); only a comment header note added.

src/routes/+layout.svelte — DELETE pre-reconcile list_courses + setCourseList pair (around L154-159, labeled `[+layout:list_courses]`). Keep installImportListeners. Keep post-reconcile list_courses (labeled `[+layout:list_courses_post_reconcile]`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add VaultIndex::reconcile_with_progress + Rust emit integration test</name>
  <files>src-tauri/src/vault_index.rs, src-tauri/tests/reconcile_emits_progress.rs</files>
  <read_first>
    - src-tauri/src/vault_index.rs L329-416 (current reconcile implementation; new variant wraps the same loop + adds total pre-count + on_progress invocation)
    - src-tauri/tests/reconcile_lazy_delete.rs (existing reconcile test — MIRROR its tempdir + vault setup; just call the new variant)
    - src-tauri/src/lib.rs L224-230 (the Tauri command wrapper — for Task 2 it will be updated to wire emit_fn)
  </read_first>
  <behavior>
    Test 1 (reconcile_with_progress_invokes_callback_per_file): tempdir vault scaffold + 3 known files. Call reconcile_with_progress with a capturing closure. Assert: captures.len() >= 3 AND every (current, total) pair has 1 <= current <= total AND the final current equals the final total.

    Test 2 (reconcile_with_progress_matches_summary_counts): same setup. summary.scanned (return value) MUST equal the max current observed in the captures.

    Test 3 (reconcile_with_progress_empty_vault_stays_consistent): fresh tempdir + create_vault_scaffold only (no user files). REQUIRED invariant: summary.scanned equals the number of file-callbacks observed; total is consistent across all calls within one run. If summary.scanned == 0, captures may be empty — accept both.
  </behavior>
  <action>
    Step A — pre-scan: walk once with the same `walkdir::WalkDir::new(vault_root).into_iter().filter_entry(|e| !is_hidden_entry(e))` filter; count `is_file()` entries → `total: usize`. This is a cheap O(N) stat pass; RESEARCH budget allows 200ms for ≤100 files; pre-scan adds <50ms. **Pre-scan trade-off note (D-14 reaffirmation):** the second walk doubles the wall-clock time of reconcile relative to the prior single-walk implementation. On a 100-file vault that is ~50ms pre-scan + ~50ms main loop ≈ 100ms total — well within the 200ms SPEC budget per D-14 ("blocking spinner is fine for ≤100 files"). The D-14 escalation trigger (>500 files OR >1000ms measured in dogfood) is UNAFFECTED by this 2x because at 500 files we are still around 500ms total (~250ms pre-scan + ~250ms main), still under the escalation threshold. If a future dogfood pass measures >1000ms, the D-14 fallback to background reconcile (DR2) handles it; no pre-scan optimization needed at this scale.

    Edit src-tauri/src/vault_index.rs: add `reconcile_with_progress<F>(&self, vault_root: &Path, on_progress: F) -> Result<ReconcileSummary, VaultIndexError> where F: Fn(usize, usize)`. Implementation strategy:

    Step B — main loop: identical to existing reconcile(). After each successful file processing (or skip due to canonicalize Err), call `on_progress(scanned_so_far, total)` where scanned_so_far = summary.scanned (incremented BEFORE the callback) and total is the pre-scan value.

    Step C — refactor existing `reconcile()` to call `reconcile_with_progress(vault_root, |_, _| {})`. Preserves existing call sites and back-compat for tests like reconcile_lazy_delete.rs.

    Step D — keep is_hidden_entry private nested fn; DO NOT change the lazy-delete pass.

    Comment block above reconcile_with_progress MUST cite: "// CR-04 fix (gap-closure 02-14): per-file progress callback enables ReconciliationOverlay N/M counter (UI-SPEC §8.9). Tauri command wrapper in lib.rs passes a closure that emits reconcile:progress + reconcile:done events. Pre-scan adds ~2x walk time vs single-walk; on a 100-file vault that is ≤100ms total (well under D-14's 200ms SPEC budget). D-14 escalation trigger (>500 files OR >1000ms in dogfood) is unaffected."

    Create src-tauri/tests/reconcile_emits_progress.rs with header: "// Phase 2 Plan 02-14 Task 1 GREEN — CR-04 (REVIEW.md L117-141). Pins per-file progress callback contract."

    Imports: `use mneme_lib::vault_index::VaultIndex;`, `use mneme_lib::vault_writer::create_vault_scaffold;`, `use std::sync::{Arc, Mutex as StdMutex};`, `use tempfile::tempdir;`.

    Implement the 3 tests from <behavior>.

    Run: cargo test --manifest-path src-tauri/Cargo.toml --test reconcile_emits_progress (MUST PASS, 3 tests green); --test reconcile_lazy_delete (MUST STILL PASS, back-compat); --test vault_index_count (MUST STILL PASS); full suite green.

    Commit message: "feat(02-14): reconcile_with_progress callback + integration test (CR-04 backend half)"
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --test reconcile_emits_progress --test reconcile_lazy_delete --test vault_index_count 2>&1 | grep -E "test result" | tail -5</automated>
  </verify>
  <done>
    grep -c 'reconcile_with_progress' src-tauri/src/vault_index.rs returns ≥ 2 (definition + reconcile() call site). cargo test reconcile_emits_progress shows "test result: ok. 3 passed". reconcile_lazy_delete still passes. Full cargo test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire reconcile_vault_index command to emit reconcile:progress + reconcile:done</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs L224-230 (current reconcile_vault_index thin wrapper)
    - src-tauri/src/lib.rs L242-270 (start_import — pattern for app_for_emit + Emitter::emit usage to mirror)
    - src-tauri/src/vault_index.rs (the new reconcile_with_progress symbol from Task 1)
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md §8.9 (L1249-1287) — N/M counter contract + throttling guidance
  </read_first>
  <action>
    Edit reconcile_vault_index command in src-tauri/src/lib.rs (around L224-230).

    1. Add app parameter: `app: tauri::AppHandle` (Tauri injects automatically; no capability change needed because we are emitting, not receiving).

    2. Replace the body: clone app for emit closure capture; build a closure `move |current: usize, total: usize| { let _ = Emitter::emit(&app_for_emit, "reconcile:progress", serde_json::json!({ "current": current, "total": total })); }`; call `index.reconcile_with_progress(Path::new(&root), emit_progress).map_err(|e| e.to_string())?`; on Ok call `let _ = Emitter::emit(&app, "reconcile:done", ());`; return the summary.

    3. Header comment above the command MUST cite: "// CR-04 fix (gap-closure 02-14): emit reconcile:progress per file + reconcile:done on completion. ReconciliationOverlay.svelte listeners at lines 21-34 already subscribe; previously dead because no Rust path emitted. UI-SPEC §8.9 N/M counter is now functional."

    4. Emitter::emit errors are swallowed per the same `let _ =` pattern used by start_import — webview teardown mid-reconcile must NOT abort the reconcile pass.

    Throttling note: per UI-SPEC §8.9 the counter is throttled to ~10 fps client-side. This task does NOT add server-side throttling — the closure fires per file (matching summary.scanned). Front-end render throttling + reduced-motion already cover the UX. ≤100 files = ≤100 events in <200ms; sub-millisecond Tauri emit cost; web-side debouncing handles render.

    Verify: cargo build succeeds; full cargo test green; audit-capabilities.sh PASS (no new permissions; emit needs no capability declaration); grep -c 'reconcile:progress' src-tauri/src/lib.rs returns ≥ 1; grep -c 'reconcile:done' src-tauri/src/lib.rs returns ≥ 1.

    Commit message: "feat(02-14): emit reconcile:progress + reconcile:done from reconcile_vault_index (CR-04)"
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && grep -c 'reconcile:progress' src-tauri/src/lib.rs && grep -c 'reconcile:done' src-tauri/src/lib.rs && bash scripts/audit-capabilities.sh 2>&1 | tail -2</automated>
  </verify>
  <done>
    cargo build succeeds. grep counts both ≥ 1. audit-capabilities.sh PASS. Full cargo test suite green (no regression from the new emit call).
  </done>
</task>

<task type="auto">
  <name>Task 3: Split VaultCategory Browse vs Move buttons (CR-01) + update vault-move-flow vitest</name>
  <files>src/lib/components/settings/VaultCategory.svelte, tests/vault-move-flow.test.ts</files>
  <read_first>
    - src/lib/components/settings/VaultCategory.svelte (full file — focus on L88-130 browseAndMove + confirmMove + cancelMove handlers, and L165-168 the button row)
    - tests/vault-move-flow.test.ts (existing vault-move vitest pattern — extend the mock + add new assertion)
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md §8.2.1 (Vault category body specification — confirms Browse-then-Move is the documented affordance shape)
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-REVIEW.md §CR-01 (L20-41) — fix options A and B documented
  </read_first>
  <action>
    Edit src/lib/components/settings/VaultCategory.svelte:

    Add a new revealInFinder handler ABOVE browseAndMove (around line 88):
    `async function revealInFinder(): Promise<void>` — opens plugin-dialog open() with directory:true, multiple:false, and defaultPath set to vaultState.vault_path when non-empty. The returned value is DISCARDED — handler exists for path discovery via the native picker, not state mutation. NO setVaultPath, NO moveConfirming. Catch and console.error any errors.

    Update the button row (around L165-168):
    Browse button: `<button type="button" class="ghost-btn" onclick={revealInFinder}>Browse…</button>`
    Move button: `<button type="button" class="ghost-btn" onclick={browseAndMove}>Move…</button>`

    DO NOT change browseAndMove body — it remains the Move handler that stages moveConfirming. DO NOT change confirmMove / cancelMove.

    Header comment block at the top of <script> MUST add (after existing DEVIATION comment): "// CR-01 fix (gap-closure 02-14): Browse and Move are now distinct handlers. Browse opens the picker for path inspection only (no state mutation); Move stages the confirm overlay. REQ-14 headline CTA is now reachable as documented in UI-SPEC §8.2.1."

    Also add an inline comment block ABOVE the revealInFinder function definition reiterating: "// CR-01 fix: read-only path inspection — discards picker result."

    Edit tests/vault-move-flow.test.ts:

    Add a new test inside the existing describe("vault-move flow (UI contract)") block titled "Browse picker invocation does NOT call move_vault (CR-01 fix)": import invoke + open; call open({directory:true, multiple:false, defaultPath: "/Users/qy/StudyVault"}); assert chosen equals "/tmp/test-vault"; CR-01 invariant — filter mockedInvoke.mock.calls for move_vault and assert length === 0.

    Add a copy-lock assertion test titled "VaultCategory.svelte has two distinct button handlers (CR-01 fix marker)": read the source file via node:fs/promises readFile; assert src matches /onclick=\{revealInFinder\}>Browse/; assert src matches /onclick=\{browseAndMove\}>Move/; assert src matches /CR-01 fix/.

    Run: npx vitest run tests/vault-move-flow.test.ts (MUST PASS, 3+ tests green); npm run check (0 errors); npx vitest run tests/settings-categories.test.ts (MUST STILL PASS); full vitest suite green.

    Commit message: "fix(02-14): split Browse from Move in VaultCategory (CR-01) + vitest"
  </action>
  <verify>
    <automated>npx vitest run tests/vault-move-flow.test.ts tests/settings-categories.test.ts 2>&1 | grep -E "Test Files|Tests" | tail -5 && grep -c 'revealInFinder' src/lib/components/settings/VaultCategory.svelte && grep -c 'CR-01 fix' src/lib/components/settings/VaultCategory.svelte</automated>
  </verify>
  <done>
    grep revealInFinder returns ≥ 3 (handler def + button onclick + comment). grep "CR-01 fix" returns ≥ 2 (header + inline). vitest vault-move-flow + settings-categories pass. Full vitest no regressions. npm run check 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove pre-reconcile list_courses race in +layout.svelte (WR-08)</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>
    - src/routes/+layout.svelte L142-185 (the post-onboarding hydration block — WR-08 is the redundant pre-reconcile list_courses pair)
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-REVIEW.md §WR-08 (L241-262) — full race description + fix recommendation
  </read_first>
  <action>
    Edit src/routes/+layout.svelte hydration branch.

    Current code flow (post-onboarding branch when state.completed_at is set):
    Step 1. load_config → setVaultPath
    Step 2. list_courses → setCourseList     ← REMOVE: pre-reconcile race surface
    Step 3. installImportListeners
    Step 4. reconcile_vault_index
    Step 5. list_courses → setCourseList     ← KEEP: canonical hydration

    DELETE the pre-reconcile try/catch block that calls invoke<string[]>("list_courses") + setCourseList(courses) (the block labeled "[+layout:list_courses]" in console.warn, around L154-159).

    KEEP installImportListeners call. KEEP the post-reconcile list_courses call (the one inside the if (vaultPath) branch, after reconcile_vault_index resolves, labeled "[+layout:list_courses_post_reconcile]").

    ADD a comment block above the kept post-reconcile call:
    "// WR-08 fix (gap-closure 02-14): pre-reconcile list_courses removed. On fresh install the DB is empty before reconcile populates it, so the previous pre-reconcile call always returned []. Components reading vault-state between hydration steps saw a stale empty list. Single source of truth is now the post-reconcile list_courses below."

    Verify the structural invariant: there is ONLY ONE setCourseList call site in the post-onboarding branch (the one inside if (vaultPath) after reconcile).

    DO NOT touch:
    - menu:open-settings Tauri listen bridge (L119-126 — SPEC-GAP-1).
    - goto("/onboarding/...") redirect logic.
    - installImportListeners call (it remains valuable as a pre-reconcile step so import events are not lost if the user starts dropping files while reconcile is mid-flight).
    - ReconciliationOverlay reconciling flag flow.

    Verify gates:
    1. npm run check returns 0 errors.
    2. Full vitest no regressions (especially tests/menu-bridge.test.ts and tests/post-onboarding-banner.test.ts must still pass since they rely on +layout.svelte structure).
    3. grep -c 'WR-08 fix' src/routes/+layout.svelte returns ≥ 1.
    4. The pre-reconcile warn tag of the deleted branch is gone from LIVE code: grep -v '^[[:space:]]*//\|^[[:space:]]*<!--' src/routes/+layout.svelte | grep -c '\[+layout:list_courses\]' returns 0. (The post-reconcile tag "[+layout:list_courses_post_reconcile]" is a DIFFERENT string and is preserved.)
    5. grep -c 'list_courses_post_reconcile' src/routes/+layout.svelte returns 1 (post-reconcile call site preserved).

    Commit message: "fix(02-14): remove pre-reconcile list_courses race (WR-08)"
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -5 && grep -c 'WR-08 fix' src/routes/+layout.svelte && grep -v '^[[:space:]]*//\|^[[:space:]]*<!--' src/routes/+layout.svelte | grep -c '\[+layout:list_courses\]' && grep -c 'list_courses_post_reconcile' src/routes/+layout.svelte && npx vitest run tests/menu-bridge.test.ts tests/post-onboarding-banner.test.ts 2>&1 | grep -E "Tests|Test Files" | tail -3</automated>
  </verify>
  <done>
    npm run check 0 errors. "WR-08 fix" grep returns ≥ 1. Pre-reconcile warn tag in LIVE (non-comment) code returns 0. Post-reconcile tag returns 1. menu-bridge + post-onboarding-banner vitests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: ReconciliationOverlay comment annotation + cross-plan integration grep gates</name>
  <files>src/lib/components/ReconciliationOverlay.svelte</files>
  <read_first>
    - src/lib/components/ReconciliationOverlay.svelte (full file — verify NO code changes needed; only comment header gets CR-04 disposition note)
    - .planning/phases/02-vault-canvas-ed-sync-onboarding/02-UI-SPEC.md §8.9 (L1249-1287 — counter contract spec)
  </read_first>
  <action>
    Edit src/lib/components/ReconciliationOverlay.svelte header comment block (around lines 1-5). Append AFTER the existing D-14 DR1 line a new <!-- ... --> block stating:

    "CR-04 fix (gap-closure 02-14): listeners (lines 21-34) were originally dead because Rust never emitted reconcile:progress / reconcile:done. Backend half of the fix landed in 02-14 Task 2 (lib.rs reconcile_vault_index + vault_index.rs reconcile_with_progress). N/M counter promised by UI-SPEC §8.9 now populates because total > 0 evaluates true after the first emit. No markup change needed; the {#if total > 0} gate already guarded the counter."

    DO NOT change markup, listeners, or CSS — the frontend code was already correct; only the backend emit was missing.

    Run the cross-plan integration smoke (validates that 02-13 + 02-14 fixes coexist):
    1. cargo test --manifest-path src-tauri/Cargo.toml — full Rust suite green (includes 02-13's inbox_basename_clash + move_vault_symlink_guard + 02-14's reconcile_emits_progress).
    2. npx vitest run — full TS suite green.
    3. npm run check — 0 errors.
    4. bash scripts/audit-capabilities.sh — PASS (no capability changes from 02-13 OR 02-14).

    Grep gates documenting the gap-closure disposition (verifier runs these during VERIFICATION.md or SUMMARY.md regeneration):
    a. grep -c 'reconcile:progress' src-tauri/src/lib.rs ≥ 1
    b. grep -c 'reconcile:done' src-tauri/src/lib.rs ≥ 1
    c. grep -c 'reconcile_with_progress' src-tauri/src/vault_index.rs ≥ 2
    d. grep -c 'CR-04 fix' src-tauri/src/lib.rs ≥ 1
    e. grep -c 'CR-04 fix' src-tauri/src/vault_index.rs ≥ 1
    f. grep -c 'CR-04 fix' src/lib/components/ReconciliationOverlay.svelte ≥ 1
    g. grep -c 'CR-01 fix' src/lib/components/settings/VaultCategory.svelte ≥ 2
    h. grep -cE 'onclick=\{browseAndMove\}>Browse' src/lib/components/settings/VaultCategory.svelte == 0 (no remaining Browse button bound to move handler)
    i. grep -c 'WR-08 fix' src/routes/+layout.svelte ≥ 1

    Commit message: "docs(02-14): cite CR-04 disposition in ReconciliationOverlay + cross-plan grep gates"
  </action>
  <verify>
    <automated>grep -c 'CR-04 fix' src/lib/components/ReconciliationOverlay.svelte && grep -c 'CR-04 fix' src-tauri/src/lib.rs && grep -c 'CR-04 fix' src-tauri/src/vault_index.rs && grep -c 'CR-01 fix' src/lib/components/settings/VaultCategory.svelte && grep -c 'WR-08 fix' src/routes/+layout.svelte && bash scripts/audit-capabilities.sh 2>&1 | tail -2 && cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | grep -E "test result" | tail -3</automated>
  </verify>
  <done>
    All 5 grep counts return per their respective thresholds (CR-04 ≥ 1 across 3 files, CR-01 ≥ 2 in VaultCategory, WR-08 ≥ 1 in +layout). audit-capabilities.sh PASS. Full cargo test suite passes. Full vitest passes. npm run check 0 errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri AppHandle → webview event bus | reconcile:progress/done events cross the IPC envelope; payload is bounded JSON (current/total integers — no PII, no path strings) |
| Settings UI button events → IPC | CR-01 fix preserves the existing browseAndMove path for Move; the new revealInFinder path is read-only (open() picker discards selection) |
| +layout.svelte hydration → vault-state singleton | WR-08 fix collapses to single source of truth post-reconcile; no intermediate stale-empty state visible to consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-09b | Denial of Service | reconcile_vault_index emit storm | accept | Emit fires per file; vault budget ≤100 files per SPEC L65; ≤100 events in <200ms well under Tauri IPC throughput. UI-SPEC §8.9 throttles render to ~10 fps client-side. No server-side throttling needed for Phase 2 vault size. Escalation trigger: vault > 500 files in dogfood (D-14) → add server-side debouncing in v1.x. |
| T-2-10 | Tampering | VaultCategory Browse-as-Move UX confusion | mitigate | CR-01 fix: Browse handler discards the picker's selection (no state mutation). User wanting to inspect vault path can now do so without accidentally staging a move. Move flow unchanged; confirm overlay still required for actual move. |
| T-2-11 | Information Disclosure | empty course_list visible during +layout hydration race | mitigate | WR-08 fix: single source of truth (post-reconcile list_courses). Any UI reading vault-state.course_list during onMount sees either nothing (covered by the reconciling overlay) or the canonical post-reconcile set. No intermediate stale-empty state surfaces. |

No NEW threat surface introduced. The reconcile emit pattern mirrors the existing import:progress / import:done pattern proven in 02-05 + 02-07.
</threat_model>

<verification>
After all 5 tasks, these commands MUST pass:

```bash
# 1. New Wave-0 test from this plan
cargo test --manifest-path src-tauri/Cargo.toml --test reconcile_emits_progress

# 2. Cross-plan regression — 02-13 fixes still green
cargo test --manifest-path src-tauri/Cargo.toml --test inbox_basename_clash
cargo test --manifest-path src-tauri/Cargo.toml --test move_vault_symlink_guard
cargo test --manifest-path src-tauri/Cargo.toml --test source_basename_clash
cargo test --manifest-path src-tauri/Cargo.toml --test move_vault_non_empty_dst

# 3. Full regression
cargo test --manifest-path src-tauri/Cargo.toml
npx vitest run
npm run check
bash scripts/audit-capabilities.sh

# 4. CR-04 disposition trail (3 files)
test "$(grep -c 'CR-04 fix' src-tauri/src/lib.rs)" -ge 1
test "$(grep -c 'CR-04 fix' src-tauri/src/vault_index.rs)" -ge 1
test "$(grep -c 'CR-04 fix' src/lib/components/ReconciliationOverlay.svelte)" -ge 1

# 5. CR-04 event symmetry (Rust emits ↔ TS listens)
test "$(grep -c 'reconcile:progress' src-tauri/src/lib.rs)" -ge 1
test "$(grep -c 'reconcile:done' src-tauri/src/lib.rs)" -ge 1
test "$(grep -c 'reconcile:progress' src/lib/components/ReconciliationOverlay.svelte)" -ge 1
test "$(grep -c 'reconcile:done' src/lib/components/ReconciliationOverlay.svelte)" -ge 1

# 6. CR-01 — no Browse-as-Move binding remains
test "$(grep -cE 'onclick=\{browseAndMove\}>Browse' src/lib/components/settings/VaultCategory.svelte)" -eq 0
test "$(grep -c 'revealInFinder' src/lib/components/settings/VaultCategory.svelte)" -ge 3

# 7. WR-08 — pre-reconcile list_courses removed; tag of deleted branch absent in LIVE code
test "$(grep -c 'WR-08 fix' src/routes/+layout.svelte)" -ge 1
test "$(grep -v '^[[:space:]]*//\|^[[:space:]]*<!--' src/routes/+layout.svelte | grep -c '\[+layout:list_courses\]')" -eq 0
test "$(grep -c 'list_courses_post_reconcile' src/routes/+layout.svelte)" -eq 1
```
</verification>

<success_criteria>
- CR-01 closed: VaultCategory's Browse opens a folder picker for inspection only; Move retains the staging+confirm overlay flow. Two distinct intent paths. New vitest case pins both handler bindings + disposition comment.
- CR-04 closed: VaultIndex::reconcile_with_progress + reconcile_vault_index command emit reconcile:progress per file + reconcile:done on completion. ReconciliationOverlay N/M counter populates per UI-SPEC §8.9. Existing reconcile() back-compat preserved; reconcile_lazy_delete + vault_index_count integration tests stay green.
- WR-08 closed: +layout.svelte post-onboarding hydration runs reconcile BEFORE list_courses; single source of truth for course_list state on fresh install. Race between empty-DB list_courses and reconcile populating the DB is structurally eliminated.
- 1 new Rust integration test (reconcile_emits_progress.rs, 3 tests).
- 2 new vitest cases in vault-move-flow.test.ts.
- 0 vitest regressions.
- 0 cargo test regressions.
- audit-capabilities.sh PASS — no new capabilities (Emitter::emit needs no permission).
- npm run check — 0 errors.
- All 9 grep gates from <verification> pass.
- After 02-13 + 02-14 ship, must_haves count from 02-VERIFICATION.md frontmatter (5/6) becomes 6/6.
</success_criteria>

<output>
Create .planning/phases/02-vault-canvas-ed-sync-onboarding/02-14-SUMMARY.md capturing:
- CR-01 / CR-04 / WR-08 disposition narrative (what was wrong, what changed, what is now invariant)
- Test commit hashes + new test name (reconcile_emits_progress.rs, 3 tests)
- Grep gates proving disposition trail across all touched files
- Confirmation that 02-13 fixes still pass (no cross-plan regression)
- Coverage delta: combined with 02-13 SUMMARY, ALL 4 BLOCKERs (CR-01..CR-04) are closed. 02-VERIFICATION.md gap list (4 truths) goes from 5/6 satisfied → 6/6 satisfied. known_issues_acceptable: yes becomes truthful.
- Forward reference: WR-02/03/06/11 still open — covered by 02-15.
- Note for verify-work re-run: human-verification items 5 (Cmd+, settings) + 6 (cold-start reconciliation with > 100 files) should be re-run after this plan — the cold-start case now has a populating counter to visually verify.
</output>
</output>
